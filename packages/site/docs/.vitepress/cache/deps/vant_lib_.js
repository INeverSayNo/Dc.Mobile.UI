import {
  require_basic,
  require_constant,
  require_divider,
  require_dom,
  require_index_cjs,
  require_locale,
  require_utils
} from "./chunk-RPIU2F4Z.js";
import {
  init_shared_esm_bundler,
  init_vue_runtime_esm_bundler,
  shared_esm_bundler_exports,
  vue_runtime_esm_bundler_exports
} from "./chunk-AB4CWU2G.js";
import {
  __commonJS,
  __toCommonJS
} from "./chunk-V4OQ3NZ2.js";

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/on-popup-reopen.js
var require_on_popup_reopen = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/on-popup-reopen.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      POPUP_TOGGLE_KEY: () => POPUP_TOGGLE_KEY,
      onPopupReopen: () => onPopupReopen
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var POPUP_TOGGLE_KEY = Symbol();
    function onPopupReopen(callback) {
      const popupToggleStatus = (0, import_vue.inject)(POPUP_TOGGLE_KEY, null);
      if (popupToggleStatus) {
        (0, import_vue.watch)(popupToggleStatus, (show) => {
          if (show) {
            callback();
          }
        });
      }
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-height.js
var require_use_height = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-height.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useHeight: () => useHeight
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_use = require_index_cjs();
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_on_popup_reopen = require_on_popup_reopen();
    var useHeight = (element, withSafeArea) => {
      const height = (0, import_vue.ref)();
      const setHeight = () => {
        height.value = (0, import_use.useRect)(element).height;
      };
      (0, import_vue.onMounted)(() => {
        (0, import_vue.nextTick)(setHeight);
        if (withSafeArea) {
          for (let i = 1; i <= 3; i++) {
            setTimeout(setHeight, 100 * i);
          }
        }
      });
      (0, import_on_popup_reopen.onPopupReopen)(() => (0, import_vue.nextTick)(setHeight));
      (0, import_vue.watch)([import_utils.windowWidth, import_utils.windowHeight], setHeight);
      return height;
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-placeholder.js
var require_use_placeholder = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-placeholder.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      usePlaceholder: () => usePlaceholder
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use_height = require_use_height();
    function usePlaceholder(contentRef, bem) {
      const height = (0, import_use_height.useHeight)(contentRef, true);
      return (renderContent) => (0, import_vue.createVNode)("div", {
        "class": bem("placeholder"),
        "style": {
          height: height.value ? `${height.value}px` : void 0
        }
      }, [renderContent()]);
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar/ActionBar.js
var require_ActionBar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar/ActionBar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ACTION_BAR_KEY: () => ACTION_BAR_KEY,
      actionBarProps: () => actionBarProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_placeholder = require_use_placeholder();
    var [name, bem] = (0, import_utils.createNamespace)("action-bar");
    var ACTION_BAR_KEY = Symbol(name);
    var actionBarProps = {
      placeholder: Boolean,
      safeAreaInsetBottom: import_utils.truthProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: actionBarProps,
      setup(props, {
        slots
      }) {
        const root = (0, import_vue.ref)();
        const renderPlaceholder = (0, import_use_placeholder.usePlaceholder)(root, bem);
        const {
          linkChildren
        } = (0, import_use.useChildren)(ACTION_BAR_KEY);
        linkChildren();
        const renderActionBar = () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "class": [bem(), {
              "van-safe-area-bottom": props.safeAreaInsetBottom
            }]
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
        return () => {
          if (props.placeholder) {
            return renderPlaceholder(renderActionBar);
          }
          return renderActionBar();
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar/index.js
var require_action_bar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ActionBar: () => ActionBar,
      actionBarProps: () => import_ActionBar2.actionBarProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ActionBar = __toESM(require_ActionBar());
    var import_ActionBar2 = require_ActionBar();
    var ActionBar = (0, import_utils.withInstall)(import_ActionBar.default);
    var stdin_default = ActionBar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-expose.js
var require_use_expose = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-expose.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useExpose: () => useExpose
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    function useExpose(apis) {
      const instance = (0, import_vue.getCurrentInstance)();
      if (instance) {
        (0, import_utils.extend)(instance.proxy, apis);
      }
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-route.js
var require_use_route = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-route.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      route: () => route,
      routeProps: () => routeProps,
      useRoute: () => useRoute
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var routeProps = {
      to: [String, Object],
      url: String,
      replace: Boolean
    };
    function route({
      to,
      url,
      replace,
      $router: router
    }) {
      if (to && router) {
        router[replace ? "replace" : "push"](to);
      } else if (url) {
        replace ? location.replace(url) : location.href = url;
      }
    }
    function useRoute() {
      const vm = (0, import_vue.getCurrentInstance)().proxy;
      return () => route(vm);
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/badge/Badge.js
var require_Badge = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/badge/Badge.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      badgeProps: () => badgeProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("badge");
    var badgeProps = {
      dot: Boolean,
      max: import_utils.numericProp,
      tag: (0, import_utils.makeStringProp)("div"),
      color: String,
      offset: Array,
      content: import_utils.numericProp,
      showZero: import_utils.truthProp,
      position: (0, import_utils.makeStringProp)("top-right")
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: badgeProps,
      setup(props, {
        slots
      }) {
        const hasContent = () => {
          if (slots.content) {
            return true;
          }
          const {
            content,
            showZero
          } = props;
          return (0, import_utils.isDef)(content) && content !== "" && (showZero || content !== 0 && content !== "0");
        };
        const renderContent = () => {
          const {
            dot,
            max,
            content
          } = props;
          if (!dot && hasContent()) {
            if (slots.content) {
              return slots.content();
            }
            if ((0, import_utils.isDef)(max) && (0, import_utils.isNumeric)(content) && +content > +max) {
              return `${max}+`;
            }
            return content;
          }
        };
        const getOffsetWithMinusString = (val) => val.startsWith("-") ? val.replace("-", "") : `-${val}`;
        const style = (0, import_vue.computed)(() => {
          const style2 = {
            background: props.color
          };
          if (props.offset) {
            const [x, y] = props.offset;
            const {
              position
            } = props;
            const [offsetY, offsetX] = position.split("-");
            if (slots.default) {
              if (typeof y === "number") {
                style2[offsetY] = (0, import_utils.addUnit)(offsetY === "top" ? y : -y);
              } else {
                style2[offsetY] = offsetY === "top" ? (0, import_utils.addUnit)(y) : getOffsetWithMinusString(y);
              }
              if (typeof x === "number") {
                style2[offsetX] = (0, import_utils.addUnit)(offsetX === "left" ? x : -x);
              } else {
                style2[offsetX] = offsetX === "left" ? (0, import_utils.addUnit)(x) : getOffsetWithMinusString(x);
              }
            } else {
              style2.marginTop = (0, import_utils.addUnit)(y);
              style2.marginLeft = (0, import_utils.addUnit)(x);
            }
          }
          return style2;
        });
        const renderBadge = () => {
          if (hasContent() || props.dot) {
            return (0, import_vue.createVNode)("div", {
              "class": bem([props.position, {
                dot: props.dot,
                fixed: !!slots.default
              }]),
              "style": style.value
            }, [renderContent()]);
          }
        };
        return () => {
          if (slots.default) {
            const {
              tag
            } = props;
            return (0, import_vue.createVNode)(tag, {
              "class": bem("wrapper")
            }, {
              default: () => [slots.default(), renderBadge()]
            });
          }
          return renderBadge();
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/badge/index.js
var require_badge = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/badge/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Badge: () => Badge,
      badgeProps: () => import_Badge2.badgeProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Badge = __toESM(require_Badge());
    var import_Badge2 = require_Badge();
    var Badge = (0, import_utils.withInstall)(import_Badge.default);
    var stdin_default = Badge;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-global-z-index.js
var require_use_global_z_index = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-global-z-index.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      setGlobalZIndex: () => setGlobalZIndex,
      useGlobalZIndex: () => useGlobalZIndex
    });
    module.exports = __toCommonJS2(stdin_exports);
    var globalZIndex = 2e3;
    var useGlobalZIndex = () => ++globalZIndex;
    var setGlobalZIndex = (val) => {
      globalZIndex = val;
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/config-provider/ConfigProvider.js
var require_ConfigProvider = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/config-provider/ConfigProvider.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      CONFIG_PROVIDER_KEY: () => CONFIG_PROVIDER_KEY,
      configProviderProps: () => configProviderProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_global_z_index = require_use_global_z_index();
    var [name, bem] = (0, import_utils.createNamespace)("config-provider");
    var CONFIG_PROVIDER_KEY = Symbol(name);
    var configProviderProps = {
      tag: (0, import_utils.makeStringProp)("div"),
      theme: (0, import_utils.makeStringProp)("light"),
      zIndex: Number,
      themeVars: Object,
      themeVarsDark: Object,
      themeVarsLight: Object,
      themeVarsScope: (0, import_utils.makeStringProp)("local"),
      iconPrefix: String
    };
    function insertDash(str) {
      return str.replace(/([a-zA-Z])(\d)/g, "$1-$2");
    }
    function mapThemeVarsToCSSVars(themeVars) {
      const cssVars = {};
      Object.keys(themeVars).forEach((key) => {
        const formattedKey = insertDash((0, import_utils.kebabCase)(key));
        cssVars[`--van-${formattedKey}`] = themeVars[key];
      });
      return cssVars;
    }
    function syncThemeVarsOnRoot(newStyle = {}, oldStyle = {}) {
      Object.keys(newStyle).forEach((key) => {
        if (newStyle[key] !== oldStyle[key]) {
          document.documentElement.style.setProperty(key, newStyle[key]);
        }
      });
      Object.keys(oldStyle).forEach((key) => {
        if (!newStyle[key]) {
          document.documentElement.style.removeProperty(key);
        }
      });
    }
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: configProviderProps,
      setup(props, {
        slots
      }) {
        const style = (0, import_vue.computed)(() => mapThemeVarsToCSSVars((0, import_utils.extend)({}, props.themeVars, props.theme === "dark" ? props.themeVarsDark : props.themeVarsLight)));
        if (import_utils.inBrowser) {
          const addTheme = () => {
            document.documentElement.classList.add(`van-theme-${props.theme}`);
          };
          const removeTheme = (theme = props.theme) => {
            document.documentElement.classList.remove(`van-theme-${theme}`);
          };
          (0, import_vue.watch)(() => props.theme, (newVal, oldVal) => {
            if (oldVal) {
              removeTheme(oldVal);
            }
            addTheme();
          }, {
            immediate: true
          });
          (0, import_vue.onActivated)(addTheme);
          (0, import_vue.onDeactivated)(removeTheme);
          (0, import_vue.onBeforeUnmount)(removeTheme);
          (0, import_vue.watch)(style, (newStyle, oldStyle) => {
            if (props.themeVarsScope === "global") {
              syncThemeVarsOnRoot(newStyle, oldStyle);
            }
          });
          (0, import_vue.watch)(() => props.themeVarsScope, (newScope, oldScope) => {
            if (oldScope === "global") {
              syncThemeVarsOnRoot({}, style.value);
            }
            if (newScope === "global") {
              syncThemeVarsOnRoot(style.value, {});
            }
          });
          if (props.themeVarsScope === "global") {
            syncThemeVarsOnRoot(style.value, {});
          }
        }
        (0, import_vue.provide)(CONFIG_PROVIDER_KEY, props);
        (0, import_vue.watchEffect)(() => {
          if (props.zIndex !== void 0) {
            (0, import_use_global_z_index.setGlobalZIndex)(props.zIndex);
          }
        });
        return () => (0, import_vue.createVNode)(props.tag, {
          "class": bem(),
          "style": props.themeVarsScope === "local" ? style.value : void 0
        }, {
          default: () => {
            var _a;
            return [(_a = slots.default) == null ? void 0 : _a.call(slots)];
          }
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/icon/Icon.js
var require_Icon = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/icon/Icon.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      iconProps: () => iconProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_badge = require_badge();
    var import_ConfigProvider = require_ConfigProvider();
    var [name, bem] = (0, import_utils.createNamespace)("icon");
    var isImage = (name2) => name2 == null ? void 0 : name2.includes("/");
    var iconProps = {
      dot: Boolean,
      tag: (0, import_utils.makeStringProp)("i"),
      name: String,
      size: import_utils.numericProp,
      badge: import_utils.numericProp,
      color: String,
      badgeProps: Object,
      classPrefix: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: iconProps,
      setup(props, {
        slots
      }) {
        const config = (0, import_vue.inject)(import_ConfigProvider.CONFIG_PROVIDER_KEY, null);
        const classPrefix = (0, import_vue.computed)(() => props.classPrefix || (config == null ? void 0 : config.iconPrefix) || bem());
        return () => {
          const {
            tag,
            dot,
            name: name2,
            size,
            badge,
            color
          } = props;
          const isImageIcon = isImage(name2);
          return (0, import_vue.createVNode)(import_badge.Badge, (0, import_vue.mergeProps)({
            "dot": dot,
            "tag": tag,
            "class": [classPrefix.value, isImageIcon ? "" : `${classPrefix.value}-${name2}`],
            "style": {
              color,
              fontSize: (0, import_utils.addUnit)(size)
            },
            "content": badge
          }, props.badgeProps), {
            default: () => {
              var _a;
              return [(_a = slots.default) == null ? void 0 : _a.call(slots), isImageIcon && (0, import_vue.createVNode)("img", {
                "class": bem("image"),
                "src": name2
              }, null)];
            }
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/icon/index.js
var require_icon = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/icon/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Icon: () => Icon,
      default: () => stdin_default,
      iconProps: () => import_Icon2.iconProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Icon = __toESM(require_Icon());
    var import_Icon2 = require_Icon();
    var Icon = (0, import_utils.withInstall)(import_Icon.default);
    var stdin_default = Icon;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/loading/Loading.js
var require_Loading = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/loading/Loading.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      loadingProps: () => loadingProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("loading");
    var SpinIcon = Array(12).fill(null).map((_, index) => (0, import_vue.createVNode)("i", {
      "class": bem("line", String(index + 1))
    }, null));
    var CircularIcon = (0, import_vue.createVNode)("svg", {
      "class": bem("circular"),
      "viewBox": "25 25 50 50"
    }, [(0, import_vue.createVNode)("circle", {
      "cx": "50",
      "cy": "50",
      "r": "20",
      "fill": "none"
    }, null)]);
    var loadingProps = {
      size: import_utils.numericProp,
      type: (0, import_utils.makeStringProp)("circular"),
      color: String,
      vertical: Boolean,
      textSize: import_utils.numericProp,
      textColor: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: loadingProps,
      setup(props, {
        slots
      }) {
        const spinnerStyle = (0, import_vue.computed)(() => (0, import_utils.extend)({
          color: props.color
        }, (0, import_utils.getSizeStyle)(props.size)));
        const renderIcon = () => {
          const DefaultIcon = props.type === "spinner" ? SpinIcon : CircularIcon;
          return (0, import_vue.createVNode)("span", {
            "class": bem("spinner", props.type),
            "style": spinnerStyle.value
          }, [slots.icon ? slots.icon() : DefaultIcon]);
        };
        const renderText = () => {
          var _a;
          if (slots.default) {
            return (0, import_vue.createVNode)("span", {
              "class": bem("text"),
              "style": {
                fontSize: (0, import_utils.addUnit)(props.textSize),
                color: (_a = props.textColor) != null ? _a : props.color
              }
            }, [slots.default()]);
          }
        };
        return () => {
          const {
            type,
            vertical
          } = props;
          return (0, import_vue.createVNode)("div", {
            "class": bem([type, {
              vertical
            }]),
            "aria-live": "polite",
            "aria-busy": true
          }, [renderIcon(), renderText()]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/loading/index.js
var require_loading = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/loading/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Loading: () => Loading,
      default: () => stdin_default,
      loadingProps: () => import_Loading2.loadingProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Loading = __toESM(require_Loading());
    var import_Loading2 = require_Loading();
    var Loading = (0, import_utils.withInstall)(import_Loading.default);
    var stdin_default = Loading;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/button/Button.js
var require_Button = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/button/Button.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      buttonProps: () => buttonProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_route = require_use_route();
    var import_icon = require_icon();
    var import_loading = require_loading();
    var [name, bem] = (0, import_utils.createNamespace)("button");
    var buttonProps = (0, import_utils.extend)({}, import_use_route.routeProps, {
      tag: (0, import_utils.makeStringProp)("button"),
      text: String,
      icon: String,
      type: (0, import_utils.makeStringProp)("default"),
      size: (0, import_utils.makeStringProp)("normal"),
      color: String,
      block: Boolean,
      plain: Boolean,
      round: Boolean,
      square: Boolean,
      loading: Boolean,
      hairline: Boolean,
      disabled: Boolean,
      iconPrefix: String,
      nativeType: (0, import_utils.makeStringProp)("button"),
      loadingSize: import_utils.numericProp,
      loadingText: String,
      loadingType: String,
      iconPosition: (0, import_utils.makeStringProp)("left")
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: buttonProps,
      emits: ["click"],
      setup(props, {
        emit,
        slots
      }) {
        const route = (0, import_use_route.useRoute)();
        const renderLoadingIcon = () => {
          if (slots.loading) {
            return slots.loading();
          }
          return (0, import_vue.createVNode)(import_loading.Loading, {
            "size": props.loadingSize,
            "type": props.loadingType,
            "class": bem("loading")
          }, null);
        };
        const renderIcon = () => {
          if (props.loading) {
            return renderLoadingIcon();
          }
          if (slots.icon) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("icon")
            }, [slots.icon()]);
          }
          if (props.icon) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "name": props.icon,
              "class": bem("icon"),
              "classPrefix": props.iconPrefix
            }, null);
          }
        };
        const renderText = () => {
          let text;
          if (props.loading) {
            text = props.loadingText;
          } else {
            text = slots.default ? slots.default() : props.text;
          }
          if (text) {
            return (0, import_vue.createVNode)("span", {
              "class": bem("text")
            }, [text]);
          }
        };
        const getStyle = () => {
          const {
            color,
            plain
          } = props;
          if (color) {
            const style = {
              color: plain ? color : "white"
            };
            if (!plain) {
              style.background = color;
            }
            if (color.includes("gradient")) {
              style.border = 0;
            } else {
              style.borderColor = color;
            }
            return style;
          }
        };
        const onClick = (event) => {
          if (props.loading) {
            (0, import_utils.preventDefault)(event);
          } else if (!props.disabled) {
            emit("click", event);
            route();
          }
        };
        return () => {
          const {
            tag,
            type,
            size,
            block,
            round,
            plain,
            square,
            loading,
            disabled,
            hairline,
            nativeType,
            iconPosition
          } = props;
          const classes = [bem([type, size, {
            plain,
            block,
            round,
            square,
            loading,
            disabled,
            hairline
          }]), {
            [import_utils.BORDER_SURROUND]: hairline
          }];
          return (0, import_vue.createVNode)(tag, {
            "type": nativeType,
            "class": classes,
            "style": getStyle(),
            "disabled": disabled,
            "onClick": onClick
          }, {
            default: () => [(0, import_vue.createVNode)("div", {
              "class": bem("content")
            }, [iconPosition === "left" && renderIcon(), renderText(), iconPosition === "right" && renderIcon()])]
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/button/index.js
var require_button = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/button/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Button: () => Button,
      buttonProps: () => import_Button2.buttonProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Button = __toESM(require_Button());
    var import_Button2 = require_Button();
    var Button = (0, import_utils.withInstall)(import_Button.default);
    var stdin_default = Button;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar-button/ActionBarButton.js
var require_ActionBarButton = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar-button/ActionBarButton.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      actionBarButtonProps: () => actionBarButtonProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_ActionBar = require_ActionBar();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_use_route = require_use_route();
    var import_button = require_button();
    var [name, bem] = (0, import_utils.createNamespace)("action-bar-button");
    var actionBarButtonProps = (0, import_utils.extend)({}, import_use_route.routeProps, {
      type: String,
      text: String,
      icon: String,
      color: String,
      loading: Boolean,
      disabled: Boolean
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: actionBarButtonProps,
      setup(props, {
        slots
      }) {
        const route = (0, import_use_route.useRoute)();
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_ActionBar.ACTION_BAR_KEY);
        const isFirst = (0, import_vue.computed)(() => {
          if (parent) {
            const prev = parent.children[index.value - 1];
            return !(prev && "isButton" in prev);
          }
        });
        const isLast = (0, import_vue.computed)(() => {
          if (parent) {
            const next = parent.children[index.value + 1];
            return !(next && "isButton" in next);
          }
        });
        (0, import_use_expose.useExpose)({
          isButton: true
        });
        return () => {
          const {
            type,
            icon,
            text,
            color,
            loading,
            disabled
          } = props;
          return (0, import_vue.createVNode)(import_button.Button, {
            "class": bem([type, {
              last: isLast.value,
              first: isFirst.value
            }]),
            "size": "large",
            "type": type,
            "icon": icon,
            "color": color,
            "loading": loading,
            "disabled": disabled,
            "onClick": route
          }, {
            default: () => [slots.default ? slots.default() : text]
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar-button/index.js
var require_action_bar_button = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar-button/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ActionBarButton: () => ActionBarButton,
      actionBarButtonProps: () => import_ActionBarButton2.actionBarButtonProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ActionBarButton = __toESM(require_ActionBarButton());
    var import_ActionBarButton2 = require_ActionBarButton();
    var ActionBarButton = (0, import_utils.withInstall)(import_ActionBarButton.default);
    var stdin_default = ActionBarButton;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar-icon/ActionBarIcon.js
var require_ActionBarIcon = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar-icon/ActionBarIcon.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      actionBarIconProps: () => actionBarIconProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_ActionBar = require_ActionBar();
    var import_use = require_index_cjs();
    var import_use_route = require_use_route();
    var import_icon = require_icon();
    var import_badge = require_badge();
    var [name, bem] = (0, import_utils.createNamespace)("action-bar-icon");
    var actionBarIconProps = (0, import_utils.extend)({}, import_use_route.routeProps, {
      dot: Boolean,
      text: String,
      icon: String,
      color: String,
      badge: import_utils.numericProp,
      iconClass: import_utils.unknownProp,
      badgeProps: Object,
      iconPrefix: String,
      disabled: Boolean
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: actionBarIconProps,
      setup(props, {
        slots
      }) {
        const route = (0, import_use_route.useRoute)();
        (0, import_use.useParent)(import_ActionBar.ACTION_BAR_KEY);
        const renderIcon = () => {
          const {
            dot,
            badge,
            icon,
            color,
            iconClass,
            badgeProps,
            iconPrefix
          } = props;
          if (slots.icon) {
            return (0, import_vue.createVNode)(import_badge.Badge, (0, import_vue.mergeProps)({
              "dot": dot,
              "class": bem("icon"),
              "content": badge
            }, badgeProps), {
              default: slots.icon
            });
          }
          return (0, import_vue.createVNode)(import_icon.Icon, {
            "tag": "div",
            "dot": dot,
            "name": icon,
            "badge": badge,
            "color": color,
            "class": [bem("icon"), iconClass],
            "badgeProps": badgeProps,
            "classPrefix": iconPrefix
          }, null);
        };
        const onClick = () => {
          if (!props.disabled) {
            route();
          }
        };
        return () => (0, import_vue.createVNode)("div", {
          "role": "button",
          "class": bem({
            disabled: props.disabled
          }),
          "tabindex": props.disabled ? -1 : 0,
          "onClick": onClick
        }, [renderIcon(), slots.default ? slots.default() : props.text]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar-icon/index.js
var require_action_bar_icon = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-bar-icon/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ActionBarIcon: () => ActionBarIcon,
      actionBarIconProps: () => import_ActionBarIcon2.actionBarIconProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ActionBarIcon = __toESM(require_ActionBarIcon());
    var import_ActionBarIcon2 = require_ActionBarIcon();
    var ActionBarIcon = (0, import_utils.withInstall)(import_ActionBarIcon.default);
    var stdin_default = ActionBarIcon;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popup/shared.js
var require_shared = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popup/shared.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      popupSharedPropKeys: () => popupSharedPropKeys,
      popupSharedProps: () => popupSharedProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var popupSharedProps = {
      // whether to show popup
      show: Boolean,
      // z-index
      zIndex: import_utils.numericProp,
      // whether to show overlay
      overlay: import_utils.truthProp,
      // transition duration
      duration: import_utils.numericProp,
      // teleport
      teleport: [String, Object],
      // prevent body scroll
      lockScroll: import_utils.truthProp,
      // whether to lazy render
      lazyRender: import_utils.truthProp,
      // callback function before close
      beforeClose: Function,
      // overlay props
      overlayProps: Object,
      // overlay custom style
      overlayStyle: Object,
      // overlay custom class name
      overlayClass: import_utils.unknownProp,
      // Initial rendering animation
      transitionAppear: Boolean,
      // whether to close popup when overlay is clicked
      closeOnClickOverlay: import_utils.truthProp
    };
    var popupSharedPropKeys = Object.keys(
      popupSharedProps
    );
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-touch.js
var require_use_touch = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-touch.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useTouch: () => useTouch
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    function getDirection(x, y) {
      if (x > y) {
        return "horizontal";
      }
      if (y > x) {
        return "vertical";
      }
      return "";
    }
    function useTouch() {
      const startX = (0, import_vue.ref)(0);
      const startY = (0, import_vue.ref)(0);
      const deltaX = (0, import_vue.ref)(0);
      const deltaY = (0, import_vue.ref)(0);
      const offsetX = (0, import_vue.ref)(0);
      const offsetY = (0, import_vue.ref)(0);
      const direction = (0, import_vue.ref)("");
      const isTap = (0, import_vue.ref)(true);
      const isVertical = () => direction.value === "vertical";
      const isHorizontal = () => direction.value === "horizontal";
      const reset = () => {
        deltaX.value = 0;
        deltaY.value = 0;
        offsetX.value = 0;
        offsetY.value = 0;
        direction.value = "";
        isTap.value = true;
      };
      const start = (event) => {
        reset();
        startX.value = event.touches[0].clientX;
        startY.value = event.touches[0].clientY;
      };
      const move = (event) => {
        const touch = event.touches[0];
        deltaX.value = (touch.clientX < 0 ? 0 : touch.clientX) - startX.value;
        deltaY.value = touch.clientY - startY.value;
        offsetX.value = Math.abs(deltaX.value);
        offsetY.value = Math.abs(deltaY.value);
        const LOCK_DIRECTION_DISTANCE = 10;
        if (!direction.value || offsetX.value < LOCK_DIRECTION_DISTANCE && offsetY.value < LOCK_DIRECTION_DISTANCE) {
          direction.value = getDirection(offsetX.value, offsetY.value);
        }
        if (isTap.value && (offsetX.value > import_utils.TAP_OFFSET || offsetY.value > import_utils.TAP_OFFSET)) {
          isTap.value = false;
        }
      };
      return {
        move,
        start,
        reset,
        startX,
        startY,
        deltaX,
        deltaY,
        offsetX,
        offsetY,
        direction,
        isVertical,
        isHorizontal,
        isTap
      };
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-lock-scroll.js
var require_use_lock_scroll = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-lock-scroll.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useLockScroll: () => useLockScroll
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use = require_index_cjs();
    var import_use_touch = require_use_touch();
    var import_utils = require_utils();
    var totalLockCount = 0;
    var BODY_LOCK_CLASS = "van-overflow-hidden";
    function useLockScroll(rootRef, shouldLock) {
      const touch = (0, import_use_touch.useTouch)();
      const DIRECTION_UP = "01";
      const DIRECTION_DOWN = "10";
      const onTouchMove = (event) => {
        touch.move(event);
        const direction = touch.deltaY.value > 0 ? DIRECTION_DOWN : DIRECTION_UP;
        const el = (0, import_use.getScrollParent)(
          event.target,
          rootRef.value
        );
        const { scrollHeight, offsetHeight, scrollTop } = el;
        let status = "11";
        if (scrollTop === 0) {
          status = offsetHeight >= scrollHeight ? "00" : "01";
        } else if (scrollTop + offsetHeight >= scrollHeight) {
          status = "10";
        }
        if (status !== "11" && touch.isVertical() && !(parseInt(status, 2) & parseInt(direction, 2))) {
          (0, import_utils.preventDefault)(event, true);
        }
      };
      const lock = () => {
        document.addEventListener("touchstart", touch.start);
        document.addEventListener("touchmove", onTouchMove, { passive: false });
        if (!totalLockCount) {
          document.body.classList.add(BODY_LOCK_CLASS);
        }
        totalLockCount++;
      };
      const unlock = () => {
        if (totalLockCount) {
          document.removeEventListener("touchstart", touch.start);
          document.removeEventListener("touchmove", onTouchMove);
          totalLockCount--;
          if (!totalLockCount) {
            document.body.classList.remove(BODY_LOCK_CLASS);
          }
        }
      };
      const init = () => shouldLock() && lock();
      const destroy = () => shouldLock() && unlock();
      (0, import_use.onMountedOrActivated)(init);
      (0, import_vue.onDeactivated)(destroy);
      (0, import_vue.onBeforeUnmount)(destroy);
      (0, import_vue.watch)(shouldLock, (value) => {
        value ? lock() : unlock();
      });
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-lazy-render.js
var require_use_lazy_render = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-lazy-render.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useLazyRender: () => useLazyRender
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    function useLazyRender(show) {
      const inited = (0, import_vue.ref)(false);
      (0, import_vue.watch)(
        show,
        (value) => {
          if (value) {
            inited.value = value;
          }
        },
        { immediate: true }
      );
      return (render) => () => inited.value ? render() : null;
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-scope-id.js
var require_use_scope_id = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-scope-id.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useScopeId: () => useScopeId
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var useScopeId = () => {
      var _a;
      const { scopeId } = ((_a = (0, import_vue.getCurrentInstance)()) == null ? void 0 : _a.vnode) || {};
      return scopeId ? { [scopeId]: "" } : null;
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/overlay/Overlay.js
var require_Overlay = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/overlay/Overlay.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      overlayProps: () => overlayProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_lazy_render = require_use_lazy_render();
    var [name, bem] = (0, import_utils.createNamespace)("overlay");
    var overlayProps = {
      show: Boolean,
      zIndex: import_utils.numericProp,
      duration: import_utils.numericProp,
      className: import_utils.unknownProp,
      lockScroll: import_utils.truthProp,
      lazyRender: import_utils.truthProp,
      customStyle: Object,
      teleport: [String, Object]
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      inheritAttrs: false,
      props: overlayProps,
      setup(props, {
        attrs,
        slots
      }) {
        const root = (0, import_vue.ref)();
        const lazyRender = (0, import_use_lazy_render.useLazyRender)(() => props.show || !props.lazyRender);
        const onTouchMove = (event) => {
          if (props.lockScroll) {
            (0, import_utils.preventDefault)(event, true);
          }
        };
        const renderOverlay = lazyRender(() => {
          var _a;
          const style = (0, import_utils.extend)((0, import_utils.getZIndexStyle)(props.zIndex), props.customStyle);
          if ((0, import_utils.isDef)(props.duration)) {
            style.animationDuration = `${props.duration}s`;
          }
          return (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", (0, import_vue.mergeProps)({
            "ref": root,
            "style": style,
            "class": [bem(), props.className]
          }, attrs), [(_a = slots.default) == null ? void 0 : _a.call(slots)]), [[import_vue.vShow, props.show]]);
        });
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: root
        });
        return () => {
          const Content = (0, import_vue.createVNode)(import_vue.Transition, {
            "name": "van-fade",
            "appear": true
          }, {
            default: renderOverlay
          });
          if (props.teleport) {
            return (0, import_vue.createVNode)(import_vue.Teleport, {
              "to": props.teleport
            }, {
              default: () => [Content]
            });
          }
          return Content;
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/overlay/index.js
var require_overlay = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/overlay/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Overlay: () => Overlay,
      default: () => stdin_default,
      overlayProps: () => import_Overlay2.overlayProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Overlay = __toESM(require_Overlay());
    var import_Overlay2 = require_Overlay();
    var Overlay = (0, import_utils.withInstall)(import_Overlay.default);
    var stdin_default = Overlay;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popup/Popup.js
var require_Popup = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popup/Popup.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      popupProps: () => popupProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_shared = require_shared();
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_use_lock_scroll = require_use_lock_scroll();
    var import_use_lazy_render = require_use_lazy_render();
    var import_on_popup_reopen = require_on_popup_reopen();
    var import_use_global_z_index = require_use_global_z_index();
    var import_use_scope_id = require_use_scope_id();
    var import_icon = require_icon();
    var import_overlay = require_overlay();
    var popupProps = (0, import_utils.extend)({}, import_shared.popupSharedProps, {
      round: Boolean,
      position: (0, import_utils.makeStringProp)("center"),
      closeIcon: (0, import_utils.makeStringProp)("cross"),
      closeable: Boolean,
      transition: String,
      iconPrefix: String,
      closeOnPopstate: Boolean,
      closeIconPosition: (0, import_utils.makeStringProp)("top-right"),
      destroyOnClose: Boolean,
      safeAreaInsetTop: Boolean,
      safeAreaInsetBottom: Boolean
    });
    var [name, bem] = (0, import_utils.createNamespace)("popup");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      inheritAttrs: false,
      props: popupProps,
      emits: ["open", "close", "opened", "closed", "keydown", "update:show", "clickOverlay", "clickCloseIcon"],
      setup(props, {
        emit,
        attrs,
        slots
      }) {
        let opened;
        let shouldReopen;
        const zIndex = (0, import_vue.ref)();
        const popupRef = (0, import_vue.ref)();
        const lazyRender = (0, import_use_lazy_render.useLazyRender)(() => props.show || !props.lazyRender);
        const style = (0, import_vue.computed)(() => {
          const style2 = {
            zIndex: zIndex.value
          };
          if ((0, import_utils.isDef)(props.duration)) {
            const key = props.position === "center" ? "animationDuration" : "transitionDuration";
            style2[key] = `${props.duration}s`;
          }
          return style2;
        });
        const open = () => {
          if (!opened) {
            opened = true;
            zIndex.value = props.zIndex !== void 0 ? +props.zIndex : (0, import_use_global_z_index.useGlobalZIndex)();
            emit("open");
          }
        };
        const close = () => {
          if (opened) {
            (0, import_utils.callInterceptor)(props.beforeClose, {
              done() {
                opened = false;
                emit("close");
                emit("update:show", false);
              }
            });
          }
        };
        const onClickOverlay = (event) => {
          emit("clickOverlay", event);
          if (props.closeOnClickOverlay) {
            close();
          }
        };
        const renderOverlay = () => {
          if (props.overlay) {
            const overlayProps = (0, import_utils.extend)({
              show: props.show,
              class: props.overlayClass,
              zIndex: zIndex.value,
              duration: props.duration,
              customStyle: props.overlayStyle,
              role: props.closeOnClickOverlay ? "button" : void 0,
              tabindex: props.closeOnClickOverlay ? 0 : void 0
            }, props.overlayProps);
            return (0, import_vue.createVNode)(import_overlay.Overlay, (0, import_vue.mergeProps)(overlayProps, (0, import_use_scope_id.useScopeId)(), {
              "onClick": onClickOverlay
            }), {
              default: slots["overlay-content"]
            });
          }
        };
        const onClickCloseIcon = (event) => {
          emit("clickCloseIcon", event);
          close();
        };
        const renderCloseIcon = () => {
          if (props.closeable) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "role": "button",
              "tabindex": 0,
              "name": props.closeIcon,
              "class": [bem("close-icon", props.closeIconPosition), import_utils.HAPTICS_FEEDBACK],
              "classPrefix": props.iconPrefix,
              "onClick": onClickCloseIcon
            }, null);
          }
        };
        let timer;
        const onOpened = () => {
          if (timer) clearTimeout(timer);
          timer = setTimeout(() => {
            emit("opened");
          });
        };
        const onClosed = () => emit("closed");
        const onKeydown = (event) => emit("keydown", event);
        const renderPopup = lazyRender(() => {
          var _a;
          const {
            destroyOnClose,
            round,
            position,
            safeAreaInsetTop,
            safeAreaInsetBottom,
            show
          } = props;
          if (!show && destroyOnClose) {
            return;
          }
          return (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", (0, import_vue.mergeProps)({
            "ref": popupRef,
            "style": style.value,
            "role": "dialog",
            "tabindex": 0,
            "class": [bem({
              round,
              [position]: position
            }), {
              "van-safe-area-top": safeAreaInsetTop,
              "van-safe-area-bottom": safeAreaInsetBottom
            }],
            "onKeydown": onKeydown
          }, attrs, (0, import_use_scope_id.useScopeId)()), [(_a = slots.default) == null ? void 0 : _a.call(slots), renderCloseIcon()]), [[import_vue.vShow, show]]);
        });
        const renderTransition = () => {
          const {
            position,
            transition,
            transitionAppear
          } = props;
          const name2 = position === "center" ? "van-fade" : `van-popup-slide-${position}`;
          return (0, import_vue.createVNode)(import_vue.Transition, {
            "name": transition || name2,
            "appear": transitionAppear,
            "onAfterEnter": onOpened,
            "onAfterLeave": onClosed
          }, {
            default: renderPopup
          });
        };
        (0, import_vue.watch)(() => props.show, (show) => {
          if (show && !opened) {
            open();
            if (attrs.tabindex === 0) {
              (0, import_vue.nextTick)(() => {
                var _a;
                (_a = popupRef.value) == null ? void 0 : _a.focus();
              });
            }
          }
          if (!show && opened) {
            opened = false;
            emit("close");
          }
        });
        (0, import_use_expose.useExpose)({
          popupRef
        });
        (0, import_use_lock_scroll.useLockScroll)(popupRef, () => props.show && props.lockScroll);
        (0, import_use.useEventListener)("popstate", () => {
          if (props.closeOnPopstate) {
            close();
            shouldReopen = false;
          }
        });
        (0, import_vue.onMounted)(() => {
          if (props.show) {
            open();
          }
        });
        (0, import_vue.onActivated)(() => {
          if (shouldReopen) {
            emit("update:show", true);
            shouldReopen = false;
          }
        });
        (0, import_vue.onDeactivated)(() => {
          if (props.show && props.teleport) {
            close();
            shouldReopen = true;
          }
        });
        (0, import_vue.provide)(import_on_popup_reopen.POPUP_TOGGLE_KEY, () => props.show);
        return () => {
          if (props.teleport) {
            return (0, import_vue.createVNode)(import_vue.Teleport, {
              "to": props.teleport
            }, {
              default: () => [renderOverlay(), renderTransition()]
            });
          }
          return (0, import_vue.createVNode)(import_vue.Fragment, null, [renderOverlay(), renderTransition()]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popup/index.js
var require_popup = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popup/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Popup: () => Popup,
      default: () => stdin_default,
      popupProps: () => import_Popup2.popupProps,
      setGlobalZIndex: () => import_use_global_z_index.setGlobalZIndex,
      useGlobalZIndex: () => import_use_global_z_index.useGlobalZIndex
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Popup = __toESM(require_Popup());
    var import_Popup2 = require_Popup();
    var import_use_global_z_index = require_use_global_z_index();
    var Popup = (0, import_utils.withInstall)(import_Popup.default);
    var stdin_default = Popup;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-sheet/ActionSheet.js
var require_ActionSheet = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-sheet/ActionSheet.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      actionSheetProps: () => actionSheetProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_icon = require_icon();
    var import_popup = require_popup();
    var import_loading = require_loading();
    var import_shared = require_shared();
    var [name, bem] = (0, import_utils.createNamespace)("action-sheet");
    var actionSheetProps = (0, import_utils.extend)({}, import_shared.popupSharedProps, {
      title: String,
      round: import_utils.truthProp,
      actions: (0, import_utils.makeArrayProp)(),
      closeIcon: (0, import_utils.makeStringProp)("cross"),
      closeable: import_utils.truthProp,
      cancelText: String,
      description: String,
      closeOnPopstate: import_utils.truthProp,
      closeOnClickAction: Boolean,
      safeAreaInsetBottom: import_utils.truthProp
    });
    var popupInheritKeys = [...import_shared.popupSharedPropKeys, "round", "closeOnPopstate", "safeAreaInsetBottom"];
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: actionSheetProps,
      emits: ["select", "cancel", "update:show"],
      setup(props, {
        slots,
        emit
      }) {
        const updateShow = (show) => emit("update:show", show);
        const onCancel = () => {
          updateShow(false);
          emit("cancel");
        };
        const renderHeader = () => {
          if (props.title) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("header")
            }, [props.title, props.closeable && (0, import_vue.createVNode)(import_icon.Icon, {
              "name": props.closeIcon,
              "class": [bem("close"), import_utils.HAPTICS_FEEDBACK],
              "onClick": onCancel
            }, null)]);
          }
        };
        const renderCancel = () => {
          if (slots.cancel || props.cancelText) {
            return [(0, import_vue.createVNode)("div", {
              "class": bem("gap")
            }, null), (0, import_vue.createVNode)("button", {
              "type": "button",
              "class": bem("cancel"),
              "onClick": onCancel
            }, [slots.cancel ? slots.cancel() : props.cancelText])];
          }
        };
        const renderIcon = (action) => {
          if (action.icon) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "class": bem("item-icon"),
              "name": action.icon
            }, null);
          }
        };
        const renderActionContent = (action, index) => {
          if (action.loading) {
            return (0, import_vue.createVNode)(import_loading.Loading, {
              "class": bem("loading-icon")
            }, null);
          }
          if (slots.action) {
            return slots.action({
              action,
              index
            });
          }
          return [(0, import_vue.createVNode)("span", {
            "class": bem("name")
          }, [action.name]), action.subname && (0, import_vue.createVNode)("div", {
            "class": bem("subname")
          }, [action.subname])];
        };
        const renderAction = (action, index) => {
          const {
            color,
            loading,
            callback,
            disabled,
            className
          } = action;
          const onClick = () => {
            if (disabled || loading) {
              return;
            }
            if (callback) {
              callback(action);
            }
            if (props.closeOnClickAction) {
              updateShow(false);
            }
            (0, import_vue.nextTick)(() => emit("select", action, index));
          };
          return (0, import_vue.createVNode)("button", {
            "type": "button",
            "style": {
              color
            },
            "class": [bem("item", {
              loading,
              disabled
            }), className],
            "onClick": onClick
          }, [renderIcon(action), renderActionContent(action, index)]);
        };
        const renderDescription = () => {
          if (props.description || slots.description) {
            const content = slots.description ? slots.description() : props.description;
            return (0, import_vue.createVNode)("div", {
              "class": bem("description")
            }, [content]);
          }
        };
        return () => (0, import_vue.createVNode)(import_popup.Popup, (0, import_vue.mergeProps)({
          "class": bem(),
          "position": "bottom",
          "onUpdate:show": updateShow
        }, (0, import_utils.pick)(props, popupInheritKeys)), {
          default: () => {
            var _a;
            return [renderHeader(), renderDescription(), (0, import_vue.createVNode)("div", {
              "class": bem("content")
            }, [props.actions.map(renderAction), (_a = slots.default) == null ? void 0 : _a.call(slots)]), renderCancel()];
          }
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-sheet/index.js
var require_action_sheet = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/action-sheet/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ActionSheet: () => ActionSheet,
      actionSheetProps: () => import_ActionSheet2.actionSheetProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ActionSheet = __toESM(require_ActionSheet());
    var import_ActionSheet2 = require_ActionSheet();
    var ActionSheet = (0, import_utils.withInstall)(import_ActionSheet.default);
    var stdin_default = ActionSheet;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/utils.js
var require_utils2 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      assignDefaultFields: () => assignDefaultFields,
      bem: () => bem,
      findIndexOfEnabledOption: () => findIndexOfEnabledOption,
      findOptionByValue: () => findOptionByValue,
      formatCascadeColumns: () => formatCascadeColumns,
      getColumnsType: () => getColumnsType,
      getElementTranslateY: () => getElementTranslateY,
      getFirstEnabledOption: () => getFirstEnabledOption,
      isOptionExist: () => isOptionExist,
      name: () => name,
      t: () => t
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var [name, bem, t] = (0, import_utils.createNamespace)("picker");
    var getFirstEnabledOption = (options) => options.find((option) => !option.disabled) || options[0];
    function getColumnsType(columns, fields) {
      const firstColumn = columns[0];
      if (firstColumn) {
        if (Array.isArray(firstColumn)) {
          return "multiple";
        }
        if (fields.children in firstColumn) {
          return "cascade";
        }
      }
      return "default";
    }
    function findIndexOfEnabledOption(options, index) {
      index = (0, import_utils.clamp)(index, 0, options.length);
      for (let i = index; i < options.length; i++) {
        if (!options[i].disabled) return i;
      }
      for (let i = index - 1; i >= 0; i--) {
        if (!options[i].disabled) return i;
      }
      return 0;
    }
    var isOptionExist = (options, value, fields) => value !== void 0 && options.some((option) => option[fields.value] === value);
    function findOptionByValue(options, value, fields) {
      const index = options.findIndex((option) => option[fields.value] === value);
      const enabledIndex = findIndexOfEnabledOption(options, index);
      return options[enabledIndex];
    }
    function formatCascadeColumns(columns, fields, selectedValues) {
      const formatted = [];
      let cursor = {
        [fields.children]: columns
      };
      let columnIndex = 0;
      while (cursor && cursor[fields.children]) {
        const options = cursor[fields.children];
        const value = selectedValues.value[columnIndex];
        cursor = (0, import_utils.isDef)(value) ? findOptionByValue(options, value, fields) : void 0;
        if (!cursor && options.length) {
          const firstValue = getFirstEnabledOption(options)[fields.value];
          cursor = findOptionByValue(options, firstValue, fields);
        }
        columnIndex++;
        formatted.push(options);
      }
      return formatted;
    }
    function getElementTranslateY(element) {
      const { transform } = window.getComputedStyle(element);
      const translateY = transform.slice(7, transform.length - 1).split(", ")[5];
      return Number(translateY);
    }
    function assignDefaultFields(fields) {
      return (0, import_utils.extend)(
        {
          text: "text",
          value: "value",
          children: "children"
        },
        fields
      );
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/PickerColumn.js
var require_PickerColumn = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/PickerColumn.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      PICKER_KEY: () => PICKER_KEY,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils2();
    var import_use = require_index_cjs();
    var import_use_touch = require_use_touch();
    var import_use_expose = require_use_expose();
    var DEFAULT_DURATION = 200;
    var MOMENTUM_TIME = 300;
    var MOMENTUM_DISTANCE = 15;
    var [name, bem] = (0, import_utils.createNamespace)("picker-column");
    var PICKER_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: {
        value: import_utils.numericProp,
        fields: (0, import_utils.makeRequiredProp)(Object),
        options: (0, import_utils.makeArrayProp)(),
        readonly: Boolean,
        allowHtml: Boolean,
        optionHeight: (0, import_utils.makeRequiredProp)(Number),
        swipeDuration: (0, import_utils.makeRequiredProp)(import_utils.numericProp),
        visibleOptionNum: (0, import_utils.makeRequiredProp)(import_utils.numericProp)
      },
      emits: ["change", "clickOption", "scrollInto"],
      setup(props, {
        emit,
        slots
      }) {
        let moving;
        let startOffset;
        let touchStartTime;
        let momentumOffset;
        let transitionEndTrigger;
        const root = (0, import_vue.ref)();
        const wrapper = (0, import_vue.ref)();
        const currentOffset = (0, import_vue.ref)(0);
        const currentDuration = (0, import_vue.ref)(0);
        const touch = (0, import_use_touch.useTouch)();
        const count = () => props.options.length;
        const baseOffset = () => props.optionHeight * (+props.visibleOptionNum - 1) / 2;
        const updateValueByIndex = (index) => {
          let enabledIndex = (0, import_utils2.findIndexOfEnabledOption)(props.options, index);
          const offset = -enabledIndex * props.optionHeight;
          const trigger = () => {
            if (enabledIndex > count() - 1) {
              enabledIndex = (0, import_utils2.findIndexOfEnabledOption)(props.options, index);
            }
            const value = props.options[enabledIndex][props.fields.value];
            if (value !== props.value) {
              emit("change", value);
            }
          };
          if (moving && offset !== currentOffset.value) {
            transitionEndTrigger = trigger;
          } else {
            trigger();
          }
          currentOffset.value = offset;
        };
        const isReadonly = () => props.readonly || !props.options.length;
        const onClickOption = (index) => {
          if (moving || isReadonly()) {
            return;
          }
          transitionEndTrigger = null;
          currentDuration.value = DEFAULT_DURATION;
          updateValueByIndex(index);
          emit("clickOption", props.options[index]);
        };
        const getIndexByOffset = (offset) => (0, import_utils.clamp)(Math.round(-offset / props.optionHeight), 0, count() - 1);
        const currentIndex = (0, import_vue.computed)(() => getIndexByOffset(currentOffset.value));
        const momentum = (distance, duration) => {
          const speed = Math.abs(distance / duration);
          distance = currentOffset.value + speed / 3e-3 * (distance < 0 ? -1 : 1);
          const index = getIndexByOffset(distance);
          currentDuration.value = +props.swipeDuration;
          updateValueByIndex(index);
        };
        const stopMomentum = () => {
          moving = false;
          currentDuration.value = 0;
          if (transitionEndTrigger) {
            transitionEndTrigger();
            transitionEndTrigger = null;
          }
        };
        const onTouchStart = (event) => {
          if (isReadonly()) {
            return;
          }
          touch.start(event);
          if (moving) {
            const translateY = (0, import_utils2.getElementTranslateY)(wrapper.value);
            currentOffset.value = Math.min(0, translateY - baseOffset());
          }
          currentDuration.value = 0;
          startOffset = currentOffset.value;
          touchStartTime = Date.now();
          momentumOffset = startOffset;
          transitionEndTrigger = null;
        };
        const onTouchMove = (event) => {
          if (isReadonly()) {
            return;
          }
          touch.move(event);
          if (touch.isVertical()) {
            moving = true;
            (0, import_utils.preventDefault)(event, true);
          }
          const newOffset = (0, import_utils.clamp)(startOffset + touch.deltaY.value, -(count() * props.optionHeight), props.optionHeight);
          const newIndex = getIndexByOffset(newOffset);
          if (newIndex !== currentIndex.value) {
            emit("scrollInto", props.options[newIndex]);
          }
          currentOffset.value = newOffset;
          const now = Date.now();
          if (now - touchStartTime > MOMENTUM_TIME) {
            touchStartTime = now;
            momentumOffset = newOffset;
          }
        };
        const onTouchEnd = () => {
          if (isReadonly()) {
            return;
          }
          const distance = currentOffset.value - momentumOffset;
          const duration = Date.now() - touchStartTime;
          const startMomentum = duration < MOMENTUM_TIME && Math.abs(distance) > MOMENTUM_DISTANCE;
          if (startMomentum) {
            momentum(distance, duration);
            return;
          }
          const index = getIndexByOffset(currentOffset.value);
          currentDuration.value = DEFAULT_DURATION;
          updateValueByIndex(index);
          setTimeout(() => {
            moving = false;
          }, 0);
        };
        const renderOptions = () => {
          const optionStyle = {
            height: `${props.optionHeight}px`
          };
          return props.options.map((option, index) => {
            const text = option[props.fields.text];
            const {
              disabled
            } = option;
            const value = option[props.fields.value];
            const data = {
              role: "button",
              style: optionStyle,
              tabindex: disabled ? -1 : 0,
              class: [bem("item", {
                disabled,
                selected: value === props.value
              }), option.className],
              onClick: () => onClickOption(index)
            };
            const childData = {
              class: "van-ellipsis",
              [props.allowHtml ? "innerHTML" : "textContent"]: text
            };
            return (0, import_vue.createVNode)("li", data, [slots.option ? slots.option(option, index) : (0, import_vue.createVNode)("div", childData, null)]);
          });
        };
        (0, import_use.useParent)(PICKER_KEY);
        (0, import_use_expose.useExpose)({
          stopMomentum
        });
        (0, import_vue.watchEffect)(() => {
          const index = moving ? Math.floor(-currentOffset.value / props.optionHeight) : props.options.findIndex((option) => option[props.fields.value] === props.value);
          const enabledIndex = (0, import_utils2.findIndexOfEnabledOption)(props.options, index);
          const offset = -enabledIndex * props.optionHeight;
          if (moving && enabledIndex < index) stopMomentum();
          currentOffset.value = offset;
        });
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: root
        });
        return () => (0, import_vue.createVNode)("div", {
          "ref": root,
          "class": bem(),
          "onTouchstartPassive": onTouchStart,
          "onTouchend": onTouchEnd,
          "onTouchcancel": onTouchEnd
        }, [(0, import_vue.createVNode)("ul", {
          "ref": wrapper,
          "style": {
            transform: `translate3d(0, ${currentOffset.value + baseOffset()}px, 0)`,
            transitionDuration: `${currentDuration.value}ms`,
            transitionProperty: currentDuration.value ? "all" : "none"
          },
          "class": bem("wrapper"),
          "onTransitionend": stopMomentum
        }, [renderOptions()])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/PickerToolbar.js
var require_PickerToolbar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/PickerToolbar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      pickerToolbarPropKeys: () => pickerToolbarPropKeys,
      pickerToolbarProps: () => pickerToolbarProps,
      pickerToolbarSlots: () => pickerToolbarSlots
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils2();
    var import_utils2 = require_utils();
    var [name] = (0, import_utils2.createNamespace)("picker-toolbar");
    var pickerToolbarProps = {
      title: String,
      cancelButtonText: String,
      confirmButtonText: String
    };
    var pickerToolbarSlots = ["cancel", "confirm", "title", "toolbar"];
    var pickerToolbarPropKeys = Object.keys(pickerToolbarProps);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: pickerToolbarProps,
      emits: ["confirm", "cancel"],
      setup(props, {
        emit,
        slots
      }) {
        const renderTitle = () => {
          if (slots.title) {
            return slots.title();
          }
          if (props.title) {
            return (0, import_vue.createVNode)("div", {
              "class": [(0, import_utils.bem)("title"), "van-ellipsis"]
            }, [props.title]);
          }
        };
        const onCancel = () => emit("cancel");
        const onConfirm = () => emit("confirm");
        const renderCancel = () => {
          var _a;
          const text = (_a = props.cancelButtonText) != null ? _a : (0, import_utils.t)("cancel");
          if (!slots.cancel && !text) {
            return;
          }
          return (0, import_vue.createVNode)("button", {
            "type": "button",
            "class": [(0, import_utils.bem)("cancel"), import_utils2.HAPTICS_FEEDBACK],
            "onClick": onCancel
          }, [slots.cancel ? slots.cancel() : text]);
        };
        const renderConfirm = () => {
          var _a;
          const text = (_a = props.confirmButtonText) != null ? _a : (0, import_utils.t)("confirm");
          if (!slots.confirm && !text) {
            return;
          }
          return (0, import_vue.createVNode)("button", {
            "type": "button",
            "class": [(0, import_utils.bem)("confirm"), import_utils2.HAPTICS_FEEDBACK],
            "onClick": onConfirm
          }, [slots.confirm ? slots.confirm() : text]);
        };
        return () => (0, import_vue.createVNode)("div", {
          "class": (0, import_utils.bem)("toolbar")
        }, [slots.toolbar ? slots.toolbar() : [renderCancel(), renderTitle(), renderConfirm()]]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-sync-prop-ref.js
var require_use_sync_prop_ref = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-sync-prop-ref.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useSyncPropRef: () => useSyncPropRef
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var useSyncPropRef = (getProp, setProp) => {
      const propRef = (0, import_vue.ref)(getProp());
      (0, import_vue.watch)(getProp, (value) => {
        if (value !== propRef.value) {
          propRef.value = value;
        }
      });
      (0, import_vue.watch)(propRef, (value) => {
        if (value !== getProp()) {
          setProp(value);
        }
      });
      return propRef;
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabs/utils.js
var require_utils3 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabs/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      scrollLeftTo: () => scrollLeftTo,
      scrollTopTo: () => scrollTopTo
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_use = require_index_cjs();
    var import_utils = require_utils();
    function scrollLeftTo(scroller, to, duration) {
      let rafId;
      let count = 0;
      const from = scroller.scrollLeft;
      const frames = duration === 0 ? 1 : Math.round(duration * 1e3 / 16);
      let scrollLeft = from;
      function cancel() {
        (0, import_use.cancelRaf)(rafId);
      }
      function animate() {
        scrollLeft += (to - from) / frames;
        scroller.scrollLeft = scrollLeft;
        if (++count < frames) {
          rafId = (0, import_use.raf)(animate);
        }
      }
      animate();
      return cancel;
    }
    function scrollTopTo(scroller, to, duration, callback) {
      let rafId;
      let current = (0, import_utils.getScrollTop)(scroller);
      const isDown = current < to;
      const frames = duration === 0 ? 1 : Math.round(duration * 1e3 / 16);
      const step = (to - current) / frames;
      function cancel() {
        (0, import_use.cancelRaf)(rafId);
      }
      function animate() {
        current += step;
        if (isDown && current > to || !isDown && current < to) {
          current = to;
        }
        (0, import_utils.setScrollTop)(scroller, current);
        if (isDown && current < to || !isDown && current > to) {
          rafId = (0, import_use.raf)(animate);
        } else if (callback) {
          rafId = (0, import_use.raf)(callback);
        }
      }
      animate();
      return cancel;
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-id.js
var require_use_id = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-id.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useId: () => useId
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var current = 0;
    function useId() {
      const vm = (0, import_vue.getCurrentInstance)();
      const { name = "unknown" } = (vm == null ? void 0 : vm.type) || {};
      if (false) {
        return name;
      }
      return `${name}-${++current}`;
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-refs.js
var require_use_refs = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-refs.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useRefs: () => useRefs
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    function useRefs() {
      const refs = (0, import_vue.ref)([]);
      const cache = [];
      (0, import_vue.onBeforeUpdate)(() => {
        refs.value = [];
      });
      const setRefs = (index) => {
        if (!cache[index]) {
          cache[index] = (el) => {
            refs.value[index] = el;
          };
        }
        return cache[index];
      };
      return [refs, setRefs];
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-visibility-change.js
var require_use_visibility_change = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-visibility-change.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      useVisibilityChange: () => useVisibilityChange
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use = require_index_cjs();
    function useVisibilityChange(target, onChange) {
      if (!import_utils.inBrowser || !window.IntersectionObserver) {
        return;
      }
      const observer = new IntersectionObserver(
        (entries) => {
          onChange(entries[0].intersectionRatio > 0);
        },
        { root: document.body }
      );
      const observe = () => {
        if (target.value) {
          observer.observe(target.value);
        }
      };
      const unobserve = () => {
        if (target.value) {
          observer.unobserve(target.value);
        }
      };
      (0, import_vue.onDeactivated)(unobserve);
      (0, import_vue.onBeforeUnmount)(unobserve);
      (0, import_use.onMountedOrActivated)(observe);
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sticky/Sticky.js
var require_Sticky = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sticky/Sticky.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      stickyProps: () => stickyProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_visibility_change = require_use_visibility_change();
    var [name, bem] = (0, import_utils.createNamespace)("sticky");
    var stickyProps = {
      zIndex: import_utils.numericProp,
      position: (0, import_utils.makeStringProp)("top"),
      container: Object,
      offsetTop: (0, import_utils.makeNumericProp)(0),
      offsetBottom: (0, import_utils.makeNumericProp)(0)
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: stickyProps,
      emits: ["scroll", "change"],
      setup(props, {
        emit,
        slots
      }) {
        const root = (0, import_vue.ref)();
        const scrollParent = (0, import_use.useScrollParent)(root);
        const state = (0, import_vue.reactive)({
          fixed: false,
          width: 0,
          // root width
          height: 0,
          // root height
          transform: 0
        });
        const isReset = (0, import_vue.ref)(false);
        const offset = (0, import_vue.computed)(() => (0, import_utils.unitToPx)(props.position === "top" ? props.offsetTop : props.offsetBottom));
        const rootStyle = (0, import_vue.computed)(() => {
          if (isReset.value) {
            return;
          }
          const {
            fixed,
            height,
            width
          } = state;
          if (fixed) {
            return {
              width: `${width}px`,
              height: `${height}px`
            };
          }
        });
        const stickyStyle = (0, import_vue.computed)(() => {
          if (!state.fixed || isReset.value) {
            return;
          }
          const style = (0, import_utils.extend)((0, import_utils.getZIndexStyle)(props.zIndex), {
            width: `${state.width}px`,
            height: `${state.height}px`,
            [props.position]: `${offset.value}px`
          });
          if (state.transform) {
            style.transform = `translate3d(0, ${state.transform}px, 0)`;
          }
          return style;
        });
        const emitScroll = (scrollTop) => emit("scroll", {
          scrollTop,
          isFixed: state.fixed
        });
        const onScroll = () => {
          if (!root.value || (0, import_utils.isHidden)(root)) {
            return;
          }
          const {
            container,
            position
          } = props;
          const rootRect = (0, import_use.useRect)(root);
          const scrollTop = (0, import_utils.getScrollTop)(window);
          state.width = rootRect.width;
          state.height = rootRect.height;
          if (position === "top") {
            if (container) {
              const containerRect = (0, import_use.useRect)(container);
              const difference = containerRect.bottom - offset.value - state.height;
              state.fixed = offset.value > rootRect.top && containerRect.bottom > 0;
              state.transform = difference < 0 ? difference : 0;
            } else {
              state.fixed = offset.value > rootRect.top;
            }
          } else {
            const {
              clientHeight
            } = document.documentElement;
            if (container) {
              const containerRect = (0, import_use.useRect)(container);
              const difference = clientHeight - containerRect.top - offset.value - state.height;
              state.fixed = clientHeight - offset.value < rootRect.bottom && clientHeight > containerRect.top;
              state.transform = difference < 0 ? -difference : 0;
            } else {
              state.fixed = clientHeight - offset.value < rootRect.bottom;
            }
          }
          emitScroll(scrollTop);
        };
        (0, import_vue.watch)(() => state.fixed, (value) => emit("change", value));
        (0, import_use.useEventListener)("scroll", onScroll, {
          target: scrollParent,
          passive: true
        });
        (0, import_use_visibility_change.useVisibilityChange)(root, onScroll);
        (0, import_vue.watch)([import_utils.windowWidth, import_utils.windowHeight], () => {
          if (!root.value || (0, import_utils.isHidden)(root) || !state.fixed) {
            return;
          }
          isReset.value = true;
          (0, import_vue.nextTick)(() => {
            const rootRect = (0, import_use.useRect)(root);
            state.width = rootRect.width;
            state.height = rootRect.height;
            isReset.value = false;
          });
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "style": rootStyle.value
          }, [(0, import_vue.createVNode)("div", {
            "class": bem({
              fixed: state.fixed && !isReset.value
            }),
            "style": stickyStyle.value
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sticky/index.js
var require_sticky = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sticky/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Sticky: () => Sticky,
      default: () => stdin_default,
      stickyProps: () => import_Sticky2.stickyProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Sticky = __toESM(require_Sticky());
    var import_Sticky2 = require_Sticky();
    var Sticky = (0, import_utils.withInstall)(import_Sticky.default);
    var stdin_default = Sticky;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe/Swipe.js
var require_Swipe = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe/Swipe.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SWIPE_KEY: () => SWIPE_KEY,
      default: () => stdin_default,
      swipeProps: () => swipeProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_touch = require_use_touch();
    var import_use_expose = require_use_expose();
    var import_on_popup_reopen = require_on_popup_reopen();
    var [name, bem] = (0, import_utils.createNamespace)("swipe");
    var swipeProps = {
      loop: import_utils.truthProp,
      width: import_utils.numericProp,
      height: import_utils.numericProp,
      vertical: Boolean,
      autoplay: (0, import_utils.makeNumericProp)(0),
      duration: (0, import_utils.makeNumericProp)(500),
      touchable: import_utils.truthProp,
      lazyRender: Boolean,
      initialSwipe: (0, import_utils.makeNumericProp)(0),
      indicatorColor: String,
      showIndicators: import_utils.truthProp,
      stopPropagation: import_utils.truthProp
    };
    var SWIPE_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: swipeProps,
      emits: ["change", "dragStart", "dragEnd"],
      setup(props, {
        emit,
        slots
      }) {
        const root = (0, import_vue.ref)();
        const track = (0, import_vue.ref)();
        const state = (0, import_vue.reactive)({
          rect: null,
          width: 0,
          height: 0,
          offset: 0,
          active: 0,
          swiping: false
        });
        let dragging = false;
        const touch = (0, import_use_touch.useTouch)();
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(SWIPE_KEY);
        const count = (0, import_vue.computed)(() => children.length);
        const size = (0, import_vue.computed)(() => state[props.vertical ? "height" : "width"]);
        const delta = (0, import_vue.computed)(() => props.vertical ? touch.deltaY.value : touch.deltaX.value);
        const minOffset = (0, import_vue.computed)(() => {
          if (state.rect) {
            const base = props.vertical ? state.rect.height : state.rect.width;
            return base - size.value * count.value;
          }
          return 0;
        });
        const maxCount = (0, import_vue.computed)(() => size.value ? Math.ceil(Math.abs(minOffset.value) / size.value) : count.value);
        const trackSize = (0, import_vue.computed)(() => count.value * size.value);
        const activeIndicator = (0, import_vue.computed)(() => (state.active + count.value) % count.value);
        const isCorrectDirection = (0, import_vue.computed)(() => {
          const expect = props.vertical ? "vertical" : "horizontal";
          return touch.direction.value === expect;
        });
        const trackStyle = (0, import_vue.computed)(() => {
          const style = {
            transitionDuration: `${state.swiping ? 0 : props.duration}ms`,
            transform: `translate${props.vertical ? "Y" : "X"}(${+state.offset.toFixed(2)}px)`
          };
          if (size.value) {
            const mainAxis = props.vertical ? "height" : "width";
            const crossAxis = props.vertical ? "width" : "height";
            style[mainAxis] = `${trackSize.value}px`;
            style[crossAxis] = props[crossAxis] ? `${props[crossAxis]}px` : "";
          }
          return style;
        });
        const getTargetActive = (pace) => {
          const {
            active
          } = state;
          if (pace) {
            if (props.loop) {
              return (0, import_utils.clamp)(active + pace, -1, count.value);
            }
            return (0, import_utils.clamp)(active + pace, 0, maxCount.value);
          }
          return active;
        };
        const getTargetOffset = (targetActive, offset = 0) => {
          let currentPosition = targetActive * size.value;
          if (!props.loop) {
            currentPosition = Math.min(currentPosition, -minOffset.value);
          }
          let targetOffset = offset - currentPosition;
          if (!props.loop) {
            targetOffset = (0, import_utils.clamp)(targetOffset, minOffset.value, 0);
          }
          return targetOffset;
        };
        const move = ({
          pace = 0,
          offset = 0,
          emitChange
        }) => {
          if (count.value <= 1) {
            return;
          }
          const {
            active
          } = state;
          const targetActive = getTargetActive(pace);
          const targetOffset = getTargetOffset(targetActive, offset);
          if (props.loop) {
            if (children[0] && targetOffset !== minOffset.value) {
              const outRightBound = targetOffset < minOffset.value;
              children[0].setOffset(outRightBound ? trackSize.value : 0);
            }
            if (children[count.value - 1] && targetOffset !== 0) {
              const outLeftBound = targetOffset > 0;
              children[count.value - 1].setOffset(outLeftBound ? -trackSize.value : 0);
            }
          }
          state.active = targetActive;
          state.offset = targetOffset;
          if (emitChange && targetActive !== active) {
            emit("change", activeIndicator.value);
          }
        };
        const correctPosition = () => {
          state.swiping = true;
          if (state.active <= -1) {
            move({
              pace: count.value
            });
          } else if (state.active >= count.value) {
            move({
              pace: -count.value
            });
          }
        };
        const prev = () => {
          correctPosition();
          touch.reset();
          (0, import_use.doubleRaf)(() => {
            state.swiping = false;
            move({
              pace: -1,
              emitChange: true
            });
          });
        };
        const next = () => {
          correctPosition();
          touch.reset();
          (0, import_use.doubleRaf)(() => {
            state.swiping = false;
            move({
              pace: 1,
              emitChange: true
            });
          });
        };
        let autoplayTimer;
        const stopAutoplay = () => clearTimeout(autoplayTimer);
        const autoplay = () => {
          stopAutoplay();
          if (+props.autoplay > 0 && count.value > 1) {
            autoplayTimer = setTimeout(() => {
              next();
              autoplay();
            }, +props.autoplay);
          }
        };
        const initialize = (active = +props.initialSwipe) => {
          if (!root.value) {
            return;
          }
          const cb = () => {
            var _a, _b;
            if (!(0, import_utils.isHidden)(root)) {
              const rect = {
                width: root.value.offsetWidth,
                height: root.value.offsetHeight
              };
              state.rect = rect;
              state.width = +((_a = props.width) != null ? _a : rect.width);
              state.height = +((_b = props.height) != null ? _b : rect.height);
            }
            if (count.value) {
              active = Math.min(count.value - 1, active);
              if (active === -1) {
                active = count.value - 1;
              }
            }
            state.active = active;
            state.swiping = true;
            state.offset = getTargetOffset(active);
            children.forEach((swipe) => {
              swipe.setOffset(0);
            });
            autoplay();
          };
          if ((0, import_utils.isHidden)(root)) {
            (0, import_vue.nextTick)().then(cb);
          } else {
            cb();
          }
        };
        const resize = () => initialize(state.active);
        let touchStartTime;
        const onTouchStart = (event) => {
          if (!props.touchable || // avoid resetting position on multi-finger touch
          event.touches.length > 1) return;
          touch.start(event);
          dragging = false;
          touchStartTime = Date.now();
          stopAutoplay();
          correctPosition();
        };
        const onTouchMove = (event) => {
          if (props.touchable && state.swiping) {
            touch.move(event);
            if (isCorrectDirection.value) {
              const isEdgeTouch = !props.loop && (state.active === 0 && delta.value > 0 || state.active === count.value - 1 && delta.value < 0);
              if (!isEdgeTouch) {
                (0, import_utils.preventDefault)(event, props.stopPropagation);
                move({
                  offset: delta.value
                });
                if (!dragging) {
                  emit("dragStart", {
                    index: activeIndicator.value
                  });
                  dragging = true;
                }
              }
            }
          }
        };
        const onTouchEnd = () => {
          if (!props.touchable || !state.swiping) {
            return;
          }
          const duration = Date.now() - touchStartTime;
          const speed = delta.value / duration;
          const shouldSwipe = Math.abs(speed) > 0.25 || Math.abs(delta.value) > size.value / 2;
          if (shouldSwipe && isCorrectDirection.value) {
            const offset = props.vertical ? touch.offsetY.value : touch.offsetX.value;
            let pace = 0;
            if (props.loop) {
              pace = offset > 0 ? delta.value > 0 ? -1 : 1 : 0;
            } else {
              pace = -Math[delta.value > 0 ? "ceil" : "floor"](delta.value / size.value);
            }
            move({
              pace,
              emitChange: true
            });
          } else if (delta.value) {
            move({
              pace: 0
            });
          }
          dragging = false;
          state.swiping = false;
          emit("dragEnd", {
            index: activeIndicator.value
          });
          autoplay();
        };
        const swipeTo = (index, options = {}) => {
          correctPosition();
          touch.reset();
          (0, import_use.doubleRaf)(() => {
            let targetIndex;
            if (props.loop && index === count.value) {
              targetIndex = state.active === 0 ? 0 : index;
            } else {
              targetIndex = index % count.value;
            }
            if (options.immediate) {
              (0, import_use.doubleRaf)(() => {
                state.swiping = false;
              });
            } else {
              state.swiping = false;
            }
            move({
              pace: targetIndex - state.active,
              emitChange: true
            });
          });
        };
        const renderDot = (_, index) => {
          const active = index === activeIndicator.value;
          const style = active ? {
            backgroundColor: props.indicatorColor
          } : void 0;
          return (0, import_vue.createVNode)("i", {
            "style": style,
            "class": bem("indicator", {
              active
            })
          }, null);
        };
        const renderIndicator = () => {
          if (slots.indicator) {
            return slots.indicator({
              active: activeIndicator.value,
              total: count.value
            });
          }
          if (props.showIndicators && count.value > 1) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("indicators", {
                vertical: props.vertical
              })
            }, [Array(count.value).fill("").map(renderDot)]);
          }
        };
        (0, import_use_expose.useExpose)({
          prev,
          next,
          state,
          resize,
          swipeTo
        });
        linkChildren({
          size,
          props,
          count,
          activeIndicator
        });
        (0, import_vue.watch)(() => props.initialSwipe, (value) => initialize(+value));
        (0, import_vue.watch)(count, () => initialize(state.active));
        (0, import_vue.watch)(() => props.autoplay, autoplay);
        (0, import_vue.watch)([import_utils.windowWidth, import_utils.windowHeight, () => props.width, () => props.height], resize);
        (0, import_vue.watch)((0, import_use.usePageVisibility)(), (visible) => {
          if (visible === "visible") {
            autoplay();
          } else {
            stopAutoplay();
          }
        });
        (0, import_vue.onMounted)(initialize);
        (0, import_vue.onActivated)(() => initialize(state.active));
        (0, import_on_popup_reopen.onPopupReopen)(() => initialize(state.active));
        (0, import_vue.onDeactivated)(stopAutoplay);
        (0, import_vue.onBeforeUnmount)(stopAutoplay);
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: track
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "class": bem()
          }, [(0, import_vue.createVNode)("div", {
            "ref": track,
            "style": trackStyle.value,
            "class": bem("track", {
              vertical: props.vertical
            }),
            "onTouchstartPassive": onTouchStart,
            "onTouchend": onTouchEnd,
            "onTouchcancel": onTouchEnd
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]), renderIndicator()]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe/index.js
var require_swipe = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Swipe: () => Swipe,
      default: () => stdin_default,
      swipeProps: () => import_Swipe2.swipeProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Swipe = __toESM(require_Swipe());
    var import_Swipe2 = require_Swipe();
    var Swipe = (0, import_utils.withInstall)(import_Swipe.default);
    var stdin_default = Swipe;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabs/TabsContent.js
var require_TabsContent = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabs/TabsContent.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_swipe = require_swipe();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("tabs");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: {
        count: (0, import_utils.makeRequiredProp)(Number),
        inited: Boolean,
        animated: Boolean,
        duration: (0, import_utils.makeRequiredProp)(import_utils.numericProp),
        swipeable: Boolean,
        lazyRender: Boolean,
        currentIndex: (0, import_utils.makeRequiredProp)(Number)
      },
      emits: ["change"],
      setup(props, {
        emit,
        slots
      }) {
        const swipeRef = (0, import_vue.ref)();
        const onChange = (index) => emit("change", index);
        const renderChildren = () => {
          var _a;
          const Content = (_a = slots.default) == null ? void 0 : _a.call(slots);
          if (props.animated || props.swipeable) {
            return (0, import_vue.createVNode)(import_swipe.Swipe, {
              "ref": swipeRef,
              "loop": false,
              "class": bem("track"),
              "duration": +props.duration * 1e3,
              "touchable": props.swipeable,
              "lazyRender": props.lazyRender,
              "showIndicators": false,
              "onChange": onChange
            }, {
              default: () => [Content]
            });
          }
          return Content;
        };
        const swipeToCurrentTab = (index) => {
          const swipe = swipeRef.value;
          if (swipe && swipe.state.active !== index) {
            swipe.swipeTo(index, {
              immediate: !props.inited
            });
          }
        };
        (0, import_vue.watch)(() => props.currentIndex, swipeToCurrentTab);
        (0, import_vue.onMounted)(() => {
          swipeToCurrentTab(props.currentIndex);
        });
        (0, import_use_expose.useExpose)({
          swipeRef
        });
        return () => (0, import_vue.createVNode)("div", {
          "class": bem("content", {
            animated: props.animated || props.swipeable
          })
        }, [renderChildren()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabs/Tabs.js
var require_Tabs = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabs/Tabs.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      TABS_KEY: () => TABS_KEY,
      default: () => stdin_default,
      tabsProps: () => tabsProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils3();
    var import_use = require_index_cjs();
    var import_use_id = require_use_id();
    var import_use_route = require_use_route();
    var import_use_refs = require_use_refs();
    var import_use_expose = require_use_expose();
    var import_on_popup_reopen = require_on_popup_reopen();
    var import_use_visibility_change = require_use_visibility_change();
    var import_sticky = require_sticky();
    var import_TabsContent = __toESM(require_TabsContent());
    var [name, bem] = (0, import_utils.createNamespace)("tabs");
    var tabsProps = {
      type: (0, import_utils.makeStringProp)("line"),
      color: String,
      border: Boolean,
      sticky: Boolean,
      shrink: Boolean,
      active: (0, import_utils.makeNumericProp)(0),
      duration: (0, import_utils.makeNumericProp)(0.3),
      animated: Boolean,
      ellipsis: import_utils.truthProp,
      swipeable: Boolean,
      scrollspy: Boolean,
      offsetTop: (0, import_utils.makeNumericProp)(0),
      background: String,
      lazyRender: import_utils.truthProp,
      showHeader: import_utils.truthProp,
      lineWidth: import_utils.numericProp,
      lineHeight: import_utils.numericProp,
      beforeChange: Function,
      swipeThreshold: (0, import_utils.makeNumericProp)(5),
      titleActiveColor: String,
      titleInactiveColor: String
    };
    var TABS_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: tabsProps,
      emits: ["change", "scroll", "rendered", "clickTab", "update:active"],
      setup(props, {
        emit,
        slots
      }) {
        let tabHeight;
        let lockScroll;
        let stickyFixed;
        let cancelScrollLeftToRaf;
        let cancelScrollTopToRaf;
        const root = (0, import_vue.ref)();
        const navRef = (0, import_vue.ref)();
        const wrapRef = (0, import_vue.ref)();
        const contentRef = (0, import_vue.ref)();
        const id = (0, import_use_id.useId)();
        const scroller = (0, import_use.useScrollParent)(root);
        const [titleRefs, setTitleRefs] = (0, import_use_refs.useRefs)();
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(TABS_KEY);
        const state = (0, import_vue.reactive)({
          inited: false,
          position: "",
          lineStyle: {},
          currentIndex: -1
        });
        const scrollable = (0, import_vue.computed)(() => children.length > +props.swipeThreshold || !props.ellipsis || props.shrink);
        const navStyle = (0, import_vue.computed)(() => ({
          borderColor: props.color,
          background: props.background
        }));
        const getTabName = (tab, index) => {
          var _a;
          return (_a = tab.name) != null ? _a : index;
        };
        const currentName = (0, import_vue.computed)(() => {
          const activeTab = children[state.currentIndex];
          if (activeTab) {
            return getTabName(activeTab, state.currentIndex);
          }
        });
        const offsetTopPx = (0, import_vue.computed)(() => (0, import_utils.unitToPx)(props.offsetTop));
        const scrollOffset = (0, import_vue.computed)(() => {
          if (props.sticky) {
            return offsetTopPx.value + tabHeight;
          }
          return 0;
        });
        const scrollIntoView = (immediate) => {
          const nav = navRef.value;
          const titles = titleRefs.value;
          if (!scrollable.value || !nav || !titles || !titles[state.currentIndex]) {
            return;
          }
          const title = titles[state.currentIndex].$el;
          const to = title.offsetLeft - (nav.offsetWidth - title.offsetWidth) / 2;
          if (cancelScrollLeftToRaf) cancelScrollLeftToRaf();
          cancelScrollLeftToRaf = (0, import_utils2.scrollLeftTo)(nav, to, immediate ? 0 : +props.duration);
        };
        const setLine = () => {
          const shouldAnimate = state.inited;
          (0, import_vue.nextTick)(() => {
            const titles = titleRefs.value;
            if (!titles || !titles[state.currentIndex] || props.type !== "line" || (0, import_utils.isHidden)(root.value)) {
              return;
            }
            const title = titles[state.currentIndex].$el;
            const {
              lineWidth,
              lineHeight
            } = props;
            const left = title.offsetLeft + title.offsetWidth / 2;
            const lineStyle = {
              width: (0, import_utils.addUnit)(lineWidth),
              backgroundColor: props.color,
              transform: `translateX(${left}px) translateX(-50%)`
            };
            if (shouldAnimate) {
              lineStyle.transitionDuration = `${props.duration}s`;
            }
            if ((0, import_utils.isDef)(lineHeight)) {
              const height = (0, import_utils.addUnit)(lineHeight);
              lineStyle.height = height;
              lineStyle.borderRadius = height;
            }
            state.lineStyle = lineStyle;
          });
        };
        const findAvailableTab = (index) => {
          const diff = index < state.currentIndex ? -1 : 1;
          while (index >= 0 && index < children.length) {
            if (!children[index].disabled) {
              return index;
            }
            index += diff;
          }
        };
        const setCurrentIndex = (currentIndex, skipScrollIntoView) => {
          const newIndex = findAvailableTab(currentIndex);
          if (!(0, import_utils.isDef)(newIndex)) {
            return;
          }
          const newTab = children[newIndex];
          const newName = getTabName(newTab, newIndex);
          const shouldEmitChange = state.currentIndex !== null;
          if (state.currentIndex !== newIndex) {
            state.currentIndex = newIndex;
            if (!skipScrollIntoView) {
              scrollIntoView();
            }
            setLine();
          }
          if (newName !== props.active) {
            emit("update:active", newName);
            if (shouldEmitChange) {
              emit("change", newName, newTab.title);
            }
          }
          if (stickyFixed && !props.scrollspy) {
            (0, import_utils.setRootScrollTop)(Math.ceil((0, import_utils.getElementTop)(root.value) - offsetTopPx.value));
          }
        };
        const setCurrentIndexByName = (name2, skipScrollIntoView) => {
          const index = children.findIndex((tab, index2) => getTabName(tab, index2) === name2);
          setCurrentIndex(index === -1 ? 0 : index, skipScrollIntoView);
        };
        const scrollToCurrentContent = (immediate = false) => {
          if (props.scrollspy) {
            const target = children[state.currentIndex].$el;
            if (target && scroller.value) {
              const to = (0, import_utils.getElementTop)(target, scroller.value) - scrollOffset.value;
              lockScroll = true;
              if (cancelScrollTopToRaf) cancelScrollTopToRaf();
              cancelScrollTopToRaf = (0, import_utils2.scrollTopTo)(scroller.value, to, immediate ? 0 : +props.duration, () => {
                lockScroll = false;
              });
            }
          }
        };
        const onClickTab = (item, index, event) => {
          const {
            title,
            disabled
          } = children[index];
          const name2 = getTabName(children[index], index);
          if (!disabled) {
            (0, import_utils.callInterceptor)(props.beforeChange, {
              args: [name2],
              done: () => {
                setCurrentIndex(index);
                scrollToCurrentContent();
              }
            });
            (0, import_use_route.route)(item);
          }
          emit("clickTab", {
            name: name2,
            title,
            event,
            disabled
          });
        };
        const onStickyScroll = (params) => {
          stickyFixed = params.isFixed;
          emit("scroll", params);
        };
        const scrollTo = (name2) => {
          (0, import_vue.nextTick)(() => {
            setCurrentIndexByName(name2);
            scrollToCurrentContent(true);
          });
        };
        const getCurrentIndexOnScroll = () => {
          for (let index = 0; index < children.length; index++) {
            const {
              top
            } = (0, import_use.useRect)(children[index].$el);
            if (top > scrollOffset.value) {
              return index === 0 ? 0 : index - 1;
            }
          }
          return children.length - 1;
        };
        const onScroll = () => {
          if (props.scrollspy && !lockScroll) {
            const index = getCurrentIndexOnScroll();
            setCurrentIndex(index);
          }
        };
        const renderLine = () => {
          if (props.type === "line" && children.length) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("line"),
              "style": state.lineStyle
            }, null);
          }
        };
        const renderHeader = () => {
          var _a, _b, _c;
          const {
            type,
            border,
            sticky
          } = props;
          const Header = [(0, import_vue.createVNode)("div", {
            "ref": sticky ? void 0 : wrapRef,
            "class": [bem("wrap"), {
              [import_utils.BORDER_TOP_BOTTOM]: type === "line" && border
            }]
          }, [(0, import_vue.createVNode)("div", {
            "ref": navRef,
            "role": "tablist",
            "class": bem("nav", [type, {
              shrink: props.shrink,
              complete: scrollable.value
            }]),
            "style": navStyle.value,
            "aria-orientation": "horizontal"
          }, [(_a = slots["nav-left"]) == null ? void 0 : _a.call(slots), children.map((item) => item.renderTitle(onClickTab)), renderLine(), (_b = slots["nav-right"]) == null ? void 0 : _b.call(slots)])]), (_c = slots["nav-bottom"]) == null ? void 0 : _c.call(slots)];
          if (sticky) {
            return (0, import_vue.createVNode)("div", {
              "ref": wrapRef
            }, [Header]);
          }
          return Header;
        };
        const resize = () => {
          setLine();
          (0, import_vue.nextTick)(() => {
            var _a, _b;
            scrollIntoView(true);
            (_b = (_a = contentRef.value) == null ? void 0 : _a.swipeRef.value) == null ? void 0 : _b.resize();
          });
        };
        (0, import_vue.watch)(() => [props.color, props.duration, props.lineWidth, props.lineHeight], setLine);
        (0, import_vue.watch)(import_utils.windowWidth, resize);
        (0, import_vue.watch)(() => props.active, (value) => {
          if (value !== currentName.value) {
            setCurrentIndexByName(value);
          }
        });
        (0, import_vue.watch)(() => children.length, () => {
          if (state.inited) {
            setCurrentIndexByName(props.active);
            setLine();
            (0, import_vue.nextTick)(() => {
              scrollIntoView(true);
            });
          }
        });
        const init = () => {
          setCurrentIndexByName(props.active, true);
          (0, import_vue.nextTick)(() => {
            state.inited = true;
            if (wrapRef.value) {
              tabHeight = (0, import_use.useRect)(wrapRef.value).height;
            }
            scrollIntoView(true);
          });
        };
        const onRendered = (name2, title) => emit("rendered", name2, title);
        (0, import_use_expose.useExpose)({
          resize,
          scrollTo
        });
        (0, import_vue.onActivated)(setLine);
        (0, import_on_popup_reopen.onPopupReopen)(setLine);
        (0, import_use.onMountedOrActivated)(init);
        (0, import_use_visibility_change.useVisibilityChange)(root, setLine);
        (0, import_use.useEventListener)("scroll", onScroll, {
          target: scroller,
          passive: true
        });
        linkChildren({
          id,
          props,
          setLine,
          scrollable,
          onRendered,
          currentName,
          setTitleRefs,
          scrollIntoView
        });
        return () => (0, import_vue.createVNode)("div", {
          "ref": root,
          "class": bem([props.type])
        }, [props.showHeader ? props.sticky ? (0, import_vue.createVNode)(import_sticky.Sticky, {
          "container": root.value,
          "offsetTop": offsetTopPx.value,
          "onScroll": onStickyScroll
        }, {
          default: () => [renderHeader()]
        }) : renderHeader() : null, (0, import_vue.createVNode)(import_TabsContent.default, {
          "ref": contentRef,
          "count": children.length,
          "inited": state.inited,
          "animated": props.animated,
          "duration": props.duration,
          "swipeable": props.swipeable,
          "lazyRender": props.lazyRender,
          "currentIndex": state.currentIndex,
          "onChange": setCurrentIndex
        }, {
          default: () => {
            var _a;
            return [(_a = slots.default) == null ? void 0 : _a.call(slots)];
          }
        })]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-tab-status.js
var require_use_tab_status = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/composables/use-tab-status.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ALL_TAB_STATUS_KEY: () => ALL_TAB_STATUS_KEY,
      TAB_STATUS_KEY: () => TAB_STATUS_KEY,
      useAllTabStatus: () => useAllTabStatus,
      useProvideTabStatus: () => useProvideTabStatus,
      useTabStatus: () => useTabStatus
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var TAB_STATUS_KEY = Symbol();
    var ALL_TAB_STATUS_KEY = Symbol();
    var useTabStatus = () => (0, import_vue.inject)(TAB_STATUS_KEY, null);
    var useAllTabStatus = () => (0, import_vue.inject)(ALL_TAB_STATUS_KEY, null);
    var useProvideTabStatus = (status) => {
      const allTabStatus = useAllTabStatus();
      (0, import_vue.provide)(TAB_STATUS_KEY, status);
      (0, import_vue.provide)(
        ALL_TAB_STATUS_KEY,
        (0, import_vue.computed)(() => {
          return (allTabStatus == null || allTabStatus.value) && status.value;
        })
      );
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tab/TabTitle.js
var require_TabTitle = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tab/TabTitle.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      TabTitle: () => TabTitle
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_badge = require_badge();
    var [name, bem] = (0, import_utils.createNamespace)("tab");
    var TabTitle = (0, import_vue.defineComponent)({
      name,
      props: {
        id: String,
        dot: Boolean,
        type: String,
        color: String,
        title: String,
        badge: import_utils.numericProp,
        shrink: Boolean,
        isActive: Boolean,
        disabled: Boolean,
        controls: String,
        scrollable: Boolean,
        activeColor: String,
        inactiveColor: String,
        showZeroBadge: import_utils.truthProp
      },
      setup(props, {
        slots
      }) {
        const style = (0, import_vue.computed)(() => {
          const style2 = {};
          const {
            type,
            color,
            disabled,
            isActive,
            activeColor,
            inactiveColor
          } = props;
          const isCard = type === "card";
          if (color && isCard) {
            style2.borderColor = color;
            if (!disabled) {
              if (isActive) {
                style2.backgroundColor = color;
              } else {
                style2.color = color;
              }
            }
          }
          const titleColor = isActive ? activeColor : inactiveColor;
          if (titleColor) {
            style2.color = titleColor;
          }
          return style2;
        });
        const renderText = () => {
          const Text = (0, import_vue.createVNode)("span", {
            "class": bem("text", {
              ellipsis: !props.scrollable
            })
          }, [slots.title ? slots.title() : props.title]);
          if (props.dot || (0, import_utils.isDef)(props.badge) && props.badge !== "") {
            return (0, import_vue.createVNode)(import_badge.Badge, {
              "dot": props.dot,
              "content": props.badge,
              "showZero": props.showZeroBadge
            }, {
              default: () => [Text]
            });
          }
          return Text;
        };
        return () => (0, import_vue.createVNode)("div", {
          "id": props.id,
          "role": "tab",
          "class": [bem([props.type, {
            grow: props.scrollable && !props.shrink,
            shrink: props.shrink,
            active: props.isActive,
            disabled: props.disabled
          }])],
          "style": style.value,
          "tabindex": props.disabled ? void 0 : props.isActive ? 0 : -1,
          "aria-selected": props.isActive,
          "aria-disabled": props.disabled || void 0,
          "aria-controls": props.controls,
          "data-allow-mismatch": "attribute"
        }, [renderText()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe-item/SwipeItem.js
var require_SwipeItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe-item/SwipeItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_Swipe = require_Swipe();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("swipe-item");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      setup(props, {
        slots
      }) {
        let rendered;
        const state = (0, import_vue.reactive)({
          offset: 0,
          inited: false,
          mounted: false
        });
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_Swipe.SWIPE_KEY);
        if (!parent) {
          if (true) {
            console.error("[Vant] <SwipeItem> must be a child component of <Swipe>.");
          }
          return;
        }
        const style = (0, import_vue.computed)(() => {
          const style2 = {};
          const {
            vertical
          } = parent.props;
          if (parent.size.value) {
            style2[vertical ? "height" : "width"] = `${parent.size.value}px`;
          }
          if (state.offset) {
            style2.transform = `translate${vertical ? "Y" : "X"}(${state.offset}px)`;
          }
          return style2;
        });
        const shouldRender = (0, import_vue.computed)(() => {
          const {
            loop,
            lazyRender
          } = parent.props;
          if (!lazyRender || rendered) {
            return true;
          }
          if (!state.mounted) {
            return false;
          }
          const active = parent.activeIndicator.value;
          const maxActive = parent.count.value - 1;
          const prevActive = active === 0 && loop ? maxActive : active - 1;
          const nextActive = active === maxActive && loop ? 0 : active + 1;
          rendered = index.value === active || index.value === prevActive || index.value === nextActive;
          return rendered;
        });
        const setOffset = (offset) => {
          state.offset = offset;
        };
        (0, import_vue.onMounted)(() => {
          (0, import_vue.nextTick)(() => {
            state.mounted = true;
          });
        });
        (0, import_use_expose.useExpose)({
          setOffset
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": bem(),
            "style": style.value
          }, [shouldRender.value ? (_a = slots.default) == null ? void 0 : _a.call(slots) : null]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe-item/index.js
var require_swipe_item = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe-item/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SwipeItem: () => SwipeItem,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_SwipeItem = __toESM(require_SwipeItem());
    var SwipeItem = (0, import_utils.withInstall)(import_SwipeItem.default);
    var stdin_default = SwipeItem;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tab/Tab.js
var require_Tab = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tab/Tab.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      tabProps: () => tabProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_shared = (init_shared_esm_bundler(), __toCommonJS(shared_esm_bundler_exports));
    var import_utils = require_utils();
    var import_Tabs = require_Tabs();
    var import_use = require_index_cjs();
    var import_use_id = require_use_id();
    var import_use_expose = require_use_expose();
    var import_use_route = require_use_route();
    var import_use_tab_status = require_use_tab_status();
    var import_TabTitle = require_TabTitle();
    var import_swipe_item = require_swipe_item();
    var [name, bem] = (0, import_utils.createNamespace)("tab");
    var tabProps = (0, import_utils.extend)({}, import_use_route.routeProps, {
      dot: Boolean,
      name: import_utils.numericProp,
      badge: import_utils.numericProp,
      title: String,
      disabled: Boolean,
      titleClass: import_utils.unknownProp,
      titleStyle: [String, Object],
      showZeroBadge: import_utils.truthProp
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: tabProps,
      setup(props, {
        slots
      }) {
        const id = (0, import_use_id.useId)();
        const inited = (0, import_vue.ref)(false);
        const instance = (0, import_vue.getCurrentInstance)();
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_Tabs.TABS_KEY);
        if (!parent) {
          if (true) {
            console.error("[Vant] <Tab> must be a child component of <Tabs>.");
          }
          return;
        }
        const getName = () => {
          var _a;
          return (_a = props.name) != null ? _a : index.value;
        };
        const init = () => {
          inited.value = true;
          if (parent.props.lazyRender) {
            (0, import_vue.nextTick)(() => {
              parent.onRendered(getName(), props.title);
            });
          }
        };
        const active = (0, import_vue.computed)(() => {
          const isActive = getName() === parent.currentName.value;
          if (isActive && !inited.value) {
            init();
          }
          return isActive;
        });
        const parsedClass = (0, import_vue.ref)("");
        const parsedStyle = (0, import_vue.ref)("");
        (0, import_vue.watchEffect)(() => {
          const {
            titleClass,
            titleStyle
          } = props;
          parsedClass.value = titleClass ? (0, import_shared.normalizeClass)(titleClass) : "";
          parsedStyle.value = titleStyle && typeof titleStyle !== "string" ? (0, import_shared.stringifyStyle)((0, import_shared.normalizeStyle)(titleStyle)) : titleStyle;
        });
        const renderTitle = (onClickTab) => (0, import_vue.createVNode)(import_TabTitle.TabTitle, (0, import_vue.mergeProps)({
          "key": id,
          "id": `${parent.id}-${index.value}`,
          "ref": parent.setTitleRefs(index.value),
          "style": parsedStyle.value,
          "class": parsedClass.value,
          "isActive": active.value,
          "controls": id,
          "scrollable": parent.scrollable.value,
          "activeColor": parent.props.titleActiveColor,
          "inactiveColor": parent.props.titleInactiveColor,
          "onClick": (event) => onClickTab(instance.proxy, index.value, event)
        }, (0, import_utils.pick)(parent.props, ["type", "color", "shrink"]), (0, import_utils.pick)(props, ["dot", "badge", "title", "disabled", "showZeroBadge"])), {
          title: slots.title
        });
        const hasInactiveClass = (0, import_vue.ref)(!active.value);
        (0, import_vue.watch)(active, (val) => {
          if (val) {
            hasInactiveClass.value = false;
          } else {
            (0, import_use.doubleRaf)(() => {
              hasInactiveClass.value = true;
            });
          }
        });
        (0, import_vue.watch)(() => props.title, () => {
          parent.setLine();
          parent.scrollIntoView();
        });
        (0, import_use_tab_status.useProvideTabStatus)(active);
        (0, import_use_expose.useExpose)({
          id,
          renderTitle
        });
        return () => {
          var _a;
          const label = `${parent.id}-${index.value}`;
          const {
            animated,
            swipeable,
            scrollspy,
            lazyRender
          } = parent.props;
          if (!slots.default && !animated) {
            return;
          }
          const show = scrollspy || active.value;
          if (animated || swipeable) {
            return (0, import_vue.createVNode)(import_swipe_item.SwipeItem, {
              "id": id,
              "role": "tabpanel",
              "class": bem("panel-wrapper", {
                inactive: hasInactiveClass.value
              }),
              "tabindex": active.value ? 0 : -1,
              "aria-hidden": !active.value,
              "aria-labelledby": label,
              "data-allow-mismatch": "attribute"
            }, {
              default: () => {
                var _a2;
                return [(0, import_vue.createVNode)("div", {
                  "class": bem("panel")
                }, [(_a2 = slots.default) == null ? void 0 : _a2.call(slots)])];
              }
            });
          }
          const shouldRender = inited.value || scrollspy || !lazyRender;
          const Content = shouldRender ? (_a = slots.default) == null ? void 0 : _a.call(slots) : null;
          return (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", {
            "id": id,
            "role": "tabpanel",
            "class": bem("panel"),
            "tabindex": show ? 0 : -1,
            "aria-labelledby": label,
            "data-allow-mismatch": "attribute"
          }, [Content]), [[import_vue.vShow, show]]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tab/index.js
var require_tab = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tab/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Tab: () => Tab,
      default: () => stdin_default,
      tabProps: () => import_Tab2.tabProps,
      useAllTabStatus: () => import_use_tab_status.useAllTabStatus,
      useTabStatus: () => import_use_tab_status.useTabStatus
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Tab = __toESM(require_Tab());
    var import_Tab2 = require_Tab();
    var import_use_tab_status = require_use_tab_status();
    var Tab = (0, import_utils.withInstall)(import_Tab.default);
    var stdin_default = Tab;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabs/index.js
var require_tabs = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabs/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Tabs: () => Tabs,
      default: () => stdin_default,
      tabsProps: () => import_Tabs2.tabsProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Tabs = __toESM(require_Tabs());
    var import_Tabs2 = require_Tabs();
    var Tabs = (0, import_utils.withInstall)(import_Tabs.default);
    var stdin_default = Tabs;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker-group/PickerGroup.js
var require_PickerGroup = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker-group/PickerGroup.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      PICKER_GROUP_KEY: () => PICKER_GROUP_KEY,
      default: () => stdin_default,
      pickerGroupProps: () => pickerGroupProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_sync_prop_ref = require_use_sync_prop_ref();
    var import_tab = require_tab();
    var import_tabs = require_tabs();
    var import_PickerToolbar = __toESM(require_PickerToolbar());
    var [name, bem] = (0, import_utils.createNamespace)("picker-group");
    var PICKER_GROUP_KEY = Symbol(name);
    var pickerGroupProps = (0, import_utils.extend)({
      tabs: (0, import_utils.makeArrayProp)(),
      activeTab: (0, import_utils.makeNumericProp)(0),
      nextStepText: String,
      showToolbar: import_utils.truthProp
    }, import_PickerToolbar.pickerToolbarProps);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: pickerGroupProps,
      emits: ["confirm", "cancel", "update:activeTab"],
      setup(props, {
        emit,
        slots
      }) {
        const activeTab = (0, import_use_sync_prop_ref.useSyncPropRef)(() => props.activeTab, (value) => emit("update:activeTab", value));
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(PICKER_GROUP_KEY);
        linkChildren();
        const showNextButton = () => +activeTab.value < props.tabs.length - 1 && props.nextStepText;
        const onConfirm = () => {
          if (showNextButton()) {
            activeTab.value = +activeTab.value + 1;
          } else {
            emit("confirm", children.map((item) => item.confirm()));
          }
        };
        const onCancel = () => emit("cancel");
        return () => {
          var _a, _b;
          let childNodes = (_b = (_a = slots.default) == null ? void 0 : _a.call(slots)) == null ? void 0 : _b.filter((node) => node.type !== import_vue.Comment).map((node) => {
            if (node.type === import_vue.Fragment) {
              return node.children;
            }
            return node;
          });
          if (childNodes) {
            childNodes = (0, import_utils.flat)(childNodes);
          }
          const confirmButtonText = showNextButton() ? props.nextStepText : props.confirmButtonText;
          return (0, import_vue.createVNode)("div", {
            "class": bem()
          }, [props.showToolbar ? (0, import_vue.createVNode)(import_PickerToolbar.default, {
            "title": props.title,
            "cancelButtonText": props.cancelButtonText,
            "confirmButtonText": confirmButtonText,
            "onConfirm": onConfirm,
            "onCancel": onCancel
          }, (0, import_utils.pick)(slots, import_PickerToolbar.pickerToolbarSlots)) : null, (0, import_vue.createVNode)(import_tabs.Tabs, {
            "active": activeTab.value,
            "onUpdate:active": ($event) => activeTab.value = $event,
            "class": bem("tabs"),
            "shrink": true,
            "animated": true,
            "lazyRender": false
          }, {
            default: () => [props.tabs.map((title, index) => (0, import_vue.createVNode)(import_tab.Tab, {
              "title": title,
              "titleClass": bem("tab-title")
            }, {
              default: () => [childNodes == null ? void 0 : childNodes[index]]
            }))]
          })]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/Picker.js
var require_Picker = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/Picker.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      pickerProps: () => pickerProps,
      pickerSharedProps: () => pickerSharedProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils2();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_loading = require_loading();
    var import_PickerColumn = __toESM(require_PickerColumn());
    var import_PickerToolbar = __toESM(require_PickerToolbar());
    var import_PickerGroup = require_PickerGroup();
    var pickerSharedProps = (0, import_utils.extend)({
      loading: Boolean,
      readonly: Boolean,
      allowHtml: Boolean,
      optionHeight: (0, import_utils.makeNumericProp)(44),
      showToolbar: import_utils.truthProp,
      swipeDuration: (0, import_utils.makeNumericProp)(1e3),
      visibleOptionNum: (0, import_utils.makeNumericProp)(6)
    }, import_PickerToolbar.pickerToolbarProps);
    var pickerProps = (0, import_utils.extend)({}, pickerSharedProps, {
      columns: (0, import_utils.makeArrayProp)(),
      modelValue: (0, import_utils.makeArrayProp)(),
      toolbarPosition: (0, import_utils.makeStringProp)("top"),
      columnsFieldNames: Object
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name: import_utils2.name,
      props: pickerProps,
      emits: ["confirm", "cancel", "change", "scrollInto", "clickOption", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const columnsRef = (0, import_vue.ref)();
        const selectedValues = (0, import_vue.ref)(props.modelValue.slice(0));
        const {
          parent
        } = (0, import_use.useParent)(import_PickerGroup.PICKER_GROUP_KEY);
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(import_PickerColumn.PICKER_KEY);
        linkChildren();
        const fields = (0, import_vue.computed)(() => (0, import_utils2.assignDefaultFields)(props.columnsFieldNames));
        const optionHeight = (0, import_vue.computed)(() => (0, import_utils.unitToPx)(props.optionHeight));
        const columnsType = (0, import_vue.computed)(() => (0, import_utils2.getColumnsType)(props.columns, fields.value));
        const currentColumns = (0, import_vue.computed)(() => {
          const {
            columns
          } = props;
          switch (columnsType.value) {
            case "multiple":
              return columns;
            case "cascade":
              return (0, import_utils2.formatCascadeColumns)(columns, fields.value, selectedValues);
            default:
              return [columns];
          }
        });
        const hasOptions = (0, import_vue.computed)(() => currentColumns.value.some((options) => options.length));
        const selectedOptions = (0, import_vue.computed)(() => currentColumns.value.map((options, index) => (0, import_utils2.findOptionByValue)(options, selectedValues.value[index], fields.value)));
        const selectedIndexes = (0, import_vue.computed)(() => currentColumns.value.map((options, index) => options.findIndex((option) => option[fields.value.value] === selectedValues.value[index])));
        const setValue = (index, value) => {
          if (selectedValues.value[index] !== value) {
            const newValues = selectedValues.value.slice(0);
            newValues[index] = value;
            selectedValues.value = newValues;
          }
        };
        const getEventParams = () => ({
          selectedValues: selectedValues.value.slice(0),
          selectedOptions: selectedOptions.value,
          selectedIndexes: selectedIndexes.value
        });
        const onChange = (value, columnIndex) => {
          setValue(columnIndex, value);
          if (columnsType.value === "cascade") {
            selectedValues.value.forEach((value2, index) => {
              const options = currentColumns.value[index];
              if (!(0, import_utils2.isOptionExist)(options, value2, fields.value)) {
                setValue(index, options.length ? options[0][fields.value.value] : void 0);
              }
            });
          }
          (0, import_vue.nextTick)(() => {
            emit("change", (0, import_utils.extend)({
              columnIndex
            }, getEventParams()));
          });
        };
        const onClickOption = (currentOption, columnIndex) => {
          const params = {
            columnIndex,
            currentOption
          };
          emit("clickOption", (0, import_utils.extend)(getEventParams(), params));
          emit("scrollInto", params);
        };
        const confirm = () => {
          children.forEach((child) => child.stopMomentum());
          const params = getEventParams();
          (0, import_vue.nextTick)(() => {
            const params2 = getEventParams();
            emit("confirm", params2);
          });
          return params;
        };
        const cancel = () => emit("cancel", getEventParams());
        const renderColumnItems = () => currentColumns.value.map((options, columnIndex) => (0, import_vue.createVNode)(import_PickerColumn.default, {
          "value": selectedValues.value[columnIndex],
          "fields": fields.value,
          "options": options,
          "readonly": props.readonly,
          "allowHtml": props.allowHtml,
          "optionHeight": optionHeight.value,
          "swipeDuration": props.swipeDuration,
          "visibleOptionNum": props.visibleOptionNum,
          "onChange": (value) => onChange(value, columnIndex),
          "onClickOption": (option) => onClickOption(option, columnIndex),
          "onScrollInto": (option) => {
            emit("scrollInto", {
              currentOption: option,
              columnIndex
            });
          }
        }, {
          option: slots.option
        }));
        const renderMask = (wrapHeight) => {
          if (hasOptions.value) {
            const frameStyle = {
              height: `${optionHeight.value}px`
            };
            const maskStyle = {
              backgroundSize: `100% ${(wrapHeight - optionHeight.value) / 2}px`
            };
            return [(0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("mask"),
              "style": maskStyle
            }, null), (0, import_vue.createVNode)("div", {
              "class": [import_utils.BORDER_UNSET_TOP_BOTTOM, (0, import_utils2.bem)("frame")],
              "style": frameStyle
            }, null)];
          }
        };
        const renderColumns = () => {
          const wrapHeight = optionHeight.value * +props.visibleOptionNum;
          const columnsStyle = {
            height: `${wrapHeight}px`
          };
          if (!props.loading && !hasOptions.value && slots.empty) {
            return slots.empty();
          }
          return (0, import_vue.createVNode)("div", {
            "ref": columnsRef,
            "class": (0, import_utils2.bem)("columns"),
            "style": columnsStyle
          }, [renderColumnItems(), renderMask(wrapHeight)]);
        };
        const renderToolbar = () => {
          if (props.showToolbar && !parent) {
            return (0, import_vue.createVNode)(import_PickerToolbar.default, (0, import_vue.mergeProps)((0, import_utils.pick)(props, import_PickerToolbar.pickerToolbarPropKeys), {
              "onConfirm": confirm,
              "onCancel": cancel
            }), (0, import_utils.pick)(slots, import_PickerToolbar.pickerToolbarSlots));
          }
        };
        const resetSelectedValues = (columns) => {
          columns.forEach((options, index) => {
            if (options.length && !(0, import_utils2.isOptionExist)(options, selectedValues.value[index], fields.value)) {
              setValue(index, (0, import_utils2.getFirstEnabledOption)(options)[fields.value.value]);
            }
          });
        };
        (0, import_vue.watch)(currentColumns, (columns) => resetSelectedValues(columns), {
          immediate: true
        });
        let lastEmittedModelValue;
        (0, import_vue.watch)(() => props.modelValue, (newValues) => {
          if (!(0, import_utils.isSameValue)(newValues, selectedValues.value) && !(0, import_utils.isSameValue)(newValues, lastEmittedModelValue)) {
            selectedValues.value = newValues.slice(0);
            lastEmittedModelValue = newValues.slice(0);
          }
          if (props.modelValue.length === 0) {
            resetSelectedValues(currentColumns.value);
          }
        }, {
          deep: true
        });
        (0, import_vue.watch)(selectedValues, (newValues) => {
          if (!(0, import_utils.isSameValue)(newValues, props.modelValue)) {
            lastEmittedModelValue = newValues.slice(0);
            emit("update:modelValue", lastEmittedModelValue);
          }
        }, {
          immediate: true
        });
        (0, import_use.useEventListener)("touchmove", import_utils.preventDefault, {
          target: columnsRef
        });
        const getSelectedOptions = () => selectedOptions.value;
        (0, import_use_expose.useExpose)({
          confirm,
          getSelectedOptions
        });
        return () => {
          var _a, _b;
          return (0, import_vue.createVNode)("div", {
            "class": (0, import_utils2.bem)()
          }, [props.toolbarPosition === "top" ? renderToolbar() : null, props.loading ? (0, import_vue.createVNode)(import_loading.Loading, {
            "class": (0, import_utils2.bem)("loading")
          }, null) : null, (_a = slots["columns-top"]) == null ? void 0 : _a.call(slots), renderColumns(), (_b = slots["columns-bottom"]) == null ? void 0 : _b.call(slots), props.toolbarPosition === "bottom" ? renderToolbar() : null]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/area/utils.js
var require_utils4 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/area/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      AREA_EMPTY_CODE: () => AREA_EMPTY_CODE,
      INHERIT_PROPS: () => INHERIT_PROPS,
      INHERIT_SLOTS: () => INHERIT_SLOTS,
      formatDataForCascade: () => formatDataForCascade
    });
    module.exports = __toCommonJS2(stdin_exports);
    var AREA_EMPTY_CODE = "000000";
    var INHERIT_SLOTS = [
      "title",
      "cancel",
      "confirm",
      "toolbar",
      "columns-top",
      "columns-bottom"
    ];
    var INHERIT_PROPS = [
      "title",
      "loading",
      "readonly",
      "optionHeight",
      "swipeDuration",
      "visibleOptionNum",
      "cancelButtonText",
      "confirmButtonText"
    ];
    var makeOption = (text = "", value = AREA_EMPTY_CODE, children = void 0) => ({
      text,
      value,
      children
    });
    function formatDataForCascade({
      areaList,
      columnsNum,
      columnsPlaceholder: placeholder
    }) {
      const {
        city_list: city = {},
        county_list: county = {},
        province_list: province = {}
      } = areaList;
      const showCity = +columnsNum > 1;
      const showCounty = +columnsNum > 2;
      const getProvinceChildren = () => {
        if (showCity) {
          return placeholder.length > 1 ? [
            makeOption(
              placeholder[1],
              AREA_EMPTY_CODE,
              showCounty ? [] : void 0
            )
          ] : [];
        }
      };
      const provinceMap = /* @__PURE__ */ new Map();
      Object.keys(province).forEach((code) => {
        provinceMap.set(
          code.slice(0, 2),
          makeOption(province[code], code, getProvinceChildren())
        );
      });
      const cityMap = /* @__PURE__ */ new Map();
      if (showCity) {
        const getCityChildren = () => {
          if (showCounty) {
            return placeholder.length > 2 ? [makeOption(placeholder[2])] : [];
          }
        };
        Object.keys(city).forEach((code) => {
          const option = makeOption(city[code], code, getCityChildren());
          cityMap.set(code.slice(0, 4), option);
          const province2 = provinceMap.get(code.slice(0, 2));
          if (province2) {
            province2.children.push(option);
          }
        });
      }
      if (showCounty) {
        Object.keys(county).forEach((code) => {
          const city2 = cityMap.get(code.slice(0, 4));
          if (city2) {
            city2.children.push(makeOption(county[code], code));
          }
        });
      }
      const options = Array.from(provinceMap.values());
      if (placeholder.length) {
        const county2 = showCounty ? [makeOption(placeholder[2])] : void 0;
        const city2 = showCity ? [makeOption(placeholder[1], AREA_EMPTY_CODE, county2)] : void 0;
        options.unshift(makeOption(placeholder[0], AREA_EMPTY_CODE, city2));
      }
      return options;
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/index.js
var require_picker = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Picker: () => Picker,
      default: () => stdin_default,
      pickerProps: () => import_Picker2.pickerProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Picker = __toESM(require_Picker());
    var import_Picker2 = require_Picker();
    var Picker = (0, import_utils.withInstall)(import_Picker.default);
    var stdin_default = Picker;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/area/Area.js
var require_Area = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/area/Area.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      areaProps: () => areaProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_Picker = require_Picker();
    var import_utils2 = require_utils4();
    var import_use_expose = require_use_expose();
    var import_picker = require_picker();
    var [name, bem] = (0, import_utils.createNamespace)("area");
    var areaProps = (0, import_utils.extend)({}, (0, import_utils.pick)(import_Picker.pickerSharedProps, import_utils2.INHERIT_PROPS), {
      modelValue: String,
      columnsNum: (0, import_utils.makeNumericProp)(3),
      columnsPlaceholder: (0, import_utils.makeArrayProp)(),
      areaList: {
        type: Object,
        default: () => ({})
      }
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: areaProps,
      emits: ["change", "confirm", "cancel", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const codes = (0, import_vue.ref)([]);
        const picker = (0, import_vue.ref)();
        const columns = (0, import_vue.computed)(() => (0, import_utils2.formatDataForCascade)(props));
        const onChange = (...args) => emit("change", ...args);
        const onCancel = (...args) => emit("cancel", ...args);
        const onConfirm = (...args) => emit("confirm", ...args);
        (0, import_vue.watch)(codes, (newCodes) => {
          const lastCode = newCodes.length ? newCodes[newCodes.length - 1] : "";
          if (lastCode && lastCode !== props.modelValue) {
            emit("update:modelValue", lastCode);
          }
        }, {
          deep: true
        });
        (0, import_vue.watch)(() => props.modelValue, (newCode) => {
          if (newCode) {
            const lastCode = codes.value.length ? codes.value[codes.value.length - 1] : "";
            if (newCode !== lastCode) {
              codes.value = [`${newCode.slice(0, 2)}0000`, `${newCode.slice(0, 4)}00`, newCode].slice(0, +props.columnsNum);
            }
          } else {
            codes.value = [];
          }
        }, {
          immediate: true
        });
        (0, import_use_expose.useExpose)({
          confirm: () => {
            var _a;
            return (_a = picker.value) == null ? void 0 : _a.confirm();
          },
          getSelectedOptions: () => {
            var _a;
            return ((_a = picker.value) == null ? void 0 : _a.getSelectedOptions()) || [];
          }
        });
        return () => (0, import_vue.createVNode)(import_picker.Picker, (0, import_vue.mergeProps)({
          "ref": picker,
          "modelValue": codes.value,
          "onUpdate:modelValue": ($event) => codes.value = $event,
          "class": bem(),
          "columns": columns.value,
          "onChange": onChange,
          "onCancel": onCancel,
          "onConfirm": onConfirm
        }, (0, import_utils.pick)(props, import_utils2.INHERIT_PROPS)), (0, import_utils.pick)(slots, import_utils2.INHERIT_SLOTS));
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/area/index.js
var require_area = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/area/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Area: () => Area,
      areaProps: () => import_Area2.areaProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Area = __toESM(require_Area());
    var import_Area2 = require_Area();
    var Area = (0, import_utils.withInstall)(import_Area.default);
    var stdin_default = Area;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cell/Cell.js
var require_Cell = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cell/Cell.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      cellProps: () => cellProps,
      cellSharedProps: () => cellSharedProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_route = require_use_route();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("cell");
    var cellSharedProps = {
      tag: (0, import_utils.makeStringProp)("div"),
      icon: String,
      size: String,
      title: import_utils.numericProp,
      value: import_utils.numericProp,
      label: import_utils.numericProp,
      center: Boolean,
      isLink: Boolean,
      border: import_utils.truthProp,
      iconPrefix: String,
      valueClass: import_utils.unknownProp,
      labelClass: import_utils.unknownProp,
      titleClass: import_utils.unknownProp,
      titleStyle: null,
      arrowDirection: String,
      required: {
        type: [Boolean, String],
        default: null
      },
      clickable: {
        type: Boolean,
        default: null
      }
    };
    var cellProps = (0, import_utils.extend)({}, cellSharedProps, import_use_route.routeProps);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: cellProps,
      setup(props, {
        slots
      }) {
        const route = (0, import_use_route.useRoute)();
        const renderLabel = () => {
          const showLabel = slots.label || (0, import_utils.isDef)(props.label);
          if (showLabel) {
            return (0, import_vue.createVNode)("div", {
              "class": [bem("label"), props.labelClass]
            }, [slots.label ? slots.label() : props.label]);
          }
        };
        const renderTitle = () => {
          var _a;
          if (slots.title || (0, import_utils.isDef)(props.title)) {
            const titleSlot = (_a = slots.title) == null ? void 0 : _a.call(slots);
            if (Array.isArray(titleSlot) && titleSlot.length === 0) {
              return;
            }
            return (0, import_vue.createVNode)("div", {
              "class": [bem("title"), props.titleClass],
              "style": props.titleStyle
            }, [titleSlot || (0, import_vue.createVNode)("span", null, [props.title]), renderLabel()]);
          }
        };
        const renderValue = () => {
          const slot = slots.value || slots.default;
          const hasValue = slot || (0, import_utils.isDef)(props.value);
          if (hasValue) {
            return (0, import_vue.createVNode)("div", {
              "class": [bem("value"), props.valueClass]
            }, [slot ? slot() : (0, import_vue.createVNode)("span", null, [props.value])]);
          }
        };
        const renderLeftIcon = () => {
          if (slots.icon) {
            return slots.icon();
          }
          if (props.icon) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "name": props.icon,
              "class": bem("left-icon"),
              "classPrefix": props.iconPrefix
            }, null);
          }
        };
        const renderRightIcon = () => {
          if (slots["right-icon"]) {
            return slots["right-icon"]();
          }
          if (props.isLink) {
            const name2 = props.arrowDirection && props.arrowDirection !== "right" ? `arrow-${props.arrowDirection}` : "arrow";
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "name": name2,
              "class": bem("right-icon")
            }, null);
          }
        };
        return () => {
          var _a;
          const {
            tag,
            size,
            center,
            border,
            isLink,
            required
          } = props;
          const clickable = (_a = props.clickable) != null ? _a : isLink;
          const classes = {
            center,
            required: !!required,
            clickable,
            borderless: !border
          };
          if (size) {
            classes[size] = !!size;
          }
          return (0, import_vue.createVNode)(tag, {
            "class": bem(classes),
            "role": clickable ? "button" : void 0,
            "tabindex": clickable ? 0 : void 0,
            "onClick": route
          }, {
            default: () => {
              var _a2;
              return [renderLeftIcon(), renderTitle(), renderValue(), renderRightIcon(), (_a2 = slots.extra) == null ? void 0 : _a2.call(slots)];
            }
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cell/index.js
var require_cell = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cell/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Cell: () => Cell,
      cellProps: () => import_Cell2.cellProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Cell = __toESM(require_Cell());
    var import_Cell2 = require_Cell();
    var Cell = (0, import_utils.withInstall)(import_Cell.default);
    var stdin_default = Cell;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/form/Form.js
var require_Form = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/form/Form.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      formProps: () => formProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("form");
    var formProps = {
      colon: Boolean,
      disabled: Boolean,
      readonly: Boolean,
      required: [Boolean, String],
      showError: Boolean,
      labelWidth: import_utils.numericProp,
      labelAlign: String,
      inputAlign: String,
      scrollToError: Boolean,
      scrollToErrorPosition: String,
      validateFirst: Boolean,
      submitOnEnter: import_utils.truthProp,
      showErrorMessage: import_utils.truthProp,
      errorMessageAlign: String,
      validateTrigger: {
        type: [String, Array],
        default: "onBlur"
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: formProps,
      emits: ["submit", "failed"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(import_utils.FORM_KEY);
        const getFieldsByNames = (names) => {
          if (names) {
            return children.filter((field) => names.includes(field.name));
          }
          return children;
        };
        const validateSeq = (names) => new Promise((resolve, reject) => {
          const errors = [];
          const fields = getFieldsByNames(names);
          fields.reduce((promise, field) => promise.then(() => {
            if (!errors.length) {
              return field.validate().then((error) => {
                if (error) {
                  errors.push(error);
                }
              });
            }
          }), Promise.resolve()).then(() => {
            if (errors.length) {
              reject(errors);
            } else {
              resolve();
            }
          });
        });
        const validateAll = (names) => new Promise((resolve, reject) => {
          const fields = getFieldsByNames(names);
          Promise.all(fields.map((item) => item.validate())).then((errors) => {
            errors = errors.filter(Boolean);
            if (errors.length) {
              reject(errors);
            } else {
              resolve();
            }
          });
        });
        const validateField = (name2) => {
          const matched = children.find((item) => item.name === name2);
          if (matched) {
            return new Promise((resolve, reject) => {
              matched.validate().then((error) => {
                if (error) {
                  reject(error);
                } else {
                  resolve();
                }
              });
            });
          }
          return Promise.reject();
        };
        const validate = (name2) => {
          if (typeof name2 === "string") {
            return validateField(name2);
          }
          return props.validateFirst ? validateSeq(name2) : validateAll(name2);
        };
        const resetValidation = (name2) => {
          if (typeof name2 === "string") {
            name2 = [name2];
          }
          const fields = getFieldsByNames(name2);
          fields.forEach((item) => {
            item.resetValidation();
          });
        };
        const getValidationStatus = () => children.reduce((form, field) => {
          form[field.name] = field.getValidationStatus();
          return form;
        }, {});
        const scrollToField = (name2, options) => {
          children.some((item) => {
            if (item.name === name2) {
              item.$el.scrollIntoView(options);
              return true;
            }
            return false;
          });
        };
        const getValues = () => children.reduce((form, field) => {
          if (field.name !== void 0) {
            form[field.name] = field.formValue.value;
          }
          return form;
        }, {});
        const submit = () => {
          const values = getValues();
          validate().then(() => emit("submit", values)).catch((errors) => {
            emit("failed", {
              values,
              errors
            });
            const {
              scrollToError,
              scrollToErrorPosition
            } = props;
            if (scrollToError && errors[0].name) {
              scrollToField(errors[0].name, scrollToErrorPosition ? {
                block: scrollToErrorPosition
              } : void 0);
            }
          });
        };
        const onSubmit = (event) => {
          (0, import_utils.preventDefault)(event);
          submit();
        };
        linkChildren({
          props
        });
        (0, import_use_expose.useExpose)({
          submit,
          validate,
          getValues,
          scrollToField,
          resetValidation,
          getValidationStatus
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("form", {
            "class": bem(),
            "onSubmit": onSubmit
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/form/index.js
var require_form = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/form/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Form: () => Form,
      default: () => stdin_default,
      formProps: () => import_Form2.formProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Form = __toESM(require_Form());
    var import_Form2 = require_Form();
    var Form = (0, import_utils.withInstall)(import_Form.default);
    var stdin_default = Form;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/field/utils.js
var require_utils5 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/field/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      cutString: () => cutString,
      endComposing: () => endComposing,
      getRuleMessage: () => getRuleMessage,
      getStringLength: () => getStringLength,
      isEmptyValue: () => isEmptyValue,
      mapInputType: () => mapInputType,
      resizeTextarea: () => resizeTextarea,
      runRuleValidator: () => runRuleValidator,
      runSyncRule: () => runSyncRule,
      startComposing: () => startComposing
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    function isEmptyValue(value) {
      if (Array.isArray(value)) {
        return !value.length;
      }
      if (value === 0) {
        return false;
      }
      return !value;
    }
    function runSyncRule(value, rule) {
      if (isEmptyValue(value)) {
        if (rule.required) {
          return false;
        }
        if (rule.validateEmpty === false) {
          return true;
        }
      }
      if (rule.pattern && !rule.pattern.test(String(value))) {
        return false;
      }
      return true;
    }
    function runRuleValidator(value, rule) {
      return new Promise((resolve) => {
        const returnVal = rule.validator(value, rule);
        if ((0, import_utils.isPromise)(returnVal)) {
          returnVal.then(resolve);
          return;
        }
        resolve(returnVal);
      });
    }
    function getRuleMessage(value, rule) {
      const { message } = rule;
      if ((0, import_utils.isFunction)(message)) {
        return message(value, rule);
      }
      return message || "";
    }
    function startComposing({ target }) {
      target.composing = true;
    }
    function endComposing({ target }) {
      if (target.composing) {
        target.composing = false;
        target.dispatchEvent(new Event("input"));
      }
    }
    function resizeTextarea(input, autosize) {
      const scrollTop = (0, import_utils.getRootScrollTop)();
      input.style.height = "auto";
      let height = input.scrollHeight;
      if ((0, import_utils.isObject)(autosize)) {
        const { maxHeight, minHeight } = autosize;
        if (maxHeight !== void 0) {
          height = Math.min(height, maxHeight);
        }
        if (minHeight !== void 0) {
          height = Math.max(height, minHeight);
        }
      }
      if (height) {
        input.style.height = `${height}px`;
        (0, import_utils.setRootScrollTop)(scrollTop);
      }
    }
    function mapInputType(type, inputmode) {
      if (type === "number") {
        type = "text";
        inputmode != null ? inputmode : inputmode = "decimal";
      }
      if (type === "digit") {
        type = "tel";
        inputmode != null ? inputmode : inputmode = "numeric";
      }
      return { type, inputmode };
    }
    function getStringLength(str) {
      return [...str].length;
    }
    function cutString(str, maxlength) {
      return [...str].slice(0, maxlength).join("");
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/field/Field.js
var require_Field = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/field/Field.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      fieldProps: () => fieldProps,
      fieldSharedProps: () => fieldSharedProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils5();
    var import_Cell = require_Cell();
    var import_use = require_index_cjs();
    var import_use_id = require_use_id();
    var import_use_expose = require_use_expose();
    var import_icon = require_icon();
    var import_cell = require_cell();
    var [name, bem] = (0, import_utils.createNamespace)("field");
    var fieldSharedProps = {
      id: String,
      name: String,
      leftIcon: String,
      rightIcon: String,
      autofocus: Boolean,
      clearable: Boolean,
      maxlength: import_utils.numericProp,
      max: Number,
      min: Number,
      formatter: Function,
      clearIcon: (0, import_utils.makeStringProp)("clear"),
      modelValue: (0, import_utils.makeNumericProp)(""),
      inputAlign: String,
      placeholder: String,
      autocomplete: String,
      autocapitalize: String,
      autocorrect: String,
      errorMessage: String,
      enterkeyhint: String,
      clearTrigger: (0, import_utils.makeStringProp)("focus"),
      formatTrigger: (0, import_utils.makeStringProp)("onChange"),
      spellcheck: {
        type: Boolean,
        default: null
      },
      error: {
        type: Boolean,
        default: null
      },
      disabled: {
        type: Boolean,
        default: null
      },
      readonly: {
        type: Boolean,
        default: null
      },
      inputmode: String
    };
    var fieldProps = (0, import_utils.extend)({}, import_Cell.cellSharedProps, fieldSharedProps, {
      rows: import_utils.numericProp,
      type: (0, import_utils.makeStringProp)("text"),
      rules: Array,
      autosize: [Boolean, Object],
      labelWidth: import_utils.numericProp,
      labelClass: import_utils.unknownProp,
      labelAlign: String,
      showWordLimit: Boolean,
      errorMessageAlign: String,
      colon: {
        type: Boolean,
        default: null
      }
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: fieldProps,
      emits: ["blur", "focus", "clear", "keypress", "clickInput", "endValidate", "startValidate", "clickLeftIcon", "clickRightIcon", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const id = (0, import_use_id.useId)();
        const state = (0, import_vue.reactive)({
          status: "unvalidated",
          focused: false,
          validateMessage: ""
        });
        const inputRef = (0, import_vue.ref)();
        const clearIconRef = (0, import_vue.ref)();
        const customValue = (0, import_vue.ref)();
        const {
          parent: form
        } = (0, import_use.useParent)(import_utils.FORM_KEY);
        const getModelValue = () => {
          var _a;
          return String((_a = props.modelValue) != null ? _a : "");
        };
        const getProp = (key) => {
          if ((0, import_utils.isDef)(props[key])) {
            return props[key];
          }
          if (form && (0, import_utils.isDef)(form.props[key])) {
            return form.props[key];
          }
        };
        const showClear = (0, import_vue.computed)(() => {
          const readonly = getProp("readonly");
          if (props.clearable && !readonly) {
            const hasValue = getModelValue() !== "";
            const trigger = props.clearTrigger === "always" || props.clearTrigger === "focus" && state.focused;
            return hasValue && trigger;
          }
          return false;
        });
        const formValue = (0, import_vue.computed)(() => {
          if (customValue.value && slots.input) {
            return customValue.value();
          }
          return props.modelValue;
        });
        const showRequiredMark = (0, import_vue.computed)(() => {
          var _a;
          const required = getProp("required");
          if (required === "auto") {
            return (_a = props.rules) == null ? void 0 : _a.some((rule) => rule.required);
          }
          return required;
        });
        const runRules = (rules) => rules.reduce((promise, rule) => promise.then(() => {
          if (state.status === "failed") {
            return;
          }
          let {
            value
          } = formValue;
          if (rule.formatter) {
            value = rule.formatter(value, rule);
          }
          if (!(0, import_utils2.runSyncRule)(value, rule)) {
            state.status = "failed";
            state.validateMessage = (0, import_utils2.getRuleMessage)(value, rule);
            return;
          }
          if (rule.validator) {
            if ((0, import_utils2.isEmptyValue)(value) && rule.validateEmpty === false) {
              return;
            }
            return (0, import_utils2.runRuleValidator)(value, rule).then((result) => {
              if (result && typeof result === "string") {
                state.status = "failed";
                state.validateMessage = result;
              } else if (result === false) {
                state.status = "failed";
                state.validateMessage = (0, import_utils2.getRuleMessage)(value, rule);
              }
            });
          }
        }), Promise.resolve());
        const resetValidation = () => {
          state.status = "unvalidated";
          state.validateMessage = "";
        };
        const endValidate = () => emit("endValidate", {
          status: state.status,
          message: state.validateMessage
        });
        const validate = (rules = props.rules) => new Promise((resolve) => {
          resetValidation();
          if (rules) {
            emit("startValidate");
            runRules(rules).then(() => {
              if (state.status === "failed") {
                resolve({
                  name: props.name,
                  message: state.validateMessage
                });
                endValidate();
              } else {
                state.status = "passed";
                resolve();
                endValidate();
              }
            });
          } else {
            resolve();
          }
        });
        const validateWithTrigger = (trigger) => {
          if (form && props.rules) {
            const {
              validateTrigger
            } = form.props;
            const defaultTrigger = (0, import_utils.toArray)(validateTrigger).includes(trigger);
            const rules = props.rules.filter((rule) => {
              if (rule.trigger) {
                return (0, import_utils.toArray)(rule.trigger).includes(trigger);
              }
              return defaultTrigger;
            });
            if (rules.length) {
              validate(rules);
            }
          }
        };
        const limitValueLength = (value) => {
          var _a;
          const {
            maxlength
          } = props;
          if ((0, import_utils.isDef)(maxlength) && (0, import_utils2.getStringLength)(value) > +maxlength) {
            const modelValue = getModelValue();
            if (modelValue && (0, import_utils2.getStringLength)(modelValue) === +maxlength) {
              return modelValue;
            }
            let selectionEnd = (_a = inputRef.value) == null ? void 0 : _a.selectionEnd;
            if (state.focused && selectionEnd) {
              const valueArr = [...value];
              const exceededLength = valueArr.length - +maxlength;
              selectionEnd = (0, import_utils2.getStringLength)(value.slice(0, selectionEnd));
              valueArr.splice(selectionEnd - exceededLength, exceededLength);
              return valueArr.join("");
            }
            return (0, import_utils2.cutString)(value, +maxlength);
          }
          return value;
        };
        const updateValue = (value, trigger = "onChange") => {
          var _a, _b;
          const originalValue = value;
          value = limitValueLength(value);
          const limitDiffLen = originalValue.length - value.length;
          if (props.type === "number" || props.type === "digit") {
            const isNumber = props.type === "number";
            value = (0, import_utils.formatNumber)(value, isNumber, isNumber);
            if (trigger === "onBlur" && value !== "" && (props.min !== void 0 || props.max !== void 0)) {
              const adjustedValue = (0, import_utils.clamp)(+value, (_a = props.min) != null ? _a : -Infinity, (_b = props.max) != null ? _b : Infinity);
              if (+value !== adjustedValue) {
                value = adjustedValue.toString();
              }
            }
          }
          let formatterDiffLen = 0;
          if (props.formatter && trigger === props.formatTrigger) {
            const {
              formatter,
              maxlength
            } = props;
            value = formatter(value);
            if ((0, import_utils.isDef)(maxlength) && (0, import_utils2.getStringLength)(value) > +maxlength) {
              value = (0, import_utils2.cutString)(value, +maxlength);
            }
            if (inputRef.value && state.focused) {
              const {
                selectionEnd
              } = inputRef.value;
              const bcoVal = (0, import_utils2.cutString)(originalValue, selectionEnd);
              formatterDiffLen = formatter(bcoVal).length - bcoVal.length;
            }
          }
          if (inputRef.value && inputRef.value.value !== value) {
            if (state.focused) {
              let {
                selectionStart,
                selectionEnd
              } = inputRef.value;
              inputRef.value.value = value;
              if ((0, import_utils.isDef)(selectionStart) && (0, import_utils.isDef)(selectionEnd)) {
                const valueLen = value.length;
                if (limitDiffLen) {
                  selectionStart -= limitDiffLen;
                  selectionEnd -= limitDiffLen;
                } else if (formatterDiffLen) {
                  selectionStart += formatterDiffLen;
                  selectionEnd += formatterDiffLen;
                }
                inputRef.value.setSelectionRange(Math.min(selectionStart, valueLen), Math.min(selectionEnd, valueLen));
              }
            } else {
              inputRef.value.value = value;
            }
          }
          if (value !== props.modelValue) {
            emit("update:modelValue", value);
          }
        };
        const onInput = (event) => {
          if (!event.target.composing) {
            updateValue(event.target.value);
          }
        };
        const blur = () => {
          var _a;
          return (_a = inputRef.value) == null ? void 0 : _a.blur();
        };
        const focus = () => {
          var _a;
          return (_a = inputRef.value) == null ? void 0 : _a.focus();
        };
        const adjustTextareaSize = () => {
          const input = inputRef.value;
          if (props.type === "textarea" && props.autosize && input) {
            (0, import_utils2.resizeTextarea)(input, props.autosize);
          }
        };
        const onFocus = (event) => {
          state.focused = true;
          emit("focus", event);
          (0, import_vue.nextTick)(adjustTextareaSize);
          if (getProp("readonly")) {
            blur();
          }
        };
        const onBlur = (event) => {
          state.focused = false;
          updateValue(getModelValue(), "onBlur");
          emit("blur", event);
          if (getProp("readonly")) {
            return;
          }
          validateWithTrigger("onBlur");
          (0, import_vue.nextTick)(adjustTextareaSize);
          (0, import_utils.resetScroll)();
        };
        const onClickInput = (event) => emit("clickInput", event);
        const onClickLeftIcon = (event) => emit("clickLeftIcon", event);
        const onClickRightIcon = (event) => emit("clickRightIcon", event);
        const onClear = (event) => {
          (0, import_utils.preventDefault)(event);
          emit("update:modelValue", "");
          emit("clear", event);
        };
        const showError = (0, import_vue.computed)(() => {
          if (typeof props.error === "boolean") {
            return props.error;
          }
          if (form && form.props.showError && state.status === "failed") {
            return true;
          }
        });
        const labelStyle = (0, import_vue.computed)(() => {
          const labelWidth = getProp("labelWidth");
          const labelAlign = getProp("labelAlign");
          if (labelWidth && labelAlign !== "top") {
            return {
              width: (0, import_utils.addUnit)(labelWidth)
            };
          }
        });
        const onKeypress = (event) => {
          const ENTER_CODE = 13;
          if (event.keyCode === ENTER_CODE) {
            const submitOnEnter = form && form.props.submitOnEnter;
            if (!submitOnEnter && props.type !== "textarea") {
              (0, import_utils.preventDefault)(event);
            }
            if (props.type === "search") {
              blur();
            }
          }
          emit("keypress", event);
        };
        const getInputId = () => props.id || `${id}-input`;
        const getValidationStatus = () => state.status;
        const renderInput = () => {
          const controlClass = bem("control", [getProp("inputAlign"), {
            error: showError.value,
            custom: !!slots.input,
            "min-height": props.type === "textarea" && !props.autosize
          }]);
          if (slots.input) {
            return (0, import_vue.createVNode)("div", {
              "class": controlClass,
              "onClick": onClickInput
            }, [slots.input()]);
          }
          const inputAttrs = {
            id: getInputId(),
            ref: inputRef,
            name: props.name,
            rows: props.rows !== void 0 ? +props.rows : void 0,
            class: controlClass,
            disabled: getProp("disabled"),
            readonly: getProp("readonly"),
            autofocus: props.autofocus,
            placeholder: props.placeholder,
            autocomplete: props.autocomplete,
            autocapitalize: props.autocapitalize,
            autocorrect: props.autocorrect,
            enterkeyhint: props.enterkeyhint,
            spellcheck: props.spellcheck,
            "aria-labelledby": props.label ? `${id}-label` : void 0,
            "data-allow-mismatch": "attribute",
            onBlur,
            onFocus,
            onInput,
            onClick: onClickInput,
            onChange: import_utils2.endComposing,
            onKeypress,
            onCompositionend: import_utils2.endComposing,
            onCompositionstart: import_utils2.startComposing
          };
          if (props.type === "textarea") {
            return (0, import_vue.createVNode)("textarea", (0, import_vue.mergeProps)(inputAttrs, {
              "inputmode": props.inputmode
            }), null);
          }
          return (0, import_vue.createVNode)("input", (0, import_vue.mergeProps)((0, import_utils2.mapInputType)(props.type, props.inputmode), inputAttrs), null);
        };
        const renderLeftIcon = () => {
          const leftIconSlot = slots["left-icon"];
          if (props.leftIcon || leftIconSlot) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("left-icon"),
              "onClick": onClickLeftIcon
            }, [leftIconSlot ? leftIconSlot() : (0, import_vue.createVNode)(import_icon.Icon, {
              "name": props.leftIcon,
              "classPrefix": props.iconPrefix
            }, null)]);
          }
        };
        const renderRightIcon = () => {
          const rightIconSlot = slots["right-icon"];
          if (props.rightIcon || rightIconSlot) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("right-icon"),
              "onClick": onClickRightIcon
            }, [rightIconSlot ? rightIconSlot() : (0, import_vue.createVNode)(import_icon.Icon, {
              "name": props.rightIcon,
              "classPrefix": props.iconPrefix
            }, null)]);
          }
        };
        const renderWordLimit = () => {
          if (props.showWordLimit && props.maxlength) {
            const count = (0, import_utils2.getStringLength)(getModelValue());
            return (0, import_vue.createVNode)("div", {
              "class": bem("word-limit")
            }, [(0, import_vue.createVNode)("span", {
              "class": bem("word-num")
            }, [count]), (0, import_vue.createTextVNode)("/"), props.maxlength]);
          }
        };
        const renderMessage = () => {
          if (form && form.props.showErrorMessage === false) {
            return;
          }
          const message = props.errorMessage || state.validateMessage;
          if (message) {
            const slot = slots["error-message"];
            const errorMessageAlign = getProp("errorMessageAlign");
            return (0, import_vue.createVNode)("div", {
              "class": bem("error-message", errorMessageAlign)
            }, [slot ? slot({
              message
            }) : message]);
          }
        };
        const renderLabel = () => {
          const labelWidth = getProp("labelWidth");
          const labelAlign = getProp("labelAlign");
          const colon = getProp("colon") ? ":" : "";
          if (slots.label) {
            return [slots.label(), colon];
          }
          if (props.label) {
            return (0, import_vue.createVNode)("label", {
              "id": `${id}-label`,
              "for": slots.input ? void 0 : getInputId(),
              "data-allow-mismatch": "attribute",
              "onClick": (event) => {
                (0, import_utils.preventDefault)(event);
                focus();
              },
              "style": labelAlign === "top" && labelWidth ? {
                width: (0, import_utils.addUnit)(labelWidth)
              } : void 0
            }, [props.label + colon]);
          }
        };
        const renderFieldBody = () => [(0, import_vue.createVNode)("div", {
          "class": bem("body")
        }, [renderInput(), showClear.value && (0, import_vue.createVNode)(import_icon.Icon, {
          "ref": clearIconRef,
          "name": props.clearIcon,
          "class": bem("clear")
        }, null), renderRightIcon(), slots.button && (0, import_vue.createVNode)("div", {
          "class": bem("button")
        }, [slots.button()])]), renderWordLimit(), renderMessage()];
        (0, import_use_expose.useExpose)({
          blur,
          focus,
          validate,
          formValue,
          resetValidation,
          getValidationStatus
        });
        (0, import_vue.provide)(import_use.CUSTOM_FIELD_INJECTION_KEY, {
          customValue,
          resetValidation,
          validateWithTrigger
        });
        (0, import_vue.watch)(() => props.modelValue, () => {
          updateValue(getModelValue());
          resetValidation();
          validateWithTrigger("onChange");
          (0, import_vue.nextTick)(adjustTextareaSize);
        });
        (0, import_vue.onMounted)(() => {
          updateValue(getModelValue(), props.formatTrigger);
          (0, import_vue.nextTick)(adjustTextareaSize);
        });
        (0, import_use.useEventListener)("touchstart", onClear, {
          target: (0, import_vue.computed)(() => {
            var _a;
            return (_a = clearIconRef.value) == null ? void 0 : _a.$el;
          })
        });
        return () => {
          const disabled = getProp("disabled");
          const labelAlign = getProp("labelAlign");
          const LeftIcon = renderLeftIcon();
          const renderTitle = () => {
            const Label = renderLabel();
            if (labelAlign === "top") {
              return [LeftIcon, Label].filter(Boolean);
            }
            return Label || [];
          };
          return (0, import_vue.createVNode)(import_cell.Cell, {
            "size": props.size,
            "class": bem({
              error: showError.value,
              disabled,
              [`label-${labelAlign}`]: labelAlign
            }),
            "center": props.center,
            "border": props.border,
            "isLink": props.isLink,
            "clickable": props.clickable,
            "titleStyle": labelStyle.value,
            "valueClass": bem("value"),
            "titleClass": [bem("label", [labelAlign, {
              required: showRequiredMark.value
            }]), props.labelClass],
            "arrowDirection": props.arrowDirection
          }, {
            icon: LeftIcon && labelAlign !== "top" ? () => LeftIcon : null,
            title: renderTitle,
            value: renderFieldBody,
            extra: slots.extra
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/field/index.js
var require_field = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/field/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Field: () => Field,
      default: () => stdin_default,
      fieldProps: () => import_Field2.fieldProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Field = __toESM(require_Field());
    var import_Field2 = require_Field();
    var Field = (0, import_utils.withInstall)(import_Field.default);
    var stdin_default = Field;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/toast/lock-click.js
var require_lock_click = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/toast/lock-click.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      lockClick: () => lockClick
    });
    module.exports = __toCommonJS2(stdin_exports);
    var lockCount = 0;
    function lockClick(lock) {
      if (lock) {
        if (!lockCount) {
          document.body.classList.add("van-toast--unclickable");
        }
        lockCount++;
      } else if (lockCount) {
        lockCount--;
        if (!lockCount) {
          document.body.classList.remove("van-toast--unclickable");
        }
      }
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/toast/Toast.js
var require_Toast = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/toast/Toast.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      toastProps: () => toastProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_lock_click = require_lock_click();
    var import_icon = require_icon();
    var import_popup = require_popup();
    var import_loading = require_loading();
    var [name, bem] = (0, import_utils.createNamespace)("toast");
    var popupInheritProps = ["show", "overlay", "teleport", "transition", "overlayClass", "overlayStyle", "closeOnClickOverlay", "zIndex"];
    var toastProps = {
      icon: String,
      show: Boolean,
      type: (0, import_utils.makeStringProp)("text"),
      overlay: Boolean,
      message: import_utils.numericProp,
      iconSize: import_utils.numericProp,
      duration: (0, import_utils.makeNumberProp)(2e3),
      position: (0, import_utils.makeStringProp)("middle"),
      teleport: [String, Object],
      wordBreak: String,
      className: import_utils.unknownProp,
      iconPrefix: String,
      transition: (0, import_utils.makeStringProp)("van-fade"),
      loadingType: String,
      forbidClick: Boolean,
      overlayClass: import_utils.unknownProp,
      overlayStyle: Object,
      closeOnClick: Boolean,
      closeOnClickOverlay: Boolean,
      zIndex: import_utils.numericProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: toastProps,
      emits: ["update:show"],
      setup(props, {
        emit,
        slots
      }) {
        let timer;
        let clickable = false;
        const toggleClickable = () => {
          const newValue = props.show && props.forbidClick;
          if (clickable !== newValue) {
            clickable = newValue;
            (0, import_lock_click.lockClick)(clickable);
          }
        };
        const updateShow = (show) => emit("update:show", show);
        const onClick = () => {
          if (props.closeOnClick) {
            updateShow(false);
          }
        };
        const clearTimer = () => clearTimeout(timer);
        const renderIcon = () => {
          const {
            icon,
            type,
            iconSize,
            iconPrefix,
            loadingType
          } = props;
          const hasIcon = icon || type === "success" || type === "fail";
          if (hasIcon) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "name": icon || type,
              "size": iconSize,
              "class": bem("icon"),
              "classPrefix": iconPrefix
            }, null);
          }
          if (type === "loading") {
            return (0, import_vue.createVNode)(import_loading.Loading, {
              "class": bem("loading"),
              "size": iconSize,
              "type": loadingType
            }, null);
          }
        };
        const renderMessage = () => {
          const {
            type,
            message
          } = props;
          if (slots.message) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("text")
            }, [slots.message()]);
          }
          if ((0, import_utils.isDef)(message) && message !== "") {
            return type === "html" ? (0, import_vue.createVNode)("div", {
              "key": 0,
              "class": bem("text"),
              "innerHTML": String(message)
            }, null) : (0, import_vue.createVNode)("div", {
              "class": bem("text")
            }, [message]);
          }
        };
        (0, import_vue.watch)(() => [props.show, props.forbidClick], toggleClickable);
        (0, import_vue.watch)(() => [props.show, props.type, props.message, props.duration], () => {
          clearTimer();
          if (props.show && props.duration > 0) {
            timer = setTimeout(() => {
              updateShow(false);
            }, props.duration);
          }
        });
        (0, import_vue.onMounted)(toggleClickable);
        (0, import_vue.onUnmounted)(toggleClickable);
        return () => (0, import_vue.createVNode)(import_popup.Popup, (0, import_vue.mergeProps)({
          "class": [bem([props.position, props.wordBreak === "normal" ? "break-normal" : props.wordBreak, {
            [props.type]: !props.icon
          }]), props.className],
          "lockScroll": false,
          "onClick": onClick,
          "onClosed": clearTimer,
          "onUpdate:show": updateShow
        }, (0, import_utils.pick)(props, popupInheritProps)), {
          default: () => [renderIcon(), renderMessage()]
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/utils/mount-component.js
var require_mount_component = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/utils/mount-component.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      mountComponent: () => mountComponent,
      usePopupState: () => usePopupState
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_basic = require_basic();
    var import_use_expose = require_use_expose();
    function usePopupState() {
      const state = (0, import_vue.reactive)({
        show: false
      });
      const toggle = (show) => {
        state.show = show;
      };
      const open = (props) => {
        (0, import_basic.extend)(state, props, { transitionAppear: true });
        toggle(true);
      };
      const close = () => toggle(false);
      (0, import_use_expose.useExpose)({ open, close, toggle });
      return {
        open,
        close,
        state,
        toggle
      };
    }
    function mountComponent(RootComponent) {
      const app = (0, import_vue.createApp)(RootComponent);
      const root = document.createElement("div");
      document.body.appendChild(root);
      return {
        instance: app.mount(root),
        unmount() {
          app.unmount();
          document.body.removeChild(root);
        }
      };
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/toast/function-call.js
var require_function_call = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/toast/function-call.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      allowMultipleToast: () => allowMultipleToast,
      closeToast: () => closeToast,
      resetToastDefaultOptions: () => resetToastDefaultOptions,
      setToastDefaultOptions: () => setToastDefaultOptions,
      showFailToast: () => showFailToast,
      showLoadingToast: () => showLoadingToast,
      showSuccessToast: () => showSuccessToast,
      showToast: () => showToast
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_mount_component = require_mount_component();
    var import_Toast = __toESM(require_Toast());
    var defaultOptions = {
      icon: "",
      type: "text",
      message: "",
      className: "",
      overlay: false,
      onClose: void 0,
      onOpened: void 0,
      duration: 2e3,
      teleport: "body",
      iconSize: void 0,
      iconPrefix: void 0,
      position: "middle",
      transition: "van-fade",
      forbidClick: false,
      loadingType: void 0,
      overlayClass: "",
      overlayStyle: void 0,
      closeOnClick: false,
      closeOnClickOverlay: false
    };
    var queue = [];
    var allowMultiple = false;
    var currentOptions = (0, import_utils.extend)({}, defaultOptions);
    var defaultOptionsMap = /* @__PURE__ */ new Map();
    function parseOptions(message) {
      if ((0, import_utils.isObject)(message)) {
        return message;
      }
      return {
        message
      };
    }
    function createInstance() {
      const {
        instance,
        unmount
      } = (0, import_mount_component.mountComponent)({
        setup() {
          const message = (0, import_vue.ref)("");
          const {
            open,
            state,
            close,
            toggle
          } = (0, import_mount_component.usePopupState)();
          const onClosed = () => {
            if (allowMultiple) {
              queue = queue.filter((item) => item !== instance);
              unmount();
            }
          };
          const render = () => {
            const attrs = {
              onClosed,
              "onUpdate:show": toggle
            };
            return (0, import_vue.createVNode)(import_Toast.default, (0, import_vue.mergeProps)(state, attrs), null);
          };
          (0, import_vue.watch)(message, (val) => {
            state.message = val;
          });
          (0, import_vue.getCurrentInstance)().render = render;
          return {
            open,
            close,
            message
          };
        }
      });
      return instance;
    }
    function getInstance() {
      if (!queue.length || allowMultiple) {
        const instance = createInstance();
        queue.push(instance);
      }
      return queue[queue.length - 1];
    }
    function showToast(options = {}) {
      if (!import_utils.inBrowser) {
        return {};
      }
      const toast = getInstance();
      const parsedOptions = parseOptions(options);
      toast.open((0, import_utils.extend)({}, currentOptions, defaultOptionsMap.get(parsedOptions.type || currentOptions.type), parsedOptions));
      return toast;
    }
    var createMethod = (type) => (options) => showToast((0, import_utils.extend)({
      type
    }, parseOptions(options)));
    var showLoadingToast = createMethod("loading");
    var showSuccessToast = createMethod("success");
    var showFailToast = createMethod("fail");
    var closeToast = (all) => {
      var _a;
      if (queue.length) {
        if (all) {
          queue.forEach((toast) => {
            toast.close();
          });
          queue = [];
        } else if (!allowMultiple) {
          queue[0].close();
        } else {
          (_a = queue.shift()) == null ? void 0 : _a.close();
        }
      }
    };
    function setToastDefaultOptions(type, options) {
      if (typeof type === "string") {
        defaultOptionsMap.set(type, options);
      } else {
        (0, import_utils.extend)(currentOptions, type);
      }
    }
    var resetToastDefaultOptions = (type) => {
      if (typeof type === "string") {
        defaultOptionsMap.delete(type);
      } else {
        currentOptions = (0, import_utils.extend)({}, defaultOptions);
        defaultOptionsMap.clear();
      }
    };
    var allowMultipleToast = (value = true) => {
      allowMultiple = value;
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/toast/index.js
var require_toast = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/toast/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Toast: () => Toast,
      allowMultipleToast: () => import_function_call.allowMultipleToast,
      closeToast: () => import_function_call.closeToast,
      default: () => stdin_default,
      resetToastDefaultOptions: () => import_function_call.resetToastDefaultOptions,
      setToastDefaultOptions: () => import_function_call.setToastDefaultOptions,
      showFailToast: () => import_function_call.showFailToast,
      showLoadingToast: () => import_function_call.showLoadingToast,
      showSuccessToast: () => import_function_call.showSuccessToast,
      showToast: () => import_function_call.showToast,
      toastProps: () => import_Toast2.toastProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Toast = __toESM(require_Toast());
    var import_Toast2 = require_Toast();
    var import_function_call = require_function_call();
    var Toast = (0, import_utils.withInstall)(import_Toast.default);
    var stdin_default = Toast;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/switch/Switch.js
var require_Switch = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/switch/Switch.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      switchProps: () => switchProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_loading = require_loading();
    var [name, bem] = (0, import_utils.createNamespace)("switch");
    var switchProps = {
      size: import_utils.numericProp,
      loading: Boolean,
      disabled: Boolean,
      modelValue: import_utils.unknownProp,
      activeColor: String,
      inactiveColor: String,
      activeValue: {
        type: import_utils.unknownProp,
        default: true
      },
      inactiveValue: {
        type: import_utils.unknownProp,
        default: false
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: switchProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const isChecked = () => props.modelValue === props.activeValue;
        const onClick = () => {
          if (!props.disabled && !props.loading) {
            const newValue = isChecked() ? props.inactiveValue : props.activeValue;
            emit("update:modelValue", newValue);
            emit("change", newValue);
          }
        };
        const renderLoading = () => {
          if (props.loading) {
            const color = isChecked() ? props.activeColor : props.inactiveColor;
            return (0, import_vue.createVNode)(import_loading.Loading, {
              "class": bem("loading"),
              "color": color
            }, null);
          }
          if (slots.node) {
            return slots.node();
          }
        };
        (0, import_use.useCustomFieldValue)(() => props.modelValue);
        return () => {
          var _a;
          const {
            size,
            loading,
            disabled,
            activeColor,
            inactiveColor
          } = props;
          const checked = isChecked();
          const style = {
            fontSize: (0, import_utils.addUnit)(size),
            backgroundColor: checked ? activeColor : inactiveColor
          };
          return (0, import_vue.createVNode)("div", {
            "role": "switch",
            "class": bem({
              on: checked,
              loading,
              disabled
            }),
            "style": style,
            "tabindex": disabled ? void 0 : 0,
            "aria-checked": checked,
            "onClick": onClick
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("node")
          }, [renderLoading()]), (_a = slots.background) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/switch/index.js
var require_switch = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/switch/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Switch: () => Switch,
      default: () => stdin_default,
      switchProps: () => import_Switch2.switchProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Switch = __toESM(require_Switch());
    var import_Switch2 = require_Switch();
    var Switch = (0, import_utils.withInstall)(import_Switch.default);
    var stdin_default = Switch;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-edit/AddressEditDetail.js
var require_AddressEditDetail = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-edit/AddressEditDetail.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_cell = require_cell();
    var import_field = require_field();
    var [name, bem] = (0, import_utils.createNamespace)("address-edit-detail");
    var t = (0, import_utils.createNamespace)("address-edit")[2];
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: {
        show: Boolean,
        rows: import_utils.numericProp,
        value: String,
        rules: Array,
        focused: Boolean,
        maxlength: import_utils.numericProp,
        searchResult: Array,
        showSearchResult: Boolean
      },
      emits: ["blur", "focus", "input", "selectSearch"],
      setup(props, {
        emit
      }) {
        const field = (0, import_vue.ref)();
        const showSearchResult = () => props.focused && props.searchResult && props.showSearchResult;
        const onSelect = (express) => {
          emit("selectSearch", express);
          emit("input", `${express.address || ""} ${express.name || ""}`.trim());
        };
        const renderSearchResult = () => {
          if (!showSearchResult()) {
            return;
          }
          const {
            searchResult
          } = props;
          return searchResult.map((express) => (0, import_vue.createVNode)(import_cell.Cell, {
            "clickable": true,
            "key": (express.name || "") + (express.address || ""),
            "icon": "location-o",
            "title": express.name,
            "label": express.address,
            "class": bem("search-item"),
            "border": false,
            "onClick": () => onSelect(express)
          }, null));
        };
        const onBlur = (event) => emit("blur", event);
        const onFocus = (event) => emit("focus", event);
        const onInput = (value) => emit("input", value);
        return () => {
          if (props.show) {
            return (0, import_vue.createVNode)(import_vue.Fragment, null, [(0, import_vue.createVNode)(import_field.Field, {
              "autosize": true,
              "clearable": true,
              "ref": field,
              "class": bem(),
              "rows": props.rows,
              "type": "textarea",
              "rules": props.rules,
              "label": t("addressDetail"),
              "border": !showSearchResult(),
              "maxlength": props.maxlength,
              "modelValue": props.value,
              "placeholder": t("addressDetail"),
              "onBlur": onBlur,
              "onFocus": onFocus,
              "onUpdate:modelValue": onInput
            }, null), renderSearchResult()]);
          }
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-edit/AddressEdit.js
var require_AddressEdit = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-edit/AddressEdit.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      addressEditProps: () => addressEditProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_expose = require_use_expose();
    var import_area = require_area();
    var import_cell = require_cell();
    var import_form = require_form();
    var import_field = require_field();
    var import_popup = require_popup();
    var import_toast = require_toast();
    var import_button = require_button();
    var import_switch = require_switch();
    var import_AddressEditDetail = __toESM(require_AddressEditDetail());
    var import_utils2 = require_utils4();
    var [name, bem, t] = (0, import_utils.createNamespace)("address-edit");
    var DEFAULT_DATA = {
      name: "",
      tel: "",
      city: "",
      county: "",
      province: "",
      areaCode: "",
      isDefault: false,
      addressDetail: ""
    };
    var addressEditProps = {
      areaList: Object,
      isSaving: Boolean,
      isDeleting: Boolean,
      validator: Function,
      showArea: import_utils.truthProp,
      showDetail: import_utils.truthProp,
      showDelete: Boolean,
      disableArea: Boolean,
      searchResult: Array,
      telMaxlength: import_utils.numericProp,
      showSetDefault: Boolean,
      saveButtonText: String,
      areaPlaceholder: String,
      deleteButtonText: String,
      showSearchResult: Boolean,
      detailRows: (0, import_utils.makeNumericProp)(1),
      detailMaxlength: (0, import_utils.makeNumericProp)(200),
      areaColumnsPlaceholder: (0, import_utils.makeArrayProp)(),
      addressInfo: {
        type: Object,
        default: () => (0, import_utils.extend)({}, DEFAULT_DATA)
      },
      telValidator: {
        type: Function,
        default: import_utils.isMobile
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: addressEditProps,
      emits: ["save", "focus", "change", "delete", "clickArea", "changeArea", "changeDetail", "selectSearch", "changeDefault"],
      setup(props, {
        emit,
        slots
      }) {
        const areaRef = (0, import_vue.ref)();
        const data = (0, import_vue.reactive)({});
        const showAreaPopup = (0, import_vue.ref)(false);
        const detailFocused = (0, import_vue.ref)(false);
        const areaListLoaded = (0, import_vue.computed)(() => (0, import_utils.isObject)(props.areaList) && Object.keys(props.areaList).length);
        const areaText = (0, import_vue.computed)(() => {
          const {
            province,
            city,
            county,
            areaCode
          } = data;
          if (areaCode) {
            const arr = [province, city, county];
            if (province && province === city) {
              arr.splice(1, 1);
            }
            return arr.filter(Boolean).join("/");
          }
          return "";
        });
        const hideBottomFields = (0, import_vue.computed)(() => {
          var _a;
          return ((_a = props.searchResult) == null ? void 0 : _a.length) && detailFocused.value;
        });
        const onFocus = (key) => {
          detailFocused.value = key === "addressDetail";
          emit("focus", key);
        };
        const onChange = (key, value) => {
          emit("change", {
            key,
            value
          });
        };
        const rules = (0, import_vue.computed)(() => {
          const {
            validator,
            telValidator
          } = props;
          const makeRule = (name2, emptyMessage) => ({
            validator: (value) => {
              if (validator) {
                const message = validator(name2, value);
                if (message) {
                  return message;
                }
              }
              if (!value) {
                return emptyMessage;
              }
              return true;
            }
          });
          return {
            name: [makeRule("name", t("nameEmpty"))],
            tel: [makeRule("tel", t("telInvalid")), {
              validator: telValidator,
              message: t("telInvalid")
            }],
            areaCode: [makeRule("areaCode", t("areaEmpty"))],
            addressDetail: [makeRule("addressDetail", t("addressEmpty"))]
          };
        });
        const onSave = () => emit("save", data);
        const onChangeDetail = (val) => {
          data.addressDetail = val;
          emit("changeDetail", val);
        };
        const assignAreaText = (options) => {
          data.province = options[0].text;
          data.city = options[1].text;
          data.county = options[2].text;
        };
        const onAreaConfirm = ({
          selectedValues,
          selectedOptions
        }) => {
          if (selectedValues.some((value) => value === import_utils2.AREA_EMPTY_CODE)) {
            (0, import_toast.showToast)(t("areaEmpty"));
          } else {
            showAreaPopup.value = false;
            assignAreaText(selectedOptions);
            emit("changeArea", selectedOptions);
          }
        };
        const onDelete = () => emit("delete", data);
        const setAreaCode = (code) => {
          data.areaCode = code || "";
        };
        const onDetailBlur = () => {
          setTimeout(() => {
            detailFocused.value = false;
          });
        };
        const setAddressDetail = (value) => {
          data.addressDetail = value;
        };
        const renderSetDefaultCell = () => {
          if (props.showSetDefault) {
            const slots2 = {
              "right-icon": () => (0, import_vue.createVNode)(import_switch.Switch, {
                "modelValue": data.isDefault,
                "onUpdate:modelValue": ($event) => data.isDefault = $event,
                "onChange": (event) => emit("changeDefault", event)
              }, null)
            };
            return (0, import_vue.withDirectives)((0, import_vue.createVNode)(import_cell.Cell, {
              "center": true,
              "border": false,
              "title": t("defaultAddress"),
              "class": bem("default")
            }, slots2), [[import_vue.vShow, !hideBottomFields.value]]);
          }
        };
        (0, import_use_expose.useExpose)({
          setAreaCode,
          setAddressDetail
        });
        (0, import_vue.watch)(() => props.addressInfo, (value) => {
          (0, import_utils.extend)(data, DEFAULT_DATA, value);
          (0, import_vue.nextTick)(() => {
            var _a;
            const options = (_a = areaRef.value) == null ? void 0 : _a.getSelectedOptions();
            if (options && options.every((option) => option && option.value !== import_utils2.AREA_EMPTY_CODE)) {
              assignAreaText(options);
            }
          });
        }, {
          deep: true,
          immediate: true
        });
        return () => {
          const {
            disableArea
          } = props;
          return (0, import_vue.createVNode)(import_form.Form, {
            "class": bem(),
            "onSubmit": onSave
          }, {
            default: () => {
              var _a;
              return [(0, import_vue.createVNode)("div", {
                "class": bem("fields")
              }, [(0, import_vue.createVNode)(import_field.Field, {
                "modelValue": data.name,
                "onUpdate:modelValue": [($event) => data.name = $event, (val) => onChange("name", val)],
                "clearable": true,
                "label": t("name"),
                "rules": rules.value.name,
                "placeholder": t("name"),
                "onFocus": () => onFocus("name")
              }, null), (0, import_vue.createVNode)(import_field.Field, {
                "modelValue": data.tel,
                "onUpdate:modelValue": [($event) => data.tel = $event, (val) => onChange("tel", val)],
                "clearable": true,
                "type": "tel",
                "label": t("tel"),
                "rules": rules.value.tel,
                "maxlength": props.telMaxlength,
                "placeholder": t("tel"),
                "onFocus": () => onFocus("tel")
              }, null), (0, import_vue.withDirectives)((0, import_vue.createVNode)(import_field.Field, {
                "readonly": true,
                "label": t("area"),
                "is-link": !disableArea,
                "modelValue": areaText.value,
                "rules": props.showArea ? rules.value.areaCode : void 0,
                "placeholder": props.areaPlaceholder || t("area"),
                "onFocus": () => onFocus("areaCode"),
                "onClick": () => {
                  emit("clickArea");
                  showAreaPopup.value = !disableArea;
                }
              }, null), [[import_vue.vShow, props.showArea]]), (0, import_vue.createVNode)(import_AddressEditDetail.default, {
                "show": props.showDetail,
                "rows": props.detailRows,
                "rules": rules.value.addressDetail,
                "value": data.addressDetail,
                "focused": detailFocused.value,
                "maxlength": props.detailMaxlength,
                "searchResult": props.searchResult,
                "showSearchResult": props.showSearchResult,
                "onBlur": onDetailBlur,
                "onFocus": () => onFocus("addressDetail"),
                "onInput": onChangeDetail,
                "onSelectSearch": (event) => emit("selectSearch", event)
              }, null), (_a = slots.default) == null ? void 0 : _a.call(slots)]), renderSetDefaultCell(), (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", {
                "class": bem("buttons")
              }, [(0, import_vue.createVNode)(import_button.Button, {
                "block": true,
                "round": true,
                "type": "primary",
                "text": props.saveButtonText || t("save"),
                "class": bem("button"),
                "loading": props.isSaving,
                "nativeType": "submit"
              }, null), props.showDelete && (0, import_vue.createVNode)(import_button.Button, {
                "block": true,
                "round": true,
                "class": bem("button"),
                "loading": props.isDeleting,
                "text": props.deleteButtonText || t("delete"),
                "onClick": onDelete
              }, null)]), [[import_vue.vShow, !hideBottomFields.value]]), (0, import_vue.createVNode)(import_popup.Popup, {
                "show": showAreaPopup.value,
                "onUpdate:show": ($event) => showAreaPopup.value = $event,
                "round": true,
                "teleport": "body",
                "position": "bottom",
                "lazyRender": false
              }, {
                default: () => [(0, import_vue.createVNode)(import_area.Area, {
                  "modelValue": data.areaCode,
                  "onUpdate:modelValue": ($event) => data.areaCode = $event,
                  "ref": areaRef,
                  "loading": !areaListLoaded.value,
                  "areaList": props.areaList,
                  "columnsPlaceholder": props.areaColumnsPlaceholder,
                  "onConfirm": onAreaConfirm,
                  "onCancel": () => {
                    showAreaPopup.value = false;
                  }
                }, null)]
              })];
            }
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-edit/index.js
var require_address_edit = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-edit/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      AddressEdit: () => AddressEdit,
      addressEditProps: () => import_AddressEdit2.addressEditProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_AddressEdit = __toESM(require_AddressEdit());
    var import_AddressEdit2 = require_AddressEdit();
    var AddressEdit = (0, import_utils.withInstall)(import_AddressEdit.default);
    var stdin_default = AddressEdit;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/radio-group/RadioGroup.js
var require_RadioGroup = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/radio-group/RadioGroup.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      RADIO_KEY: () => RADIO_KEY,
      default: () => stdin_default,
      radioGroupProps: () => radioGroupProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var [name, bem] = (0, import_utils.createNamespace)("radio-group");
    var radioGroupProps = {
      shape: String,
      disabled: Boolean,
      iconSize: import_utils.numericProp,
      direction: String,
      modelValue: import_utils.unknownProp,
      checkedColor: String
    };
    var RADIO_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: radioGroupProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          linkChildren
        } = (0, import_use.useChildren)(RADIO_KEY);
        const updateValue = (value) => emit("update:modelValue", value);
        (0, import_vue.watch)(() => props.modelValue, (value) => emit("change", value));
        linkChildren({
          props,
          updateValue
        });
        (0, import_use.useCustomFieldValue)(() => props.modelValue);
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": bem([props.direction]),
            "role": "radiogroup"
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/radio-group/index.js
var require_radio_group = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/radio-group/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      RadioGroup: () => RadioGroup,
      default: () => stdin_default,
      radioGroupProps: () => import_RadioGroup2.radioGroupProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_RadioGroup = __toESM(require_RadioGroup());
    var import_RadioGroup2 = require_RadioGroup();
    var RadioGroup = (0, import_utils.withInstall)(import_RadioGroup.default);
    var stdin_default = RadioGroup;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox-group/CheckboxGroup.js
var require_CheckboxGroup = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox-group/CheckboxGroup.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      CHECKBOX_GROUP_KEY: () => CHECKBOX_GROUP_KEY,
      checkboxGroupProps: () => checkboxGroupProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("checkbox-group");
    var checkboxGroupProps = {
      max: import_utils.numericProp,
      shape: (0, import_utils.makeStringProp)("round"),
      disabled: Boolean,
      iconSize: import_utils.numericProp,
      direction: String,
      modelValue: (0, import_utils.makeArrayProp)(),
      checkedColor: String
    };
    var CHECKBOX_GROUP_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: checkboxGroupProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(CHECKBOX_GROUP_KEY);
        const updateValue = (value) => emit("update:modelValue", value);
        const toggleAll = (options = {}) => {
          if (typeof options === "boolean") {
            options = {
              checked: options
            };
          }
          const {
            checked,
            skipDisabled
          } = options;
          const checkedChildren = children.filter((item) => {
            if (!item.props.bindGroup) {
              return false;
            }
            if (item.props.disabled && skipDisabled) {
              return item.checked.value;
            }
            return checked != null ? checked : !item.checked.value;
          });
          const names = checkedChildren.map((item) => item.name);
          updateValue(names);
        };
        (0, import_vue.watch)(() => props.modelValue, (value) => emit("change", value));
        (0, import_use_expose.useExpose)({
          toggleAll
        });
        (0, import_use.useCustomFieldValue)(() => props.modelValue);
        linkChildren({
          props,
          updateValue
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": bem([props.direction])
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox-group/index.js
var require_checkbox_group = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox-group/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      CheckboxGroup: () => CheckboxGroup,
      checkboxGroupProps: () => import_CheckboxGroup2.checkboxGroupProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_CheckboxGroup = __toESM(require_CheckboxGroup());
    var import_CheckboxGroup2 = require_CheckboxGroup();
    var CheckboxGroup = (0, import_utils.withInstall)(import_CheckboxGroup.default);
    var stdin_default = CheckboxGroup;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tag/Tag.js
var require_Tag = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tag/Tag.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      tagProps: () => tagProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("tag");
    var tagProps = {
      size: String,
      mark: Boolean,
      show: import_utils.truthProp,
      type: (0, import_utils.makeStringProp)("default"),
      color: String,
      plain: Boolean,
      round: Boolean,
      textColor: String,
      closeable: Boolean
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: tagProps,
      emits: ["close"],
      setup(props, {
        slots,
        emit
      }) {
        const onClose = (event) => {
          event.stopPropagation();
          emit("close", event);
        };
        const getStyle = () => {
          if (props.plain) {
            return {
              color: props.textColor || props.color,
              borderColor: props.color
            };
          }
          return {
            color: props.textColor,
            background: props.color
          };
        };
        const renderTag = () => {
          var _a;
          const {
            type,
            mark,
            plain,
            round,
            size,
            closeable
          } = props;
          const classes = {
            mark,
            plain,
            round
          };
          if (size) {
            classes[size] = size;
          }
          const CloseIcon = closeable && (0, import_vue.createVNode)(import_icon.Icon, {
            "name": "cross",
            "class": [bem("close"), import_utils.HAPTICS_FEEDBACK],
            "onClick": onClose
          }, null);
          return (0, import_vue.createVNode)("span", {
            "style": getStyle(),
            "class": bem([classes, type])
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots), CloseIcon]);
        };
        return () => (0, import_vue.createVNode)(import_vue.Transition, {
          "name": props.closeable ? "van-fade" : void 0
        }, {
          default: () => [props.show ? renderTag() : null]
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tag/index.js
var require_tag = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tag/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Tag: () => Tag,
      default: () => stdin_default,
      tagProps: () => import_Tag2.tagProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Tag = __toESM(require_Tag());
    var import_Tag2 = require_Tag();
    var Tag = (0, import_utils.withInstall)(import_Tag.default);
    var stdin_default = Tag;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox/Checker.js
var require_Checker = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox/Checker.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      checkerProps: () => checkerProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_icon = require_icon();
    var checkerProps = {
      name: import_utils.unknownProp,
      disabled: Boolean,
      iconSize: import_utils.numericProp,
      modelValue: import_utils.unknownProp,
      checkedColor: String,
      labelPosition: String,
      labelDisabled: Boolean
    };
    var stdin_default = (0, import_vue.defineComponent)({
      props: (0, import_utils.extend)({}, checkerProps, {
        bem: (0, import_utils.makeRequiredProp)(Function),
        role: String,
        shape: String,
        parent: Object,
        checked: Boolean,
        bindGroup: import_utils.truthProp,
        indeterminate: {
          type: Boolean,
          default: null
        }
      }),
      emits: ["click", "toggle"],
      setup(props, {
        emit,
        slots
      }) {
        const iconRef = (0, import_vue.ref)();
        const getParentProp = (name) => {
          if (props.parent && props.bindGroup) {
            return props.parent.props[name];
          }
        };
        const disabled = (0, import_vue.computed)(() => {
          if (props.parent && props.bindGroup) {
            const disabled2 = getParentProp("disabled") || props.disabled;
            if (props.role === "checkbox") {
              const checkedCount = getParentProp("modelValue").length;
              const max = getParentProp("max");
              const overlimit = max && checkedCount >= +max;
              return disabled2 || overlimit && !props.checked;
            }
            return disabled2;
          }
          return props.disabled;
        });
        const direction = (0, import_vue.computed)(() => getParentProp("direction"));
        const iconStyle = (0, import_vue.computed)(() => {
          const checkedColor = props.checkedColor || getParentProp("checkedColor");
          if (checkedColor && (props.checked || props.indeterminate) && !disabled.value) {
            return {
              borderColor: checkedColor,
              backgroundColor: checkedColor
            };
          }
        });
        const shape = (0, import_vue.computed)(() => {
          return props.shape || getParentProp("shape") || "round";
        });
        const onClick = (event) => {
          const {
            target
          } = event;
          const icon = iconRef.value;
          const iconClicked = icon === target || (icon == null ? void 0 : icon.contains(target));
          if (!disabled.value && (iconClicked || !props.labelDisabled)) {
            emit("toggle");
          }
          emit("click", event);
        };
        const renderIcon = () => {
          var _a, _b;
          const {
            bem,
            checked,
            indeterminate
          } = props;
          const iconSize = props.iconSize || getParentProp("iconSize");
          return (0, import_vue.createVNode)("div", {
            "ref": iconRef,
            "class": bem("icon", [shape.value, {
              disabled: disabled.value,
              checked,
              indeterminate
            }]),
            "style": shape.value !== "dot" ? {
              fontSize: (0, import_utils.addUnit)(iconSize)
            } : {
              width: (0, import_utils.addUnit)(iconSize),
              height: (0, import_utils.addUnit)(iconSize),
              borderColor: (_a = iconStyle.value) == null ? void 0 : _a.borderColor
            }
          }, [slots.icon ? slots.icon({
            checked,
            disabled: disabled.value
          }) : shape.value !== "dot" ? (0, import_vue.createVNode)(import_icon.Icon, {
            "name": indeterminate ? "minus" : "success",
            "style": iconStyle.value
          }, null) : (0, import_vue.createVNode)("div", {
            "class": bem("icon--dot__icon"),
            "style": {
              backgroundColor: (_b = iconStyle.value) == null ? void 0 : _b.backgroundColor
            }
          }, null)]);
        };
        const renderLabel = () => {
          const {
            checked
          } = props;
          if (slots.default) {
            return (0, import_vue.createVNode)("span", {
              "class": props.bem("label", [props.labelPosition, {
                disabled: disabled.value
              }])
            }, [slots.default({
              checked,
              disabled: disabled.value
            })]);
          }
        };
        return () => {
          const nodes = props.labelPosition === "left" ? [renderLabel(), renderIcon()] : [renderIcon(), renderLabel()];
          return (0, import_vue.createVNode)("div", {
            "role": props.role,
            "class": props.bem([{
              disabled: disabled.value,
              "label-disabled": props.labelDisabled
            }, direction.value]),
            "tabindex": disabled.value ? void 0 : 0,
            "aria-checked": props.checked,
            "onClick": onClick
          }, [nodes]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/radio/Radio.js
var require_Radio = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/radio/Radio.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      radioProps: () => radioProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_RadioGroup = require_RadioGroup();
    var import_use = require_index_cjs();
    var import_Checker = __toESM(require_Checker());
    var radioProps = (0, import_utils.extend)({}, import_Checker.checkerProps, {
      shape: String
    });
    var [name, bem] = (0, import_utils.createNamespace)("radio");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: radioProps,
      emits: ["update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          parent
        } = (0, import_use.useParent)(import_RadioGroup.RADIO_KEY);
        const checked = () => {
          const value = parent ? parent.props.modelValue : props.modelValue;
          return value === props.name;
        };
        const toggle = () => {
          if (parent) {
            parent.updateValue(props.name);
          } else {
            emit("update:modelValue", props.name);
          }
        };
        return () => (0, import_vue.createVNode)(import_Checker.default, (0, import_vue.mergeProps)({
          "bem": bem,
          "role": "radio",
          "parent": parent,
          "checked": checked(),
          "onToggle": toggle
        }, props), (0, import_utils.pick)(slots, ["default", "icon"]));
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/radio/index.js
var require_radio = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/radio/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Radio: () => Radio,
      default: () => stdin_default,
      radioProps: () => import_Radio2.radioProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Radio = __toESM(require_Radio());
    var import_Radio2 = require_Radio();
    var Radio = (0, import_utils.withInstall)(import_Radio.default);
    var stdin_default = Radio;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox/Checkbox.js
var require_Checkbox = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox/Checkbox.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      checkboxProps: () => checkboxProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_CheckboxGroup = require_CheckboxGroup();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_Checker = __toESM(require_Checker());
    var [name, bem] = (0, import_utils.createNamespace)("checkbox");
    var checkboxProps = (0, import_utils.extend)({}, import_Checker.checkerProps, {
      shape: String,
      bindGroup: import_utils.truthProp,
      indeterminate: {
        type: Boolean,
        default: null
      }
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: checkboxProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          parent
        } = (0, import_use.useParent)(import_CheckboxGroup.CHECKBOX_GROUP_KEY);
        const setParentValue = (checked2) => {
          const {
            name: name2
          } = props;
          const {
            max,
            modelValue
          } = parent.props;
          const value = modelValue.slice();
          if (checked2) {
            const overlimit = max && value.length >= +max;
            if (!overlimit && !value.includes(name2)) {
              value.push(name2);
              if (props.bindGroup) {
                parent.updateValue(value);
              }
            }
          } else {
            const index = value.indexOf(name2);
            if (index !== -1) {
              value.splice(index, 1);
              if (props.bindGroup) {
                parent.updateValue(value);
              }
            }
          }
        };
        const checked = (0, import_vue.computed)(() => {
          if (parent && props.bindGroup) {
            return parent.props.modelValue.indexOf(props.name) !== -1;
          }
          return !!props.modelValue;
        });
        const toggle = (newValue = !checked.value) => {
          if (parent && props.bindGroup) {
            setParentValue(newValue);
          } else {
            emit("update:modelValue", newValue);
          }
          if (props.indeterminate !== null) emit("change", newValue);
        };
        (0, import_vue.watch)(() => props.modelValue, (value) => {
          if (props.indeterminate === null) emit("change", value);
        });
        (0, import_use_expose.useExpose)({
          toggle,
          props,
          checked
        });
        (0, import_use.useCustomFieldValue)(() => props.modelValue);
        return () => (0, import_vue.createVNode)(import_Checker.default, (0, import_vue.mergeProps)({
          "bem": bem,
          "role": "checkbox",
          "parent": parent,
          "checked": checked.value,
          "onToggle": toggle
        }, props), (0, import_utils.pick)(slots, ["default", "icon"]));
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox/index.js
var require_checkbox = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/checkbox/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Checkbox: () => Checkbox,
      checkboxProps: () => import_Checkbox2.checkboxProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Checkbox = __toESM(require_Checkbox());
    var import_Checkbox2 = require_Checkbox();
    var Checkbox = (0, import_utils.withInstall)(import_Checkbox.default);
    var stdin_default = Checkbox;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-list/AddressListItem.js
var require_AddressListItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-list/AddressListItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_tag = require_tag();
    var import_icon = require_icon();
    var import_cell = require_cell();
    var import_radio = require_radio();
    var import_checkbox = require_checkbox();
    var [name, bem] = (0, import_utils.createNamespace)("address-item");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: {
        address: (0, import_utils.makeRequiredProp)(Object),
        disabled: Boolean,
        switchable: Boolean,
        singleChoice: Boolean,
        defaultTagText: String,
        rightIcon: (0, import_utils.makeStringProp)("edit")
      },
      emits: ["edit", "click", "select"],
      setup(props, {
        slots,
        emit
      }) {
        const onClick = (event) => {
          if (props.switchable) {
            emit("select");
          }
          emit("click", event);
        };
        const renderRightIcon = () => (0, import_vue.createVNode)(import_icon.Icon, {
          "name": props.rightIcon,
          "class": bem("edit"),
          "onClick": (event) => {
            event.stopPropagation();
            emit("edit");
            emit("click", event);
          }
        }, null);
        const renderTag = () => {
          if (slots.tag) {
            return slots.tag(props.address);
          }
          if (props.address.isDefault && props.defaultTagText) {
            return (0, import_vue.createVNode)(import_tag.Tag, {
              "type": "primary",
              "round": true,
              "class": bem("tag")
            }, {
              default: () => [props.defaultTagText]
            });
          }
        };
        const renderContent = () => {
          const {
            address,
            disabled,
            switchable,
            singleChoice
          } = props;
          const Info = [(0, import_vue.createVNode)("div", {
            "class": bem("name")
          }, [`${address.name} ${address.tel}`, renderTag()]), (0, import_vue.createVNode)("div", {
            "class": bem("address")
          }, [address.address])];
          if (switchable && !disabled) {
            if (singleChoice) {
              return (0, import_vue.createVNode)(import_radio.Radio, {
                "name": address.id,
                "iconSize": 18
              }, {
                default: () => [Info]
              });
            } else {
              return (0, import_vue.createVNode)(import_checkbox.Checkbox, {
                "name": address.id,
                "iconSize": 18
              }, {
                default: () => [Info]
              });
            }
          }
          return Info;
        };
        return () => {
          var _a;
          const {
            disabled
          } = props;
          return (0, import_vue.createVNode)("div", {
            "class": bem({
              disabled
            }),
            "onClick": onClick
          }, [(0, import_vue.createVNode)(import_cell.Cell, {
            "border": false,
            "titleClass": bem("title")
          }, {
            title: renderContent,
            "right-icon": renderRightIcon
          }), (_a = slots.bottom) == null ? void 0 : _a.call(slots, (0, import_utils.extend)({}, props.address, {
            disabled
          }))]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-list/AddressList.js
var require_AddressList = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-list/AddressList.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      addressListProps: () => addressListProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_button = require_button();
    var import_radio_group = require_radio_group();
    var import_checkbox_group = require_checkbox_group();
    var import_AddressListItem = __toESM(require_AddressListItem());
    var [name, bem, t] = (0, import_utils.createNamespace)("address-list");
    var addressListProps = {
      list: (0, import_utils.makeArrayProp)(),
      modelValue: [...import_utils.numericProp, Array],
      switchable: import_utils.truthProp,
      disabledText: String,
      disabledList: (0, import_utils.makeArrayProp)(),
      showAddButton: import_utils.truthProp,
      addButtonText: String,
      defaultTagText: String,
      rightIcon: (0, import_utils.makeStringProp)("edit")
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: addressListProps,
      emits: ["add", "edit", "select", "clickItem", "editDisabled", "selectDisabled", "update:modelValue"],
      setup(props, {
        slots,
        emit
      }) {
        const singleChoice = (0, import_vue.computed)(() => !Array.isArray(props.modelValue));
        const renderItem = (item, index, disabled) => {
          const onEdit = () => emit(disabled ? "editDisabled" : "edit", item, index);
          const onClick = (event) => emit("clickItem", item, index, {
            event
          });
          const onSelect = () => {
            emit(disabled ? "selectDisabled" : "select", item, index);
            if (!disabled) {
              if (singleChoice.value) {
                emit("update:modelValue", item.id);
              } else {
                const value = props.modelValue;
                if (value.includes(item.id)) {
                  emit("update:modelValue", value.filter((id) => id !== item.id));
                } else {
                  emit("update:modelValue", [...value, item.id]);
                }
              }
            }
          };
          return (0, import_vue.createVNode)(import_AddressListItem.default, {
            "key": item.id,
            "address": item,
            "disabled": disabled,
            "switchable": props.switchable,
            "singleChoice": singleChoice.value,
            "defaultTagText": props.defaultTagText,
            "rightIcon": props.rightIcon,
            "onEdit": onEdit,
            "onClick": onClick,
            "onSelect": onSelect
          }, {
            bottom: slots["item-bottom"],
            tag: slots.tag
          });
        };
        const renderList = (list, disabled) => {
          if (list) {
            return list.map((item, index) => renderItem(item, index, disabled));
          }
        };
        const renderBottom = () => props.showAddButton ? (0, import_vue.createVNode)("div", {
          "class": [bem("bottom"), "van-safe-area-bottom"]
        }, [(0, import_vue.createVNode)(import_button.Button, {
          "round": true,
          "block": true,
          "type": "primary",
          "text": props.addButtonText || t("add"),
          "class": bem("add"),
          "onClick": () => emit("add")
        }, null)]) : void 0;
        return () => {
          var _a, _b;
          const List = renderList(props.list);
          const DisabledList = renderList(props.disabledList, true);
          const DisabledText = props.disabledText && (0, import_vue.createVNode)("div", {
            "class": bem("disabled-text")
          }, [props.disabledText]);
          return (0, import_vue.createVNode)("div", {
            "class": bem()
          }, [(_a = slots.top) == null ? void 0 : _a.call(slots), !singleChoice.value && Array.isArray(props.modelValue) ? (0, import_vue.createVNode)(import_checkbox_group.CheckboxGroup, {
            "modelValue": props.modelValue
          }, {
            default: () => [List]
          }) : (0, import_vue.createVNode)(import_radio_group.RadioGroup, {
            "modelValue": props.modelValue
          }, {
            default: () => [List]
          }), DisabledText, DisabledList, (_b = slots.default) == null ? void 0 : _b.call(slots), renderBottom()]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-list/index.js
var require_address_list = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/address-list/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      AddressList: () => AddressList,
      addressListProps: () => import_AddressList2.addressListProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_AddressList = __toESM(require_AddressList());
    var import_AddressList2 = require_AddressList();
    var AddressList = (0, import_utils.withInstall)(import_AddressList.default);
    var stdin_default = AddressList;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/util.js
var require_util = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/util.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ImageCache: () => ImageCache,
      getBestSelectionFromSrcset: () => getBestSelectionFromSrcset,
      getDPR: () => getDPR,
      hasIntersectionObserver: () => hasIntersectionObserver,
      loadImageAsync: () => loadImageAsync,
      modeType: () => modeType,
      off: () => off,
      on: () => on,
      remove: () => remove,
      supportWebp: () => supportWebp,
      throttle: () => throttle
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_use = require_index_cjs();
    var hasIntersectionObserver = import_use.inBrowser && "IntersectionObserver" in window && "IntersectionObserverEntry" in window && "intersectionRatio" in window.IntersectionObserverEntry.prototype;
    var modeType = {
      event: "event",
      observer: "observer"
    };
    function remove(arr, item) {
      if (!arr.length) return;
      const index = arr.indexOf(item);
      if (index > -1) return arr.splice(index, 1);
    }
    function getBestSelectionFromSrcset(el, scale) {
      if (el.tagName !== "IMG" || !el.getAttribute("data-srcset")) return;
      let options = el.getAttribute("data-srcset");
      const container = el.parentNode;
      const containerWidth = container.offsetWidth * scale;
      let spaceIndex;
      let tmpSrc;
      let tmpWidth;
      options = options.trim().split(",");
      const result = options.map((item) => {
        item = item.trim();
        spaceIndex = item.lastIndexOf(" ");
        if (spaceIndex === -1) {
          tmpSrc = item;
          tmpWidth = 999998;
        } else {
          tmpSrc = item.substr(0, spaceIndex);
          tmpWidth = parseInt(
            item.substr(spaceIndex + 1, item.length - spaceIndex - 2),
            10
          );
        }
        return [tmpWidth, tmpSrc];
      });
      result.sort((a, b) => {
        if (a[0] < b[0]) {
          return 1;
        }
        if (a[0] > b[0]) {
          return -1;
        }
        if (a[0] === b[0]) {
          if (b[1].indexOf(".webp", b[1].length - 5) !== -1) {
            return 1;
          }
          if (a[1].indexOf(".webp", a[1].length - 5) !== -1) {
            return -1;
          }
        }
        return 0;
      });
      let bestSelectedSrc = "";
      let tmpOption;
      for (let i = 0; i < result.length; i++) {
        tmpOption = result[i];
        bestSelectedSrc = tmpOption[1];
        const next = result[i + 1];
        if (next && next[0] < containerWidth) {
          bestSelectedSrc = tmpOption[1];
          break;
        } else if (!next) {
          bestSelectedSrc = tmpOption[1];
          break;
        }
      }
      return bestSelectedSrc;
    }
    var getDPR = (scale = 1) => import_use.inBrowser ? window.devicePixelRatio || scale : scale;
    function supportWebp() {
      if (!import_use.inBrowser) return false;
      let support = true;
      try {
        const elem = document.createElement("canvas");
        if (elem.getContext && elem.getContext("2d")) {
          support = elem.toDataURL("image/webp").indexOf("data:image/webp") === 0;
        }
      } catch (err) {
        support = false;
      }
      return support;
    }
    function throttle(action, delay) {
      let timeout = null;
      let lastRun = 0;
      return function(...args) {
        if (timeout) {
          return;
        }
        const elapsed = Date.now() - lastRun;
        const runCallback = () => {
          lastRun = Date.now();
          timeout = false;
          action.apply(this, args);
        };
        if (elapsed >= delay) {
          runCallback();
        } else {
          timeout = setTimeout(runCallback, delay);
        }
      };
    }
    function on(el, type, func) {
      el.addEventListener(type, func, {
        capture: false,
        passive: true
      });
    }
    function off(el, type, func) {
      el.removeEventListener(type, func, false);
    }
    var loadImageAsync = (item, resolve, reject) => {
      const image = new Image();
      if (!item || !item.src) {
        return reject(new Error("image src is required"));
      }
      image.src = item.src;
      if (item.cors) {
        image.crossOrigin = item.cors;
      }
      image.onload = () => resolve({
        naturalHeight: image.naturalHeight,
        naturalWidth: image.naturalWidth,
        src: image.src
      });
      image.onerror = (e) => reject(e);
    };
    var ImageCache = class {
      constructor({ max }) {
        this.options = {
          max: max || 100
        };
        this.caches = [];
      }
      has(key) {
        return this.caches.indexOf(key) > -1;
      }
      add(key) {
        if (this.has(key)) return;
        this.caches.push(key);
        if (this.caches.length > this.options.max) {
          this.free();
        }
      }
      free() {
        this.caches.shift();
      }
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/back-top/BackTop.js
var require_BackTop = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/back-top/BackTop.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      backTopProps: () => backTopProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_util = require_util();
    var import_use = require_index_cjs();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("back-top");
    var backTopProps = {
      right: import_utils.numericProp,
      bottom: import_utils.numericProp,
      zIndex: import_utils.numericProp,
      target: [String, Object],
      offset: (0, import_utils.makeNumericProp)(200),
      immediate: Boolean,
      teleport: {
        type: [String, Object],
        default: "body"
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      inheritAttrs: false,
      props: backTopProps,
      emits: ["click"],
      setup(props, {
        emit,
        slots,
        attrs
      }) {
        let shouldReshow = false;
        const show = (0, import_vue.ref)(false);
        const root = (0, import_vue.ref)();
        const scrollParent = (0, import_vue.ref)();
        const style = (0, import_vue.computed)(() => (0, import_utils.extend)((0, import_utils.getZIndexStyle)(props.zIndex), {
          right: (0, import_utils.addUnit)(props.right),
          bottom: (0, import_utils.addUnit)(props.bottom)
        }));
        const onClick = (event) => {
          var _a;
          emit("click", event);
          (_a = scrollParent.value) == null ? void 0 : _a.scrollTo({
            top: 0,
            behavior: props.immediate ? "auto" : "smooth"
          });
        };
        const scroll = () => {
          show.value = scrollParent.value ? (0, import_utils.getScrollTop)(scrollParent.value) >= +props.offset : false;
        };
        const getTarget = () => {
          const {
            target
          } = props;
          if (typeof target === "string") {
            const el = document.querySelector(target);
            if (el) {
              return el;
            }
            if (true) {
              console.error(`[Vant] BackTop: target element "${target}" was not found, the BackTop component will not be rendered.`);
            }
          } else {
            return target;
          }
        };
        const updateTarget = () => {
          if (import_utils.inBrowser) {
            (0, import_vue.nextTick)(() => {
              scrollParent.value = props.target ? getTarget() : (0, import_use.getScrollParent)(root.value);
              scroll();
            });
          }
        };
        (0, import_use.useEventListener)("scroll", (0, import_util.throttle)(scroll, 100), {
          target: scrollParent
        });
        (0, import_vue.onMounted)(updateTarget);
        (0, import_vue.onActivated)(() => {
          if (shouldReshow) {
            show.value = true;
            shouldReshow = false;
          }
        });
        (0, import_vue.onDeactivated)(() => {
          if (show.value && props.teleport) {
            show.value = false;
            shouldReshow = true;
          }
        });
        (0, import_vue.watch)(() => props.target, updateTarget);
        return () => {
          const Content = (0, import_vue.createVNode)("div", (0, import_vue.mergeProps)({
            "ref": !props.teleport ? root : void 0,
            "class": bem({
              active: show.value
            }),
            "style": style.value,
            "onClick": onClick
          }, attrs), [slots.default ? slots.default() : (0, import_vue.createVNode)(import_icon.Icon, {
            "name": "back-top",
            "class": bem("icon")
          }, null)]);
          if (props.teleport) {
            return [(0, import_vue.createVNode)("div", {
              "ref": root,
              "class": bem("placeholder")
            }, null), (0, import_vue.createVNode)(import_vue.Teleport, {
              "to": props.teleport
            }, {
              default: () => [Content]
            })];
          }
          return Content;
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/back-top/index.js
var require_back_top = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/back-top/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      BackTop: () => BackTop,
      backTopProps: () => import_BackTop2.backTopProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_BackTop = __toESM(require_BackTop());
    var import_BackTop2 = require_BackTop();
    var BackTop = (0, import_utils.withInstall)(import_BackTop.default);
    var stdin_default = BackTop;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/barrage/Barrage.js
var require_Barrage = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/barrage/Barrage.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var __async = (__this, __arguments, generator) => {
      return new Promise((resolve, reject) => {
        var fulfilled = (value) => {
          try {
            step(generator.next(value));
          } catch (e) {
            reject(e);
          }
        };
        var rejected = (value) => {
          try {
            step(generator.throw(value));
          } catch (e) {
            reject(e);
          }
        };
        var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
        step((generator = generator.apply(__this, __arguments)).next());
      });
    };
    var stdin_exports = {};
    __export(stdin_exports, {
      barrageProps: () => barrageProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use_expose = require_use_expose();
    var import_utils = require_utils();
    var barrageProps = {
      top: (0, import_utils.makeNumericProp)(10),
      rows: (0, import_utils.makeNumericProp)(4),
      duration: (0, import_utils.makeNumericProp)(4e3),
      autoPlay: import_utils.truthProp,
      delay: (0, import_utils.makeNumberProp)(300),
      modelValue: (0, import_utils.makeArrayProp)()
    };
    var [name, bem] = (0, import_utils.createNamespace)("barrage");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: barrageProps,
      emits: ["update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const barrageWrapper = (0, import_vue.ref)();
        const className = bem("item");
        const total = (0, import_vue.ref)(0);
        const barrageItems = [];
        const createBarrageItem = (text, delay = props.delay) => {
          const item = document.createElement("span");
          item.className = className;
          item.innerText = String(text);
          item.style.animationDuration = `${props.duration}ms`;
          item.style.animationDelay = `${delay}ms`;
          item.style.animationName = "van-barrage";
          item.style.animationTimingFunction = "linear";
          return item;
        };
        const isInitBarrage = (0, import_vue.ref)(true);
        const isPlay = (0, import_vue.ref)(props.autoPlay);
        const appendBarrageItem = ({
          id,
          text
        }, i) => {
          var _a;
          const item = createBarrageItem(text, isInitBarrage.value ? i * props.delay : void 0);
          if (!props.autoPlay && isPlay.value === false) {
            item.style.animationPlayState = "paused";
          }
          (_a = barrageWrapper.value) == null ? void 0 : _a.append(item);
          total.value++;
          const top = (total.value - 1) % +props.rows * item.offsetHeight + +props.top;
          item.style.top = `${top}px`;
          item.dataset.id = String(id);
          barrageItems.push(item);
          item.addEventListener("animationend", () => {
            emit("update:modelValue", [...props.modelValue].filter((v) => String(v.id) !== item.dataset.id));
          });
        };
        const updateBarrages = (newValue, oldValue) => {
          const map = new Map(oldValue.map((item) => [item.id, item]));
          newValue.forEach((item, i) => {
            if (map.has(item.id)) {
              map.delete(item.id);
            } else {
              appendBarrageItem(item, i);
            }
          });
          map.forEach((item) => {
            const index = barrageItems.findIndex((span) => span.dataset.id === String(item.id));
            if (index > -1) {
              barrageItems[index].remove();
              barrageItems.splice(index, 1);
            }
          });
          isInitBarrage.value = false;
        };
        (0, import_vue.watch)(() => props.modelValue.slice(), (newValue, oldValue) => updateBarrages(newValue != null ? newValue : [], oldValue != null ? oldValue : []), {
          deep: true
        });
        const rootStyle = (0, import_vue.ref)({});
        (0, import_vue.onMounted)(() => __async(null, null, function* () {
          var _a;
          rootStyle.value["--move-distance"] = `-${(_a = barrageWrapper.value) == null ? void 0 : _a.offsetWidth}px`;
          yield (0, import_vue.nextTick)();
          updateBarrages(props.modelValue, []);
        }));
        const play = () => {
          isPlay.value = true;
          barrageItems.forEach((item) => {
            item.style.animationPlayState = "running";
          });
        };
        const pause = () => {
          isPlay.value = false;
          barrageItems.forEach((item) => {
            item.style.animationPlayState = "paused";
          });
        };
        (0, import_use_expose.useExpose)({
          play,
          pause
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": bem(),
            "ref": barrageWrapper,
            "style": rootStyle.value
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/barrage/index.js
var require_barrage = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/barrage/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Barrage: () => Barrage,
      barrageProps: () => import_Barrage2.barrageProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Barrage = __toESM(require_Barrage());
    var import_Barrage2 = require_Barrage();
    var Barrage = (0, import_utils.withInstall)(import_Barrage.default);
    var stdin_default = Barrage;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/utils.js
var require_utils6 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      bem: () => bem,
      calcDateNum: () => calcDateNum,
      cloneDate: () => cloneDate,
      cloneDates: () => cloneDates,
      compareDay: () => compareDay,
      compareMonth: () => compareMonth,
      formatMonthTitle: () => formatMonthTitle,
      getDayByOffset: () => getDayByOffset,
      getMonthByOffset: () => getMonthByOffset,
      getNextDay: () => getNextDay,
      getNextMonth: () => getNextMonth,
      getNextYear: () => getNextYear,
      getPrevDay: () => getPrevDay,
      getPrevMonth: () => getPrevMonth,
      getPrevYear: () => getPrevYear,
      getToday: () => getToday,
      getYearByOffset: () => getYearByOffset,
      isLastRowInMonth: () => isLastRowInMonth,
      name: () => name,
      t: () => t
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var [name, bem, t] = (0, import_utils.createNamespace)("calendar");
    var formatMonthTitle = (date) => t("monthTitle", date.getFullYear(), date.getMonth() + 1);
    function compareMonth(date1, date2) {
      const year1 = date1.getFullYear();
      const year2 = date2.getFullYear();
      if (year1 === year2) {
        const month1 = date1.getMonth();
        const month2 = date2.getMonth();
        return month1 === month2 ? 0 : month1 > month2 ? 1 : -1;
      }
      return year1 > year2 ? 1 : -1;
    }
    function compareDay(day1, day2) {
      const compareMonthResult = compareMonth(day1, day2);
      if (compareMonthResult === 0) {
        const date1 = day1.getDate();
        const date2 = day2.getDate();
        return date1 === date2 ? 0 : date1 > date2 ? 1 : -1;
      }
      return compareMonthResult;
    }
    var cloneDate = (date) => new Date(date);
    var cloneDates = (dates) => Array.isArray(dates) ? dates.map(cloneDate) : cloneDate(dates);
    function getDayByOffset(date, offset) {
      const cloned = cloneDate(date);
      cloned.setDate(cloned.getDate() + offset);
      return cloned;
    }
    function getMonthByOffset(date, offset) {
      const cloned = cloneDate(date);
      cloned.setMonth(cloned.getMonth() + offset);
      if (cloned.getDate() !== date.getDate()) {
        cloned.setDate(0);
      }
      return cloned;
    }
    function getYearByOffset(date, offset) {
      const cloned = cloneDate(date);
      cloned.setFullYear(cloned.getFullYear() + offset);
      if (cloned.getDate() !== date.getDate()) {
        cloned.setDate(0);
      }
      return cloned;
    }
    var getPrevDay = (date) => getDayByOffset(date, -1);
    var getNextDay = (date) => getDayByOffset(date, 1);
    var getPrevMonth = (date) => getMonthByOffset(date, -1);
    var getNextMonth = (date) => getMonthByOffset(date, 1);
    var getPrevYear = (date) => getYearByOffset(date, -1);
    var getNextYear = (date) => getYearByOffset(date, 1);
    var getToday = () => {
      const today = /* @__PURE__ */ new Date();
      today.setHours(0, 0, 0, 0);
      return today;
    };
    function calcDateNum(date) {
      const day1 = date[0].getTime();
      const day2 = date[1].getTime();
      return (day2 - day1) / (1e3 * 60 * 60 * 24) + 1;
    }
    function isLastRowInMonth(date, offset = 0) {
      const lastDay = new Date(date.getFullYear(), date.getMonth() + 1, 0);
      const currentPos = offset + date.getDate() - 1;
      const lastDayPos = offset + lastDay.getDate() - 1;
      return Math.floor(currentPos / 7) === Math.floor(lastDayPos / 7);
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/date-picker/utils.js
var require_utils7 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/date-picker/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      formatValueRange: () => formatValueRange,
      genOptions: () => genOptions,
      getMonthEndDay: () => getMonthEndDay,
      pickerInheritKeys: () => pickerInheritKeys,
      sharedProps: () => sharedProps,
      times: () => times
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Picker = require_Picker();
    var sharedProps = (0, import_utils.extend)({}, import_Picker.pickerSharedProps, {
      modelValue: (0, import_utils.makeArrayProp)(),
      filter: Function,
      formatter: {
        type: Function,
        default: (type, option) => option
      }
    });
    var pickerInheritKeys = Object.keys(import_Picker.pickerSharedProps);
    function times(n, iteratee) {
      if (n < 0) {
        return [];
      }
      const result = Array(n);
      let index = -1;
      while (++index < n) {
        result[index] = iteratee(index);
      }
      return result;
    }
    var getMonthEndDay = (year, month) => 32 - new Date(year, month - 1, 32).getDate();
    var genOptions = (min, max, type, formatter, filter, values) => {
      const options = times(max - min + 1, (index) => {
        const value = (0, import_utils.padZero)(min + index);
        return formatter(type, {
          text: value,
          value
        });
      });
      return filter ? filter(type, options, values) : options;
    };
    var formatValueRange = (values, columns) => values.map((value, index) => {
      const column = columns[index];
      if (column.length) {
        const minValue = +column[0].value;
        const maxValue = +column[column.length - 1].value;
        return (0, import_utils.padZero)((0, import_utils.clamp)(+value, minValue, maxValue));
      }
      return value;
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/CalendarDay.js
var require_CalendarDay = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/CalendarDay.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils6();
    var [name] = (0, import_utils.createNamespace)("calendar-day");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: {
        item: (0, import_utils.makeRequiredProp)(Object),
        color: String,
        index: Number,
        offset: (0, import_utils.makeNumberProp)(0),
        rowHeight: String
      },
      emits: ["click", "clickDisabledDate"],
      setup(props, {
        emit,
        slots
      }) {
        const style = (0, import_vue.computed)(() => {
          const {
            item,
            index,
            color,
            offset,
            rowHeight
          } = props;
          const style2 = {
            height: rowHeight
          };
          if (item.type === "placeholder") {
            style2.width = "100%";
            return style2;
          }
          if (index === 0) {
            style2.marginLeft = `${100 * offset / 7}%`;
          }
          if (color) {
            switch (item.type) {
              case "end":
              case "start":
              case "start-end":
              case "multiple-middle":
              case "multiple-selected":
                style2.background = color;
                break;
              case "middle":
                style2.color = color;
                break;
            }
          }
          if (item.date && (0, import_utils2.isLastRowInMonth)(item.date, offset)) {
            style2.marginBottom = 0;
          }
          return style2;
        });
        const onClick = () => {
          if (props.item.type !== "disabled") {
            emit("click", props.item);
          } else {
            emit("clickDisabledDate", props.item);
          }
        };
        const renderTopInfo = () => {
          const {
            topInfo
          } = props.item;
          if (topInfo || slots["top-info"]) {
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("top-info")
            }, [slots["top-info"] ? slots["top-info"](props.item) : topInfo]);
          }
        };
        const renderBottomInfo = () => {
          const {
            bottomInfo
          } = props.item;
          if (bottomInfo || slots["bottom-info"]) {
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("bottom-info")
            }, [slots["bottom-info"] ? slots["bottom-info"](props.item) : bottomInfo]);
          }
        };
        const renderText = () => {
          return slots.text ? slots.text(props.item) : props.item.text;
        };
        const renderContent = () => {
          const {
            item,
            color,
            rowHeight
          } = props;
          const {
            type
          } = item;
          const Nodes = [renderTopInfo(), renderText(), renderBottomInfo()];
          if (type === "selected") {
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("selected-day"),
              "style": {
                width: rowHeight,
                height: rowHeight,
                background: color
              }
            }, [Nodes]);
          }
          return Nodes;
        };
        return () => {
          const {
            type,
            className
          } = props.item;
          if (type === "placeholder") {
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("day"),
              "style": style.value
            }, null);
          }
          return (0, import_vue.createVNode)("div", {
            "role": "gridcell",
            "style": style.value,
            "class": [(0, import_utils2.bem)("day", type), className],
            "tabindex": type === "disabled" ? void 0 : -1,
            "onClick": onClick
          }, [renderContent()]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/CalendarMonth.js
var require_CalendarMonth = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/CalendarMonth.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils7();
    var import_utils3 = require_utils6();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_use_height = require_use_height();
    var import_CalendarDay = __toESM(require_CalendarDay());
    var [name] = (0, import_utils.createNamespace)("calendar-month");
    var calendarMonthProps = {
      date: (0, import_utils.makeRequiredProp)(Date),
      type: String,
      color: String,
      minDate: Date,
      maxDate: Date,
      showMark: Boolean,
      rowHeight: import_utils.numericProp,
      formatter: Function,
      lazyRender: Boolean,
      currentDate: [Date, Array],
      allowSameDay: Boolean,
      showSubtitle: Boolean,
      showMonthTitle: Boolean,
      firstDayOfWeek: Number
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: calendarMonthProps,
      emits: ["click", "clickDisabledDate"],
      setup(props, {
        emit,
        slots
      }) {
        const [visible, setVisible] = (0, import_use.useToggle)();
        const daysRef = (0, import_vue.ref)();
        const monthRef = (0, import_vue.ref)();
        const height = (0, import_use_height.useHeight)(monthRef);
        const title = (0, import_vue.computed)(() => (0, import_utils3.formatMonthTitle)(props.date));
        const rowHeight = (0, import_vue.computed)(() => (0, import_utils.addUnit)(props.rowHeight));
        const offset = (0, import_vue.computed)(() => {
          const date = props.date.getDate();
          const day = props.date.getDay();
          const realDay = (day - date % 7 + 8) % 7;
          if (props.firstDayOfWeek) {
            return (realDay + 7 - props.firstDayOfWeek) % 7;
          }
          return realDay;
        });
        const totalDay = (0, import_vue.computed)(() => (0, import_utils2.getMonthEndDay)(props.date.getFullYear(), props.date.getMonth() + 1));
        const shouldRender = (0, import_vue.computed)(() => visible.value || !props.lazyRender);
        const getTitle = () => title.value;
        const getMultipleDayType = (day) => {
          const isSelected = (date) => props.currentDate.some((item) => (0, import_utils3.compareDay)(item, date) === 0);
          if (isSelected(day)) {
            const prevDay = (0, import_utils3.getPrevDay)(day);
            const nextDay = (0, import_utils3.getNextDay)(day);
            const prevSelected = isSelected(prevDay);
            const nextSelected = isSelected(nextDay);
            if (prevSelected && nextSelected) {
              return "multiple-middle";
            }
            if (prevSelected) {
              return "end";
            }
            if (nextSelected) {
              return "start";
            }
            return "multiple-selected";
          }
          return "";
        };
        const getRangeDayType = (day) => {
          const [startDay, endDay] = props.currentDate;
          if (!startDay) {
            return "";
          }
          const compareToStart = (0, import_utils3.compareDay)(day, startDay);
          if (!endDay) {
            return compareToStart === 0 ? "start" : "";
          }
          const compareToEnd = (0, import_utils3.compareDay)(day, endDay);
          if (props.allowSameDay && compareToStart === 0 && compareToEnd === 0) {
            return "start-end";
          }
          if (compareToStart === 0) {
            return "start";
          }
          if (compareToEnd === 0) {
            return "end";
          }
          if (compareToStart > 0 && compareToEnd < 0) {
            return "middle";
          }
          return "";
        };
        const getDayType = (day) => {
          const {
            type,
            minDate,
            maxDate,
            currentDate
          } = props;
          if (minDate && (0, import_utils3.compareDay)(day, minDate) < 0 || maxDate && (0, import_utils3.compareDay)(day, maxDate) > 0) {
            return "disabled";
          }
          if (currentDate === null) {
            return "";
          }
          if (Array.isArray(currentDate)) {
            if (type === "multiple") {
              return getMultipleDayType(day);
            }
            if (type === "range") {
              return getRangeDayType(day);
            }
          } else if (type === "single") {
            return (0, import_utils3.compareDay)(day, currentDate) === 0 ? "selected" : "";
          }
          return "";
        };
        const getBottomInfo = (dayType) => {
          if (props.type === "range") {
            if (dayType === "start" || dayType === "end") {
              return (0, import_utils3.t)(dayType);
            }
            if (dayType === "start-end") {
              return `${(0, import_utils3.t)("start")}/${(0, import_utils3.t)("end")}`;
            }
          }
        };
        const renderTitle = () => {
          if (props.showMonthTitle) {
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils3.bem)("month-title")
            }, [slots["month-title"] ? slots["month-title"]({
              date: props.date,
              text: title.value
            }) : title.value]);
          }
        };
        const renderMark = () => {
          if (props.showMark && shouldRender.value) {
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils3.bem)("month-mark")
            }, [props.date.getMonth() + 1]);
          }
        };
        const placeholders = (0, import_vue.computed)(() => {
          const count = Math.ceil((totalDay.value + offset.value) / 7);
          return Array(count).fill({
            type: "placeholder"
          });
        });
        const days = (0, import_vue.computed)(() => {
          const days2 = [];
          const year = props.date.getFullYear();
          const month = props.date.getMonth();
          for (let day = 1; day <= totalDay.value; day++) {
            const date = new Date(year, month, day);
            const type = getDayType(date);
            let config = {
              date,
              type,
              text: day,
              bottomInfo: getBottomInfo(type)
            };
            if (props.formatter) {
              config = props.formatter(config);
            }
            days2.push(config);
          }
          return days2;
        });
        const disabledDays = (0, import_vue.computed)(() => days.value.filter((day) => day.type === "disabled"));
        const scrollToDate = (body, targetDate) => {
          if (daysRef.value) {
            const daysRect = (0, import_use.useRect)(daysRef.value);
            const totalRows = placeholders.value.length;
            const currentRow = Math.ceil((targetDate.getDate() + offset.value) / 7);
            const rowOffset = (currentRow - 1) * daysRect.height / totalRows;
            (0, import_utils.setScrollTop)(body, daysRect.top + rowOffset + body.scrollTop - (0, import_use.useRect)(body).top);
          }
        };
        const renderDay = (item, index) => (0, import_vue.createVNode)(import_CalendarDay.default, {
          "item": item,
          "index": index,
          "color": props.color,
          "offset": offset.value,
          "rowHeight": rowHeight.value,
          "onClick": (item2) => emit("click", item2),
          "onClickDisabledDate": (item2) => emit("clickDisabledDate", item2)
        }, (0, import_utils.pick)(slots, ["top-info", "bottom-info", "text"]));
        const renderDays = () => (0, import_vue.createVNode)("div", {
          "ref": daysRef,
          "role": "grid",
          "class": (0, import_utils3.bem)("days")
        }, [renderMark(), (shouldRender.value ? days : placeholders).value.map(renderDay)]);
        (0, import_use_expose.useExpose)({
          getTitle,
          getHeight: () => height.value,
          setVisible,
          scrollToDate,
          disabledDays
        });
        return () => (0, import_vue.createVNode)("div", {
          "class": (0, import_utils3.bem)("month"),
          "ref": monthRef
        }, [renderTitle(), renderDays()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/CalendarHeader.js
var require_CalendarHeader = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/CalendarHeader.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils6();
    var import_icon = require_icon();
    var [name] = (0, import_utils.createNamespace)("calendar-header");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: {
        date: Date,
        minDate: Date,
        maxDate: Date,
        title: String,
        subtitle: String,
        showTitle: Boolean,
        showSubtitle: Boolean,
        firstDayOfWeek: Number,
        switchMode: (0, import_utils.makeStringProp)("none")
      },
      emits: ["clickSubtitle", "panelChange"],
      setup(props, {
        slots,
        emit
      }) {
        const prevMonthDisabled = (0, import_vue.computed)(() => props.date && props.minDate && (0, import_utils2.compareMonth)((0, import_utils2.getPrevMonth)(props.date), props.minDate) < 0);
        const prevYearDisabled = (0, import_vue.computed)(() => props.date && props.minDate && (0, import_utils2.compareMonth)((0, import_utils2.getPrevYear)(props.date), props.minDate) < 0);
        const nextMonthDisabled = (0, import_vue.computed)(() => props.date && props.maxDate && (0, import_utils2.compareMonth)((0, import_utils2.getNextMonth)(props.date), props.maxDate) > 0);
        const nextYearDisabled = (0, import_vue.computed)(() => props.date && props.maxDate && (0, import_utils2.compareMonth)((0, import_utils2.getNextYear)(props.date), props.maxDate) > 0);
        const renderTitle = () => {
          if (props.showTitle) {
            const text = props.title || (0, import_utils2.t)("title");
            const title = slots.title ? slots.title() : text;
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("header-title")
            }, [title]);
          }
        };
        const onClickSubtitle = (event) => emit("clickSubtitle", event);
        const onPanelChange = (date) => emit("panelChange", date);
        const renderAction = (isNext) => {
          const showYearAction = props.switchMode === "year-month";
          const monthSlot = slots[isNext ? "next-month" : "prev-month"];
          const yearSlot = slots[isNext ? "next-year" : "prev-year"];
          const monthDisabled = isNext ? nextMonthDisabled.value : prevMonthDisabled.value;
          const yearDisabled = isNext ? nextYearDisabled.value : prevYearDisabled.value;
          const monthIconName = isNext ? "arrow" : "arrow-left";
          const yearIconName = isNext ? "arrow-double-right" : "arrow-double-left";
          const onMonthChange = () => onPanelChange((isNext ? import_utils2.getNextMonth : import_utils2.getPrevMonth)(props.date));
          const onYearChange = () => onPanelChange((isNext ? import_utils2.getNextYear : import_utils2.getPrevYear)(props.date));
          const MonthAction = (0, import_vue.createVNode)("view", {
            "class": (0, import_utils2.bem)("header-action", {
              disabled: monthDisabled
            }),
            "onClick": monthDisabled ? void 0 : onMonthChange
          }, [monthSlot ? monthSlot({
            disabled: monthDisabled
          }) : (0, import_vue.createVNode)(import_icon.Icon, {
            "class": {
              [import_utils.HAPTICS_FEEDBACK]: !monthDisabled
            },
            "name": monthIconName
          }, null)]);
          const YearAction = showYearAction && (0, import_vue.createVNode)("view", {
            "class": (0, import_utils2.bem)("header-action", {
              disabled: yearDisabled
            }),
            "onClick": yearDisabled ? void 0 : onYearChange
          }, [yearSlot ? yearSlot({
            disabled: yearDisabled
          }) : (0, import_vue.createVNode)(import_icon.Icon, {
            "class": {
              [import_utils.HAPTICS_FEEDBACK]: !yearDisabled
            },
            "name": yearIconName
          }, null)]);
          return isNext ? [MonthAction, YearAction] : [YearAction, MonthAction];
        };
        const renderSubtitle = () => {
          if (props.showSubtitle) {
            const title = slots.subtitle ? slots.subtitle({
              date: props.date,
              text: props.subtitle
            }) : props.subtitle;
            const canSwitch = props.switchMode !== "none";
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("header-subtitle", {
                "with-switch": canSwitch
              }),
              "onClick": onClickSubtitle
            }, [canSwitch ? [renderAction(), (0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("header-subtitle-text")
            }, [title]), renderAction(true)] : title]);
          }
        };
        const renderWeekDays = () => {
          const {
            firstDayOfWeek
          } = props;
          const weekdays = (0, import_utils2.t)("weekdays");
          const renderWeekDays2 = [...weekdays.slice(firstDayOfWeek, 7), ...weekdays.slice(0, firstDayOfWeek)];
          return (0, import_vue.createVNode)("div", {
            "class": (0, import_utils2.bem)("weekdays")
          }, [renderWeekDays2.map((text) => (0, import_vue.createVNode)("span", {
            "class": (0, import_utils2.bem)("weekday")
          }, [text]))]);
        };
        return () => (0, import_vue.createVNode)("div", {
          "class": (0, import_utils2.bem)("header")
        }, [renderTitle(), renderSubtitle(), renderWeekDays()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/Calendar.js
var require_Calendar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/Calendar.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      calendarProps: () => calendarProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils6();
    var import_use = require_index_cjs();
    var import_use_refs = require_use_refs();
    var import_use_expose = require_use_expose();
    var import_popup = require_popup();
    var import_button = require_button();
    var import_toast = require_toast();
    var import_CalendarMonth = __toESM(require_CalendarMonth());
    var import_CalendarHeader = __toESM(require_CalendarHeader());
    var calendarProps = {
      show: Boolean,
      type: (0, import_utils.makeStringProp)("single"),
      switchMode: (0, import_utils.makeStringProp)("none"),
      title: String,
      color: String,
      round: import_utils.truthProp,
      readonly: Boolean,
      poppable: import_utils.truthProp,
      maxRange: (0, import_utils.makeNumericProp)(null),
      position: (0, import_utils.makeStringProp)("bottom"),
      teleport: [String, Object],
      showMark: import_utils.truthProp,
      showTitle: import_utils.truthProp,
      formatter: Function,
      rowHeight: import_utils.numericProp,
      confirmText: String,
      rangePrompt: String,
      lazyRender: import_utils.truthProp,
      showConfirm: import_utils.truthProp,
      defaultDate: [Date, Array],
      allowSameDay: Boolean,
      showSubtitle: import_utils.truthProp,
      closeOnPopstate: import_utils.truthProp,
      showRangePrompt: import_utils.truthProp,
      confirmDisabledText: String,
      closeOnClickOverlay: import_utils.truthProp,
      safeAreaInsetTop: Boolean,
      safeAreaInsetBottom: import_utils.truthProp,
      minDate: {
        type: Date,
        validator: import_utils.isDate
      },
      maxDate: {
        type: Date,
        validator: import_utils.isDate
      },
      firstDayOfWeek: {
        type: import_utils.numericProp,
        default: 0,
        validator: (val) => val >= 0 && val <= 6
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name: import_utils2.name,
      props: calendarProps,
      emits: ["select", "confirm", "unselect", "monthShow", "overRange", "update:show", "clickSubtitle", "clickDisabledDate", "clickOverlay", "panelChange"],
      setup(props, {
        emit,
        slots
      }) {
        const canSwitch = (0, import_vue.computed)(() => props.switchMode !== "none");
        const minDate = (0, import_vue.computed)(() => {
          if (!props.minDate && !canSwitch.value) {
            return (0, import_utils2.getToday)();
          }
          return props.minDate;
        });
        const maxDate = (0, import_vue.computed)(() => {
          if (!props.maxDate && !canSwitch.value) {
            return (0, import_utils2.getMonthByOffset)((0, import_utils2.getToday)(), 6);
          }
          return props.maxDate;
        });
        const limitDateRange = (date, min = minDate.value, max = maxDate.value) => {
          if (min && (0, import_utils2.compareDay)(date, min) === -1) {
            return min;
          }
          if (max && (0, import_utils2.compareDay)(date, max) === 1) {
            return max;
          }
          return date;
        };
        const getInitialDate = (defaultDate = props.defaultDate) => {
          const {
            type,
            allowSameDay
          } = props;
          if (defaultDate === null) {
            return defaultDate;
          }
          const now = (0, import_utils2.getToday)();
          if (type === "range") {
            if (!Array.isArray(defaultDate)) {
              defaultDate = [];
            }
            if (defaultDate.length === 1 && (0, import_utils2.compareDay)(defaultDate[0], now) === 1) {
              defaultDate = [];
            }
            const min = minDate.value;
            const max = maxDate.value;
            const start = limitDateRange(defaultDate[0] || now, min, max ? allowSameDay ? max : (0, import_utils2.getPrevDay)(max) : void 0);
            const end = limitDateRange(defaultDate[1] || (allowSameDay ? now : (0, import_utils2.getNextDay)(now)), min ? allowSameDay ? min : (0, import_utils2.getNextDay)(min) : void 0);
            return [start, end];
          }
          if (type === "multiple") {
            if (Array.isArray(defaultDate)) {
              return defaultDate.map((date) => limitDateRange(date));
            }
            return [limitDateRange(now)];
          }
          if (!defaultDate || Array.isArray(defaultDate)) {
            defaultDate = now;
          }
          return limitDateRange(defaultDate);
        };
        const getInitialPanelDate = () => {
          const date = Array.isArray(currentDate.value) ? currentDate.value[0] : currentDate.value;
          return date ? date : limitDateRange((0, import_utils2.getToday)());
        };
        let bodyHeight;
        const bodyRef = (0, import_vue.ref)();
        const currentDate = (0, import_vue.ref)(getInitialDate());
        const currentPanelDate = (0, import_vue.ref)(getInitialPanelDate());
        const currentMonthRef = (0, import_vue.ref)();
        const [monthRefs, setMonthRefs] = (0, import_use_refs.useRefs)();
        const dayOffset = (0, import_vue.computed)(() => props.firstDayOfWeek ? +props.firstDayOfWeek % 7 : 0);
        const months = (0, import_vue.computed)(() => {
          const months2 = [];
          if (!minDate.value || !maxDate.value) {
            return months2;
          }
          const cursor = new Date(minDate.value);
          cursor.setDate(1);
          do {
            months2.push(new Date(cursor));
            cursor.setMonth(cursor.getMonth() + 1);
          } while ((0, import_utils2.compareMonth)(cursor, maxDate.value) !== 1);
          return months2;
        });
        const buttonDisabled = (0, import_vue.computed)(() => {
          if (currentDate.value) {
            if (props.type === "range") {
              return !currentDate.value[0] || !currentDate.value[1];
            }
            if (props.type === "multiple") {
              return !currentDate.value.length;
            }
          }
          return !currentDate.value;
        });
        const getSelectedDate = () => currentDate.value;
        const onScroll = () => {
          const top = (0, import_utils.getScrollTop)(bodyRef.value);
          const bottom = top + bodyHeight;
          const heights = months.value.map((item, index) => monthRefs.value[index].getHeight());
          const heightSum = heights.reduce((a, b) => a + b, 0);
          if (bottom > heightSum && top > 0) {
            return;
          }
          let height = 0;
          let currentMonth;
          const visibleRange = [-1, -1];
          for (let i = 0; i < months.value.length; i++) {
            const month = monthRefs.value[i];
            const visible = height <= bottom && height + heights[i] >= top;
            if (visible) {
              visibleRange[1] = i;
              if (!currentMonth) {
                currentMonth = month;
                visibleRange[0] = i;
              }
              if (!monthRefs.value[i].showed) {
                monthRefs.value[i].showed = true;
                emit("monthShow", {
                  date: month.date,
                  title: month.getTitle()
                });
              }
            }
            height += heights[i];
          }
          months.value.forEach((month, index) => {
            const visible = index >= visibleRange[0] - 1 && index <= visibleRange[1] + 1;
            monthRefs.value[index].setVisible(visible);
          });
          if (currentMonth) {
            currentMonthRef.value = currentMonth;
          }
        };
        const scrollToDate = (targetDate) => {
          if (canSwitch.value) {
            currentPanelDate.value = targetDate;
          } else {
            (0, import_use.raf)(() => {
              months.value.some((month, index) => {
                if ((0, import_utils2.compareMonth)(month, targetDate) === 0) {
                  if (bodyRef.value) {
                    monthRefs.value[index].scrollToDate(bodyRef.value, targetDate);
                  }
                  return true;
                }
                return false;
              });
              onScroll();
            });
          }
        };
        const scrollToCurrentDate = () => {
          if (props.poppable && !props.show) {
            return;
          }
          if (currentDate.value) {
            const targetDate = props.type === "single" ? currentDate.value : currentDate.value[0];
            if ((0, import_utils.isDate)(targetDate)) {
              scrollToDate(targetDate);
            }
          } else if (!canSwitch.value) {
            (0, import_use.raf)(onScroll);
          }
        };
        const init = () => {
          if (props.poppable && !props.show) {
            return;
          }
          if (!canSwitch.value) {
            (0, import_use.raf)(() => {
              bodyHeight = Math.floor((0, import_use.useRect)(bodyRef).height);
            });
          }
          scrollToCurrentDate();
        };
        const reset = (date = getInitialDate()) => {
          currentDate.value = date;
          scrollToCurrentDate();
        };
        const checkRange = (date) => {
          const {
            maxRange,
            rangePrompt,
            showRangePrompt
          } = props;
          if (maxRange && (0, import_utils2.calcDateNum)(date) > +maxRange) {
            if (showRangePrompt) {
              (0, import_toast.showToast)(rangePrompt || (0, import_utils2.t)("rangePrompt", maxRange));
            }
            emit("overRange");
            return false;
          }
          return true;
        };
        const onPanelChange = (date) => {
          currentPanelDate.value = date;
          emit("panelChange", {
            date
          });
        };
        const onConfirm = () => {
          var _a;
          return emit("confirm", (_a = currentDate.value) != null ? _a : (0, import_utils2.cloneDates)(currentDate.value));
        };
        const select = (date, complete) => {
          const setCurrentDate = (date2) => {
            currentDate.value = date2;
            emit("select", (0, import_utils2.cloneDates)(date2));
          };
          if (complete && props.type === "range") {
            const valid = checkRange(date);
            if (!valid) {
              setCurrentDate([date[0], (0, import_utils2.getDayByOffset)(date[0], +props.maxRange - 1)]);
              return;
            }
          }
          setCurrentDate(date);
          if (complete && !props.showConfirm) {
            onConfirm();
          }
        };
        const getDisabledDate = (disabledDays2, startDay, date) => {
          var _a;
          return (_a = disabledDays2.find((day) => (0, import_utils2.compareDay)(startDay, day.date) === -1 && (0, import_utils2.compareDay)(day.date, date) === -1)) == null ? void 0 : _a.date;
        };
        const disabledDays = (0, import_vue.computed)(() => monthRefs.value.reduce((arr, ref2) => {
          var _a, _b;
          arr.push(...(_b = (_a = ref2.disabledDays) == null ? void 0 : _a.value) != null ? _b : []);
          return arr;
        }, []));
        const onClickDay = (item) => {
          if (props.readonly || !item.date) {
            return;
          }
          const {
            date
          } = item;
          const {
            type
          } = props;
          if (type === "range") {
            if (!currentDate.value) {
              select([date]);
              return;
            }
            const [startDay, endDay] = currentDate.value;
            if (startDay && !endDay) {
              const compareToStart = (0, import_utils2.compareDay)(date, startDay);
              if (compareToStart === 1) {
                const disabledDay = getDisabledDate(disabledDays.value, startDay, date);
                if (disabledDay) {
                  const endDay2 = (0, import_utils2.getPrevDay)(disabledDay);
                  if ((0, import_utils2.compareDay)(startDay, endDay2) === -1) {
                    select([startDay, endDay2]);
                  } else {
                    select([date]);
                  }
                } else {
                  select([startDay, date], true);
                }
              } else if (compareToStart === -1) {
                select([date]);
              } else if (props.allowSameDay) {
                select([date, date], true);
              }
            } else {
              select([date]);
            }
          } else if (type === "multiple") {
            if (!currentDate.value) {
              select([date]);
              return;
            }
            const dates = currentDate.value;
            const selectedIndex = dates.findIndex((dateItem) => (0, import_utils2.compareDay)(dateItem, date) === 0);
            if (selectedIndex !== -1) {
              const [unselectedDate] = dates.splice(selectedIndex, 1);
              emit("unselect", (0, import_utils2.cloneDate)(unselectedDate));
            } else if (props.maxRange && dates.length >= +props.maxRange) {
              (0, import_toast.showToast)(props.rangePrompt || (0, import_utils2.t)("rangePrompt", props.maxRange));
            } else {
              select([...dates, date]);
            }
          } else {
            select(date, true);
          }
        };
        const onClickOverlay = (event) => emit("clickOverlay", event);
        const updateShow = (value) => emit("update:show", value);
        const renderMonth = (date, index) => {
          const showMonthTitle = index !== 0 || !props.showSubtitle;
          return (0, import_vue.createVNode)(import_CalendarMonth.default, (0, import_vue.mergeProps)({
            "ref": canSwitch.value ? currentMonthRef : setMonthRefs(index),
            "date": date,
            "currentDate": currentDate.value,
            "showMonthTitle": showMonthTitle,
            "firstDayOfWeek": dayOffset.value,
            "lazyRender": canSwitch.value ? false : props.lazyRender,
            "maxDate": maxDate.value,
            "minDate": minDate.value
          }, (0, import_utils.pick)(props, ["type", "color", "showMark", "formatter", "rowHeight", "showSubtitle", "allowSameDay"]), {
            "onClick": onClickDay,
            "onClickDisabledDate": (item) => emit("clickDisabledDate", item)
          }), (0, import_utils.pick)(slots, ["top-info", "bottom-info", "month-title", "text"]));
        };
        const renderFooterButton = () => {
          if (slots.footer) {
            return slots.footer();
          }
          if (props.showConfirm) {
            const slot = slots["confirm-text"];
            const disabled = buttonDisabled.value;
            const text = disabled ? props.confirmDisabledText : props.confirmText;
            return (0, import_vue.createVNode)(import_button.Button, {
              "round": true,
              "block": true,
              "type": "primary",
              "color": props.color,
              "class": (0, import_utils2.bem)("confirm"),
              "disabled": disabled,
              "nativeType": "button",
              "onClick": onConfirm
            }, {
              default: () => [slot ? slot({
                disabled
              }) : text || (0, import_utils2.t)("confirm")]
            });
          }
        };
        const renderFooter = () => (0, import_vue.createVNode)("div", {
          "class": [(0, import_utils2.bem)("footer"), {
            "van-safe-area-bottom": props.safeAreaInsetBottom
          }]
        }, [renderFooterButton()]);
        const renderCalendar = () => {
          var _a, _b;
          return (0, import_vue.createVNode)("div", {
            "class": (0, import_utils2.bem)()
          }, [(0, import_vue.createVNode)(import_CalendarHeader.default, {
            "date": (_a = currentMonthRef.value) == null ? void 0 : _a.date,
            "maxDate": maxDate.value,
            "minDate": minDate.value,
            "title": props.title,
            "subtitle": (_b = currentMonthRef.value) == null ? void 0 : _b.getTitle(),
            "showTitle": props.showTitle,
            "showSubtitle": props.showSubtitle,
            "switchMode": props.switchMode,
            "firstDayOfWeek": dayOffset.value,
            "onClickSubtitle": (event) => emit("clickSubtitle", event),
            "onPanelChange": onPanelChange
          }, (0, import_utils.pick)(slots, ["title", "subtitle", "prev-month", "prev-year", "next-month", "next-year"])), (0, import_vue.createVNode)("div", {
            "ref": bodyRef,
            "class": (0, import_utils2.bem)("body"),
            "onScroll": canSwitch.value ? void 0 : onScroll
          }, [canSwitch.value ? renderMonth(currentPanelDate.value, 0) : months.value.map(renderMonth)]), renderFooter()]);
        };
        (0, import_vue.watch)(() => props.show, init);
        (0, import_vue.watch)(() => [props.type, props.minDate, props.maxDate, props.switchMode], () => reset(getInitialDate(currentDate.value)));
        (0, import_vue.watch)(() => props.defaultDate, (value) => {
          reset(value);
        });
        (0, import_use_expose.useExpose)({
          reset,
          scrollToDate,
          getSelectedDate
        });
        (0, import_use.onMountedOrActivated)(init);
        return () => {
          if (props.poppable) {
            return (0, import_vue.createVNode)(import_popup.Popup, {
              "show": props.show,
              "class": (0, import_utils2.bem)("popup"),
              "round": props.round,
              "position": props.position,
              "closeable": props.showTitle || props.showSubtitle,
              "teleport": props.teleport,
              "closeOnPopstate": props.closeOnPopstate,
              "safeAreaInsetTop": props.safeAreaInsetTop,
              "closeOnClickOverlay": props.closeOnClickOverlay,
              "onClickOverlay": onClickOverlay,
              "onUpdate:show": updateShow
            }, {
              default: renderCalendar
            });
          }
          return renderCalendar();
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/index.js
var require_calendar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/calendar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Calendar: () => Calendar,
      calendarProps: () => import_Calendar2.calendarProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Calendar = __toESM(require_Calendar());
    var import_Calendar2 = require_Calendar();
    var Calendar = (0, import_utils.withInstall)(import_Calendar.default);
    var stdin_default = Calendar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image/Image.js
var require_Image = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image/Image.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      imageProps: () => imageProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("image");
    var imageProps = {
      src: String,
      alt: String,
      fit: String,
      position: String,
      round: Boolean,
      block: Boolean,
      width: import_utils.numericProp,
      height: import_utils.numericProp,
      radius: import_utils.numericProp,
      lazyLoad: Boolean,
      iconSize: import_utils.numericProp,
      showError: import_utils.truthProp,
      errorIcon: (0, import_utils.makeStringProp)("photo-fail"),
      iconPrefix: String,
      showLoading: import_utils.truthProp,
      loadingIcon: (0, import_utils.makeStringProp)("photo"),
      crossorigin: String,
      referrerpolicy: String,
      decoding: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: imageProps,
      emits: ["load", "error"],
      setup(props, {
        emit,
        slots
      }) {
        const error = (0, import_vue.ref)(false);
        const loading = (0, import_vue.ref)(true);
        const imageRef = (0, import_vue.ref)();
        const {
          $Lazyload
        } = (0, import_vue.getCurrentInstance)().proxy;
        const style = (0, import_vue.computed)(() => {
          const style2 = {
            width: (0, import_utils.addUnit)(props.width),
            height: (0, import_utils.addUnit)(props.height)
          };
          if ((0, import_utils.isDef)(props.radius)) {
            style2.overflow = "hidden";
            style2.borderRadius = (0, import_utils.addUnit)(props.radius);
          }
          return style2;
        });
        (0, import_vue.watch)(() => props.src, () => {
          error.value = false;
          loading.value = true;
        });
        const onLoad = (event) => {
          if (loading.value) {
            loading.value = false;
            emit("load", event);
          }
        };
        const triggerLoad = () => {
          const loadEvent = new Event("load");
          Object.defineProperty(loadEvent, "target", {
            value: imageRef.value,
            enumerable: true
          });
          onLoad(loadEvent);
        };
        const onError = (event) => {
          error.value = true;
          loading.value = false;
          emit("error", event);
        };
        const renderIcon = (name2, className, slot) => {
          if (slot) {
            return slot();
          }
          return (0, import_vue.createVNode)(import_icon.Icon, {
            "name": name2,
            "size": props.iconSize,
            "class": className,
            "classPrefix": props.iconPrefix
          }, null);
        };
        const renderPlaceholder = () => {
          if (loading.value && props.showLoading) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("loading")
            }, [renderIcon(props.loadingIcon, bem("loading-icon"), slots.loading)]);
          }
          if (error.value && props.showError) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("error")
            }, [renderIcon(props.errorIcon, bem("error-icon"), slots.error)]);
          }
        };
        const renderImage = () => {
          if (error.value || !props.src) {
            return;
          }
          const attrs = {
            alt: props.alt,
            class: bem("img"),
            decoding: props.decoding,
            style: {
              objectFit: props.fit,
              objectPosition: props.position
            },
            crossorigin: props.crossorigin,
            referrerpolicy: props.referrerpolicy
          };
          if (props.lazyLoad) {
            return (0, import_vue.withDirectives)((0, import_vue.createVNode)("img", (0, import_vue.mergeProps)({
              "ref": imageRef
            }, attrs), null), [[(0, import_vue.resolveDirective)("lazy"), props.src]]);
          }
          return (0, import_vue.createVNode)("img", (0, import_vue.mergeProps)({
            "ref": imageRef,
            "src": props.src,
            "onLoad": onLoad,
            "onError": onError
          }, attrs), null);
        };
        const onLazyLoaded = ({
          el
        }) => {
          const check = () => {
            if (el === imageRef.value && loading.value) {
              triggerLoad();
            }
          };
          if (imageRef.value) {
            check();
          } else {
            (0, import_vue.nextTick)(check);
          }
        };
        const onLazyLoadError = ({
          el
        }) => {
          if (el === imageRef.value && !error.value) {
            onError();
          }
        };
        if ($Lazyload && import_utils.inBrowser) {
          $Lazyload.$on("loaded", onLazyLoaded);
          $Lazyload.$on("error", onLazyLoadError);
          (0, import_vue.onBeforeUnmount)(() => {
            $Lazyload.$off("loaded", onLazyLoaded);
            $Lazyload.$off("error", onLazyLoadError);
          });
        }
        (0, import_vue.onMounted)(() => {
          (0, import_vue.nextTick)(() => {
            var _a;
            if (((_a = imageRef.value) == null ? void 0 : _a.complete) && !props.lazyLoad) {
              triggerLoad();
            }
          });
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": bem({
              round: props.round,
              block: props.block
            }),
            "style": style.value
          }, [renderImage(), renderPlaceholder(), (_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image/index.js
var require_image = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Image: () => Image2,
      default: () => stdin_default,
      imageProps: () => import_Image2.imageProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Image = __toESM(require_Image());
    var import_Image2 = require_Image();
    var Image2 = (0, import_utils.withInstall)(import_Image.default);
    var stdin_default = Image2;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/card/Card.js
var require_Card = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/card/Card.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      cardProps: () => cardProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_tag = require_tag();
    var import_image = require_image();
    var [name, bem] = (0, import_utils.createNamespace)("card");
    var cardProps = {
      tag: String,
      num: import_utils.numericProp,
      desc: String,
      thumb: String,
      title: String,
      price: import_utils.numericProp,
      centered: Boolean,
      lazyLoad: Boolean,
      currency: (0, import_utils.makeStringProp)("¥"),
      thumbLink: String,
      originPrice: import_utils.numericProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: cardProps,
      emits: ["clickThumb"],
      setup(props, {
        slots,
        emit
      }) {
        const renderTitle = () => {
          if (slots.title) {
            return slots.title();
          }
          if (props.title) {
            return (0, import_vue.createVNode)("div", {
              "class": [bem("title"), "van-multi-ellipsis--l2"]
            }, [props.title]);
          }
        };
        const renderThumbTag = () => {
          if (slots.tag || props.tag) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("tag")
            }, [slots.tag ? slots.tag() : (0, import_vue.createVNode)(import_tag.Tag, {
              "mark": true,
              "type": "primary"
            }, {
              default: () => [props.tag]
            })]);
          }
        };
        const renderThumbImage = () => {
          if (slots.thumb) {
            return slots.thumb();
          }
          return (0, import_vue.createVNode)(import_image.Image, {
            "src": props.thumb,
            "fit": "cover",
            "width": "100%",
            "height": "100%",
            "lazyLoad": props.lazyLoad
          }, null);
        };
        const renderThumb = () => {
          if (slots.thumb || props.thumb) {
            return (0, import_vue.createVNode)("a", {
              "href": props.thumbLink,
              "class": bem("thumb"),
              "onClick": (event) => emit("clickThumb", event)
            }, [renderThumbImage(), renderThumbTag()]);
          }
        };
        const renderDesc = () => {
          if (slots.desc) {
            return slots.desc();
          }
          if (props.desc) {
            return (0, import_vue.createVNode)("div", {
              "class": [bem("desc"), "van-ellipsis"]
            }, [props.desc]);
          }
        };
        const renderPriceText = () => {
          const priceArr = props.price.toString().split(".");
          return (0, import_vue.createVNode)("div", null, [(0, import_vue.createVNode)("span", {
            "class": bem("price-currency")
          }, [props.currency]), (0, import_vue.createVNode)("span", {
            "class": bem("price-integer")
          }, [priceArr[0]]), priceArr.length > 1 && (0, import_vue.createVNode)(import_vue.Fragment, null, [(0, import_vue.createTextVNode)("."), (0, import_vue.createVNode)("span", {
            "class": bem("price-decimal")
          }, [priceArr[1]])])]);
        };
        return () => {
          var _a, _b, _c;
          const showNum = slots.num || (0, import_utils.isDef)(props.num);
          const showPrice = slots.price || (0, import_utils.isDef)(props.price);
          const showOriginPrice = slots["origin-price"] || (0, import_utils.isDef)(props.originPrice);
          const showBottom = showNum || showPrice || showOriginPrice || slots.bottom;
          const Price = showPrice && (0, import_vue.createVNode)("div", {
            "class": bem("price")
          }, [slots.price ? slots.price() : renderPriceText()]);
          const OriginPrice = showOriginPrice && (0, import_vue.createVNode)("div", {
            "class": bem("origin-price")
          }, [slots["origin-price"] ? slots["origin-price"]() : `${props.currency} ${props.originPrice}`]);
          const Num = showNum && (0, import_vue.createVNode)("div", {
            "class": bem("num")
          }, [slots.num ? slots.num() : `x${props.num}`]);
          const Footer = slots.footer && (0, import_vue.createVNode)("div", {
            "class": bem("footer")
          }, [slots.footer()]);
          const Bottom = showBottom && (0, import_vue.createVNode)("div", {
            "class": bem("bottom")
          }, [(_a = slots["price-top"]) == null ? void 0 : _a.call(slots), Price, OriginPrice, Num, (_b = slots.bottom) == null ? void 0 : _b.call(slots)]);
          return (0, import_vue.createVNode)("div", {
            "class": bem()
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("header")
          }, [renderThumb(), (0, import_vue.createVNode)("div", {
            "class": bem("content", {
              centered: props.centered
            })
          }, [(0, import_vue.createVNode)("div", null, [renderTitle(), renderDesc(), (_c = slots.tags) == null ? void 0 : _c.call(slots)]), Bottom])]), Footer]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/card/index.js
var require_card = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/card/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Card: () => Card,
      cardProps: () => import_Card2.cardProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Card = __toESM(require_Card());
    var import_Card2 = require_Card();
    var Card = (0, import_utils.withInstall)(import_Card.default);
    var stdin_default = Card;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cascader/Cascader.js
var require_Cascader = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cascader/Cascader.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      cascaderProps: () => cascaderProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_refs = require_use_refs();
    var import_tab = require_tab();
    var import_tabs = require_tabs();
    var import_icon = require_icon();
    var [name, bem, t] = (0, import_utils.createNamespace)("cascader");
    var cascaderProps = {
      title: String,
      options: (0, import_utils.makeArrayProp)(),
      closeable: import_utils.truthProp,
      swipeable: import_utils.truthProp,
      closeIcon: (0, import_utils.makeStringProp)("cross"),
      showHeader: import_utils.truthProp,
      modelValue: import_utils.numericProp,
      fieldNames: Object,
      placeholder: String,
      activeColor: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: cascaderProps,
      emits: ["close", "change", "finish", "clickTab", "update:modelValue"],
      setup(props, {
        slots,
        emit
      }) {
        const tabs = (0, import_vue.ref)([]);
        const activeTab = (0, import_vue.ref)(0);
        const [selectedElementRefs, setSelectedElementRefs] = (0, import_use_refs.useRefs)();
        const {
          text: textKey,
          value: valueKey,
          children: childrenKey
        } = (0, import_utils.extend)({
          text: "text",
          value: "value",
          children: "children"
        }, props.fieldNames);
        const getSelectedOptionsByValue = (options, value) => {
          for (const option of options) {
            if (option[valueKey] === value) {
              return [option];
            }
            if (option[childrenKey]) {
              const selectedOptions = getSelectedOptionsByValue(option[childrenKey], value);
              if (selectedOptions) {
                return [option, ...selectedOptions];
              }
            }
          }
        };
        const updateTabs = () => {
          const {
            options,
            modelValue
          } = props;
          if (modelValue !== void 0) {
            const selectedOptions = getSelectedOptionsByValue(options, modelValue);
            if (selectedOptions) {
              let optionsCursor = options;
              tabs.value = selectedOptions.map((option) => {
                const tab = {
                  options: optionsCursor,
                  selected: option
                };
                const next = optionsCursor.find((item) => item[valueKey] === option[valueKey]);
                if (next) {
                  optionsCursor = next[childrenKey];
                }
                return tab;
              });
              if (optionsCursor) {
                tabs.value.push({
                  options: optionsCursor,
                  selected: null
                });
              }
              (0, import_vue.nextTick)(() => {
                activeTab.value = tabs.value.length - 1;
              });
              return;
            }
          }
          tabs.value = [{
            options,
            selected: null
          }];
        };
        const onSelect = (option, tabIndex) => {
          if (option.disabled) {
            return;
          }
          tabs.value[tabIndex].selected = option;
          if (tabs.value.length > tabIndex + 1) {
            tabs.value = tabs.value.slice(0, tabIndex + 1);
          }
          if (option[childrenKey]) {
            const nextTab = {
              options: option[childrenKey],
              selected: null
            };
            if (tabs.value[tabIndex + 1]) {
              tabs.value[tabIndex + 1] = nextTab;
            } else {
              tabs.value.push(nextTab);
            }
            (0, import_vue.nextTick)(() => {
              activeTab.value++;
            });
          }
          const selectedOptions = tabs.value.map((tab) => tab.selected).filter(Boolean);
          emit("update:modelValue", option[valueKey]);
          const params = {
            value: option[valueKey],
            tabIndex,
            selectedOptions
          };
          emit("change", params);
          if (!option[childrenKey]) {
            emit("finish", params);
          }
        };
        const onClose = () => emit("close");
        const onClickTab = ({
          name: name2,
          title
        }) => emit("clickTab", name2, title);
        const renderHeader = () => props.showHeader ? (0, import_vue.createVNode)("div", {
          "class": bem("header")
        }, [(0, import_vue.createVNode)("h2", {
          "class": bem("title")
        }, [slots.title ? slots.title() : props.title]), props.closeable ? (0, import_vue.createVNode)(import_icon.Icon, {
          "name": props.closeIcon,
          "class": [bem("close-icon"), import_utils.HAPTICS_FEEDBACK],
          "onClick": onClose
        }, null) : null]) : null;
        const renderOption = (option, selectedOption, tabIndex) => {
          const {
            disabled
          } = option;
          const selected = !!(selectedOption && option[valueKey] === selectedOption[valueKey]);
          const color = option.color || (selected ? props.activeColor : void 0);
          const Text = slots.option ? slots.option({
            option,
            selected
          }) : (0, import_vue.createVNode)("span", null, [option[textKey]]);
          return (0, import_vue.createVNode)("li", {
            "ref": selected ? setSelectedElementRefs(tabIndex) : void 0,
            "role": "menuitemradio",
            "class": [bem("option", {
              selected,
              disabled
            }), option.className],
            "style": {
              color
            },
            "tabindex": disabled ? void 0 : selected ? 0 : -1,
            "aria-checked": selected,
            "aria-disabled": disabled || void 0,
            "onClick": () => onSelect(option, tabIndex)
          }, [Text, selected ? (0, import_vue.createVNode)(import_icon.Icon, {
            "name": "success",
            "class": bem("selected-icon")
          }, null) : null]);
        };
        const renderOptions = (options, selectedOption, tabIndex) => (0, import_vue.createVNode)("ul", {
          "role": "menu",
          "class": bem("options")
        }, [options.map((option) => renderOption(option, selectedOption, tabIndex))]);
        const renderTab = (tab, tabIndex) => {
          const {
            options,
            selected
          } = tab;
          const placeholder = props.placeholder || t("select");
          const title = selected ? selected[textKey] : placeholder;
          return (0, import_vue.createVNode)(import_tab.Tab, {
            "title": title,
            "titleClass": bem("tab", {
              unselected: !selected
            })
          }, {
            default: () => {
              var _a, _b;
              return [(_a = slots["options-top"]) == null ? void 0 : _a.call(slots, {
                tabIndex
              }), renderOptions(options, selected, tabIndex), (_b = slots["options-bottom"]) == null ? void 0 : _b.call(slots, {
                tabIndex
              })];
            }
          });
        };
        const renderTabs = () => (0, import_vue.createVNode)(import_tabs.Tabs, {
          "active": activeTab.value,
          "onUpdate:active": ($event) => activeTab.value = $event,
          "shrink": true,
          "animated": true,
          "class": bem("tabs"),
          "color": props.activeColor,
          "swipeable": props.swipeable,
          "onClickTab": onClickTab
        }, {
          default: () => [tabs.value.map(renderTab)]
        });
        const scrollIntoView = (el) => {
          const scrollParent = el.parentElement;
          if (scrollParent) {
            scrollParent.scrollTop = el.offsetTop - (scrollParent.offsetHeight - el.offsetHeight) / 2;
          }
        };
        updateTabs();
        (0, import_vue.watch)(activeTab, (value) => {
          const el = selectedElementRefs.value[value];
          if (el) scrollIntoView(el);
        });
        (0, import_vue.watch)(() => props.options, updateTabs, {
          deep: true
        });
        (0, import_vue.watch)(() => props.modelValue, (value) => {
          if (value !== void 0) {
            const values = tabs.value.map((tab) => {
              var _a;
              return (_a = tab.selected) == null ? void 0 : _a[valueKey];
            });
            if (values.includes(value)) {
              return;
            }
          }
          updateTabs();
        });
        return () => (0, import_vue.createVNode)("div", {
          "class": bem()
        }, [renderHeader(), renderTabs()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cascader/index.js
var require_cascader = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cascader/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Cascader: () => Cascader,
      cascaderProps: () => import_Cascader2.cascaderProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Cascader = __toESM(require_Cascader());
    var import_Cascader2 = require_Cascader();
    var Cascader = (0, import_utils.withInstall)(import_Cascader.default);
    var stdin_default = Cascader;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cell-group/CellGroup.js
var require_CellGroup = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cell-group/CellGroup.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      cellGroupProps: () => cellGroupProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_scope_id = require_use_scope_id();
    var [name, bem] = (0, import_utils.createNamespace)("cell-group");
    var cellGroupProps = {
      title: String,
      inset: Boolean,
      border: import_utils.truthProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      inheritAttrs: false,
      props: cellGroupProps,
      setup(props, {
        slots,
        attrs
      }) {
        const renderGroup = () => {
          var _a;
          return (0, import_vue.createVNode)("div", (0, import_vue.mergeProps)({
            "class": [bem({
              inset: props.inset
            }), {
              [import_utils.BORDER_TOP_BOTTOM]: props.border && !props.inset
            }]
          }, attrs, (0, import_use_scope_id.useScopeId)()), [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
        const renderTitle = () => (0, import_vue.createVNode)("div", {
          "class": bem("title", {
            inset: props.inset
          })
        }, [slots.title ? slots.title() : props.title]);
        return () => {
          if (props.title || slots.title) {
            return (0, import_vue.createVNode)(import_vue.Fragment, null, [renderTitle(), renderGroup()]);
          }
          return renderGroup();
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cell-group/index.js
var require_cell_group = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/cell-group/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      CellGroup: () => CellGroup,
      cellGroupProps: () => import_CellGroup2.cellGroupProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_CellGroup = __toESM(require_CellGroup());
    var import_CellGroup2 = require_CellGroup();
    var CellGroup = (0, import_utils.withInstall)(import_CellGroup.default);
    var stdin_default = CellGroup;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/circle/Circle.js
var require_Circle = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/circle/Circle.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      circleProps: () => circleProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use = require_index_cjs();
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("circle");
    var uid = 0;
    var format = (rate) => Math.min(Math.max(+rate, 0), 100);
    function getPath(clockwise, viewBoxSize) {
      const sweepFlag = clockwise ? 1 : 0;
      return `M ${viewBoxSize / 2} ${viewBoxSize / 2} m 0, -500 a 500, 500 0 1, ${sweepFlag} 0, 1000 a 500, 500 0 1, ${sweepFlag} 0, -1000`;
    }
    var circleProps = {
      text: String,
      size: import_utils.numericProp,
      fill: (0, import_utils.makeStringProp)("none"),
      rate: (0, import_utils.makeNumericProp)(100),
      speed: (0, import_utils.makeNumericProp)(0),
      color: [String, Object],
      clockwise: import_utils.truthProp,
      layerColor: String,
      currentRate: (0, import_utils.makeNumberProp)(0),
      strokeWidth: (0, import_utils.makeNumericProp)(40),
      strokeLinecap: String,
      startPosition: (0, import_utils.makeStringProp)("top")
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: circleProps,
      emits: ["update:currentRate"],
      setup(props, {
        emit,
        slots
      }) {
        const id = `van-circle-${uid++}`;
        const viewBoxSize = (0, import_vue.computed)(() => +props.strokeWidth + 1e3);
        const path = (0, import_vue.computed)(() => getPath(props.clockwise, viewBoxSize.value));
        const svgStyle = (0, import_vue.computed)(() => {
          const ROTATE_ANGLE_MAP = {
            top: 0,
            right: 90,
            bottom: 180,
            left: 270
          };
          const angleValue = ROTATE_ANGLE_MAP[props.startPosition];
          if (angleValue) {
            return {
              transform: `rotate(${angleValue}deg)`
            };
          }
        });
        (0, import_vue.watch)(() => props.rate, (rate) => {
          let rafId;
          const startTime = Date.now();
          const startRate = props.currentRate;
          const endRate = format(rate);
          const duration = Math.abs((startRate - endRate) * 1e3 / +props.speed);
          const animate = () => {
            const now = Date.now();
            const progress = Math.min((now - startTime) / duration, 1);
            const rate2 = progress * (endRate - startRate) + startRate;
            emit("update:currentRate", format(parseFloat(rate2.toFixed(1))));
            if (endRate > startRate ? rate2 < endRate : rate2 > endRate) {
              rafId = (0, import_use.raf)(animate);
            }
          };
          if (props.speed) {
            if (rafId) {
              (0, import_use.cancelRaf)(rafId);
            }
            rafId = (0, import_use.raf)(animate);
          } else {
            emit("update:currentRate", endRate);
          }
        }, {
          immediate: true
        });
        const renderHover = () => {
          const PERIMETER = 3140;
          const {
            strokeWidth,
            currentRate,
            strokeLinecap
          } = props;
          const offset = PERIMETER * currentRate / 100;
          const color = (0, import_utils.isObject)(props.color) ? `url(#${id})` : props.color;
          const style = {
            stroke: color,
            strokeWidth: `${+strokeWidth + 1}px`,
            strokeLinecap,
            strokeDasharray: `${offset}px ${PERIMETER}px`
          };
          return (0, import_vue.createVNode)("path", {
            "d": path.value,
            "style": style,
            "class": bem("hover"),
            "stroke": color
          }, null);
        };
        const renderLayer = () => {
          const style = {
            fill: props.fill,
            stroke: props.layerColor,
            strokeWidth: `${props.strokeWidth}px`
          };
          return (0, import_vue.createVNode)("path", {
            "class": bem("layer"),
            "style": style,
            "d": path.value
          }, null);
        };
        const renderGradient = () => {
          const {
            color
          } = props;
          if (!(0, import_utils.isObject)(color)) {
            return;
          }
          const Stops = Object.keys(color).sort((a, b) => parseFloat(a) - parseFloat(b)).map((key, index) => (0, import_vue.createVNode)("stop", {
            "key": index,
            "offset": key,
            "stop-color": color[key]
          }, null));
          return (0, import_vue.createVNode)("defs", null, [(0, import_vue.createVNode)("linearGradient", {
            "id": id,
            "x1": "100%",
            "y1": "0%",
            "x2": "0%",
            "y2": "0%"
          }, [Stops])]);
        };
        const renderText = () => {
          if (slots.default) {
            return slots.default();
          }
          if (props.text) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("text")
            }, [props.text]);
          }
        };
        return () => (0, import_vue.createVNode)("div", {
          "class": bem(),
          "style": (0, import_utils.getSizeStyle)(props.size)
        }, [(0, import_vue.createVNode)("svg", {
          "viewBox": `0 0 ${viewBoxSize.value} ${viewBoxSize.value}`,
          "style": svgStyle.value
        }, [renderGradient(), renderLayer(), renderHover()]), renderText()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/circle/index.js
var require_circle = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/circle/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Circle: () => Circle,
      circleProps: () => import_Circle2.circleProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Circle = __toESM(require_Circle());
    var import_Circle2 = require_Circle();
    var Circle = (0, import_utils.withInstall)(import_Circle.default);
    var stdin_default = Circle;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/row/Row.js
var require_Row = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/row/Row.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ROW_KEY: () => ROW_KEY,
      default: () => stdin_default,
      rowProps: () => rowProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var [name, bem] = (0, import_utils.createNamespace)("row");
    var ROW_KEY = Symbol(name);
    var rowProps = {
      tag: (0, import_utils.makeStringProp)("div"),
      wrap: import_utils.truthProp,
      align: String,
      gutter: {
        type: [String, Number, Array],
        default: 0
      },
      justify: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: rowProps,
      setup(props, {
        slots
      }) {
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(ROW_KEY);
        const groups = (0, import_vue.computed)(() => {
          const groups2 = [[]];
          let totalSpan = 0;
          children.forEach((child, index) => {
            totalSpan += Number(child.span);
            if (totalSpan > 24) {
              groups2.push([index]);
              totalSpan -= 24;
            } else {
              groups2[groups2.length - 1].push(index);
            }
          });
          return groups2;
        });
        const spaces = (0, import_vue.computed)(() => {
          let gutter = 0;
          if (Array.isArray(props.gutter)) {
            gutter = Number(props.gutter[0]) || 0;
          } else {
            gutter = Number(props.gutter);
          }
          const spaces2 = [];
          if (!gutter) {
            return spaces2;
          }
          groups.value.forEach((group) => {
            const averagePadding = gutter * (group.length - 1) / group.length;
            group.forEach((item, index) => {
              if (index === 0) {
                spaces2.push({
                  right: averagePadding
                });
              } else {
                const left = gutter - spaces2[item - 1].right;
                const right = averagePadding - left;
                spaces2.push({
                  left,
                  right
                });
              }
            });
          });
          return spaces2;
        });
        const verticalSpaces = (0, import_vue.computed)(() => {
          const {
            gutter
          } = props;
          const spaces2 = [];
          if (Array.isArray(gutter) && gutter.length > 1) {
            const bottom = Number(gutter[1]) || 0;
            if (bottom <= 0) {
              return spaces2;
            }
            groups.value.forEach((group, index) => {
              if (index === groups.value.length - 1) return;
              group.forEach(() => {
                spaces2.push({
                  bottom
                });
              });
            });
          }
          return spaces2;
        });
        linkChildren({
          spaces,
          verticalSpaces
        });
        return () => {
          const {
            tag,
            wrap,
            align,
            justify
          } = props;
          return (0, import_vue.createVNode)(tag, {
            "class": bem({
              [`align-${align}`]: align,
              [`justify-${justify}`]: justify,
              nowrap: !wrap
            })
          }, {
            default: () => {
              var _a;
              return [(_a = slots.default) == null ? void 0 : _a.call(slots)];
            }
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/col/Col.js
var require_Col = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/col/Col.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      colProps: () => colProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_Row = require_Row();
    var [name, bem] = (0, import_utils.createNamespace)("col");
    var colProps = {
      tag: (0, import_utils.makeStringProp)("div"),
      span: (0, import_utils.makeNumericProp)(0),
      offset: import_utils.numericProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: colProps,
      setup(props, {
        slots
      }) {
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_Row.ROW_KEY);
        const style = (0, import_vue.computed)(() => {
          if (!parent) {
            return;
          }
          const {
            spaces,
            verticalSpaces
          } = parent;
          let styles = {};
          if (spaces && spaces.value && spaces.value[index.value]) {
            const {
              left,
              right
            } = spaces.value[index.value];
            styles = {
              paddingLeft: left ? `${left}px` : null,
              paddingRight: right ? `${right}px` : null
            };
          }
          const {
            bottom
          } = verticalSpaces.value[index.value] || {};
          return (0, import_utils.extend)(styles, {
            marginBottom: bottom ? `${bottom}px` : null
          });
        });
        return () => {
          const {
            tag,
            span,
            offset
          } = props;
          return (0, import_vue.createVNode)(tag, {
            "style": style.value,
            "class": bem({
              [span]: span,
              [`offset-${offset}`]: offset
            })
          }, {
            default: () => {
              var _a;
              return [(_a = slots.default) == null ? void 0 : _a.call(slots)];
            }
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/col/index.js
var require_col = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/col/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Col: () => Col,
      colProps: () => import_Col2.colProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Col = __toESM(require_Col());
    var import_Col2 = require_Col();
    var Col = (0, import_utils.withInstall)(import_Col.default);
    var stdin_default = Col;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/collapse/Collapse.js
var require_Collapse = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/collapse/Collapse.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      COLLAPSE_KEY: () => COLLAPSE_KEY,
      collapseProps: () => collapseProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("collapse");
    var COLLAPSE_KEY = Symbol(name);
    var collapseProps = {
      border: import_utils.truthProp,
      accordion: Boolean,
      modelValue: {
        type: [String, Number, Array],
        default: ""
      }
    };
    function validateModelValue(modelValue, accordion) {
      if (accordion && Array.isArray(modelValue)) {
        console.error('[Vant] Collapse: "v-model" should not be Array in accordion mode');
        return false;
      }
      if (!accordion && !Array.isArray(modelValue)) {
        console.error('[Vant] Collapse: "v-model" should be Array in non-accordion mode');
        return false;
      }
      return true;
    }
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: collapseProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          linkChildren,
          children
        } = (0, import_use.useChildren)(COLLAPSE_KEY);
        const updateName = (name2) => {
          emit("change", name2);
          emit("update:modelValue", name2);
        };
        const toggle = (name2, expanded) => {
          const {
            accordion,
            modelValue
          } = props;
          if (accordion) {
            updateName(name2 === modelValue ? "" : name2);
          } else if (expanded) {
            updateName(modelValue.concat(name2));
          } else {
            updateName(modelValue.filter((activeName) => activeName !== name2));
          }
        };
        const toggleAll = (options = {}) => {
          if (props.accordion) {
            return;
          }
          if (typeof options === "boolean") {
            options = {
              expanded: options
            };
          }
          const {
            expanded,
            skipDisabled
          } = options;
          const expandedChildren = children.filter((item) => {
            if (item.disabled && skipDisabled) {
              return item.expanded.value;
            }
            return expanded != null ? expanded : !item.expanded.value;
          });
          const names = expandedChildren.map((item) => item.itemName.value);
          updateName(names);
        };
        const isExpanded = (name2) => {
          const {
            accordion,
            modelValue
          } = props;
          if (!validateModelValue(modelValue, accordion)) {
            return false;
          }
          return accordion ? modelValue === name2 : modelValue.includes(name2);
        };
        (0, import_use_expose.useExpose)({
          toggleAll
        });
        linkChildren({
          toggle,
          isExpanded
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": [bem(), {
              [import_utils.BORDER_TOP_BOTTOM]: props.border
            }]
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/collapse/index.js
var require_collapse = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/collapse/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Collapse: () => Collapse,
      collapseProps: () => import_Collapse2.collapseProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Collapse = __toESM(require_Collapse());
    var import_Collapse2 = require_Collapse();
    var Collapse = (0, import_utils.withInstall)(import_Collapse.default);
    var stdin_default = Collapse;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/collapse-item/CollapseItem.js
var require_CollapseItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/collapse-item/CollapseItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      collapseItemProps: () => collapseItemProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_Cell = require_Cell();
    var import_utils = require_utils();
    var import_Collapse = require_Collapse();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_use_lazy_render = require_use_lazy_render();
    var import_cell = require_cell();
    var [name, bem] = (0, import_utils.createNamespace)("collapse-item");
    var CELL_SLOTS = ["icon", "title", "value", "label", "right-icon"];
    var collapseItemProps = (0, import_utils.extend)({}, import_Cell.cellSharedProps, {
      name: import_utils.numericProp,
      isLink: import_utils.truthProp,
      disabled: Boolean,
      readonly: Boolean,
      lazyRender: import_utils.truthProp
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: collapseItemProps,
      setup(props, {
        slots
      }) {
        const wrapperRef = (0, import_vue.ref)();
        const contentRef = (0, import_vue.ref)();
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_Collapse.COLLAPSE_KEY);
        if (!parent) {
          if (true) {
            console.error("[Vant] <CollapseItem> must be a child component of <Collapse>.");
          }
          return;
        }
        const name2 = (0, import_vue.computed)(() => {
          var _a;
          return (_a = props.name) != null ? _a : index.value;
        });
        const expanded = (0, import_vue.computed)(() => parent.isExpanded(name2.value));
        const show = (0, import_vue.ref)(expanded.value);
        const lazyRender = (0, import_use_lazy_render.useLazyRender)(() => show.value || !props.lazyRender);
        const onTransitionEnd = () => {
          if (!expanded.value) {
            show.value = false;
          } else if (wrapperRef.value) {
            wrapperRef.value.style.height = "";
          }
        };
        (0, import_vue.watch)(expanded, (value, oldValue) => {
          if (oldValue === null) {
            return;
          }
          if (value) {
            show.value = true;
          }
          const tick = value ? import_vue.nextTick : import_use.raf;
          tick(() => {
            if (!contentRef.value || !wrapperRef.value) {
              return;
            }
            const {
              offsetHeight
            } = contentRef.value;
            if (offsetHeight) {
              const contentHeight = `${offsetHeight}px`;
              wrapperRef.value.style.height = value ? "0" : contentHeight;
              (0, import_use.doubleRaf)(() => {
                if (wrapperRef.value) {
                  wrapperRef.value.style.height = value ? contentHeight : "0";
                }
              });
            } else {
              onTransitionEnd();
            }
          });
        });
        const toggle = (newValue = !expanded.value) => {
          parent.toggle(name2.value, newValue);
        };
        const onClickTitle = () => {
          if (!props.disabled && !props.readonly) {
            toggle();
          }
        };
        const renderTitle = () => {
          const {
            border,
            disabled,
            readonly
          } = props;
          const attrs = (0, import_utils.pick)(props, Object.keys(import_Cell.cellSharedProps));
          if (readonly) {
            attrs.isLink = false;
          }
          if (disabled || readonly) {
            attrs.clickable = false;
          }
          return (0, import_vue.createVNode)(import_cell.Cell, (0, import_vue.mergeProps)({
            "role": "button",
            "class": bem("title", {
              disabled,
              expanded: expanded.value,
              borderless: !border
            }),
            "aria-expanded": String(expanded.value),
            "onClick": onClickTitle
          }, attrs), (0, import_utils.pick)(slots, CELL_SLOTS));
        };
        const renderContent = lazyRender(() => {
          var _a;
          return (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", {
            "ref": wrapperRef,
            "class": bem("wrapper"),
            "onTransitionend": onTransitionEnd
          }, [(0, import_vue.createVNode)("div", {
            "ref": contentRef,
            "class": bem("content")
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)])]), [[import_vue.vShow, show.value]]);
        });
        (0, import_use_expose.useExpose)({
          toggle,
          expanded,
          itemName: name2
        });
        return () => (0, import_vue.createVNode)("div", {
          "class": [bem({
            border: index.value && props.border
          })]
        }, [renderTitle(), renderContent()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/collapse-item/index.js
var require_collapse_item = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/collapse-item/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      CollapseItem: () => CollapseItem,
      collapseItemProps: () => import_CollapseItem2.collapseItemProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_CollapseItem = __toESM(require_CollapseItem());
    var import_CollapseItem2 = require_CollapseItem();
    var CollapseItem = (0, import_utils.withInstall)(import_CollapseItem.default);
    var stdin_default = CollapseItem;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/config-provider/index.js
var require_config_provider = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/config-provider/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ConfigProvider: () => ConfigProvider,
      configProviderProps: () => import_ConfigProvider2.configProviderProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ConfigProvider = __toESM(require_ConfigProvider());
    var import_ConfigProvider2 = require_ConfigProvider();
    var ConfigProvider = (0, import_utils.withInstall)(import_ConfigProvider.default);
    var stdin_default = ConfigProvider;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-card/ContactCard.js
var require_ContactCard = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-card/ContactCard.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      contactCardProps: () => contactCardProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_cell = require_cell();
    var [name, bem, t] = (0, import_utils.createNamespace)("contact-card");
    var contactCardProps = {
      tel: String,
      name: String,
      type: (0, import_utils.makeStringProp)("add"),
      addText: String,
      editable: import_utils.truthProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: contactCardProps,
      emits: ["click"],
      setup(props, {
        emit
      }) {
        const onClick = (event) => {
          if (props.editable) {
            emit("click", event);
          }
        };
        const renderContent = () => {
          if (props.type === "add") {
            return props.addText || t("addContact");
          }
          return [(0, import_vue.createVNode)("div", null, [`${t("name")}：${props.name}`]), (0, import_vue.createVNode)("div", null, [`${t("tel")}：${props.tel}`])];
        };
        return () => (0, import_vue.createVNode)(import_cell.Cell, {
          "center": true,
          "icon": props.type === "edit" ? "contact" : "add-square",
          "class": bem([props.type]),
          "border": false,
          "isLink": props.editable,
          "titleClass": bem("title"),
          "onClick": onClick
        }, {
          title: renderContent
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-card/index.js
var require_contact_card = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-card/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ContactCard: () => ContactCard,
      contactCardProps: () => import_ContactCard2.contactCardProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ContactCard = __toESM(require_ContactCard());
    var import_ContactCard2 = require_ContactCard();
    var ContactCard = (0, import_utils.withInstall)(import_ContactCard.default);
    var stdin_default = ContactCard;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-edit/ContactEdit.js
var require_ContactEdit = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-edit/ContactEdit.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      contactEditProps: () => contactEditProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_cell = require_cell();
    var import_form = require_form();
    var import_field = require_field();
    var import_button = require_button();
    var import_switch = require_switch();
    var [name, bem, t] = (0, import_utils.createNamespace)("contact-edit");
    var DEFAULT_CONTACT = {
      tel: "",
      name: ""
    };
    var contactEditProps = {
      isEdit: Boolean,
      isSaving: Boolean,
      isDeleting: Boolean,
      showSetDefault: Boolean,
      setDefaultLabel: String,
      contactInfo: {
        type: Object,
        default: () => (0, import_utils.extend)({}, DEFAULT_CONTACT)
      },
      telValidator: {
        type: Function,
        default: import_utils.isMobile
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: contactEditProps,
      emits: ["save", "delete", "changeDefault"],
      setup(props, {
        emit
      }) {
        const contact = (0, import_vue.reactive)((0, import_utils.extend)({}, DEFAULT_CONTACT, props.contactInfo));
        const onSave = () => {
          if (!props.isSaving) {
            emit("save", contact);
          }
        };
        const onDelete = () => emit("delete", contact);
        const renderButtons = () => (0, import_vue.createVNode)("div", {
          "class": bem("buttons")
        }, [(0, import_vue.createVNode)(import_button.Button, {
          "block": true,
          "round": true,
          "type": "primary",
          "text": t("save"),
          "class": bem("button"),
          "loading": props.isSaving,
          "nativeType": "submit"
        }, null), props.isEdit && (0, import_vue.createVNode)(import_button.Button, {
          "block": true,
          "round": true,
          "text": t("delete"),
          "class": bem("button"),
          "loading": props.isDeleting,
          "onClick": onDelete
        }, null)]);
        const renderSwitch = () => (0, import_vue.createVNode)(import_switch.Switch, {
          "modelValue": contact.isDefault,
          "onUpdate:modelValue": ($event) => contact.isDefault = $event,
          "onChange": (checked) => emit("changeDefault", checked)
        }, null);
        const renderSetDefault = () => {
          if (props.showSetDefault) {
            return (0, import_vue.createVNode)(import_cell.Cell, {
              "title": props.setDefaultLabel,
              "class": bem("switch-cell"),
              "border": false
            }, {
              "right-icon": renderSwitch
            });
          }
        };
        (0, import_vue.watch)(() => props.contactInfo, (value) => (0, import_utils.extend)(contact, DEFAULT_CONTACT, value));
        return () => (0, import_vue.createVNode)(import_form.Form, {
          "class": bem(),
          "onSubmit": onSave
        }, {
          default: () => [(0, import_vue.createVNode)("div", {
            "class": bem("fields")
          }, [(0, import_vue.createVNode)(import_field.Field, {
            "modelValue": contact.name,
            "onUpdate:modelValue": ($event) => contact.name = $event,
            "clearable": true,
            "label": t("name"),
            "rules": [{
              required: true,
              message: t("nameEmpty")
            }],
            "maxlength": "30",
            "placeholder": t("name")
          }, null), (0, import_vue.createVNode)(import_field.Field, {
            "modelValue": contact.tel,
            "onUpdate:modelValue": ($event) => contact.tel = $event,
            "clearable": true,
            "type": "tel",
            "label": t("tel"),
            "rules": [{
              validator: props.telValidator,
              message: t("telInvalid")
            }],
            "placeholder": t("tel")
          }, null)]), renderSetDefault(), renderButtons()]
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-edit/index.js
var require_contact_edit = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-edit/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ContactEdit: () => ContactEdit,
      contactEditProps: () => import_ContactEdit2.contactEditProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ContactEdit = __toESM(require_ContactEdit());
    var import_ContactEdit2 = require_ContactEdit();
    var ContactEdit = (0, import_utils.withInstall)(import_ContactEdit.default);
    var stdin_default = ContactEdit;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-list/ContactList.js
var require_ContactList = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-list/ContactList.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      contactListProps: () => contactListProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_tag = require_tag();
    var import_icon = require_icon();
    var import_cell = require_cell();
    var import_radio = require_radio();
    var import_button = require_button();
    var import_radio_group = require_radio_group();
    var [name, bem, t] = (0, import_utils.createNamespace)("contact-list");
    var contactListProps = {
      list: Array,
      addText: String,
      modelValue: import_utils.unknownProp,
      defaultTagText: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: contactListProps,
      emits: ["add", "edit", "select", "update:modelValue"],
      setup(props, {
        emit
      }) {
        const renderItem = (item, index) => {
          const onClick = () => {
            emit("update:modelValue", item.id);
            emit("select", item, index);
          };
          const renderRightIcon = () => (0, import_vue.createVNode)(import_radio.Radio, {
            "class": bem("radio"),
            "name": item.id,
            "iconSize": 18
          }, null);
          const renderEditIcon = () => (0, import_vue.createVNode)(import_icon.Icon, {
            "name": "edit",
            "class": bem("edit"),
            "onClick": (event) => {
              event.stopPropagation();
              emit("edit", item, index);
            }
          }, null);
          const renderContent = () => {
            const nodes = [`${item.name}，${item.tel}`];
            if (item.isDefault && props.defaultTagText) {
              nodes.push((0, import_vue.createVNode)(import_tag.Tag, {
                "type": "primary",
                "round": true,
                "class": bem("item-tag")
              }, {
                default: () => [props.defaultTagText]
              }));
            }
            return nodes;
          };
          return (0, import_vue.createVNode)(import_cell.Cell, {
            "key": item.id,
            "isLink": true,
            "center": true,
            "class": bem("item"),
            "titleClass": bem("item-title"),
            "onClick": onClick
          }, {
            icon: renderEditIcon,
            title: renderContent,
            "right-icon": renderRightIcon
          });
        };
        return () => (0, import_vue.createVNode)("div", {
          "class": bem()
        }, [(0, import_vue.createVNode)(import_radio_group.RadioGroup, {
          "modelValue": props.modelValue,
          "class": bem("group")
        }, {
          default: () => [props.list && props.list.map(renderItem)]
        }), (0, import_vue.createVNode)("div", {
          "class": [bem("bottom"), "van-safe-area-bottom"]
        }, [(0, import_vue.createVNode)(import_button.Button, {
          "round": true,
          "block": true,
          "type": "primary",
          "class": bem("add"),
          "text": props.addText || t("addContact"),
          "onClick": () => emit("add")
        }, null)])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-list/index.js
var require_contact_list = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/contact-list/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ContactList: () => ContactList,
      contactListProps: () => import_ContactList2.contactListProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ContactList = __toESM(require_ContactList());
    var import_ContactList2 = require_ContactList();
    var ContactList = (0, import_utils.withInstall)(import_ContactList.default);
    var stdin_default = ContactList;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/count-down/utils.js
var require_utils8 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/count-down/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      parseFormat: () => parseFormat
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    function parseFormat(format, currentTime) {
      const { days } = currentTime;
      let { hours, minutes, seconds, milliseconds } = currentTime;
      if (format.includes("DD")) {
        format = format.replace("DD", (0, import_utils.padZero)(days));
      } else {
        hours += days * 24;
      }
      if (format.includes("HH")) {
        format = format.replace("HH", (0, import_utils.padZero)(hours));
      } else {
        minutes += hours * 60;
      }
      if (format.includes("mm")) {
        format = format.replace("mm", (0, import_utils.padZero)(minutes));
      } else {
        seconds += minutes * 60;
      }
      if (format.includes("ss")) {
        format = format.replace("ss", (0, import_utils.padZero)(seconds));
      } else {
        milliseconds += seconds * 1e3;
      }
      if (format.includes("S")) {
        const ms = (0, import_utils.padZero)(milliseconds, 3);
        if (format.includes("SSS")) {
          format = format.replace("SSS", ms);
        } else if (format.includes("SS")) {
          format = format.replace("SS", ms.slice(0, 2));
        } else {
          format = format.replace("S", ms.charAt(0));
        }
      }
      return format;
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/count-down/CountDown.js
var require_CountDown = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/count-down/CountDown.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      countDownProps: () => countDownProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils8();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("count-down");
    var countDownProps = {
      time: (0, import_utils.makeNumericProp)(0),
      format: (0, import_utils.makeStringProp)("HH:mm:ss"),
      autoStart: import_utils.truthProp,
      millisecond: Boolean
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: countDownProps,
      emits: ["change", "finish"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          start,
          pause,
          reset,
          current
        } = (0, import_use.useCountDown)({
          time: +props.time,
          millisecond: props.millisecond,
          onChange: (current2) => emit("change", current2),
          onFinish: () => emit("finish")
        });
        const timeText = (0, import_vue.computed)(() => (0, import_utils2.parseFormat)(props.format, current.value));
        const resetTime = () => {
          reset(+props.time);
          if (props.autoStart) {
            start();
          }
        };
        (0, import_vue.watch)(() => props.time, resetTime, {
          immediate: true
        });
        (0, import_use_expose.useExpose)({
          start,
          pause,
          reset: resetTime
        });
        return () => (0, import_vue.createVNode)("div", {
          "role": "timer",
          "class": bem()
        }, [slots.default ? slots.default(current.value) : timeText.value]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/count-down/index.js
var require_count_down = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/count-down/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      CountDown: () => CountDown,
      countDownProps: () => import_CountDown2.countDownProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_CountDown = __toESM(require_CountDown());
    var import_CountDown2 = require_CountDown();
    var CountDown = (0, import_utils.withInstall)(import_CountDown.default);
    var stdin_default = CountDown;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon/utils.js
var require_utils9 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      formatAmount: () => formatAmount,
      formatDiscount: () => formatDiscount,
      getDate: () => getDate
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    function getDate(timeStamp) {
      const date = new Date(timeStamp * 1e3);
      return `${date.getFullYear()}.${(0, import_utils.padZero)(date.getMonth() + 1)}.${(0, import_utils.padZero)(
        date.getDate()
      )}`;
    }
    var formatDiscount = (discount) => (discount / 10).toFixed(discount % 10 === 0 ? 0 : 1);
    var formatAmount = (amount) => (amount / 100).toFixed(amount % 100 === 0 ? 0 : amount % 10 === 0 ? 1 : 2);
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon/Coupon.js
var require_Coupon = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon/Coupon.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils9();
    var import_checkbox = require_checkbox();
    var [name, bem, t] = (0, import_utils.createNamespace)("coupon");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: {
        chosen: Boolean,
        coupon: (0, import_utils.makeRequiredProp)(Object),
        disabled: Boolean,
        currency: (0, import_utils.makeStringProp)("¥")
      },
      setup(props) {
        const validPeriod = (0, import_vue.computed)(() => {
          const {
            startAt,
            endAt
          } = props.coupon;
          return `${(0, import_utils2.getDate)(startAt)} - ${(0, import_utils2.getDate)(endAt)}`;
        });
        const faceAmount = (0, import_vue.computed)(() => {
          const {
            coupon,
            currency
          } = props;
          if (coupon.valueDesc) {
            return [coupon.valueDesc, (0, import_vue.createVNode)("span", null, [coupon.unitDesc || ""])];
          }
          if (coupon.denominations) {
            const denominations = (0, import_utils2.formatAmount)(coupon.denominations);
            return [(0, import_vue.createVNode)("span", null, [currency]), ` ${denominations}`];
          }
          if (coupon.discount) {
            return t("discount", (0, import_utils2.formatDiscount)(coupon.discount));
          }
          return "";
        });
        const conditionMessage = (0, import_vue.computed)(() => {
          const condition = (0, import_utils2.formatAmount)(props.coupon.originCondition || 0);
          return condition === "0" ? t("unlimited") : t("condition", condition);
        });
        return () => {
          const {
            chosen,
            coupon,
            disabled
          } = props;
          const description = disabled && coupon.reason || coupon.description;
          return (0, import_vue.createVNode)("div", {
            "class": bem({
              disabled
            })
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("content")
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("head")
          }, [(0, import_vue.createVNode)("h2", {
            "class": bem("amount")
          }, [faceAmount.value]), (0, import_vue.createVNode)("p", {
            "class": bem("condition")
          }, [coupon.condition || conditionMessage.value])]), (0, import_vue.createVNode)("div", {
            "class": bem("body")
          }, [(0, import_vue.createVNode)("p", {
            "class": bem("name")
          }, [coupon.name]), (0, import_vue.createVNode)("p", {
            "class": bem("valid")
          }, [validPeriod.value]), !disabled && (0, import_vue.createVNode)(import_checkbox.Checkbox, {
            "class": bem("corner"),
            "modelValue": chosen
          }, null)])]), description && (0, import_vue.createVNode)("p", {
            "class": bem("description")
          }, [description])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon/index.js
var require_coupon = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Coupon: () => Coupon,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Coupon = __toESM(require_Coupon());
    var Coupon = (0, import_utils.withInstall)(import_Coupon.default);
    var stdin_default = Coupon;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon-cell/CouponCell.js
var require_CouponCell = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon-cell/CouponCell.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      couponCellProps: () => couponCellProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_cell = require_cell();
    var [name, bem, t] = (0, import_utils.createNamespace)("coupon-cell");
    var couponCellProps = {
      title: String,
      border: import_utils.truthProp,
      editable: import_utils.truthProp,
      coupons: (0, import_utils.makeArrayProp)(),
      currency: (0, import_utils.makeStringProp)("¥"),
      chosenCoupon: {
        type: [Number, Array],
        default: -1
      }
    };
    var getValue = (coupon) => {
      const {
        value,
        denominations
      } = coupon;
      if ((0, import_utils.isDef)(value)) {
        return value;
      }
      if ((0, import_utils.isDef)(denominations)) {
        return denominations;
      }
      return 0;
    };
    function formatValue({
      coupons,
      chosenCoupon,
      currency
    }) {
      let value = 0;
      let isExist = false;
      (Array.isArray(chosenCoupon) ? chosenCoupon : [chosenCoupon]).forEach((i) => {
        const coupon = coupons[+i];
        if (coupon) {
          isExist = true;
          value += getValue(coupon);
        }
      });
      if (isExist) {
        return `-${currency} ${(value / 100).toFixed(2)}`;
      }
      return coupons.length === 0 ? t("noCoupon") : t("count", coupons.length);
    }
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: couponCellProps,
      setup(props) {
        return () => {
          const selected = Array.isArray(props.chosenCoupon) ? props.chosenCoupon.length : props.coupons[+props.chosenCoupon];
          return (0, import_vue.createVNode)(import_cell.Cell, {
            "class": bem(),
            "value": formatValue(props),
            "title": props.title || t("title"),
            "border": props.border,
            "isLink": props.editable,
            "valueClass": bem("value", {
              selected
            })
          }, null);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon-cell/index.js
var require_coupon_cell = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon-cell/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      CouponCell: () => CouponCell,
      couponCellProps: () => import_CouponCell2.couponCellProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_CouponCell = __toESM(require_CouponCell());
    var import_CouponCell2 = require_CouponCell();
    var CouponCell = (0, import_utils.withInstall)(import_CouponCell.default);
    var stdin_default = CouponCell;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/empty/Empty.js
var require_Empty = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/empty/Empty.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      emptyProps: () => emptyProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use_id = require_use_id();
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("empty");
    var emptyProps = {
      image: (0, import_utils.makeStringProp)("default"),
      imageSize: [Number, String, Array],
      description: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: emptyProps,
      setup(props, {
        slots
      }) {
        const renderDescription = () => {
          const description = slots.description ? slots.description() : props.description;
          if (description) {
            return (0, import_vue.createVNode)("p", {
              "class": bem("description")
            }, [description]);
          }
        };
        const renderBottom = () => {
          if (slots.default) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("bottom")
            }, [slots.default()]);
          }
        };
        const baseId = (0, import_use_id.useId)();
        const getId = (num) => `${baseId}-${num}`;
        const getUrlById = (num) => `url(#${getId(num)})`;
        const renderStop = (color, offset, opacity) => (0, import_vue.createVNode)("stop", {
          "stop-color": color,
          "offset": `${offset}%`,
          "stop-opacity": opacity
        }, null);
        const renderStops = (fromColor, toColor) => [renderStop(fromColor, 0), renderStop(toColor, 100)];
        const renderShadow = (id) => [(0, import_vue.createVNode)("defs", null, [(0, import_vue.createVNode)("radialGradient", {
          "id": getId(id),
          "cx": "50%",
          "cy": "54%",
          "fx": "50%",
          "fy": "54%",
          "r": "297%",
          "gradientTransform": "matrix(-.16 0 0 -.33 .58 .72)",
          "data-allow-mismatch": "attribute"
        }, [renderStop("#EBEDF0", 0), renderStop("#F2F3F5", 100, 0.3)])]), (0, import_vue.createVNode)("ellipse", {
          "fill": getUrlById(id),
          "opacity": ".8",
          "cx": "80",
          "cy": "140",
          "rx": "46",
          "ry": "8",
          "data-allow-mismatch": "attribute"
        }, null)];
        const renderBuilding = () => [(0, import_vue.createVNode)("defs", null, [(0, import_vue.createVNode)("linearGradient", {
          "id": getId("a"),
          "x1": "64%",
          "y1": "100%",
          "x2": "64%",
          "data-allow-mismatch": "attribute"
        }, [renderStop("#FFF", 0, 0.5), renderStop("#F2F3F5", 100)])]), (0, import_vue.createVNode)("g", {
          "opacity": ".8",
          "data-allow-mismatch": "children"
        }, [(0, import_vue.createVNode)("path", {
          "d": "M36 131V53H16v20H2v58h34z",
          "fill": getUrlById("a")
        }, null), (0, import_vue.createVNode)("path", {
          "d": "M123 15h22v14h9v77h-31V15z",
          "fill": getUrlById("a")
        }, null)])];
        const renderCloud = () => [(0, import_vue.createVNode)("defs", null, [(0, import_vue.createVNode)("linearGradient", {
          "id": getId("b"),
          "x1": "64%",
          "y1": "97%",
          "x2": "64%",
          "y2": "0%",
          "data-allow-mismatch": "attribute"
        }, [renderStop("#F2F3F5", 0, 0.3), renderStop("#F2F3F5", 100)])]), (0, import_vue.createVNode)("g", {
          "opacity": ".8",
          "data-allow-mismatch": "children"
        }, [(0, import_vue.createVNode)("path", {
          "d": "M87 6c3 0 7 3 8 6a8 8 0 1 1-1 16H80a7 7 0 0 1-8-6c0-4 3-7 6-7 0-5 4-9 9-9Z",
          "fill": getUrlById("b")
        }, null), (0, import_vue.createVNode)("path", {
          "d": "M19 23c2 0 3 1 4 3 2 0 4 2 4 4a4 4 0 0 1-4 3v1h-7v-1l-1 1c-2 0-3-2-3-4 0-1 1-3 3-3 0-2 2-4 4-4Z",
          "fill": getUrlById("b")
        }, null)])];
        const renderNetwork = () => (0, import_vue.createVNode)("svg", {
          "viewBox": "0 0 160 160"
        }, [(0, import_vue.createVNode)("defs", {
          "data-allow-mismatch": "children"
        }, [(0, import_vue.createVNode)("linearGradient", {
          "id": getId(1),
          "x1": "64%",
          "y1": "100%",
          "x2": "64%"
        }, [renderStop("#FFF", 0, 0.5), renderStop("#F2F3F5", 100)]), (0, import_vue.createVNode)("linearGradient", {
          "id": getId(2),
          "x1": "50%",
          "x2": "50%",
          "y2": "84%"
        }, [renderStop("#EBEDF0", 0), renderStop("#DCDEE0", 100, 0)]), (0, import_vue.createVNode)("linearGradient", {
          "id": getId(3),
          "x1": "100%",
          "x2": "100%",
          "y2": "100%"
        }, [renderStops("#EAEDF0", "#DCDEE0")]), (0, import_vue.createVNode)("radialGradient", {
          "id": getId(4),
          "cx": "50%",
          "cy": "0%",
          "fx": "50%",
          "fy": "0%",
          "r": "100%",
          "gradientTransform": "matrix(0 1 -.54 0 .5 -.5)"
        }, [renderStop("#EBEDF0", 0), renderStop("#FFF", 100, 0)])]), (0, import_vue.createVNode)("g", {
          "fill": "none"
        }, [renderBuilding(), (0, import_vue.createVNode)("path", {
          "fill": getUrlById(4),
          "d": "M0 139h160v21H0z",
          "data-allow-mismatch": "attribute"
        }, null), (0, import_vue.createVNode)("path", {
          "d": "M80 54a7 7 0 0 1 3 13v27l-2 2h-2a2 2 0 0 1-2-2V67a7 7 0 0 1 3-13z",
          "fill": getUrlById(2),
          "data-allow-mismatch": "attribute"
        }, null), (0, import_vue.createVNode)("g", {
          "opacity": ".6",
          "stroke-linecap": "round",
          "stroke-width": "7",
          "data-allow-mismatch": "children"
        }, [(0, import_vue.createVNode)("path", {
          "d": "M64 47a19 19 0 0 0-5 13c0 5 2 10 5 13",
          "stroke": getUrlById(3)
        }, null), (0, import_vue.createVNode)("path", {
          "d": "M53 36a34 34 0 0 0 0 48",
          "stroke": getUrlById(3)
        }, null), (0, import_vue.createVNode)("path", {
          "d": "M95 73a19 19 0 0 0 6-13c0-5-2-9-6-13",
          "stroke": getUrlById(3)
        }, null), (0, import_vue.createVNode)("path", {
          "d": "M106 84a34 34 0 0 0 0-48",
          "stroke": getUrlById(3)
        }, null)]), (0, import_vue.createVNode)("g", {
          "transform": "translate(31 105)"
        }, [(0, import_vue.createVNode)("rect", {
          "fill": "#EBEDF0",
          "width": "98",
          "height": "34",
          "rx": "2"
        }, null), (0, import_vue.createVNode)("rect", {
          "fill": "#FFF",
          "x": "9",
          "y": "8",
          "width": "80",
          "height": "18",
          "rx": "1.1"
        }, null), (0, import_vue.createVNode)("rect", {
          "fill": "#EBEDF0",
          "x": "15",
          "y": "12",
          "width": "18",
          "height": "6",
          "rx": "1.1"
        }, null)])])]);
        const renderMaterial = () => (0, import_vue.createVNode)("svg", {
          "viewBox": "0 0 160 160"
        }, [(0, import_vue.createVNode)("defs", {
          "data-allow-mismatch": "children"
        }, [(0, import_vue.createVNode)("linearGradient", {
          "x1": "50%",
          "x2": "50%",
          "y2": "100%",
          "id": getId(5)
        }, [renderStops("#F2F3F5", "#DCDEE0")]), (0, import_vue.createVNode)("linearGradient", {
          "x1": "95%",
          "y1": "48%",
          "x2": "5.5%",
          "y2": "51%",
          "id": getId(6)
        }, [renderStops("#EAEDF1", "#DCDEE0")]), (0, import_vue.createVNode)("linearGradient", {
          "y1": "45%",
          "x2": "100%",
          "y2": "54%",
          "id": getId(7)
        }, [renderStops("#EAEDF1", "#DCDEE0")])]), renderBuilding(), renderCloud(), (0, import_vue.createVNode)("g", {
          "transform": "translate(36 50)",
          "fill": "none"
        }, [(0, import_vue.createVNode)("g", {
          "transform": "translate(8)"
        }, [(0, import_vue.createVNode)("rect", {
          "fill": "#EBEDF0",
          "opacity": ".6",
          "x": "38",
          "y": "13",
          "width": "36",
          "height": "53",
          "rx": "2"
        }, null), (0, import_vue.createVNode)("rect", {
          "fill": getUrlById(5),
          "width": "64",
          "height": "66",
          "rx": "2",
          "data-allow-mismatch": "attribute"
        }, null), (0, import_vue.createVNode)("rect", {
          "fill": "#FFF",
          "x": "6",
          "y": "6",
          "width": "52",
          "height": "55",
          "rx": "1"
        }, null), (0, import_vue.createVNode)("g", {
          "transform": "translate(15 17)",
          "fill": getUrlById(6),
          "data-allow-mismatch": "attribute"
        }, [(0, import_vue.createVNode)("rect", {
          "width": "34",
          "height": "6",
          "rx": "1"
        }, null), (0, import_vue.createVNode)("path", {
          "d": "M0 14h34v6H0z"
        }, null), (0, import_vue.createVNode)("rect", {
          "y": "28",
          "width": "34",
          "height": "6",
          "rx": "1"
        }, null)])]), (0, import_vue.createVNode)("rect", {
          "fill": getUrlById(7),
          "y": "61",
          "width": "88",
          "height": "28",
          "rx": "1",
          "data-allow-mismatch": "attribute"
        }, null), (0, import_vue.createVNode)("rect", {
          "fill": "#F7F8FA",
          "x": "29",
          "y": "72",
          "width": "30",
          "height": "6",
          "rx": "1"
        }, null)])]);
        const renderError = () => (0, import_vue.createVNode)("svg", {
          "viewBox": "0 0 160 160"
        }, [(0, import_vue.createVNode)("defs", null, [(0, import_vue.createVNode)("linearGradient", {
          "x1": "50%",
          "x2": "50%",
          "y2": "100%",
          "id": getId(8),
          "data-allow-mismatch": "attribute"
        }, [renderStops("#EAEDF1", "#DCDEE0")])]), renderBuilding(), renderCloud(), renderShadow("c"), (0, import_vue.createVNode)("path", {
          "d": "m59 60 21 21 21-21h3l9 9v3L92 93l21 21v3l-9 9h-3l-21-21-21 21h-3l-9-9v-3l21-21-21-21v-3l9-9h3Z",
          "fill": getUrlById(8),
          "data-allow-mismatch": "attribute"
        }, null)]);
        const renderSearch = () => (0, import_vue.createVNode)("svg", {
          "viewBox": "0 0 160 160"
        }, [(0, import_vue.createVNode)("defs", {
          "data-allow-mismatch": "children"
        }, [(0, import_vue.createVNode)("linearGradient", {
          "x1": "50%",
          "y1": "100%",
          "x2": "50%",
          "id": getId(9)
        }, [renderStops("#EEE", "#D8D8D8")]), (0, import_vue.createVNode)("linearGradient", {
          "x1": "100%",
          "y1": "50%",
          "y2": "50%",
          "id": getId(10)
        }, [renderStops("#F2F3F5", "#DCDEE0")]), (0, import_vue.createVNode)("linearGradient", {
          "x1": "50%",
          "x2": "50%",
          "y2": "100%",
          "id": getId(11)
        }, [renderStops("#F2F3F5", "#DCDEE0")]), (0, import_vue.createVNode)("linearGradient", {
          "x1": "50%",
          "x2": "50%",
          "y2": "100%",
          "id": getId(12)
        }, [renderStops("#FFF", "#F7F8FA")])]), renderBuilding(), renderCloud(), renderShadow("d"), (0, import_vue.createVNode)("g", {
          "transform": "rotate(-45 113 -4)",
          "fill": "none",
          "data-allow-mismatch": "children"
        }, [(0, import_vue.createVNode)("rect", {
          "fill": getUrlById(9),
          "x": "24",
          "y": "52.8",
          "width": "5.8",
          "height": "19",
          "rx": "1"
        }, null), (0, import_vue.createVNode)("rect", {
          "fill": getUrlById(10),
          "x": "22.1",
          "y": "67.3",
          "width": "9.9",
          "height": "28",
          "rx": "1"
        }, null), (0, import_vue.createVNode)("circle", {
          "stroke": getUrlById(11),
          "stroke-width": "8",
          "cx": "27",
          "cy": "27",
          "r": "27"
        }, null), (0, import_vue.createVNode)("circle", {
          "fill": getUrlById(12),
          "cx": "27",
          "cy": "27",
          "r": "16"
        }, null), (0, import_vue.createVNode)("path", {
          "d": "M37 7c-8 0-15 5-16 12",
          "stroke": getUrlById(11),
          "stroke-width": "3",
          "opacity": ".5",
          "stroke-linecap": "round",
          "transform": "rotate(45 29 13)"
        }, null)])]);
        const renderImage = () => {
          var _a;
          if (slots.image) {
            return slots.image();
          }
          const PRESET_IMAGES = {
            error: renderError,
            search: renderSearch,
            network: renderNetwork,
            default: renderMaterial
          };
          return ((_a = PRESET_IMAGES[props.image]) == null ? void 0 : _a.call(PRESET_IMAGES)) || (0, import_vue.createVNode)("img", {
            "src": props.image
          }, null);
        };
        return () => (0, import_vue.createVNode)("div", {
          "class": bem()
        }, [(0, import_vue.createVNode)("div", {
          "class": bem("image"),
          "style": (0, import_utils.getSizeStyle)(props.imageSize)
        }, [renderImage()]), renderDescription(), renderBottom()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/empty/index.js
var require_empty = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/empty/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Empty: () => Empty,
      default: () => stdin_default,
      emptyProps: () => import_Empty2.emptyProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Empty = __toESM(require_Empty());
    var import_Empty2 = require_Empty();
    var Empty = (0, import_utils.withInstall)(import_Empty.default);
    var stdin_default = Empty;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon-list/CouponList.js
var require_CouponList = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon-list/CouponList.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      couponListProps: () => couponListProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_refs = require_use_refs();
    var import_tab = require_tab();
    var import_tabs = require_tabs();
    var import_empty = require_empty();
    var import_field = require_field();
    var import_button = require_button();
    var import_coupon = require_coupon();
    var import_use = require_index_cjs();
    var [name, bem, t] = (0, import_utils.createNamespace)("coupon-list");
    var couponListProps = {
      code: (0, import_utils.makeStringProp)(""),
      coupons: (0, import_utils.makeArrayProp)(),
      currency: (0, import_utils.makeStringProp)("¥"),
      showCount: import_utils.truthProp,
      emptyImage: String,
      enabledTitle: String,
      disabledTitle: String,
      disabledCoupons: (0, import_utils.makeArrayProp)(),
      showExchangeBar: import_utils.truthProp,
      showCloseButton: import_utils.truthProp,
      closeButtonText: String,
      inputPlaceholder: String,
      exchangeMinLength: (0, import_utils.makeNumberProp)(1),
      exchangeButtonText: String,
      displayedCouponIndex: (0, import_utils.makeNumberProp)(-1),
      exchangeButtonLoading: Boolean,
      exchangeButtonDisabled: Boolean,
      chosenCoupon: {
        type: [Number, Array],
        default: -1
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: couponListProps,
      emits: ["change", "exchange", "update:code"],
      setup(props, {
        emit,
        slots
      }) {
        const [couponRefs, setCouponRefs] = (0, import_use_refs.useRefs)();
        const root = (0, import_vue.ref)();
        const barRef = (0, import_vue.ref)();
        const activeTab = (0, import_vue.ref)(0);
        const listHeight = (0, import_vue.ref)(0);
        const currentCode = (0, import_vue.ref)(props.code);
        const buttonDisabled = (0, import_vue.computed)(() => !props.exchangeButtonLoading && (props.exchangeButtonDisabled || !currentCode.value || currentCode.value.length < props.exchangeMinLength));
        const updateListHeight = () => {
          const TABS_HEIGHT = 44;
          const rootHeight = (0, import_use.useRect)(root).height;
          const headerHeight = (0, import_use.useRect)(barRef).height + TABS_HEIGHT;
          listHeight.value = (rootHeight > headerHeight ? rootHeight : import_utils.windowHeight.value) - headerHeight;
        };
        const onExchange = () => {
          emit("exchange", currentCode.value);
          if (!props.code) {
            currentCode.value = "";
          }
        };
        const scrollToCoupon = (index) => {
          (0, import_vue.nextTick)(() => {
            var _a;
            return (_a = couponRefs.value[index]) == null ? void 0 : _a.scrollIntoView();
          });
        };
        const renderEmpty = () => (0, import_vue.createVNode)(import_empty.Empty, {
          "image": props.emptyImage
        }, {
          default: () => [(0, import_vue.createVNode)("p", {
            "class": bem("empty-tip")
          }, [t("noCoupon")])]
        });
        const renderExchangeBar = () => {
          if (props.showExchangeBar) {
            return (0, import_vue.createVNode)("div", {
              "ref": barRef,
              "class": bem("exchange-bar")
            }, [(0, import_vue.createVNode)(import_field.Field, {
              "modelValue": currentCode.value,
              "onUpdate:modelValue": ($event) => currentCode.value = $event,
              "clearable": true,
              "border": false,
              "class": bem("field"),
              "placeholder": props.inputPlaceholder || t("placeholder"),
              "maxlength": "20"
            }, null), (0, import_vue.createVNode)(import_button.Button, {
              "plain": true,
              "type": "primary",
              "class": bem("exchange"),
              "text": props.exchangeButtonText || t("exchange"),
              "loading": props.exchangeButtonLoading,
              "disabled": buttonDisabled.value,
              "onClick": onExchange
            }, null)]);
          }
        };
        const renderCouponTab = () => {
          const {
            coupons,
            chosenCoupon
          } = props;
          const count = props.showCount ? ` (${coupons.length})` : "";
          const title = (props.enabledTitle || t("enable")) + count;
          const updateChosenCoupon = (currentValues = [], value = 0) => {
            if (currentValues.includes(value)) {
              return currentValues.filter((item) => item !== value);
            }
            return [...currentValues, value];
          };
          return (0, import_vue.createVNode)(import_tab.Tab, {
            "title": title
          }, {
            default: () => {
              var _a;
              return [(0, import_vue.createVNode)("div", {
                "class": bem("list", {
                  "with-bottom": props.showCloseButton
                }),
                "style": {
                  height: `${listHeight.value}px`
                }
              }, [coupons.map((coupon, index) => (0, import_vue.createVNode)(import_coupon.Coupon, {
                "key": coupon.id,
                "ref": setCouponRefs(index),
                "coupon": coupon,
                "chosen": Array.isArray(chosenCoupon) ? chosenCoupon.includes(index) : index === chosenCoupon,
                "currency": props.currency,
                "onClick": () => emit("change", Array.isArray(chosenCoupon) ? updateChosenCoupon(chosenCoupon, index) : index)
              }, null)), !coupons.length && renderEmpty(), (_a = slots["list-footer"]) == null ? void 0 : _a.call(slots)])];
            }
          });
        };
        const renderDisabledTab = () => {
          const {
            disabledCoupons
          } = props;
          const count = props.showCount ? ` (${disabledCoupons.length})` : "";
          const title = (props.disabledTitle || t("disabled")) + count;
          return (0, import_vue.createVNode)(import_tab.Tab, {
            "title": title
          }, {
            default: () => {
              var _a;
              return [(0, import_vue.createVNode)("div", {
                "class": bem("list", {
                  "with-bottom": props.showCloseButton
                }),
                "style": {
                  height: `${listHeight.value}px`
                }
              }, [disabledCoupons.map((coupon) => (0, import_vue.createVNode)(import_coupon.Coupon, {
                "disabled": true,
                "key": coupon.id,
                "coupon": coupon,
                "currency": props.currency
              }, null)), !disabledCoupons.length && renderEmpty(), (_a = slots["disabled-list-footer"]) == null ? void 0 : _a.call(slots)])];
            }
          });
        };
        (0, import_vue.watch)(() => props.code, (value) => {
          currentCode.value = value;
        });
        (0, import_vue.watch)(import_utils.windowHeight, updateListHeight);
        (0, import_vue.watch)(currentCode, (value) => emit("update:code", value));
        (0, import_vue.watch)(() => props.displayedCouponIndex, scrollToCoupon);
        (0, import_vue.onMounted)(() => {
          updateListHeight();
          scrollToCoupon(props.displayedCouponIndex);
        });
        return () => (0, import_vue.createVNode)("div", {
          "ref": root,
          "class": bem()
        }, [renderExchangeBar(), (0, import_vue.createVNode)(import_tabs.Tabs, {
          "active": activeTab.value,
          "onUpdate:active": ($event) => activeTab.value = $event,
          "class": bem("tab")
        }, {
          default: () => [renderCouponTab(), renderDisabledTab()]
        }), (0, import_vue.createVNode)("div", {
          "class": bem("bottom")
        }, [slots["list-button"] ? slots["list-button"]() : (0, import_vue.withDirectives)((0, import_vue.createVNode)(import_button.Button, {
          "round": true,
          "block": true,
          "type": "primary",
          "class": bem("close"),
          "text": props.closeButtonText || t("close"),
          "onClick": () => emit("change", Array.isArray(props.chosenCoupon) ? [] : -1)
        }, null), [[import_vue.vShow, props.showCloseButton]])])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon-list/index.js
var require_coupon_list = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/coupon-list/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      CouponList: () => CouponList,
      couponListProps: () => import_CouponList2.couponListProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_CouponList = __toESM(require_CouponList());
    var import_CouponList2 = require_CouponList();
    var CouponList = (0, import_utils.withInstall)(import_CouponList.default);
    var stdin_default = CouponList;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/date-picker/DatePicker.js
var require_DatePicker = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/date-picker/DatePicker.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      datePickerProps: () => datePickerProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils7();
    var import_use_expose = require_use_expose();
    var import_picker = require_picker();
    var currentYear = (/* @__PURE__ */ new Date()).getFullYear();
    var [name] = (0, import_utils.createNamespace)("date-picker");
    var datePickerProps = (0, import_utils.extend)({}, import_utils2.sharedProps, {
      columnsType: {
        type: Array,
        default: () => ["year", "month", "day"]
      },
      minDate: {
        type: Date,
        default: () => new Date(currentYear - 10, 0, 1),
        validator: import_utils.isDate
      },
      maxDate: {
        type: Date,
        default: () => new Date(currentYear + 10, 11, 31),
        validator: import_utils.isDate
      }
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: datePickerProps,
      emits: ["confirm", "cancel", "change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const currentValues = (0, import_vue.ref)(props.modelValue);
        const updatedByExternalSources = (0, import_vue.ref)(false);
        const pickerRef = (0, import_vue.ref)();
        const computedValues = (0, import_vue.computed)(() => updatedByExternalSources.value ? props.modelValue : currentValues.value);
        const isMinYear = (year) => year === props.minDate.getFullYear();
        const isMaxYear = (year) => year === props.maxDate.getFullYear();
        const isMinMonth = (month) => month === props.minDate.getMonth() + 1;
        const isMaxMonth = (month) => month === props.maxDate.getMonth() + 1;
        const getValue = (type) => {
          const {
            minDate,
            columnsType
          } = props;
          const index = columnsType.indexOf(type);
          const value = computedValues.value[index];
          if (value) {
            return +value;
          }
          switch (type) {
            case "year":
              return minDate.getFullYear();
            case "month":
              return minDate.getMonth() + 1;
            case "day":
              return minDate.getDate();
          }
        };
        const genYearOptions = () => {
          const minYear = props.minDate.getFullYear();
          const maxYear = props.maxDate.getFullYear();
          return (0, import_utils2.genOptions)(minYear, maxYear, "year", props.formatter, props.filter, computedValues.value);
        };
        const genMonthOptions = () => {
          const year = getValue("year");
          const minMonth = isMinYear(year) ? props.minDate.getMonth() + 1 : 1;
          const maxMonth = isMaxYear(year) ? props.maxDate.getMonth() + 1 : 12;
          return (0, import_utils2.genOptions)(minMonth, maxMonth, "month", props.formatter, props.filter, computedValues.value);
        };
        const genDayOptions = () => {
          const year = getValue("year");
          const month = getValue("month");
          const minDate = isMinYear(year) && isMinMonth(month) ? props.minDate.getDate() : 1;
          const maxDate = isMaxYear(year) && isMaxMonth(month) ? props.maxDate.getDate() : (0, import_utils2.getMonthEndDay)(year, month);
          return (0, import_utils2.genOptions)(minDate, maxDate, "day", props.formatter, props.filter, computedValues.value);
        };
        const confirm = () => {
          var _a;
          return (_a = pickerRef.value) == null ? void 0 : _a.confirm();
        };
        const getSelectedDate = () => currentValues.value;
        const columns = (0, import_vue.computed)(() => props.columnsType.map((type) => {
          switch (type) {
            case "year":
              return genYearOptions();
            case "month":
              return genMonthOptions();
            case "day":
              return genDayOptions();
            default:
              if (true) {
                throw new Error(`[Vant] DatePicker: unsupported columns type: ${type}`);
              }
              return [];
          }
        }));
        (0, import_vue.watch)(currentValues, (newValues) => {
          if (!(0, import_utils.isSameValue)(newValues, props.modelValue)) {
            emit("update:modelValue", newValues);
          }
        });
        (0, import_vue.watch)(() => props.modelValue, (newValues, oldValues) => {
          updatedByExternalSources.value = (0, import_utils.isSameValue)(oldValues, currentValues.value);
          newValues = (0, import_utils2.formatValueRange)(newValues, columns.value);
          if (!(0, import_utils.isSameValue)(newValues, currentValues.value)) {
            currentValues.value = newValues;
          }
          updatedByExternalSources.value = false;
        }, {
          immediate: true
        });
        const onChange = (...args) => emit("change", ...args);
        const onCancel = (...args) => emit("cancel", ...args);
        const onConfirm = (...args) => emit("confirm", ...args);
        (0, import_use_expose.useExpose)({
          confirm,
          getSelectedDate
        });
        return () => (0, import_vue.createVNode)(import_picker.Picker, (0, import_vue.mergeProps)({
          "ref": pickerRef,
          "modelValue": currentValues.value,
          "onUpdate:modelValue": ($event) => currentValues.value = $event,
          "columns": columns.value,
          "onChange": onChange,
          "onCancel": onCancel,
          "onConfirm": onConfirm
        }, (0, import_utils.pick)(props, import_utils2.pickerInheritKeys)), slots);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/date-picker/index.js
var require_date_picker = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/date-picker/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      DatePicker: () => DatePicker,
      datePickerProps: () => import_DatePicker2.datePickerProps,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_DatePicker = __toESM(require_DatePicker());
    var import_DatePicker2 = require_DatePicker();
    var DatePicker = (0, import_utils.withInstall)(import_DatePicker.default);
    var stdin_default = DatePicker;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dialog/Dialog.js
var require_Dialog = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dialog/Dialog.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      dialogProps: () => dialogProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_shared = require_shared();
    var import_popup = require_popup();
    var import_button = require_button();
    var import_action_bar = require_action_bar();
    var import_action_bar_button = require_action_bar_button();
    var [name, bem, t] = (0, import_utils.createNamespace)("dialog");
    var dialogProps = (0, import_utils.extend)({}, import_shared.popupSharedProps, {
      title: String,
      theme: String,
      width: import_utils.numericProp,
      message: [String, Function],
      callback: Function,
      allowHtml: Boolean,
      className: import_utils.unknownProp,
      transition: (0, import_utils.makeStringProp)("van-dialog-bounce"),
      messageAlign: String,
      closeOnPopstate: import_utils.truthProp,
      showCancelButton: Boolean,
      cancelButtonText: String,
      cancelButtonColor: String,
      cancelButtonDisabled: Boolean,
      confirmButtonText: String,
      confirmButtonColor: String,
      confirmButtonDisabled: Boolean,
      showConfirmButton: import_utils.truthProp,
      closeOnClickOverlay: Boolean,
      keyboardEnabled: import_utils.truthProp,
      destroyOnClose: Boolean
    });
    var popupInheritKeys = [...import_shared.popupSharedPropKeys, "transition", "closeOnPopstate", "destroyOnClose"];
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: dialogProps,
      emits: ["confirm", "cancel", "keydown", "update:show"],
      setup(props, {
        emit,
        slots
      }) {
        const root = (0, import_vue.ref)();
        const loading = (0, import_vue.reactive)({
          confirm: false,
          cancel: false
        });
        const updateShow = (value) => emit("update:show", value);
        const close = (action) => {
          var _a;
          updateShow(false);
          (_a = props.callback) == null ? void 0 : _a.call(props, action);
        };
        const getActionHandler = (action) => () => {
          if (!props.show) {
            return;
          }
          emit(action);
          if (props.beforeClose) {
            loading[action] = true;
            (0, import_utils.callInterceptor)(props.beforeClose, {
              args: [action],
              done() {
                close(action);
                loading[action] = false;
              },
              canceled() {
                loading[action] = false;
              }
            });
          } else {
            close(action);
          }
        };
        const onCancel = getActionHandler("cancel");
        const onConfirm = getActionHandler("confirm");
        const onKeydown = (0, import_vue.withKeys)((event) => {
          var _a, _b;
          if (!props.keyboardEnabled) {
            return;
          }
          if (event.target !== ((_b = (_a = root.value) == null ? void 0 : _a.popupRef) == null ? void 0 : _b.value)) {
            return;
          }
          const onEventType = {
            Enter: props.showConfirmButton ? onConfirm : import_utils.noop,
            Escape: props.showCancelButton ? onCancel : import_utils.noop
          };
          onEventType[event.key]();
          emit("keydown", event);
        }, ["enter", "esc"]);
        const renderTitle = () => {
          const title = slots.title ? slots.title() : props.title;
          if (title) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("header", {
                isolated: !props.message && !slots.default
              })
            }, [title]);
          }
        };
        const renderMessage = (hasTitle) => {
          const {
            message,
            allowHtml,
            messageAlign
          } = props;
          const classNames = bem("message", {
            "has-title": hasTitle,
            [messageAlign]: messageAlign
          });
          const content = (0, import_utils.isFunction)(message) ? message() : message;
          if (allowHtml && typeof content === "string") {
            return (0, import_vue.createVNode)("div", {
              "class": classNames,
              "innerHTML": content
            }, null);
          }
          return (0, import_vue.createVNode)("div", {
            "class": classNames
          }, [content]);
        };
        const renderContent = () => {
          if (slots.default) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("content")
            }, [slots.default()]);
          }
          const {
            title,
            message,
            allowHtml
          } = props;
          if (message) {
            const hasTitle = !!(title || slots.title);
            return (0, import_vue.createVNode)("div", {
              "key": allowHtml ? 1 : 0,
              "class": bem("content", {
                isolated: !hasTitle
              })
            }, [renderMessage(hasTitle)]);
          }
        };
        const renderButtons = () => (0, import_vue.createVNode)("div", {
          "class": [import_utils.BORDER_TOP, bem("footer")]
        }, [props.showCancelButton && (0, import_vue.createVNode)(import_button.Button, {
          "size": "large",
          "text": props.cancelButtonText || t("cancel"),
          "class": bem("cancel"),
          "style": {
            color: props.cancelButtonColor
          },
          "loading": loading.cancel,
          "disabled": props.cancelButtonDisabled,
          "onClick": onCancel
        }, null), props.showConfirmButton && (0, import_vue.createVNode)(import_button.Button, {
          "size": "large",
          "text": props.confirmButtonText || t("confirm"),
          "class": [bem("confirm"), {
            [import_utils.BORDER_LEFT]: props.showCancelButton
          }],
          "style": {
            color: props.confirmButtonColor
          },
          "loading": loading.confirm,
          "disabled": props.confirmButtonDisabled,
          "onClick": onConfirm
        }, null)]);
        const renderRoundButtons = () => (0, import_vue.createVNode)(import_action_bar.ActionBar, {
          "class": bem("footer")
        }, {
          default: () => [props.showCancelButton && (0, import_vue.createVNode)(import_action_bar_button.ActionBarButton, {
            "type": "warning",
            "text": props.cancelButtonText || t("cancel"),
            "class": bem("cancel"),
            "color": props.cancelButtonColor,
            "loading": loading.cancel,
            "disabled": props.cancelButtonDisabled,
            "onClick": onCancel
          }, null), props.showConfirmButton && (0, import_vue.createVNode)(import_action_bar_button.ActionBarButton, {
            "type": "danger",
            "text": props.confirmButtonText || t("confirm"),
            "class": bem("confirm"),
            "color": props.confirmButtonColor,
            "loading": loading.confirm,
            "disabled": props.confirmButtonDisabled,
            "onClick": onConfirm
          }, null)]
        });
        const renderFooter = () => {
          if (slots.footer) {
            return slots.footer();
          }
          return props.theme === "round-button" ? renderRoundButtons() : renderButtons();
        };
        return () => {
          const {
            width,
            title,
            theme,
            message,
            className
          } = props;
          return (0, import_vue.createVNode)(import_popup.Popup, (0, import_vue.mergeProps)({
            "ref": root,
            "role": "dialog",
            "class": [bem([theme]), className],
            "style": {
              width: (0, import_utils.addUnit)(width)
            },
            "tabindex": 0,
            "aria-labelledby": title || message,
            "onKeydown": onKeydown,
            "onUpdate:show": updateShow
          }, (0, import_utils.pick)(props, popupInheritKeys)), {
            default: () => [renderTitle(), renderContent(), renderFooter()]
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dialog/function-call.js
var require_function_call2 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dialog/function-call.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      closeDialog: () => closeDialog,
      resetDialogDefaultOptions: () => resetDialogDefaultOptions,
      setDialogDefaultOptions: () => setDialogDefaultOptions,
      showConfirmDialog: () => showConfirmDialog,
      showDialog: () => showDialog
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_mount_component = require_mount_component();
    var import_Dialog = __toESM(require_Dialog());
    var instance;
    var DEFAULT_OPTIONS = {
      title: "",
      width: "",
      theme: null,
      message: "",
      overlay: true,
      callback: null,
      teleport: "body",
      className: "",
      allowHtml: false,
      lockScroll: true,
      transition: void 0,
      beforeClose: null,
      overlayClass: "",
      overlayStyle: void 0,
      messageAlign: "",
      cancelButtonText: "",
      cancelButtonColor: null,
      cancelButtonDisabled: false,
      confirmButtonText: "",
      confirmButtonColor: null,
      confirmButtonDisabled: false,
      showConfirmButton: true,
      showCancelButton: false,
      closeOnPopstate: true,
      closeOnClickOverlay: false,
      destroyOnClose: false
    };
    var currentOptions = (0, import_utils.extend)({}, DEFAULT_OPTIONS);
    function initInstance() {
      const Wrapper = {
        setup() {
          const {
            state,
            toggle
          } = (0, import_mount_component.usePopupState)();
          return () => (0, import_vue.createVNode)(import_Dialog.default, (0, import_vue.mergeProps)(state, {
            "onUpdate:show": toggle
          }), null);
        }
      };
      ({
        instance
      } = (0, import_mount_component.mountComponent)(Wrapper));
    }
    function showDialog(options) {
      if (!import_utils.inBrowser) {
        return Promise.resolve(void 0);
      }
      return new Promise((resolve, reject) => {
        if (!instance) {
          initInstance();
        }
        instance.open((0, import_utils.extend)({}, currentOptions, options, {
          callback: (action) => {
            (action === "confirm" ? resolve : reject)(action);
          }
        }));
      });
    }
    var setDialogDefaultOptions = (options) => {
      (0, import_utils.extend)(currentOptions, options);
    };
    var resetDialogDefaultOptions = () => {
      currentOptions = (0, import_utils.extend)({}, DEFAULT_OPTIONS);
    };
    var showConfirmDialog = (options) => showDialog((0, import_utils.extend)({
      showCancelButton: true
    }, options));
    var closeDialog = () => {
      if (instance) {
        instance.toggle(false);
      }
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dialog/index.js
var require_dialog = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dialog/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Dialog: () => Dialog,
      closeDialog: () => import_function_call.closeDialog,
      default: () => stdin_default,
      dialogProps: () => import_Dialog2.dialogProps,
      resetDialogDefaultOptions: () => import_function_call.resetDialogDefaultOptions,
      setDialogDefaultOptions: () => import_function_call.setDialogDefaultOptions,
      showConfirmDialog: () => import_function_call.showConfirmDialog,
      showDialog: () => import_function_call.showDialog
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Dialog = __toESM(require_Dialog());
    var import_Dialog2 = require_Dialog();
    var import_function_call = require_function_call2();
    var Dialog = (0, import_utils.withInstall)(import_Dialog.default);
    var stdin_default = Dialog;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dropdown-menu/DropdownMenu.js
var require_DropdownMenu = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dropdown-menu/DropdownMenu.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      DROPDOWN_KEY: () => DROPDOWN_KEY,
      default: () => stdin_default,
      dropdownMenuProps: () => dropdownMenuProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_id = require_use_id();
    var import_use_expose = require_use_expose();
    var import_use = require_index_cjs();
    var [name, bem] = (0, import_utils.createNamespace)("dropdown-menu");
    var dropdownMenuProps = {
      overlay: import_utils.truthProp,
      zIndex: import_utils.numericProp,
      duration: (0, import_utils.makeNumericProp)(0.2),
      direction: (0, import_utils.makeStringProp)("down"),
      activeColor: String,
      autoLocate: Boolean,
      closeOnClickOutside: import_utils.truthProp,
      closeOnClickOverlay: import_utils.truthProp,
      swipeThreshold: import_utils.numericProp
    };
    var DROPDOWN_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: dropdownMenuProps,
      setup(props, {
        slots
      }) {
        const id = (0, import_use_id.useId)();
        const root = (0, import_vue.ref)();
        const barRef = (0, import_vue.ref)();
        const offset = (0, import_vue.ref)(0);
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(DROPDOWN_KEY);
        const scrollParent = (0, import_use.useScrollParent)(root);
        const opened = (0, import_vue.computed)(() => children.some((item) => item.state.showWrapper));
        const scrollable = (0, import_vue.computed)(() => props.swipeThreshold && children.length > +props.swipeThreshold);
        const barStyle = (0, import_vue.computed)(() => {
          if (opened.value && (0, import_utils.isDef)(props.zIndex)) {
            return {
              zIndex: +props.zIndex + 1
            };
          }
        });
        const close = () => {
          children.forEach((item) => {
            item.toggle(false);
          });
        };
        const onClickAway = () => {
          if (props.closeOnClickOutside) {
            close();
          }
        };
        const updateOffset = () => {
          if (barRef.value) {
            const rect = (0, import_use.useRect)(barRef);
            if (props.direction === "down") {
              offset.value = rect.bottom;
            } else {
              offset.value = import_utils.windowHeight.value - rect.top;
            }
          }
        };
        const onScroll = () => {
          if (opened.value) {
            updateOffset();
          }
        };
        const toggleItem = (active) => {
          children.forEach((item, index) => {
            if (index === active) {
              item.toggle();
            } else if (item.state.showPopup) {
              item.toggle(false, {
                immediate: true
              });
            }
          });
        };
        const renderTitle = (item, index) => {
          const {
            showPopup
          } = item.state;
          const {
            disabled,
            titleClass
          } = item;
          return (0, import_vue.createVNode)("div", {
            "id": `${id}-${index}`,
            "role": "button",
            "tabindex": disabled ? void 0 : 0,
            "data-allow-mismatch": "attribute",
            "class": [bem("item", {
              disabled,
              grow: scrollable.value
            }), {
              [import_utils.HAPTICS_FEEDBACK]: !disabled
            }],
            "onClick": () => {
              if (!disabled) {
                toggleItem(index);
              }
            }
          }, [(0, import_vue.createVNode)("span", {
            "class": [bem("title", {
              down: showPopup === (props.direction === "down"),
              active: showPopup
            }), titleClass],
            "style": {
              color: showPopup ? props.activeColor : ""
            }
          }, [(0, import_vue.createVNode)("div", {
            "class": "van-ellipsis"
          }, [item.renderTitle()])])]);
        };
        (0, import_use_expose.useExpose)({
          close,
          opened
        });
        linkChildren({
          id,
          props,
          offset,
          opened,
          updateOffset
        });
        (0, import_use.useClickAway)(root, onClickAway);
        (0, import_use.useEventListener)("scroll", onScroll, {
          target: scrollParent,
          passive: true
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "class": bem()
          }, [(0, import_vue.createVNode)("div", {
            "ref": barRef,
            "style": barStyle.value,
            "class": bem("bar", {
              opened: opened.value,
              scrollable: scrollable.value
            })
          }, [children.map(renderTitle)]), (_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dropdown-item/DropdownItem.js
var require_DropdownItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dropdown-item/DropdownItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      dropdownItemProps: () => dropdownItemProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_DropdownMenu = require_DropdownMenu();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_cell = require_cell();
    var import_icon = require_icon();
    var import_popup = require_popup();
    var [name, bem] = (0, import_utils.createNamespace)("dropdown-item");
    var dropdownItemProps = {
      title: String,
      options: (0, import_utils.makeArrayProp)(),
      disabled: Boolean,
      teleport: [String, Object],
      lazyRender: import_utils.truthProp,
      modelValue: import_utils.unknownProp,
      titleClass: import_utils.unknownProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      inheritAttrs: false,
      props: dropdownItemProps,
      emits: ["open", "opened", "close", "closed", "change", "update:modelValue"],
      setup(props, {
        emit,
        slots,
        attrs
      }) {
        const state = (0, import_vue.reactive)({
          showPopup: false,
          transition: true,
          showWrapper: false
        });
        const wrapperRef = (0, import_vue.ref)();
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_DropdownMenu.DROPDOWN_KEY);
        if (!parent) {
          if (true) {
            console.error("[Vant] <DropdownItem> must be a child component of <DropdownMenu>.");
          }
          return;
        }
        const getEmitter = (name2) => () => emit(name2);
        const onOpen = getEmitter("open");
        const onClose = getEmitter("close");
        const onOpened = getEmitter("opened");
        const onClosed = () => {
          state.showWrapper = false;
          emit("closed");
        };
        const onClickWrapper = (event) => {
          if (props.teleport) {
            event.stopPropagation();
          }
        };
        const toggle = (show = !state.showPopup, options = {}) => {
          if (show === state.showPopup) {
            return;
          }
          state.showPopup = show;
          state.transition = !options.immediate;
          if (show) {
            parent.updateOffset();
            state.showWrapper = true;
          }
        };
        const renderTitle = () => {
          if (slots.title) {
            return slots.title();
          }
          if (props.title) {
            return props.title;
          }
          const match = props.options.find((option) => option.value === props.modelValue);
          return match ? match.text : "";
        };
        const renderOption = (option) => {
          const {
            activeColor
          } = parent.props;
          const {
            disabled
          } = option;
          const active = option.value === props.modelValue;
          const onClick = () => {
            if (disabled) {
              return;
            }
            state.showPopup = false;
            if (option.value !== props.modelValue) {
              emit("update:modelValue", option.value);
              emit("change", option.value);
            }
          };
          const renderIcon = () => {
            if (active) {
              return (0, import_vue.createVNode)(import_icon.Icon, {
                "class": bem("icon"),
                "color": disabled ? void 0 : activeColor,
                "name": "success"
              }, null);
            }
          };
          return (0, import_vue.createVNode)(import_cell.Cell, {
            "role": "menuitem",
            "key": String(option.value),
            "icon": option.icon,
            "title": option.text,
            "class": bem("option", {
              active,
              disabled
            }),
            "style": {
              color: active ? activeColor : ""
            },
            "tabindex": active ? 0 : -1,
            "clickable": !disabled,
            "onClick": onClick
          }, {
            value: renderIcon
          });
        };
        const renderContent = () => {
          const {
            offset
          } = parent;
          const {
            autoLocate,
            zIndex,
            overlay,
            duration,
            direction,
            closeOnClickOverlay
          } = parent.props;
          const style = (0, import_utils.getZIndexStyle)(zIndex);
          let offsetValue = offset.value;
          if (autoLocate && wrapperRef.value) {
            const offsetParent = (0, import_utils.getContainingBlock)(wrapperRef.value);
            if (offsetParent) {
              offsetValue -= (0, import_use.useRect)(offsetParent).top;
            }
          }
          if (direction === "down") {
            style.top = `${offsetValue}px`;
          } else {
            style.bottom = `${offsetValue}px`;
          }
          return (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", (0, import_vue.mergeProps)({
            "ref": wrapperRef,
            "style": style,
            "class": bem([direction]),
            "onClick": onClickWrapper
          }, attrs), [(0, import_vue.createVNode)(import_popup.Popup, {
            "show": state.showPopup,
            "onUpdate:show": ($event) => state.showPopup = $event,
            "role": "menu",
            "class": bem("content"),
            "overlay": overlay,
            "overlayProps": {
              duration: state.transition && !parent.opened.value ? duration : 0
            },
            "position": direction === "down" ? "top" : "bottom",
            "duration": state.transition ? duration : 0,
            "lazyRender": props.lazyRender,
            "overlayStyle": {
              position: "absolute"
            },
            "aria-labelledby": `${parent.id}-${index.value}`,
            "data-allow-mismatch": "attribute",
            "closeOnClickOverlay": closeOnClickOverlay,
            "onOpen": onOpen,
            "onClose": onClose,
            "onOpened": onOpened,
            "onClosed": onClosed
          }, {
            default: () => {
              var _a;
              return [props.options.map(renderOption), (_a = slots.default) == null ? void 0 : _a.call(slots)];
            }
          })]), [[import_vue.vShow, state.showWrapper]]);
        };
        (0, import_use_expose.useExpose)({
          state,
          toggle,
          renderTitle
        });
        return () => {
          if (props.teleport) {
            return (0, import_vue.createVNode)(import_vue.Teleport, {
              "to": props.teleport
            }, {
              default: () => [renderContent()]
            });
          }
          return renderContent();
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dropdown-item/index.js
var require_dropdown_item = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dropdown-item/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      DropdownItem: () => DropdownItem,
      default: () => stdin_default,
      dropdownItemProps: () => import_DropdownItem2.dropdownItemProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_DropdownItem = __toESM(require_DropdownItem());
    var import_DropdownItem2 = require_DropdownItem();
    var DropdownItem = (0, import_utils.withInstall)(import_DropdownItem.default);
    var stdin_default = DropdownItem;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dropdown-menu/index.js
var require_dropdown_menu = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/dropdown-menu/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      DropdownMenu: () => DropdownMenu,
      default: () => stdin_default,
      dropdownMenuProps: () => import_DropdownMenu2.dropdownMenuProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_DropdownMenu = __toESM(require_DropdownMenu());
    var import_DropdownMenu2 = require_DropdownMenu();
    var DropdownMenu = (0, import_utils.withInstall)(import_DropdownMenu.default);
    var stdin_default = DropdownMenu;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/floating-bubble/FloatingBubble.js
var require_FloatingBubble = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/floating-bubble/FloatingBubble.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      floatingBubbleProps: () => floatingBubbleProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_touch = require_use_touch();
    var import_icon = __toESM(require_icon());
    var floatingBubbleProps = {
      gap: {
        type: [Number, Object],
        default: 24
      },
      icon: String,
      axis: (0, import_utils.makeStringProp)("y"),
      magnetic: String,
      offset: Object,
      teleport: {
        type: [String, Object],
        default: "body"
      }
    };
    var [name, bem] = (0, import_utils.createNamespace)("floating-bubble");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      inheritAttrs: false,
      props: floatingBubbleProps,
      emits: ["click", "update:offset", "offsetChange"],
      setup(props, {
        slots,
        emit,
        attrs
      }) {
        const rootRef = (0, import_vue.ref)();
        const state = (0, import_vue.ref)({
          x: 0,
          y: 0,
          width: 0,
          height: 0
        });
        const gapX = (0, import_vue.computed)(() => (0, import_utils.isObject)(props.gap) ? props.gap.x : props.gap);
        const gapY = (0, import_vue.computed)(() => (0, import_utils.isObject)(props.gap) ? props.gap.y : props.gap);
        const boundary = (0, import_vue.computed)(() => ({
          top: gapY.value,
          right: import_utils.windowWidth.value - state.value.width - gapX.value,
          bottom: import_utils.windowHeight.value - state.value.height - gapY.value,
          left: gapX.value
        }));
        const dragging = (0, import_vue.ref)(false);
        let initialized = false;
        const rootStyle = (0, import_vue.computed)(() => {
          const style = {};
          const x = (0, import_utils.addUnit)(state.value.x);
          const y = (0, import_utils.addUnit)(state.value.y);
          style.transform = `translate3d(${x}, ${y}, 0)`;
          if (dragging.value || !initialized) {
            style.transition = "none";
          }
          return style;
        });
        const updateState = () => {
          if (!show.value) return;
          const {
            width,
            height
          } = (0, import_use.useRect)(rootRef.value);
          const {
            offset
          } = props;
          state.value = {
            x: offset ? offset.x : import_utils.windowWidth.value - width - gapX.value,
            y: offset ? offset.y : import_utils.windowHeight.value - height - gapY.value,
            width,
            height
          };
        };
        const touch = (0, import_use_touch.useTouch)();
        let prevX = 0;
        let prevY = 0;
        const onTouchStart = (e) => {
          touch.start(e);
          dragging.value = true;
          prevX = state.value.x;
          prevY = state.value.y;
        };
        const onTouchMove = (e) => {
          e.preventDefault();
          touch.move(e);
          if (props.axis === "lock") return;
          if (!touch.isTap.value) {
            if (props.axis === "x" || props.axis === "xy") {
              let nextX = prevX + touch.deltaX.value;
              if (nextX < boundary.value.left) nextX = boundary.value.left;
              if (nextX > boundary.value.right) nextX = boundary.value.right;
              state.value.x = nextX;
            }
            if (props.axis === "y" || props.axis === "xy") {
              let nextY = prevY + touch.deltaY.value;
              if (nextY < boundary.value.top) nextY = boundary.value.top;
              if (nextY > boundary.value.bottom) nextY = boundary.value.bottom;
              state.value.y = nextY;
            }
            const offset = (0, import_utils.pick)(state.value, ["x", "y"]);
            emit("update:offset", offset);
          }
        };
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: rootRef
        });
        const onTouchEnd = () => {
          dragging.value = false;
          (0, import_vue.nextTick)(() => {
            if (props.magnetic === "x") {
              const nextX = (0, import_utils.closest)([boundary.value.left, boundary.value.right], state.value.x);
              state.value.x = nextX;
            }
            if (props.magnetic === "y") {
              const nextY = (0, import_utils.closest)([boundary.value.top, boundary.value.bottom], state.value.y);
              state.value.y = nextY;
            }
            if (!touch.isTap.value) {
              const offset = (0, import_utils.pick)(state.value, ["x", "y"]);
              emit("update:offset", offset);
              if (prevX !== offset.x || prevY !== offset.y) {
                emit("offsetChange", offset);
              }
            }
          });
        };
        const onClick = (e) => {
          if (touch.isTap.value) emit("click", e);
          else e.stopPropagation();
        };
        (0, import_vue.onMounted)(() => {
          updateState();
          (0, import_vue.nextTick)(() => {
            initialized = true;
          });
        });
        (0, import_vue.watch)([import_utils.windowWidth, import_utils.windowHeight, gapX, gapY, () => props.offset], updateState, {
          deep: true
        });
        const show = (0, import_vue.ref)(true);
        (0, import_vue.onActivated)(() => {
          show.value = true;
        });
        (0, import_vue.onDeactivated)(() => {
          if (props.teleport) {
            show.value = false;
          }
        });
        return () => {
          const Content = (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", (0, import_vue.mergeProps)({
            "class": bem(),
            "ref": rootRef,
            "onTouchstartPassive": onTouchStart,
            "onTouchend": onTouchEnd,
            "onTouchcancel": onTouchEnd,
            "onClickCapture": onClick,
            "style": rootStyle.value
          }, attrs), [slots.default ? slots.default() : (0, import_vue.createVNode)(import_icon.default, {
            "name": props.icon,
            "class": bem("icon")
          }, null)]), [[import_vue.vShow, show.value]]);
          return props.teleport ? (0, import_vue.createVNode)(import_vue.Teleport, {
            "to": props.teleport
          }, {
            default: () => [Content]
          }) : Content;
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/floating-bubble/index.js
var require_floating_bubble = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/floating-bubble/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      FloatingBubble: () => FloatingBubble,
      default: () => stdin_default,
      floatingBubbleProps: () => import_FloatingBubble2.floatingBubbleProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_FloatingBubble = __toESM(require_FloatingBubble());
    var import_FloatingBubble2 = require_FloatingBubble();
    var FloatingBubble = (0, import_utils.withInstall)(import_FloatingBubble.default);
    var stdin_default = FloatingBubble;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/floating-panel/FloatingPanel.js
var require_FloatingPanel = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/floating-panel/FloatingPanel.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      floatingPanelProps: () => floatingPanelProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_lock_scroll = require_use_lock_scroll();
    var import_use_touch = require_use_touch();
    var import_use_sync_prop_ref = require_use_sync_prop_ref();
    var floatingPanelProps = {
      height: (0, import_utils.makeNumericProp)(0),
      anchors: (0, import_utils.makeArrayProp)(),
      duration: (0, import_utils.makeNumericProp)(0.3),
      magnetic: import_utils.truthProp,
      draggable: import_utils.truthProp,
      contentDraggable: import_utils.truthProp,
      lockScroll: Boolean,
      safeAreaInsetBottom: import_utils.truthProp
    };
    var [name, bem] = (0, import_utils.createNamespace)("floating-panel");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: floatingPanelProps,
      emits: ["heightChange", "update:height"],
      setup(props, {
        emit,
        slots
      }) {
        const DAMP = 0.2;
        const rootRef = (0, import_vue.ref)();
        const contentRef = (0, import_vue.ref)();
        const height = (0, import_use_sync_prop_ref.useSyncPropRef)(() => +props.height, (value) => emit("update:height", value));
        const boundary = (0, import_vue.computed)(() => {
          var _a, _b;
          return {
            min: (_a = props.anchors[0]) != null ? _a : 100,
            max: (_b = props.anchors[props.anchors.length - 1]) != null ? _b : Math.round(import_utils.windowHeight.value * 0.6)
          };
        });
        const anchors = (0, import_vue.computed)(() => props.anchors.length >= 2 ? props.anchors : [boundary.value.min, boundary.value.max]);
        const dragging = (0, import_vue.ref)(false);
        const rootStyle = (0, import_vue.computed)(() => ({
          height: (0, import_utils.addUnit)(boundary.value.max),
          transform: `translateY(calc(100% + ${(0, import_utils.addUnit)(-height.value)}))`,
          transition: !dragging.value ? `transform ${props.duration}s cubic-bezier(0.18, 0.89, 0.32, 1.28)` : "none"
        }));
        const ease = (moveY) => {
          const absDistance = Math.abs(moveY);
          const {
            min,
            max
          } = boundary.value;
          if (absDistance > max) {
            return -(max + (absDistance - max) * DAMP);
          }
          if (absDistance < min) {
            return -(min - (min - absDistance) * DAMP);
          }
          return moveY;
        };
        let startY;
        let maxScroll = -1;
        const touch = (0, import_use_touch.useTouch)();
        const onTouchstart = (e) => {
          if (!props.draggable) return;
          touch.start(e);
          dragging.value = true;
          startY = -height.value;
          maxScroll = -1;
        };
        const onTouchmove = (e) => {
          var _a;
          if (!props.draggable) return;
          touch.move(e);
          const target = e.target;
          if (contentRef.value === target || ((_a = contentRef.value) == null ? void 0 : _a.contains(target))) {
            const {
              scrollTop
            } = contentRef.value;
            maxScroll = Math.max(maxScroll, scrollTop);
            if (!props.contentDraggable) return;
            if (-startY < boundary.value.max) {
              (0, import_utils.preventDefault)(e, true);
            } else if (!(scrollTop <= 0 && touch.deltaY.value > 0) || maxScroll > 0) {
              return;
            }
          }
          const moveY = touch.deltaY.value + startY;
          height.value = -ease(moveY);
        };
        const onTouchend = () => {
          maxScroll = -1;
          if (!dragging.value) {
            return;
          }
          dragging.value = false;
          if (!props.draggable) {
            return;
          }
          if (props.magnetic) {
            height.value = (0, import_utils.closest)(anchors.value, height.value);
          } else {
            const {
              min,
              max
            } = boundary.value;
            height.value = Math.max(min, Math.min(max, height.value));
          }
          if (height.value !== -startY) {
            emit("heightChange", {
              height: height.value
            });
          }
        };
        (0, import_vue.watch)(boundary, () => {
          height.value = (0, import_utils.closest)(anchors.value, height.value);
        }, {
          immediate: true
        });
        (0, import_use_lock_scroll.useLockScroll)(rootRef, () => props.lockScroll || dragging.value);
        (0, import_use.useEventListener)("touchmove", onTouchmove, {
          target: rootRef
        });
        const renderHeader = () => {
          if (slots.header) {
            return slots.header();
          }
          if (!props.draggable) {
            return null;
          }
          return (0, import_vue.createVNode)("div", {
            "class": bem("header")
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("header-bar")
          }, null)]);
        };
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": [bem(), {
              "van-safe-area-bottom": props.safeAreaInsetBottom
            }],
            "ref": rootRef,
            "style": rootStyle.value,
            "onTouchstartPassive": onTouchstart,
            "onTouchend": onTouchend,
            "onTouchcancel": onTouchend
          }, [renderHeader(), (0, import_vue.createVNode)("div", {
            "class": bem("content"),
            "ref": contentRef,
            "style": {
              paddingBottom: (0, import_utils.addUnit)(boundary.value.max - height.value)
            }
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/floating-panel/index.js
var require_floating_panel = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/floating-panel/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      FloatingPanel: () => FloatingPanel,
      default: () => stdin_default,
      floatingPanelProps: () => import_FloatingPanel2.floatingPanelProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_FloatingPanel = __toESM(require_FloatingPanel());
    var import_FloatingPanel2 = require_FloatingPanel();
    var FloatingPanel = (0, import_utils.withInstall)(import_FloatingPanel.default);
    var stdin_default = FloatingPanel;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/grid/Grid.js
var require_Grid = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/grid/Grid.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      GRID_KEY: () => GRID_KEY,
      default: () => stdin_default,
      gridProps: () => gridProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_constant = require_constant();
    var import_use = require_index_cjs();
    var [name, bem] = (0, import_utils.createNamespace)("grid");
    var gridProps = {
      square: Boolean,
      center: import_utils.truthProp,
      border: import_utils.truthProp,
      gutter: import_utils.numericProp,
      reverse: Boolean,
      iconSize: import_utils.numericProp,
      direction: String,
      clickable: Boolean,
      columnNum: (0, import_utils.makeNumericProp)(4)
    };
    var GRID_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: gridProps,
      setup(props, {
        slots
      }) {
        const {
          linkChildren
        } = (0, import_use.useChildren)(GRID_KEY);
        linkChildren({
          props
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "style": {
              paddingLeft: (0, import_utils.addUnit)(props.gutter)
            },
            "class": [bem(), {
              [import_constant.BORDER_TOP]: props.border && !props.gutter
            }]
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/grid/index.js
var require_grid = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/grid/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Grid: () => Grid,
      default: () => stdin_default,
      gridProps: () => import_Grid2.gridProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Grid = __toESM(require_Grid());
    var import_Grid2 = require_Grid();
    var Grid = (0, import_utils.withInstall)(import_Grid.default);
    var stdin_default = Grid;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/grid-item/GridItem.js
var require_GridItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/grid-item/GridItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      gridItemProps: () => gridItemProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_Grid = require_Grid();
    var import_use = require_index_cjs();
    var import_use_route = require_use_route();
    var import_icon = require_icon();
    var import_badge = require_badge();
    var [name, bem] = (0, import_utils.createNamespace)("grid-item");
    var gridItemProps = (0, import_utils.extend)({}, import_use_route.routeProps, {
      dot: Boolean,
      text: String,
      icon: String,
      badge: import_utils.numericProp,
      iconColor: String,
      iconPrefix: String,
      badgeProps: Object
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: gridItemProps,
      setup(props, {
        slots
      }) {
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_Grid.GRID_KEY);
        const route = (0, import_use_route.useRoute)();
        if (!parent) {
          if (true) {
            console.error("[Vant] <GridItem> must be a child component of <Grid>.");
          }
          return;
        }
        const rootStyle = (0, import_vue.computed)(() => {
          const {
            square,
            gutter,
            columnNum
          } = parent.props;
          const percent = `${100 / +columnNum}%`;
          const style = {
            flexBasis: percent
          };
          if (square) {
            style.paddingTop = percent;
          } else if (gutter) {
            const gutterValue = (0, import_utils.addUnit)(gutter);
            style.paddingRight = gutterValue;
            if (index.value >= +columnNum) {
              style.marginTop = gutterValue;
            }
          }
          return style;
        });
        const contentStyle = (0, import_vue.computed)(() => {
          const {
            square,
            gutter
          } = parent.props;
          if (square && gutter) {
            const gutterValue = (0, import_utils.addUnit)(gutter);
            return {
              right: gutterValue,
              bottom: gutterValue,
              height: "auto"
            };
          }
        });
        const renderIcon = () => {
          if (slots.icon) {
            return (0, import_vue.createVNode)(import_badge.Badge, (0, import_vue.mergeProps)({
              "dot": props.dot,
              "content": props.badge
            }, props.badgeProps), {
              default: slots.icon
            });
          }
          if (props.icon) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "dot": props.dot,
              "name": props.icon,
              "size": parent.props.iconSize,
              "badge": props.badge,
              "class": bem("icon"),
              "color": props.iconColor,
              "badgeProps": props.badgeProps,
              "classPrefix": props.iconPrefix
            }, null);
          }
        };
        const renderText = () => {
          if (slots.text) {
            return slots.text();
          }
          if (props.text) {
            return (0, import_vue.createVNode)("span", {
              "class": bem("text")
            }, [props.text]);
          }
        };
        const renderContent = () => {
          if (slots.default) {
            return slots.default();
          }
          return [renderIcon(), renderText()];
        };
        return () => {
          const {
            center,
            border,
            square,
            gutter,
            reverse,
            direction,
            clickable
          } = parent.props;
          const classes = [bem("content", [direction, {
            center,
            square,
            reverse,
            clickable,
            surround: border && gutter
          }]), {
            [import_utils.BORDER]: border
          }];
          return (0, import_vue.createVNode)("div", {
            "class": [bem({
              square
            })],
            "style": rootStyle.value
          }, [(0, import_vue.createVNode)("div", {
            "role": clickable ? "button" : void 0,
            "class": classes,
            "style": contentStyle.value,
            "tabindex": clickable ? 0 : void 0,
            "onClick": route
          }, [renderContent()])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/grid-item/index.js
var require_grid_item = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/grid-item/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      GridItem: () => GridItem,
      default: () => stdin_default,
      gridItemProps: () => import_GridItem2.gridItemProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_GridItem = __toESM(require_GridItem());
    var import_GridItem2 = require_GridItem();
    var GridItem = (0, import_utils.withInstall)(import_GridItem.default);
    var stdin_default = GridItem;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/highlight/Highlight.js
var require_Highlight = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/highlight/Highlight.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      highlightProps: () => highlightProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("highlight");
    var highlightProps = {
      autoEscape: import_utils.truthProp,
      caseSensitive: Boolean,
      highlightClass: String,
      highlightTag: (0, import_utils.makeStringProp)("span"),
      keywords: (0, import_utils.makeRequiredProp)([String, Array]),
      sourceString: (0, import_utils.makeStringProp)(""),
      tag: (0, import_utils.makeStringProp)("div"),
      unhighlightClass: String,
      unhighlightTag: (0, import_utils.makeStringProp)("span")
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: highlightProps,
      setup(props) {
        const highlightChunks = (0, import_vue.computed)(() => {
          const {
            autoEscape,
            caseSensitive,
            keywords,
            sourceString
          } = props;
          const flags = caseSensitive ? "g" : "gi";
          const _keywords = Array.isArray(keywords) ? keywords : [keywords];
          let chunks = _keywords.filter((keyword) => keyword).reduce((chunks2, keyword) => {
            if (autoEscape) {
              keyword = keyword.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
            }
            const regex = new RegExp(keyword, flags);
            let match;
            while (match = regex.exec(sourceString)) {
              const start = match.index;
              const end = regex.lastIndex;
              if (start >= end) {
                regex.lastIndex++;
                continue;
              }
              chunks2.push({
                start,
                end,
                highlight: true
              });
            }
            return chunks2;
          }, []);
          chunks = chunks.sort((a, b) => a.start - b.start).reduce((chunks2, currentChunk) => {
            const prevChunk = chunks2[chunks2.length - 1];
            if (!prevChunk || currentChunk.start > prevChunk.end) {
              const unhighlightStart = prevChunk ? prevChunk.end : 0;
              const unhighlightEnd = currentChunk.start;
              if (unhighlightStart !== unhighlightEnd) {
                chunks2.push({
                  start: unhighlightStart,
                  end: unhighlightEnd,
                  highlight: false
                });
              }
              chunks2.push(currentChunk);
            } else {
              prevChunk.end = Math.max(prevChunk.end, currentChunk.end);
            }
            return chunks2;
          }, []);
          const lastChunk = chunks[chunks.length - 1];
          if (!lastChunk) {
            chunks.push({
              start: 0,
              end: sourceString.length,
              highlight: false
            });
          }
          if (lastChunk && lastChunk.end < sourceString.length) {
            chunks.push({
              start: lastChunk.end,
              end: sourceString.length,
              highlight: false
            });
          }
          return chunks;
        });
        const renderContent = () => {
          const {
            sourceString,
            highlightClass,
            unhighlightClass,
            highlightTag,
            unhighlightTag
          } = props;
          return highlightChunks.value.map((chunk) => {
            const {
              start,
              end,
              highlight
            } = chunk;
            const text = sourceString.slice(start, end);
            if (highlight) {
              return (0, import_vue.createVNode)(highlightTag, {
                "class": [bem("tag"), highlightClass]
              }, {
                default: () => [text]
              });
            }
            return (0, import_vue.createVNode)(unhighlightTag, {
              "class": unhighlightClass
            }, {
              default: () => [text]
            });
          });
        };
        return () => {
          const {
            tag
          } = props;
          return (0, import_vue.createVNode)(tag, {
            "class": bem()
          }, {
            default: () => [renderContent()]
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/highlight/index.js
var require_highlight = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/highlight/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Highlight: () => Highlight,
      default: () => stdin_default,
      highlightProps: () => import_Highlight2.highlightProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Highlight = __toESM(require_Highlight());
    var import_Highlight2 = require_Highlight();
    var Highlight = (0, import_utils.withInstall)(import_Highlight.default);
    var stdin_default = Highlight;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image-preview/ImagePreviewItem.js
var require_ImagePreviewItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image-preview/ImagePreviewItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_expose = require_use_expose();
    var import_use_touch = require_use_touch();
    var import_use = require_index_cjs();
    var import_image = require_image();
    var import_loading = require_loading();
    var import_swipe_item = require_swipe_item();
    var getDistance = (touches) => Math.sqrt((touches[0].clientX - touches[1].clientX) ** 2 + (touches[0].clientY - touches[1].clientY) ** 2);
    var getCenter = (touches) => ({
      x: (touches[0].clientX + touches[1].clientX) / 2,
      y: (touches[0].clientY + touches[1].clientY) / 2
    });
    var bem = (0, import_utils.createNamespace)("image-preview")[1];
    var longImageRatio = 2.6;
    var imagePreviewItemProps = {
      src: String,
      show: Boolean,
      active: Number,
      minZoom: (0, import_utils.makeRequiredProp)(import_utils.numericProp),
      maxZoom: (0, import_utils.makeRequiredProp)(import_utils.numericProp),
      rootWidth: (0, import_utils.makeRequiredProp)(Number),
      rootHeight: (0, import_utils.makeRequiredProp)(Number),
      disableZoom: Boolean,
      doubleScale: Boolean,
      closeOnClickImage: Boolean,
      closeOnClickOverlay: Boolean,
      vertical: Boolean
    };
    var stdin_default = (0, import_vue.defineComponent)({
      props: imagePreviewItemProps,
      emits: ["scale", "close", "longPress"],
      setup(props, {
        emit,
        slots
      }) {
        const state = (0, import_vue.reactive)({
          scale: 1,
          moveX: 0,
          moveY: 0,
          moving: false,
          zooming: false,
          initializing: false,
          imageRatio: 0
        });
        const touch = (0, import_use_touch.useTouch)();
        const imageRef = (0, import_vue.ref)();
        const swipeItem = (0, import_vue.ref)();
        const vertical = (0, import_vue.ref)(false);
        const isLongImage = (0, import_vue.ref)(false);
        let initialMoveY = 0;
        const imageStyle = (0, import_vue.computed)(() => {
          const {
            scale,
            moveX,
            moveY,
            moving,
            zooming,
            initializing
          } = state;
          const style = {
            transitionDuration: zooming || moving || initializing ? "0s" : ".3s"
          };
          if (scale !== 1 || isLongImage.value) {
            style.transform = `matrix(${scale}, 0, 0, ${scale}, ${moveX}, ${moveY})`;
          }
          return style;
        });
        const maxMoveX = (0, import_vue.computed)(() => {
          if (state.imageRatio) {
            const {
              rootWidth,
              rootHeight
            } = props;
            const displayWidth = vertical.value ? rootHeight / state.imageRatio : rootWidth;
            return Math.max(0, (state.scale * displayWidth - rootWidth) / 2);
          }
          return 0;
        });
        const maxMoveY = (0, import_vue.computed)(() => {
          if (state.imageRatio) {
            const {
              rootWidth,
              rootHeight
            } = props;
            const displayHeight = vertical.value ? rootHeight : rootWidth * state.imageRatio;
            return Math.max(0, (state.scale * displayHeight - rootHeight) / 2);
          }
          return 0;
        });
        const setScale = (scale, center) => {
          var _a;
          scale = (0, import_utils.clamp)(scale, +props.minZoom, +props.maxZoom + 1);
          if (scale !== state.scale) {
            const ratio = scale / state.scale;
            state.scale = scale;
            if (center) {
              const imageRect = (0, import_use.useRect)((_a = imageRef.value) == null ? void 0 : _a.$el);
              const origin = {
                x: imageRect.width * 0.5,
                y: imageRect.height * 0.5
              };
              const moveX = state.moveX - (center.x - imageRect.left - origin.x) * (ratio - 1);
              const moveY = state.moveY - (center.y - imageRect.top - origin.y) * (ratio - 1);
              state.moveX = (0, import_utils.clamp)(moveX, -maxMoveX.value, maxMoveX.value);
              state.moveY = (0, import_utils.clamp)(moveY, -maxMoveY.value, maxMoveY.value);
            } else {
              state.moveX = 0;
              state.moveY = isLongImage.value ? initialMoveY : 0;
            }
            emit("scale", {
              scale,
              index: props.active
            });
          }
        };
        const resetScale = () => {
          setScale(1);
        };
        const toggleScale = () => {
          const scale = state.scale > 1 ? 1 : 2;
          setScale(scale, scale === 2 || isLongImage.value ? {
            x: touch.startX.value,
            y: touch.startY.value
          } : void 0);
        };
        let fingerNum;
        let startMoveX;
        let startMoveY;
        let startScale;
        let startDistance;
        let lastCenter;
        let doubleTapTimer;
        let touchStartTime;
        let isImageMoved = false;
        const onTouchStart = (event) => {
          const {
            touches
          } = event;
          fingerNum = touches.length;
          if (fingerNum === 2 && props.disableZoom) {
            return;
          }
          const {
            offsetX
          } = touch;
          touch.start(event);
          startMoveX = state.moveX;
          startMoveY = state.moveY;
          touchStartTime = Date.now();
          isImageMoved = false;
          state.moving = fingerNum === 1 && (state.scale !== 1 || isLongImage.value);
          state.zooming = fingerNum === 2 && !offsetX.value;
          if (state.zooming) {
            startScale = state.scale;
            startDistance = getDistance(touches);
          }
        };
        const onTouchMove = (event) => {
          const {
            touches
          } = event;
          touch.move(event);
          if (state.moving) {
            const {
              deltaX,
              deltaY
            } = touch;
            const moveX = deltaX.value + startMoveX;
            const moveY = deltaY.value + startMoveY;
            if ((props.vertical ? touch.isVertical() && Math.abs(moveY) > maxMoveY.value : touch.isHorizontal() && Math.abs(moveX) > maxMoveX.value) && !isImageMoved) {
              state.moving = false;
              return;
            }
            isImageMoved = true;
            (0, import_utils.preventDefault)(event, true);
            state.moveX = (0, import_utils.clamp)(moveX, -maxMoveX.value, maxMoveX.value);
            state.moveY = (0, import_utils.clamp)(moveY, -maxMoveY.value, maxMoveY.value);
          }
          if (state.zooming) {
            (0, import_utils.preventDefault)(event, true);
            if (touches.length === 2) {
              const distance = getDistance(touches);
              const scale = startScale * distance / startDistance;
              lastCenter = getCenter(touches);
              setScale(scale, lastCenter);
            }
          }
        };
        const checkClose = (event) => {
          var _a;
          const swipeItemEl = (_a = swipeItem.value) == null ? void 0 : _a.$el;
          if (!swipeItemEl) return;
          const imageEl = swipeItemEl.firstElementChild;
          const isClickOverlay = event.target === swipeItemEl;
          const isClickImage = imageEl == null ? void 0 : imageEl.contains(event.target);
          if (!props.closeOnClickImage && isClickImage) return;
          if (!props.closeOnClickOverlay && isClickOverlay) return;
          emit("close");
        };
        const checkTap = (event) => {
          if (fingerNum > 1) {
            return;
          }
          const deltaTime = Date.now() - touchStartTime;
          const TAP_TIME = 250;
          if (touch.isTap.value) {
            if (deltaTime < TAP_TIME) {
              if (props.doubleScale) {
                if (doubleTapTimer) {
                  clearTimeout(doubleTapTimer);
                  doubleTapTimer = null;
                  toggleScale();
                } else {
                  doubleTapTimer = setTimeout(() => {
                    checkClose(event);
                    doubleTapTimer = null;
                  }, TAP_TIME);
                }
              } else {
                checkClose(event);
              }
            } else if (deltaTime > import_utils.LONG_PRESS_START_TIME) {
              emit("longPress");
            }
          }
        };
        const onTouchEnd = (event) => {
          let stopPropagation = false;
          if (state.moving || state.zooming) {
            stopPropagation = true;
            if (state.moving && startMoveX === state.moveX && startMoveY === state.moveY) {
              stopPropagation = false;
            }
            if (!event.touches.length) {
              if (state.zooming) {
                state.moveX = (0, import_utils.clamp)(state.moveX, -maxMoveX.value, maxMoveX.value);
                state.moveY = (0, import_utils.clamp)(state.moveY, -maxMoveY.value, maxMoveY.value);
                state.zooming = false;
              }
              state.moving = false;
              startMoveX = 0;
              startMoveY = 0;
              startScale = 1;
              if (state.scale < 1) {
                resetScale();
              }
              const maxZoom = +props.maxZoom;
              if (state.scale > maxZoom) {
                setScale(maxZoom, lastCenter);
              }
            }
          }
          (0, import_utils.preventDefault)(event, stopPropagation);
          checkTap(event);
          touch.reset();
        };
        const resize = () => {
          const {
            rootWidth,
            rootHeight
          } = props;
          const rootRatio = rootHeight / rootWidth;
          const {
            imageRatio
          } = state;
          vertical.value = state.imageRatio > rootRatio && imageRatio < longImageRatio;
          isLongImage.value = state.imageRatio > rootRatio && imageRatio >= longImageRatio;
          if (isLongImage.value) {
            initialMoveY = (imageRatio * rootWidth - rootHeight) / 2;
            state.moveY = initialMoveY;
            state.initializing = true;
            (0, import_use.raf)(() => {
              state.initializing = false;
            });
          }
          resetScale();
        };
        const onLoad = (event) => {
          const {
            naturalWidth,
            naturalHeight
          } = event.target;
          state.imageRatio = naturalHeight / naturalWidth;
          resize();
        };
        (0, import_vue.watch)(() => props.active, resetScale);
        (0, import_vue.watch)(() => props.show, (value) => {
          if (!value) {
            resetScale();
          }
        });
        (0, import_vue.watch)(() => [props.rootWidth, props.rootHeight], resize);
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: (0, import_vue.computed)(() => {
            var _a;
            return (_a = swipeItem.value) == null ? void 0 : _a.$el;
          })
        });
        (0, import_use_expose.useExpose)({
          resetScale
        });
        return () => {
          const imageSlots = {
            loading: () => (0, import_vue.createVNode)(import_loading.Loading, {
              "type": "spinner"
            }, null)
          };
          return (0, import_vue.createVNode)(import_swipe_item.SwipeItem, {
            "ref": swipeItem,
            "class": bem("swipe-item"),
            "onTouchstartPassive": onTouchStart,
            "onTouchend": onTouchEnd,
            "onTouchcancel": onTouchEnd
          }, {
            default: () => [slots.image ? (0, import_vue.createVNode)("div", {
              "class": bem("image-wrap")
            }, [slots.image({
              src: props.src,
              onLoad,
              style: imageStyle.value
            })]) : (0, import_vue.createVNode)(import_image.Image, {
              "ref": imageRef,
              "src": props.src,
              "fit": "contain",
              "class": bem("image", {
                vertical: vertical.value
              }),
              "style": imageStyle.value,
              "onLoad": onLoad
            }, imageSlots)]
          });
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image-preview/ImagePreview.js
var require_ImagePreview = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image-preview/ImagePreview.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      imagePreviewProps: () => imagePreviewProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_icon = require_icon();
    var import_swipe = require_swipe();
    var import_popup = require_popup();
    var import_ImagePreviewItem = __toESM(require_ImagePreviewItem());
    var [name, bem] = (0, import_utils.createNamespace)("image-preview");
    var popupProps = ["show", "teleport", "transition", "overlayStyle", "closeOnPopstate"];
    var imagePreviewProps = {
      show: Boolean,
      loop: import_utils.truthProp,
      images: (0, import_utils.makeArrayProp)(),
      minZoom: (0, import_utils.makeNumericProp)(1 / 3),
      maxZoom: (0, import_utils.makeNumericProp)(3),
      overlay: import_utils.truthProp,
      vertical: Boolean,
      closeable: Boolean,
      showIndex: import_utils.truthProp,
      className: import_utils.unknownProp,
      closeIcon: (0, import_utils.makeStringProp)("clear"),
      transition: String,
      beforeClose: Function,
      doubleScale: import_utils.truthProp,
      overlayClass: import_utils.unknownProp,
      overlayStyle: Object,
      swipeDuration: (0, import_utils.makeNumericProp)(300),
      startPosition: (0, import_utils.makeNumericProp)(0),
      showIndicators: Boolean,
      closeOnPopstate: import_utils.truthProp,
      closeOnClickImage: import_utils.truthProp,
      closeOnClickOverlay: import_utils.truthProp,
      closeIconPosition: (0, import_utils.makeStringProp)("top-right"),
      teleport: [String, Object]
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: imagePreviewProps,
      emits: ["scale", "close", "closed", "change", "longPress", "update:show"],
      setup(props, {
        emit,
        slots
      }) {
        const swipeRef = (0, import_vue.ref)();
        const activedPreviewItemRef = (0, import_vue.ref)();
        const state = (0, import_vue.reactive)({
          active: 0,
          rootWidth: 0,
          rootHeight: 0,
          disableZoom: false
        });
        const resize = () => {
          if (swipeRef.value) {
            const rect = (0, import_use.useRect)(swipeRef.value.$el);
            state.rootWidth = rect.width;
            state.rootHeight = rect.height;
            swipeRef.value.resize();
          }
        };
        const emitScale = (args) => emit("scale", args);
        const updateShow = (show) => emit("update:show", show);
        const emitClose = () => {
          (0, import_utils.callInterceptor)(props.beforeClose, {
            args: [state.active],
            done: () => updateShow(false)
          });
        };
        const setActive = (active) => {
          if (active !== state.active) {
            state.active = active;
            emit("change", active);
          }
        };
        const renderIndex = () => {
          if (props.showIndex) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("index")
            }, [slots.index ? slots.index({
              index: state.active
            }) : `${state.active + 1} / ${props.images.length}`]);
          }
        };
        const renderCover = () => {
          if (slots.cover) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("cover")
            }, [slots.cover()]);
          }
        };
        const onDragStart = () => {
          state.disableZoom = true;
        };
        const onDragEnd = () => {
          state.disableZoom = false;
        };
        const renderImages = () => (0, import_vue.createVNode)(import_swipe.Swipe, {
          "ref": swipeRef,
          "lazyRender": true,
          "loop": props.loop,
          "class": bem("swipe"),
          "vertical": props.vertical,
          "duration": props.swipeDuration,
          "initialSwipe": props.startPosition,
          "showIndicators": props.showIndicators,
          "indicatorColor": "white",
          "onChange": setActive,
          "onDragEnd": onDragEnd,
          "onDragStart": onDragStart
        }, {
          default: () => [props.images.map((image, index) => (0, import_vue.createVNode)(import_ImagePreviewItem.default, {
            "ref": (item) => {
              if (index === state.active) {
                activedPreviewItemRef.value = item;
              }
            },
            "src": image,
            "show": props.show,
            "active": state.active,
            "maxZoom": props.maxZoom,
            "minZoom": props.minZoom,
            "rootWidth": state.rootWidth,
            "rootHeight": state.rootHeight,
            "disableZoom": state.disableZoom,
            "doubleScale": props.doubleScale,
            "closeOnClickImage": props.closeOnClickImage,
            "closeOnClickOverlay": props.closeOnClickOverlay,
            "vertical": props.vertical,
            "onScale": emitScale,
            "onClose": emitClose,
            "onLongPress": () => emit("longPress", {
              index
            })
          }, {
            image: slots.image
          }))]
        });
        const renderClose = () => {
          if (props.closeable) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "role": "button",
              "name": props.closeIcon,
              "class": [bem("close-icon", props.closeIconPosition), import_utils.HAPTICS_FEEDBACK],
              "onClick": emitClose
            }, null);
          }
        };
        const onClosed = () => emit("closed");
        const swipeTo = (index, options) => {
          var _a;
          return (_a = swipeRef.value) == null ? void 0 : _a.swipeTo(index, options);
        };
        (0, import_use_expose.useExpose)({
          resetScale: () => {
            var _a;
            (_a = activedPreviewItemRef.value) == null ? void 0 : _a.resetScale();
          },
          swipeTo
        });
        (0, import_vue.onMounted)(resize);
        (0, import_vue.watch)([import_utils.windowWidth, import_utils.windowHeight], resize);
        (0, import_vue.watch)(() => props.startPosition, (value) => setActive(+value));
        (0, import_vue.watch)(() => props.show, (value) => {
          const {
            images,
            startPosition
          } = props;
          if (value) {
            setActive(+startPosition);
            (0, import_vue.nextTick)(() => {
              resize();
              swipeTo(+startPosition, {
                immediate: true
              });
            });
          } else {
            emit("close", {
              index: state.active,
              url: images[state.active]
            });
          }
        });
        return () => (0, import_vue.createVNode)(import_popup.Popup, (0, import_vue.mergeProps)({
          "class": [bem(), props.className],
          "overlayClass": [bem("overlay"), props.overlayClass],
          "onClosed": onClosed,
          "onUpdate:show": updateShow
        }, (0, import_utils.pick)(props, popupProps)), {
          default: () => [renderClose(), renderImages(), renderIndex(), renderCover()]
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image-preview/function-call.js
var require_function_call3 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image-preview/function-call.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      showImagePreview: () => showImagePreview
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_mount_component = require_mount_component();
    var import_ImagePreview = __toESM(require_ImagePreview());
    var instance;
    var defaultConfig = {
      loop: true,
      images: [],
      maxZoom: 3,
      minZoom: 1 / 3,
      onScale: void 0,
      onClose: void 0,
      onChange: void 0,
      vertical: false,
      teleport: "body",
      className: "",
      showIndex: true,
      closeable: false,
      closeIcon: "clear",
      transition: void 0,
      beforeClose: void 0,
      doubleScale: true,
      overlayStyle: void 0,
      overlayClass: void 0,
      startPosition: 0,
      swipeDuration: 300,
      showIndicators: false,
      closeOnPopstate: true,
      closeOnClickOverlay: true,
      closeIconPosition: "top-right"
    };
    function initInstance() {
      ({
        instance
      } = (0, import_mount_component.mountComponent)({
        setup() {
          const {
            state,
            toggle
          } = (0, import_mount_component.usePopupState)();
          const onClosed = () => {
            state.images = [];
          };
          return () => (0, import_vue.createVNode)(import_ImagePreview.default, (0, import_vue.mergeProps)(state, {
            "onClosed": onClosed,
            "onUpdate:show": toggle
          }), null);
        }
      }));
    }
    var showImagePreview = (options, startPosition = 0) => {
      if (!import_utils.inBrowser) {
        return;
      }
      if (!instance) {
        initInstance();
      }
      options = Array.isArray(options) ? {
        images: options,
        startPosition
      } : options;
      instance.open((0, import_utils.extend)({}, defaultConfig, options));
      return instance;
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image-preview/index.js
var require_image_preview = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/image-preview/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ImagePreview: () => ImagePreview,
      default: () => stdin_default,
      imagePreviewProps: () => import_ImagePreview2.imagePreviewProps,
      showImagePreview: () => import_function_call.showImagePreview
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ImagePreview = __toESM(require_ImagePreview());
    var import_ImagePreview2 = require_ImagePreview();
    var import_function_call = require_function_call3();
    var ImagePreview = (0, import_utils.withInstall)(import_ImagePreview.default);
    var stdin_default = ImagePreview;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index-bar/IndexBar.js
var require_IndexBar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index-bar/IndexBar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      INDEX_BAR_KEY: () => INDEX_BAR_KEY,
      default: () => stdin_default,
      indexBarProps: () => indexBarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_touch = require_use_touch();
    var import_use_expose = require_use_expose();
    function genAlphabet() {
      const charCodeOfA = "A".charCodeAt(0);
      const indexList = Array(26).fill("").map((_, i) => String.fromCharCode(charCodeOfA + i));
      return indexList;
    }
    var [name, bem] = (0, import_utils.createNamespace)("index-bar");
    var indexBarProps = {
      sticky: import_utils.truthProp,
      zIndex: import_utils.numericProp,
      teleport: [String, Object],
      highlightColor: String,
      stickyOffsetTop: (0, import_utils.makeNumberProp)(0),
      indexList: {
        type: Array,
        default: genAlphabet
      }
    };
    var INDEX_BAR_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: indexBarProps,
      emits: ["select", "change"],
      setup(props, {
        emit,
        slots
      }) {
        const root = (0, import_vue.ref)();
        const sidebar = (0, import_vue.ref)();
        const activeAnchor = (0, import_vue.ref)("");
        const touch = (0, import_use_touch.useTouch)();
        const scrollParent = (0, import_use.useScrollParent)(root);
        const {
          children,
          linkChildren
        } = (0, import_use.useChildren)(INDEX_BAR_KEY);
        let selectActiveIndex;
        linkChildren({
          props
        });
        const sidebarStyle = (0, import_vue.computed)(() => {
          if ((0, import_utils.isDef)(props.zIndex)) {
            return {
              zIndex: +props.zIndex + 1
            };
          }
        });
        const highlightStyle = (0, import_vue.computed)(() => {
          if (props.highlightColor) {
            return {
              color: props.highlightColor
            };
          }
        });
        const getActiveAnchor = (scrollTop, rects) => {
          for (let i = children.length - 1; i >= 0; i--) {
            const prevHeight = i > 0 ? rects[i - 1].height : 0;
            const reachTop = props.sticky ? prevHeight + props.stickyOffsetTop : 0;
            if (scrollTop + reachTop >= rects[i].top) {
              return i;
            }
          }
          return -1;
        };
        const getMatchAnchor = (index) => children.find((item) => String(item.index) === index);
        const onScroll = () => {
          if ((0, import_utils.isHidden)(root)) {
            return;
          }
          const {
            sticky,
            indexList
          } = props;
          const scrollTop = (0, import_utils.getScrollTop)(scrollParent.value);
          const scrollParentRect = (0, import_use.useRect)(scrollParent);
          const rects = children.map((item) => item.getRect(scrollParent.value, scrollParentRect));
          let active = -1;
          if (selectActiveIndex) {
            const match = getMatchAnchor(selectActiveIndex);
            if (match) {
              const rect = match.getRect(scrollParent.value, scrollParentRect);
              if (props.sticky && props.stickyOffsetTop) {
                active = getActiveAnchor(rect.top - props.stickyOffsetTop, rects);
              } else {
                active = getActiveAnchor(rect.top, rects);
              }
            }
          } else {
            active = getActiveAnchor(scrollTop, rects);
          }
          activeAnchor.value = indexList[active];
          if (sticky) {
            children.forEach((item, index) => {
              const {
                state,
                $el
              } = item;
              if (index === active || index === active - 1) {
                const rect = $el.getBoundingClientRect();
                state.left = rect.left;
                state.width = rect.width;
              } else {
                state.left = null;
                state.width = null;
              }
              if (index === active) {
                state.active = true;
                state.top = Math.max(props.stickyOffsetTop, rects[index].top - scrollTop) + scrollParentRect.top;
              } else if (index === active - 1 && selectActiveIndex === "") {
                const activeItemTop = rects[active].top - scrollTop;
                state.active = activeItemTop > 0;
                state.top = activeItemTop + scrollParentRect.top - rects[index].height;
              } else {
                state.active = false;
              }
            });
          }
          selectActiveIndex = "";
        };
        const init = () => {
          (0, import_vue.nextTick)(onScroll);
        };
        (0, import_use.useEventListener)("scroll", onScroll, {
          target: scrollParent,
          passive: true
        });
        (0, import_vue.onMounted)(init);
        (0, import_vue.watch)(() => props.indexList, init);
        (0, import_vue.watch)(activeAnchor, (value) => {
          if (value) {
            emit("change", value);
          }
        });
        const renderIndexes = () => props.indexList.map((index) => {
          const active = index === activeAnchor.value;
          return (0, import_vue.createVNode)("span", {
            "class": bem("index", {
              active
            }),
            "style": active ? highlightStyle.value : void 0,
            "data-index": index
          }, [index]);
        });
        const scrollTo = (index) => {
          selectActiveIndex = String(index);
          const match = getMatchAnchor(selectActiveIndex);
          if (match) {
            const scrollTop = (0, import_utils.getScrollTop)(scrollParent.value);
            const scrollParentRect = (0, import_use.useRect)(scrollParent);
            const {
              offsetHeight
            } = document.documentElement;
            match.$el.scrollIntoView();
            if (scrollTop === offsetHeight - scrollParentRect.height) {
              onScroll();
              return;
            }
            if (props.sticky && props.stickyOffsetTop) {
              if ((0, import_utils.getRootScrollTop)() === offsetHeight - scrollParentRect.height) {
                (0, import_utils.setRootScrollTop)((0, import_utils.getRootScrollTop)());
              } else {
                (0, import_utils.setRootScrollTop)((0, import_utils.getRootScrollTop)() - props.stickyOffsetTop);
              }
            }
            emit("select", match.index);
          }
        };
        const scrollToElement = (element) => {
          const {
            index
          } = element.dataset;
          if (index) {
            scrollTo(index);
          }
        };
        const onClickSidebar = (event) => {
          scrollToElement(event.target);
        };
        let touchActiveIndex;
        const onTouchMove = (event) => {
          touch.move(event);
          if (touch.isVertical()) {
            (0, import_utils.preventDefault)(event);
            const {
              clientX,
              clientY
            } = event.touches[0];
            const target = document.elementFromPoint(clientX, clientY);
            if (target) {
              const {
                index
              } = target.dataset;
              if (index && touchActiveIndex !== index) {
                touchActiveIndex = index;
                scrollToElement(target);
              }
            }
          }
        };
        const renderSidebar = () => (0, import_vue.createVNode)("div", {
          "ref": sidebar,
          "class": bem("sidebar"),
          "style": sidebarStyle.value,
          "onClick": onClickSidebar,
          "onTouchstartPassive": touch.start
        }, [renderIndexes()]);
        (0, import_use_expose.useExpose)({
          scrollTo
        });
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: sidebar
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "class": bem()
          }, [props.teleport ? (0, import_vue.createVNode)(import_vue.Teleport, {
            "to": props.teleport
          }, {
            default: () => [renderSidebar()]
          }) : renderSidebar(), (_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index-anchor/IndexAnchor.js
var require_IndexAnchor = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index-anchor/IndexAnchor.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      indexAnchorProps: () => indexAnchorProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_IndexBar = require_IndexBar();
    var import_dom = require_dom();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("index-anchor");
    var indexAnchorProps = {
      index: import_utils.numericProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: indexAnchorProps,
      setup(props, {
        slots
      }) {
        const state = (0, import_vue.reactive)({
          top: 0,
          left: null,
          rect: {
            top: 0,
            height: 0
          },
          width: null,
          active: false
        });
        const root = (0, import_vue.ref)();
        const {
          parent
        } = (0, import_use.useParent)(import_IndexBar.INDEX_BAR_KEY);
        if (!parent) {
          if (true) {
            console.error("[Vant] <IndexAnchor> must be a child component of <IndexBar>.");
          }
          return;
        }
        const isSticky = () => state.active && parent.props.sticky;
        const anchorStyle = (0, import_vue.computed)(() => {
          const {
            zIndex,
            highlightColor
          } = parent.props;
          if (isSticky()) {
            return (0, import_utils.extend)((0, import_utils.getZIndexStyle)(zIndex), {
              left: state.left ? `${state.left}px` : void 0,
              width: state.width ? `${state.width}px` : void 0,
              transform: state.top ? `translate3d(0, ${state.top}px, 0)` : void 0,
              color: highlightColor
            });
          }
        });
        const getRect = (scrollParent, scrollParentRect) => {
          const rootRect = (0, import_use.useRect)(root);
          state.rect.height = rootRect.height;
          if (scrollParent === window || scrollParent === document.body) {
            state.rect.top = rootRect.top + (0, import_dom.getRootScrollTop)();
          } else {
            state.rect.top = rootRect.top + (0, import_dom.getScrollTop)(scrollParent) - scrollParentRect.top;
          }
          return state.rect;
        };
        (0, import_use_expose.useExpose)({
          state,
          getRect
        });
        return () => {
          const sticky = isSticky();
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "style": {
              height: sticky ? `${state.rect.height}px` : void 0
            }
          }, [(0, import_vue.createVNode)("div", {
            "style": anchorStyle.value,
            "class": [bem({
              sticky
            }), {
              [import_utils.BORDER_BOTTOM]: sticky
            }]
          }, [slots.default ? slots.default() : props.index])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index-anchor/index.js
var require_index_anchor = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index-anchor/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      IndexAnchor: () => IndexAnchor,
      default: () => stdin_default,
      indexAnchorProps: () => import_IndexAnchor2.indexAnchorProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_IndexAnchor = __toESM(require_IndexAnchor());
    var import_IndexAnchor2 = require_IndexAnchor();
    var IndexAnchor = (0, import_utils.withInstall)(import_IndexAnchor.default);
    var stdin_default = IndexAnchor;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index-bar/index.js
var require_index_bar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index-bar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      IndexBar: () => IndexBar,
      default: () => stdin_default,
      indexBarProps: () => import_IndexBar2.indexBarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_IndexBar = __toESM(require_IndexBar());
    var import_IndexBar2 = require_IndexBar();
    var IndexBar = (0, import_utils.withInstall)(import_IndexBar.default);
    var stdin_default = IndexBar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/list/List.js
var require_List = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/list/List.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      listProps: () => listProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_use_tab_status = require_use_tab_status();
    var import_loading = require_loading();
    var [name, bem, t] = (0, import_utils.createNamespace)("list");
    var listProps = {
      error: Boolean,
      offset: (0, import_utils.makeNumericProp)(300),
      loading: Boolean,
      disabled: Boolean,
      finished: Boolean,
      scroller: Object,
      errorText: String,
      direction: (0, import_utils.makeStringProp)("down"),
      loadingText: {
        type: String,
        default: ""
      },
      finishedText: String,
      immediateCheck: import_utils.truthProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: listProps,
      emits: ["load", "update:error", "update:loading"],
      setup(props, {
        emit,
        slots
      }) {
        const loading = (0, import_vue.ref)(props.loading);
        const root = (0, import_vue.ref)();
        const placeholder = (0, import_vue.ref)();
        const tabStatus = (0, import_use_tab_status.useAllTabStatus)();
        const scrollParent = (0, import_use.useScrollParent)(root);
        const scroller = (0, import_vue.computed)(() => props.scroller || scrollParent.value);
        const check = () => {
          (0, import_vue.nextTick)(() => {
            if (loading.value || props.finished || props.disabled || props.error || // skip check when inside an inactive tab
            (tabStatus == null ? void 0 : tabStatus.value) === false) {
              return;
            }
            const {
              direction
            } = props;
            const offset = +props.offset;
            const scrollParentRect = (0, import_use.useRect)(scroller);
            if (!scrollParentRect.height || (0, import_utils.isHidden)(root)) {
              return;
            }
            let isReachEdge = false;
            const placeholderRect = (0, import_use.useRect)(placeholder);
            if (direction === "up") {
              isReachEdge = scrollParentRect.top - placeholderRect.top <= offset;
            } else {
              isReachEdge = placeholderRect.bottom - scrollParentRect.bottom <= offset;
            }
            if (isReachEdge) {
              loading.value = true;
              emit("update:loading", true);
              emit("load");
            }
          });
        };
        const renderFinishedText = () => {
          if (props.finished) {
            const text = slots.finished ? slots.finished() : props.finishedText;
            if (text) {
              return (0, import_vue.createVNode)("div", {
                "class": bem("finished-text")
              }, [text]);
            }
          }
        };
        const clickErrorText = () => {
          emit("update:error", false);
          check();
        };
        const renderErrorText = () => {
          if (props.error) {
            const text = slots.error ? slots.error() : props.errorText;
            if (text) {
              return (0, import_vue.createVNode)("div", {
                "role": "button",
                "class": bem("error-text"),
                "tabindex": 0,
                "onClick": clickErrorText
              }, [text]);
            }
          }
        };
        const renderLoading = () => {
          if (loading.value && !props.finished && !props.disabled) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("loading")
            }, [slots.loading ? slots.loading() : props.loadingText != null && (0, import_vue.createVNode)(import_loading.Loading, {
              "class": bem("loading-icon")
            }, {
              default: () => [props.loadingText || t("loading")]
            })]);
          }
        };
        (0, import_vue.watch)(() => [props.loading, props.finished, props.error], check);
        if (tabStatus) {
          (0, import_vue.watch)(tabStatus, (tabActive) => {
            if (tabActive) {
              check();
            }
          });
        }
        (0, import_vue.onUpdated)(() => {
          loading.value = props.loading;
        });
        (0, import_vue.onMounted)(() => {
          if (props.immediateCheck) {
            check();
          }
        });
        (0, import_use_expose.useExpose)({
          check
        });
        (0, import_use.useEventListener)("scroll", check, {
          target: scroller,
          passive: true
        });
        return () => {
          var _a;
          const Content = (_a = slots.default) == null ? void 0 : _a.call(slots);
          const Placeholder = (0, import_vue.createVNode)("div", {
            "ref": placeholder,
            "class": bem("placeholder")
          }, null);
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "role": "feed",
            "class": bem(),
            "aria-busy": loading.value
          }, [props.direction === "down" ? Content : Placeholder, renderLoading(), renderFinishedText(), renderErrorText(), props.direction === "up" ? Content : Placeholder]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/list/index.js
var require_list = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/list/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      List: () => List,
      default: () => stdin_default,
      listProps: () => import_List2.listProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_List = __toESM(require_List());
    var import_List2 = require_List();
    var List = (0, import_utils.withInstall)(import_List.default);
    var stdin_default = List;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/nav-bar/NavBar.js
var require_NavBar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/nav-bar/NavBar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      navBarProps: () => navBarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_placeholder = require_use_placeholder();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("nav-bar");
    var navBarProps = {
      title: String,
      fixed: Boolean,
      zIndex: import_utils.numericProp,
      border: import_utils.truthProp,
      leftText: String,
      rightText: String,
      leftDisabled: Boolean,
      rightDisabled: Boolean,
      leftArrow: Boolean,
      placeholder: Boolean,
      safeAreaInsetTop: Boolean,
      clickable: import_utils.truthProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: navBarProps,
      emits: ["clickLeft", "clickRight"],
      setup(props, {
        emit,
        slots
      }) {
        const navBarRef = (0, import_vue.ref)();
        const renderPlaceholder = (0, import_use_placeholder.usePlaceholder)(navBarRef, bem);
        const onClickLeft = (event) => {
          if (!props.leftDisabled) {
            emit("clickLeft", event);
          }
        };
        const onClickRight = (event) => {
          if (!props.rightDisabled) {
            emit("clickRight", event);
          }
        };
        const renderLeft = () => {
          if (slots.left) {
            return slots.left();
          }
          return [props.leftArrow && (0, import_vue.createVNode)(import_icon.Icon, {
            "class": bem("arrow"),
            "name": "arrow-left"
          }, null), props.leftText && (0, import_vue.createVNode)("span", {
            "class": bem("text")
          }, [props.leftText])];
        };
        const renderRight = () => {
          if (slots.right) {
            return slots.right();
          }
          return (0, import_vue.createVNode)("span", {
            "class": bem("text")
          }, [props.rightText]);
        };
        const renderNavBar = () => {
          const {
            title,
            fixed,
            border,
            zIndex
          } = props;
          const style = (0, import_utils.getZIndexStyle)(zIndex);
          const hasLeft = props.leftArrow || props.leftText || slots.left;
          const hasRight = props.rightText || slots.right;
          return (0, import_vue.createVNode)("div", {
            "ref": navBarRef,
            "style": style,
            "class": [bem({
              fixed
            }), {
              [import_utils.BORDER_BOTTOM]: border,
              "van-safe-area-top": props.safeAreaInsetTop
            }]
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("content")
          }, [hasLeft && (0, import_vue.createVNode)("div", {
            "class": [bem("left", {
              disabled: props.leftDisabled
            }), props.clickable && !props.leftDisabled ? import_utils.HAPTICS_FEEDBACK : ""],
            "onClick": onClickLeft
          }, [renderLeft()]), (0, import_vue.createVNode)("div", {
            "class": [bem("title"), "van-ellipsis"]
          }, [slots.title ? slots.title() : title]), hasRight && (0, import_vue.createVNode)("div", {
            "class": [bem("right", {
              disabled: props.rightDisabled
            }), props.clickable && !props.rightDisabled ? import_utils.HAPTICS_FEEDBACK : ""],
            "onClick": onClickRight
          }, [renderRight()])])]);
        };
        return () => {
          if (props.fixed && props.placeholder) {
            return renderPlaceholder(renderNavBar);
          }
          return renderNavBar();
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/nav-bar/index.js
var require_nav_bar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/nav-bar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      NavBar: () => NavBar,
      default: () => stdin_default,
      navBarProps: () => import_NavBar2.navBarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_NavBar = __toESM(require_NavBar());
    var import_NavBar2 = require_NavBar();
    var NavBar = (0, import_utils.withInstall)(import_NavBar.default);
    var stdin_default = NavBar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notice-bar/NoticeBar.js
var require_NoticeBar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notice-bar/NoticeBar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      noticeBarProps: () => noticeBarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_on_popup_reopen = require_on_popup_reopen();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("notice-bar");
    var noticeBarProps = {
      text: String,
      mode: String,
      color: String,
      delay: (0, import_utils.makeNumericProp)(1),
      speed: (0, import_utils.makeNumericProp)(60),
      leftIcon: String,
      wrapable: Boolean,
      background: String,
      scrollable: {
        type: Boolean,
        default: null
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: noticeBarProps,
      emits: ["close", "replay"],
      setup(props, {
        emit,
        slots
      }) {
        let wrapWidth = 0;
        let contentWidth = 0;
        let startTimer;
        const wrapRef = (0, import_vue.ref)();
        const contentRef = (0, import_vue.ref)();
        const state = (0, import_vue.reactive)({
          show: true,
          offset: 0,
          duration: 0
        });
        const renderLeftIcon = () => {
          if (slots["left-icon"]) {
            return slots["left-icon"]();
          }
          if (props.leftIcon) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "class": bem("left-icon"),
              "name": props.leftIcon
            }, null);
          }
        };
        const getRightIconName = () => {
          if (props.mode === "closeable") {
            return "cross";
          }
          if (props.mode === "link") {
            return "arrow";
          }
        };
        const onClickRightIcon = (event) => {
          if (props.mode === "closeable") {
            state.show = false;
            emit("close", event);
          }
        };
        const renderRightIcon = () => {
          if (slots["right-icon"]) {
            return slots["right-icon"]();
          }
          const name2 = getRightIconName();
          if (name2) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "name": name2,
              "class": bem("right-icon"),
              "onClick": onClickRightIcon
            }, null);
          }
        };
        const onTransitionEnd = () => {
          state.offset = wrapWidth;
          state.duration = 0;
          (0, import_use.raf)(() => {
            (0, import_use.doubleRaf)(() => {
              state.offset = -contentWidth;
              state.duration = (contentWidth + wrapWidth) / +props.speed;
              emit("replay");
            });
          });
        };
        const renderMarquee = () => {
          const ellipsis = props.scrollable === false && !props.wrapable;
          const style = {
            transform: state.offset ? `translateX(${state.offset}px)` : "",
            transitionDuration: `${state.duration}s`
          };
          return (0, import_vue.createVNode)("div", {
            "ref": wrapRef,
            "role": "marquee",
            "class": bem("wrap")
          }, [(0, import_vue.createVNode)("div", {
            "ref": contentRef,
            "style": style,
            "class": [bem("content"), {
              "van-ellipsis": ellipsis
            }],
            "onTransitionend": onTransitionEnd
          }, [slots.default ? slots.default() : props.text])]);
        };
        const reset = () => {
          const {
            delay,
            speed,
            scrollable
          } = props;
          const ms = (0, import_utils.isDef)(delay) ? +delay * 1e3 : 0;
          wrapWidth = 0;
          contentWidth = 0;
          state.offset = 0;
          state.duration = 0;
          clearTimeout(startTimer);
          startTimer = setTimeout(() => {
            if (!wrapRef.value || !contentRef.value || scrollable === false) {
              return;
            }
            const wrapRefWidth = (0, import_use.useRect)(wrapRef).width;
            const contentRefWidth = (0, import_use.useRect)(contentRef).width;
            if (scrollable || contentRefWidth > wrapRefWidth) {
              (0, import_use.doubleRaf)(() => {
                wrapWidth = wrapRefWidth;
                contentWidth = contentRefWidth;
                state.offset = -contentWidth;
                state.duration = contentWidth / +speed;
              });
            }
          }, ms);
        };
        (0, import_on_popup_reopen.onPopupReopen)(reset);
        (0, import_use.onMountedOrActivated)(reset);
        (0, import_use.useEventListener)("pageshow", reset);
        (0, import_use_expose.useExpose)({
          reset
        });
        (0, import_vue.watch)(() => [props.text, props.scrollable], reset);
        return () => {
          const {
            color,
            wrapable,
            background
          } = props;
          return (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", {
            "role": "alert",
            "class": bem({
              wrapable
            }),
            "style": {
              color,
              background
            }
          }, [renderLeftIcon(), renderMarquee(), renderRightIcon()]), [[import_vue.vShow, state.show]]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notice-bar/index.js
var require_notice_bar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notice-bar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      NoticeBar: () => NoticeBar,
      default: () => stdin_default,
      noticeBarProps: () => import_NoticeBar2.noticeBarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_NoticeBar = __toESM(require_NoticeBar());
    var import_NoticeBar2 = require_NoticeBar();
    var NoticeBar = (0, import_utils.withInstall)(import_NoticeBar.default);
    var stdin_default = NoticeBar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notify/Notify.js
var require_Notify = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notify/Notify.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      notifyProps: () => notifyProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_popup = require_popup();
    var import_shared = require_shared();
    var [name, bem] = (0, import_utils.createNamespace)("notify");
    var popupInheritProps = ["lockScroll", "position", "show", "teleport", "zIndex"];
    var notifyProps = (0, import_utils.extend)({}, import_shared.popupSharedProps, {
      type: (0, import_utils.makeStringProp)("danger"),
      color: String,
      message: import_utils.numericProp,
      position: (0, import_utils.makeStringProp)("top"),
      className: import_utils.unknownProp,
      background: String,
      lockScroll: Boolean
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: notifyProps,
      emits: ["update:show"],
      setup(props, {
        emit,
        slots
      }) {
        const updateShow = (show) => emit("update:show", show);
        return () => (0, import_vue.createVNode)(import_popup.Popup, (0, import_vue.mergeProps)({
          "class": [bem([props.type]), props.className],
          "style": {
            color: props.color,
            background: props.background
          },
          "overlay": false,
          "duration": 0.2,
          "onUpdate:show": updateShow
        }, (0, import_utils.pick)(props, popupInheritProps)), {
          default: () => [slots.default ? slots.default() : props.message]
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notify/function-call.js
var require_function_call4 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notify/function-call.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      closeNotify: () => closeNotify,
      resetNotifyDefaultOptions: () => resetNotifyDefaultOptions,
      setNotifyDefaultOptions: () => setNotifyDefaultOptions,
      showNotify: () => showNotify
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_mount_component = require_mount_component();
    var import_Notify = __toESM(require_Notify());
    var timer;
    var instance;
    var parseOptions = (message) => (0, import_utils.isObject)(message) ? message : {
      message
    };
    function initInstance() {
      ({
        instance
      } = (0, import_mount_component.mountComponent)({
        setup() {
          const {
            state,
            toggle
          } = (0, import_mount_component.usePopupState)();
          return () => (0, import_vue.createVNode)(import_Notify.default, (0, import_vue.mergeProps)(state, {
            "onUpdate:show": toggle
          }), null);
        }
      }));
    }
    var getDefaultOptions = () => ({
      type: "danger",
      color: void 0,
      message: "",
      onClose: void 0,
      onClick: void 0,
      onOpened: void 0,
      duration: 3e3,
      position: void 0,
      className: "",
      lockScroll: false,
      background: void 0
    });
    var currentOptions = getDefaultOptions();
    var closeNotify = () => {
      if (instance) {
        instance.toggle(false);
      }
    };
    function showNotify(options) {
      if (!import_utils.inBrowser) {
        return;
      }
      if (!instance) {
        initInstance();
      }
      options = (0, import_utils.extend)({}, currentOptions, parseOptions(options));
      instance.open(options);
      clearTimeout(timer);
      if (options.duration > 0) {
        timer = setTimeout(closeNotify, options.duration);
      }
      return instance;
    }
    var setNotifyDefaultOptions = (options) => (0, import_utils.extend)(currentOptions, options);
    var resetNotifyDefaultOptions = () => {
      currentOptions = getDefaultOptions();
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notify/index.js
var require_notify = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/notify/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Notify: () => Notify,
      closeNotify: () => import_function_call.closeNotify,
      default: () => stdin_default,
      notifyProps: () => import_Notify2.notifyProps,
      resetNotifyDefaultOptions: () => import_function_call.resetNotifyDefaultOptions,
      setNotifyDefaultOptions: () => import_function_call.setNotifyDefaultOptions,
      showNotify: () => import_function_call.showNotify
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Notify = __toESM(require_Notify());
    var import_Notify2 = require_Notify();
    var import_function_call = require_function_call4();
    var Notify = (0, import_utils.withInstall)(import_Notify.default);
    var stdin_default = Notify;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/number-keyboard/NumberKeyboardKey.js
var require_NumberKeyboardKey = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/number-keyboard/NumberKeyboardKey.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_touch = require_use_touch();
    var import_loading = require_loading();
    var [name, bem] = (0, import_utils.createNamespace)("key");
    var CollapseIcon = (0, import_vue.createVNode)("svg", {
      "class": bem("collapse-icon"),
      "viewBox": "0 0 30 24"
    }, [(0, import_vue.createVNode)("path", {
      "d": "M26 13h-2v2h2v-2zm-8-3h2V8h-2v2zm2-4h2V4h-2v2zm2 4h4V4h-2v4h-2v2zm-7 14 3-3h-6l3 3zM6 13H4v2h2v-2zm16 0H8v2h14v-2zm-12-3h2V8h-2v2zM28 0l1 1 1 1v15l-1 2H1l-1-2V2l1-1 1-1zm0 2H2v15h26V2zM6 4v2H4V4zm10 2h2V4h-2v2zM8 9v1H4V8zm8 0v1h-2V8zm-6-5v2H8V4zm4 0v2h-2V4z",
      "fill": "currentColor"
    }, null)]);
    var DeleteIcon = (0, import_vue.createVNode)("svg", {
      "class": bem("delete-icon"),
      "viewBox": "0 0 32 22"
    }, [(0, import_vue.createVNode)("path", {
      "d": "M28 0a4 4 0 0 1 4 4v14a4 4 0 0 1-4 4H10.4a2 2 0 0 1-1.4-.6L1 13.1c-.6-.5-.9-1.3-.9-2 0-1 .3-1.7.9-2.2L9 .6a2 2 0 0 1 1.4-.6zm0 2H10.4l-8.2 8.3a1 1 0 0 0-.3.7c0 .3.1.5.3.7l8.2 8.4H28a2 2 0 0 0 2-2V4c0-1.1-.9-2-2-2zm-5 4a1 1 0 0 1 .7.3 1 1 0 0 1 0 1.4L20.4 11l3.3 3.3c.2.2.3.5.3.7 0 .3-.1.5-.3.7a1 1 0 0 1-.7.3 1 1 0 0 1-.7-.3L19 12.4l-3.4 3.3a1 1 0 0 1-.6.3 1 1 0 0 1-.7-.3 1 1 0 0 1-.3-.7c0-.2.1-.5.3-.7l3.3-3.3-3.3-3.3A1 1 0 0 1 14 7c0-.3.1-.5.3-.7A1 1 0 0 1 15 6a1 1 0 0 1 .6.3L19 9.6l3.3-3.3A1 1 0 0 1 23 6z",
      "fill": "currentColor"
    }, null)]);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: {
        type: String,
        text: import_utils.numericProp,
        color: String,
        wider: Boolean,
        large: Boolean,
        loading: Boolean
      },
      emits: ["press"],
      setup(props, {
        emit,
        slots
      }) {
        const active = (0, import_vue.ref)(false);
        const touch = (0, import_use_touch.useTouch)();
        const onTouchStart = (event) => {
          touch.start(event);
          active.value = true;
        };
        const onTouchMove = (event) => {
          touch.move(event);
          if (touch.direction.value) {
            active.value = false;
          }
        };
        const onTouchEnd = (event) => {
          if (active.value) {
            if (!slots.default) {
              (0, import_utils.preventDefault)(event);
            }
            active.value = false;
            emit("press", props.text, props.type);
          }
        };
        const renderContent = () => {
          if (props.loading) {
            return (0, import_vue.createVNode)(import_loading.Loading, {
              "class": bem("loading-icon")
            }, null);
          }
          const text = slots.default ? slots.default() : props.text;
          switch (props.type) {
            case "delete":
              return text || DeleteIcon;
            case "extra":
              return text || CollapseIcon;
            default:
              return text;
          }
        };
        return () => (0, import_vue.createVNode)("div", {
          "class": bem("wrapper", {
            wider: props.wider
          }),
          "onTouchstartPassive": onTouchStart,
          "onTouchmovePassive": onTouchMove,
          "onTouchend": onTouchEnd,
          "onTouchcancel": onTouchEnd
        }, [(0, import_vue.createVNode)("div", {
          "role": "button",
          "tabindex": 0,
          "class": bem([props.color, {
            large: props.large,
            active: active.value,
            delete: props.type === "delete"
          }])
        }, [renderContent()])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/number-keyboard/NumberKeyboard.js
var require_NumberKeyboard = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/number-keyboard/NumberKeyboard.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      numberKeyboardProps: () => numberKeyboardProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_NumberKeyboardKey = __toESM(require_NumberKeyboardKey());
    var [name, bem] = (0, import_utils.createNamespace)("number-keyboard");
    var numberKeyboardProps = {
      show: Boolean,
      title: String,
      theme: (0, import_utils.makeStringProp)("default"),
      zIndex: import_utils.numericProp,
      teleport: [String, Object],
      maxlength: (0, import_utils.makeNumericProp)(Infinity),
      modelValue: (0, import_utils.makeStringProp)(""),
      transition: import_utils.truthProp,
      blurOnClose: import_utils.truthProp,
      showDeleteKey: import_utils.truthProp,
      randomKeyOrder: Boolean,
      closeButtonText: String,
      deleteButtonText: String,
      closeButtonLoading: Boolean,
      hideOnClickOutside: import_utils.truthProp,
      safeAreaInsetBottom: import_utils.truthProp,
      extraKey: {
        type: [String, Array],
        default: ""
      }
    };
    function shuffle(array) {
      for (let i = array.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        const temp = array[i];
        array[i] = array[j];
        array[j] = temp;
      }
      return array;
    }
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      inheritAttrs: false,
      props: numberKeyboardProps,
      emits: ["show", "hide", "blur", "input", "close", "delete", "update:modelValue"],
      setup(props, {
        emit,
        slots,
        attrs
      }) {
        const root = (0, import_vue.ref)();
        const genBasicKeys = () => {
          const keys2 = Array(9).fill("").map((_, i) => ({
            text: i + 1
          }));
          if (props.randomKeyOrder) {
            shuffle(keys2);
          }
          return keys2;
        };
        const genDefaultKeys = () => [...genBasicKeys(), {
          text: props.extraKey,
          type: "extra"
        }, {
          text: 0
        }, {
          text: props.showDeleteKey ? props.deleteButtonText : "",
          type: props.showDeleteKey ? "delete" : ""
        }];
        const genCustomKeys = () => {
          const keys2 = genBasicKeys();
          const {
            extraKey
          } = props;
          const extraKeys = Array.isArray(extraKey) ? extraKey : [extraKey];
          if (extraKeys.length === 0) {
            keys2.push({
              text: 0,
              wider: true
            });
          } else if (extraKeys.length === 1) {
            keys2.push({
              text: 0,
              wider: true
            }, {
              text: extraKeys[0],
              type: "extra"
            });
          } else if (extraKeys.length === 2) {
            keys2.push({
              text: extraKeys[0],
              type: "extra"
            }, {
              text: 0
            }, {
              text: extraKeys[1],
              type: "extra"
            });
          }
          return keys2;
        };
        const keys = (0, import_vue.computed)(() => props.theme === "custom" ? genCustomKeys() : genDefaultKeys());
        const onBlur = () => {
          if (props.show) {
            emit("blur");
          }
        };
        const onClose = () => {
          emit("close");
          if (props.blurOnClose) {
            onBlur();
          }
        };
        const onAnimationEnd = () => emit(props.show ? "show" : "hide");
        const onPress = (text, type) => {
          if (text === "") {
            if (type === "extra") {
              onBlur();
            }
            return;
          }
          const value = props.modelValue;
          if (type === "delete") {
            emit("delete");
            emit("update:modelValue", value.slice(0, value.length - 1));
          } else if (type === "close") {
            onClose();
          } else if (value.length < +props.maxlength) {
            emit("input", text);
            emit("update:modelValue", value + text);
          }
        };
        const renderTitle = () => {
          const {
            title,
            theme,
            closeButtonText
          } = props;
          const leftSlot = slots["title-left"];
          const showClose = closeButtonText && theme === "default";
          const showTitle = title || showClose || leftSlot;
          if (!showTitle) {
            return;
          }
          return (0, import_vue.createVNode)("div", {
            "class": bem("header")
          }, [leftSlot && (0, import_vue.createVNode)("span", {
            "class": bem("title-left")
          }, [leftSlot()]), title && (0, import_vue.createVNode)("h2", {
            "class": bem("title")
          }, [title]), showClose && (0, import_vue.createVNode)("button", {
            "type": "button",
            "class": [bem("close"), import_utils.HAPTICS_FEEDBACK],
            "onClick": onClose
          }, [closeButtonText])]);
        };
        const renderKeys = () => keys.value.map((key) => {
          const keySlots = {};
          if (key.type === "delete") {
            keySlots.default = slots.delete;
          }
          if (key.type === "extra") {
            keySlots.default = slots["extra-key"];
          }
          return (0, import_vue.createVNode)(import_NumberKeyboardKey.default, {
            "key": key.text,
            "text": key.text,
            "type": key.type,
            "wider": key.wider,
            "color": key.color,
            "onPress": onPress
          }, keySlots);
        });
        const renderSidebar = () => {
          if (props.theme === "custom") {
            return (0, import_vue.createVNode)("div", {
              "class": bem("sidebar")
            }, [props.showDeleteKey && (0, import_vue.createVNode)(import_NumberKeyboardKey.default, {
              "large": true,
              "text": props.deleteButtonText,
              "type": "delete",
              "onPress": onPress
            }, {
              default: slots.delete
            }), (0, import_vue.createVNode)(import_NumberKeyboardKey.default, {
              "large": true,
              "text": props.closeButtonText,
              "type": "close",
              "color": "blue",
              "loading": props.closeButtonLoading,
              "onPress": onPress
            }, null)]);
          }
        };
        (0, import_vue.watch)(() => props.show, (value) => {
          if (!props.transition) {
            emit(value ? "show" : "hide");
          }
        });
        if (props.hideOnClickOutside) {
          (0, import_use.useClickAway)(root, onBlur, {
            eventName: "touchstart"
          });
        }
        return () => {
          const Title = renderTitle();
          const Content = (0, import_vue.createVNode)(import_vue.Transition, {
            "name": props.transition ? "van-slide-up" : ""
          }, {
            default: () => [(0, import_vue.withDirectives)((0, import_vue.createVNode)("div", (0, import_vue.mergeProps)({
              "ref": root,
              "style": (0, import_utils.getZIndexStyle)(props.zIndex),
              "class": bem({
                unfit: !props.safeAreaInsetBottom,
                "with-title": !!Title
              }),
              "onAnimationend": onAnimationEnd,
              "onTouchstartPassive": import_utils.stopPropagation
            }, attrs), [Title, (0, import_vue.createVNode)("div", {
              "class": bem("body")
            }, [(0, import_vue.createVNode)("div", {
              "class": bem("keys")
            }, [renderKeys()]), renderSidebar()])]), [[import_vue.vShow, props.show]])]
          });
          if (props.teleport) {
            return (0, import_vue.createVNode)(import_vue.Teleport, {
              "to": props.teleport
            }, {
              default: () => [Content]
            });
          }
          return Content;
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/number-keyboard/index.js
var require_number_keyboard = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/number-keyboard/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      NumberKeyboard: () => NumberKeyboard,
      default: () => stdin_default,
      numberKeyboardProps: () => import_NumberKeyboard2.numberKeyboardProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_NumberKeyboard = __toESM(require_NumberKeyboard());
    var import_NumberKeyboard2 = require_NumberKeyboard();
    var NumberKeyboard = (0, import_utils.withInstall)(import_NumberKeyboard.default);
    var stdin_default = NumberKeyboard;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/pagination/Pagination.js
var require_Pagination = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/pagination/Pagination.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      paginationProps: () => paginationProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem, t] = (0, import_utils.createNamespace)("pagination");
    var makePage = (number, text, active) => ({
      number,
      text,
      active
    });
    var paginationProps = {
      mode: (0, import_utils.makeStringProp)("multi"),
      prevText: String,
      nextText: String,
      pageCount: (0, import_utils.makeNumericProp)(0),
      modelValue: (0, import_utils.makeNumberProp)(0),
      totalItems: (0, import_utils.makeNumericProp)(0),
      showPageSize: (0, import_utils.makeNumericProp)(5),
      itemsPerPage: (0, import_utils.makeNumericProp)(10),
      forceEllipses: Boolean,
      showPrevButton: import_utils.truthProp,
      showNextButton: import_utils.truthProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: paginationProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const count = (0, import_vue.computed)(() => {
          const {
            pageCount,
            totalItems,
            itemsPerPage
          } = props;
          const count2 = +pageCount || Math.ceil(+totalItems / +itemsPerPage);
          return Math.max(1, count2);
        });
        const pages = (0, import_vue.computed)(() => {
          const items = [];
          const pageCount = count.value;
          const showPageSize = +props.showPageSize;
          const {
            modelValue,
            forceEllipses
          } = props;
          let startPage = 1;
          let endPage = pageCount;
          const isMaxSized = showPageSize < pageCount;
          if (isMaxSized) {
            startPage = Math.max(modelValue - Math.floor(showPageSize / 2), 1);
            endPage = startPage + showPageSize - 1;
            if (endPage > pageCount) {
              endPage = pageCount;
              startPage = endPage - showPageSize + 1;
            }
          }
          for (let number = startPage; number <= endPage; number++) {
            const page = makePage(number, number, number === modelValue);
            items.push(page);
          }
          if (isMaxSized && showPageSize > 0 && forceEllipses) {
            if (startPage > 1) {
              const prevPages = makePage(startPage - 1, "...");
              items.unshift(prevPages);
            }
            if (endPage < pageCount) {
              const nextPages = makePage(endPage + 1, "...");
              items.push(nextPages);
            }
          }
          return items;
        });
        const updateModelValue = (value, emitChange) => {
          value = (0, import_utils.clamp)(value, 1, count.value);
          if (props.modelValue !== value) {
            emit("update:modelValue", value);
            if (emitChange) {
              emit("change", value);
            }
          }
        };
        (0, import_vue.watchEffect)(() => updateModelValue(props.modelValue));
        const renderDesc = () => (0, import_vue.createVNode)("li", {
          "class": bem("page-desc")
        }, [slots.pageDesc ? slots.pageDesc() : `${props.modelValue}/${count.value}`]);
        const renderPrevButton = () => {
          const {
            mode,
            modelValue,
            showPrevButton
          } = props;
          if (!showPrevButton) {
            return;
          }
          const slot = slots["prev-text"];
          const disabled = modelValue === 1;
          return (0, import_vue.createVNode)("li", {
            "class": [bem("item", {
              disabled,
              border: mode === "simple",
              prev: true
            }), import_utils.BORDER_SURROUND]
          }, [(0, import_vue.createVNode)("button", {
            "type": "button",
            "disabled": disabled,
            "onClick": () => updateModelValue(modelValue - 1, true)
          }, [slot ? slot() : props.prevText || t("prev")])]);
        };
        const renderNextButton = () => {
          const {
            mode,
            modelValue,
            showNextButton
          } = props;
          if (!showNextButton) {
            return;
          }
          const slot = slots["next-text"];
          const disabled = modelValue === count.value;
          return (0, import_vue.createVNode)("li", {
            "class": [bem("item", {
              disabled,
              border: mode === "simple",
              next: true
            }), import_utils.BORDER_SURROUND]
          }, [(0, import_vue.createVNode)("button", {
            "type": "button",
            "disabled": disabled,
            "onClick": () => updateModelValue(modelValue + 1, true)
          }, [slot ? slot() : props.nextText || t("next")])]);
        };
        const renderPages = () => pages.value.map((page) => (0, import_vue.createVNode)("li", {
          "class": [bem("item", {
            active: page.active,
            page: true
          }), import_utils.BORDER_SURROUND]
        }, [(0, import_vue.createVNode)("button", {
          "type": "button",
          "aria-current": page.active || void 0,
          "onClick": () => updateModelValue(page.number, true)
        }, [slots.page ? slots.page(page) : page.text])]));
        return () => (0, import_vue.createVNode)("nav", {
          "role": "navigation",
          "class": bem()
        }, [(0, import_vue.createVNode)("ul", {
          "class": bem("items")
        }, [renderPrevButton(), props.mode === "simple" ? renderDesc() : renderPages(), renderNextButton()])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/pagination/index.js
var require_pagination = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/pagination/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Pagination: () => Pagination,
      default: () => stdin_default,
      paginationProps: () => import_Pagination2.paginationProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Pagination = __toESM(require_Pagination());
    var import_Pagination2 = require_Pagination();
    var Pagination = (0, import_utils.withInstall)(import_Pagination.default);
    var stdin_default = Pagination;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/password-input/PasswordInput.js
var require_PasswordInput = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/password-input/PasswordInput.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      passwordInputProps: () => passwordInputProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("password-input");
    var passwordInputProps = {
      info: String,
      mask: import_utils.truthProp,
      value: (0, import_utils.makeStringProp)(""),
      gutter: import_utils.numericProp,
      length: (0, import_utils.makeNumericProp)(6),
      focused: Boolean,
      errorInfo: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: passwordInputProps,
      emits: ["focus"],
      setup(props, {
        emit
      }) {
        const onTouchStart = (event) => {
          event.stopPropagation();
          emit("focus", event);
        };
        const renderPoints = () => {
          const Points = [];
          const {
            mask,
            value,
            gutter,
            focused
          } = props;
          const length = +props.length;
          for (let i = 0; i < length; i++) {
            const char = value[i];
            const showBorder = i !== 0 && !gutter;
            const showCursor = focused && i === value.length;
            let style;
            if (i !== 0 && gutter) {
              style = {
                marginLeft: (0, import_utils.addUnit)(gutter)
              };
            }
            Points.push((0, import_vue.createVNode)("li", {
              "class": [{
                [import_utils.BORDER_LEFT]: showBorder
              }, bem("item", {
                focus: showCursor
              })],
              "style": style
            }, [mask ? (0, import_vue.createVNode)("i", {
              "style": {
                visibility: char ? "visible" : "hidden"
              }
            }, null) : char, showCursor && (0, import_vue.createVNode)("div", {
              "class": bem("cursor")
            }, null)]));
          }
          return Points;
        };
        return () => {
          const info = props.errorInfo || props.info;
          return (0, import_vue.createVNode)("div", {
            "class": bem()
          }, [(0, import_vue.createVNode)("ul", {
            "class": [bem("security"), {
              [import_utils.BORDER_SURROUND]: !props.gutter
            }],
            "onTouchstartPassive": onTouchStart
          }, [renderPoints()]), info && (0, import_vue.createVNode)("div", {
            "class": bem(props.errorInfo ? "error-info" : "info")
          }, [info])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/password-input/index.js
var require_password_input = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/password-input/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      PasswordInput: () => PasswordInput,
      default: () => stdin_default,
      passwordInputProps: () => import_PasswordInput2.passwordInputProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_PasswordInput = __toESM(require_PasswordInput());
    var import_PasswordInput2 = require_PasswordInput();
    var PasswordInput = (0, import_utils.withInstall)(import_PasswordInput.default);
    var stdin_default = PasswordInput;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker-group/index.js
var require_picker_group = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/picker-group/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      PickerGroup: () => PickerGroup,
      default: () => stdin_default,
      pickerGroupProps: () => import_PickerGroup2.pickerGroupProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_PickerGroup = __toESM(require_PickerGroup());
    var import_PickerGroup2 = require_PickerGroup();
    var PickerGroup = (0, import_utils.withInstall)(import_PickerGroup.default);
    var stdin_default = PickerGroup;
  }
});

// ../../node_modules/.pnpm/@vant+popperjs@1.3.0/node_modules/@vant/popperjs/dist/index.cjs.js
var require_index_cjs2 = __commonJS({
  "../../node_modules/.pnpm/@vant+popperjs@1.3.0/node_modules/@vant/popperjs/dist/index.cjs.js"(exports, module) {
    "use strict";
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var src_exports = {};
    __export(src_exports, {
      createPopper: () => createPopper,
      offsetModifier: () => offset_default
    });
    module.exports = __toCommonJS2(src_exports);
    function getWindow(node) {
      if (node == null) {
        return window;
      }
      if (node.toString() !== "[object Window]") {
        var ownerDocument = node.ownerDocument;
        return ownerDocument ? ownerDocument.defaultView || window : window;
      }
      return node;
    }
    function isElement(node) {
      var OwnElement = getWindow(node).Element;
      return node instanceof OwnElement || node instanceof Element;
    }
    function isHTMLElement(node) {
      var OwnElement = getWindow(node).HTMLElement;
      return node instanceof OwnElement || node instanceof HTMLElement;
    }
    function isShadowRoot(node) {
      if (typeof ShadowRoot === "undefined") {
        return false;
      }
      var OwnElement = getWindow(node).ShadowRoot;
      return node instanceof OwnElement || node instanceof ShadowRoot;
    }
    var round = Math.round;
    function getUAString() {
      var uaData = navigator.userAgentData;
      if (uaData != null && uaData.brands) {
        return uaData.brands.map(function(item) {
          return item.brand + "/" + item.version;
        }).join(" ");
      }
      return navigator.userAgent;
    }
    function isLayoutViewport() {
      return !/^((?!chrome|android).)*safari/i.test(getUAString());
    }
    function getBoundingClientRect(element, includeScale, isFixedStrategy) {
      if (includeScale === void 0) {
        includeScale = false;
      }
      if (isFixedStrategy === void 0) {
        isFixedStrategy = false;
      }
      var clientRect = element.getBoundingClientRect();
      var scaleX = 1;
      var scaleY = 1;
      if (includeScale && isHTMLElement(element)) {
        scaleX = element.offsetWidth > 0 ? round(clientRect.width) / element.offsetWidth || 1 : 1;
        scaleY = element.offsetHeight > 0 ? round(clientRect.height) / element.offsetHeight || 1 : 1;
      }
      var _ref = isElement(element) ? getWindow(element) : window, visualViewport = _ref.visualViewport;
      var addVisualOffsets = !isLayoutViewport() && isFixedStrategy;
      var x = (clientRect.left + (addVisualOffsets && visualViewport ? visualViewport.offsetLeft : 0)) / scaleX;
      var y = (clientRect.top + (addVisualOffsets && visualViewport ? visualViewport.offsetTop : 0)) / scaleY;
      var width = clientRect.width / scaleX;
      var height = clientRect.height / scaleY;
      return {
        width,
        height,
        top: y,
        right: x + width,
        bottom: y + height,
        left: x,
        x,
        y
      };
    }
    function getWindowScroll(node) {
      var win = getWindow(node);
      var scrollLeft = win.pageXOffset;
      var scrollTop = win.pageYOffset;
      return {
        scrollLeft,
        scrollTop
      };
    }
    function getHTMLElementScroll(element) {
      return {
        scrollLeft: element.scrollLeft,
        scrollTop: element.scrollTop
      };
    }
    function getNodeScroll(node) {
      if (node === getWindow(node) || !isHTMLElement(node)) {
        return getWindowScroll(node);
      } else {
        return getHTMLElementScroll(node);
      }
    }
    function getNodeName(element) {
      return element ? (element.nodeName || "").toLowerCase() : null;
    }
    function getDocumentElement(element) {
      return ((isElement(element) ? element.ownerDocument : element.document) || window.document).documentElement;
    }
    function getWindowScrollBarX(element) {
      return getBoundingClientRect(getDocumentElement(element)).left + getWindowScroll(element).scrollLeft;
    }
    function getComputedStyle(element) {
      return getWindow(element).getComputedStyle(element);
    }
    function isScrollParent(element) {
      var _getComputedStyle = getComputedStyle(element), overflow = _getComputedStyle.overflow, overflowX = _getComputedStyle.overflowX, overflowY = _getComputedStyle.overflowY;
      return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
    }
    function isElementScaled(element) {
      var rect = element.getBoundingClientRect();
      var scaleX = round(rect.width) / element.offsetWidth || 1;
      var scaleY = round(rect.height) / element.offsetHeight || 1;
      return scaleX !== 1 || scaleY !== 1;
    }
    function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
      if (isFixed === void 0) {
        isFixed = false;
      }
      var isOffsetParentAnElement = isHTMLElement(offsetParent);
      var offsetParentIsScaled = isHTMLElement(offsetParent) && isElementScaled(offsetParent);
      var documentElement = getDocumentElement(offsetParent);
      var rect = getBoundingClientRect(elementOrVirtualElement, offsetParentIsScaled, isFixed);
      var scroll = {
        scrollLeft: 0,
        scrollTop: 0
      };
      var offsets = {
        x: 0,
        y: 0
      };
      if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
        if (getNodeName(offsetParent) !== "body" || isScrollParent(documentElement)) {
          scroll = getNodeScroll(offsetParent);
        }
        if (isHTMLElement(offsetParent)) {
          offsets = getBoundingClientRect(offsetParent, true);
          offsets.x += offsetParent.clientLeft;
          offsets.y += offsetParent.clientTop;
        } else if (documentElement) {
          offsets.x = getWindowScrollBarX(documentElement);
        }
      }
      return {
        x: rect.left + scroll.scrollLeft - offsets.x,
        y: rect.top + scroll.scrollTop - offsets.y,
        width: rect.width,
        height: rect.height
      };
    }
    function getLayoutRect(element) {
      var clientRect = getBoundingClientRect(element);
      var width = element.offsetWidth;
      var height = element.offsetHeight;
      if (Math.abs(clientRect.width - width) <= 1) {
        width = clientRect.width;
      }
      if (Math.abs(clientRect.height - height) <= 1) {
        height = clientRect.height;
      }
      return {
        x: element.offsetLeft,
        y: element.offsetTop,
        width,
        height
      };
    }
    function getParentNode(element) {
      if (getNodeName(element) === "html") {
        return element;
      }
      return element.assignedSlot || element.parentNode || (isShadowRoot(element) ? element.host : null) || getDocumentElement(element);
    }
    function getScrollParent(node) {
      if (["html", "body", "#document"].indexOf(getNodeName(node)) >= 0) {
        return node.ownerDocument.body;
      }
      if (isHTMLElement(node) && isScrollParent(node)) {
        return node;
      }
      return getScrollParent(getParentNode(node));
    }
    function listScrollParents(element, list) {
      var _element$ownerDocumen;
      if (list === void 0) {
        list = [];
      }
      var scrollParent = getScrollParent(element);
      var isBody = scrollParent === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
      var win = getWindow(scrollParent);
      var target = isBody ? [win].concat(win.visualViewport || [], isScrollParent(scrollParent) ? scrollParent : []) : scrollParent;
      var updatedList = list.concat(target);
      return isBody ? updatedList : updatedList.concat(listScrollParents(getParentNode(target)));
    }
    function isTableElement(element) {
      return ["table", "td", "th"].indexOf(getNodeName(element)) >= 0;
    }
    function getTrueOffsetParent(element) {
      if (!isHTMLElement(element) || getComputedStyle(element).position === "fixed") {
        return null;
      }
      return element.offsetParent;
    }
    function getContainingBlock(element) {
      var isFirefox = /firefox/i.test(getUAString());
      var isIE = /Trident/i.test(getUAString());
      if (isIE && isHTMLElement(element)) {
        var elementCss = getComputedStyle(element);
        if (elementCss.position === "fixed") {
          return null;
        }
      }
      var currentNode = getParentNode(element);
      if (isShadowRoot(currentNode)) {
        currentNode = currentNode.host;
      }
      while (isHTMLElement(currentNode) && ["html", "body"].indexOf(getNodeName(currentNode)) < 0) {
        var css = getComputedStyle(currentNode);
        if (css.transform !== "none" || css.perspective !== "none" || css.contain === "paint" || ["transform", "perspective"].indexOf(css.willChange) !== -1 || isFirefox && css.willChange === "filter" || isFirefox && css.filter && css.filter !== "none") {
          return currentNode;
        } else {
          currentNode = currentNode.parentNode;
        }
      }
      return null;
    }
    function getOffsetParent(element) {
      var window2 = getWindow(element);
      var offsetParent = getTrueOffsetParent(element);
      while (offsetParent && isTableElement(offsetParent) && getComputedStyle(offsetParent).position === "static") {
        offsetParent = getTrueOffsetParent(offsetParent);
      }
      if (offsetParent && (getNodeName(offsetParent) === "html" || getNodeName(offsetParent) === "body" && getComputedStyle(offsetParent).position === "static")) {
        return window2;
      }
      return offsetParent || getContainingBlock(element) || window2;
    }
    var top = "top";
    var bottom = "bottom";
    var right = "right";
    var left = "left";
    var auto = "auto";
    var basePlacements = [top, bottom, right, left];
    var start = "start";
    var end = "end";
    var placements = [].concat(basePlacements, [auto]).reduce(function(acc, placement) {
      return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
    }, []);
    var beforeRead = "beforeRead";
    var read = "read";
    var afterRead = "afterRead";
    var beforeMain = "beforeMain";
    var main = "main";
    var afterMain = "afterMain";
    var beforeWrite = "beforeWrite";
    var write = "write";
    var afterWrite = "afterWrite";
    var modifierPhases = [beforeRead, read, afterRead, beforeMain, main, afterMain, beforeWrite, write, afterWrite];
    function order(modifiers) {
      var map = /* @__PURE__ */ new Map();
      var visited = /* @__PURE__ */ new Set();
      var result = [];
      modifiers.forEach(function(modifier) {
        map.set(modifier.name, modifier);
      });
      function sort(modifier) {
        visited.add(modifier.name);
        var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
        requires.forEach(function(dep) {
          if (!visited.has(dep)) {
            var depModifier = map.get(dep);
            if (depModifier) {
              sort(depModifier);
            }
          }
        });
        result.push(modifier);
      }
      modifiers.forEach(function(modifier) {
        if (!visited.has(modifier.name)) {
          sort(modifier);
        }
      });
      return result;
    }
    function orderModifiers(modifiers) {
      var orderedModifiers = order(modifiers);
      return modifierPhases.reduce(function(acc, phase) {
        return acc.concat(orderedModifiers.filter(function(modifier) {
          return modifier.phase === phase;
        }));
      }, []);
    }
    function debounce(fn2) {
      var pending;
      return function() {
        if (!pending) {
          pending = new Promise(function(resolve) {
            Promise.resolve().then(function() {
              pending = void 0;
              resolve(fn2());
            });
          });
        }
        return pending;
      };
    }
    function format(str) {
      for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
      }
      return [].concat(args).reduce(function(p, c) {
        return p.replace(/%s/, c);
      }, str);
    }
    var INVALID_MODIFIER_ERROR = 'Popper: modifier "%s" provided an invalid %s property, expected %s but got %s';
    var MISSING_DEPENDENCY_ERROR = 'Popper: modifier "%s" requires "%s", but "%s" modifier is not available';
    var VALID_PROPERTIES = ["name", "enabled", "phase", "fn", "effect", "requires", "options"];
    function validateModifiers(modifiers) {
      modifiers.forEach(function(modifier) {
        [].concat(Object.keys(modifier), VALID_PROPERTIES).filter(function(value, index, self) {
          return self.indexOf(value) === index;
        }).forEach(function(key) {
          switch (key) {
            case "name":
              if (typeof modifier.name !== "string") {
                console.error(format(INVALID_MODIFIER_ERROR, String(modifier.name), '"name"', '"string"', '"' + String(modifier.name) + '"'));
              }
              break;
            case "enabled":
              if (typeof modifier.enabled !== "boolean") {
                console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"enabled"', '"boolean"', '"' + String(modifier.enabled) + '"'));
              }
              break;
            case "phase":
              if (modifierPhases.indexOf(modifier.phase) < 0) {
                console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"phase"', "either " + modifierPhases.join(", "), '"' + String(modifier.phase) + '"'));
              }
              break;
            case "fn":
              if (typeof modifier.fn !== "function") {
                console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"fn"', '"function"', '"' + String(modifier.fn) + '"'));
              }
              break;
            case "effect":
              if (modifier.effect != null && typeof modifier.effect !== "function") {
                console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"effect"', '"function"', '"' + String(modifier.fn) + '"'));
              }
              break;
            case "requires":
              if (modifier.requires != null && !Array.isArray(modifier.requires)) {
                console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"requires"', '"array"', '"' + String(modifier.requires) + '"'));
              }
              break;
            case "requiresIfExists":
              if (!Array.isArray(modifier.requiresIfExists)) {
                console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"requiresIfExists"', '"array"', '"' + String(modifier.requiresIfExists) + '"'));
              }
              break;
            case "options":
            case "data":
              break;
            default:
              console.error('PopperJS: an invalid property has been provided to the "' + modifier.name + '" modifier, valid properties are ' + VALID_PROPERTIES.map(function(s) {
                return '"' + s + '"';
              }).join(", ") + '; but "' + key + '" was provided.');
          }
          modifier.requires && modifier.requires.forEach(function(requirement) {
            if (modifiers.find(function(mod) {
              return mod.name === requirement;
            }) == null) {
              console.error(format(MISSING_DEPENDENCY_ERROR, String(modifier.name), requirement, requirement));
            }
          });
        });
      });
    }
    function uniqueBy(arr, fn2) {
      var identifiers = /* @__PURE__ */ new Set();
      return arr.filter(function(item) {
        var identifier = fn2(item);
        if (!identifiers.has(identifier)) {
          identifiers.add(identifier);
          return true;
        }
      });
    }
    function getBasePlacement(placement) {
      return placement.split("-")[0];
    }
    function mergeByName(modifiers) {
      var merged = modifiers.reduce(function(merged2, current) {
        var existing = merged2[current.name];
        merged2[current.name] = existing ? Object.assign({}, existing, current, {
          options: Object.assign({}, existing.options, current.options),
          data: Object.assign({}, existing.data, current.data)
        }) : current;
        return merged2;
      }, {});
      return Object.keys(merged).map(function(key) {
        return merged[key];
      });
    }
    function getVariation(placement) {
      return placement.split("-")[1];
    }
    function getMainAxisFromPlacement(placement) {
      return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
    }
    function computeOffsets(_ref) {
      var reference = _ref.reference, element = _ref.element, placement = _ref.placement;
      var basePlacement = placement ? getBasePlacement(placement) : null;
      var variation = placement ? getVariation(placement) : null;
      var commonX = reference.x + reference.width / 2 - element.width / 2;
      var commonY = reference.y + reference.height / 2 - element.height / 2;
      var offsets;
      switch (basePlacement) {
        case top:
          offsets = {
            x: commonX,
            y: reference.y - element.height
          };
          break;
        case bottom:
          offsets = {
            x: commonX,
            y: reference.y + reference.height
          };
          break;
        case right:
          offsets = {
            x: reference.x + reference.width,
            y: commonY
          };
          break;
        case left:
          offsets = {
            x: reference.x - element.width,
            y: commonY
          };
          break;
        default:
          offsets = {
            x: reference.x,
            y: reference.y
          };
      }
      var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
      if (mainAxis != null) {
        var len = mainAxis === "y" ? "height" : "width";
        switch (variation) {
          case start:
            offsets[mainAxis] = offsets[mainAxis] - (reference[len] / 2 - element[len] / 2);
            break;
          case end:
            offsets[mainAxis] = offsets[mainAxis] + (reference[len] / 2 - element[len] / 2);
            break;
          default:
        }
      }
      return offsets;
    }
    var INVALID_ELEMENT_ERROR = "Popper: Invalid reference or popper argument provided. They must be either a DOM element or virtual element.";
    var INFINITE_LOOP_ERROR = "Popper: An infinite loop in the modifiers cycle has been detected! The cycle has been interrupted to prevent a browser crash.";
    var DEFAULT_OPTIONS = {
      placement: "bottom",
      modifiers: [],
      strategy: "absolute"
    };
    function areValidElements() {
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      return !args.some(function(element) {
        return !(element && typeof element.getBoundingClientRect === "function");
      });
    }
    function popperGenerator(generatorOptions) {
      if (generatorOptions === void 0) {
        generatorOptions = {};
      }
      var _generatorOptions = generatorOptions, _generatorOptions$def = _generatorOptions.defaultModifiers, defaultModifiers2 = _generatorOptions$def === void 0 ? [] : _generatorOptions$def, _generatorOptions$def2 = _generatorOptions.defaultOptions, defaultOptions = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
      return function createPopper2(reference, popper, options) {
        if (options === void 0) {
          options = defaultOptions;
        }
        var state = {
          placement: "bottom",
          orderedModifiers: [],
          options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions),
          modifiersData: {},
          elements: {
            reference,
            popper
          },
          attributes: {},
          styles: {}
        };
        var effectCleanupFns = [];
        var isDestroyed = false;
        var instance = {
          state,
          setOptions: function setOptions(setOptionsAction) {
            var options2 = typeof setOptionsAction === "function" ? setOptionsAction(state.options) : setOptionsAction;
            cleanupModifierEffects();
            state.options = Object.assign({}, defaultOptions, state.options, options2);
            state.scrollParents = {
              reference: isElement(reference) ? listScrollParents(reference) : reference.contextElement ? listScrollParents(reference.contextElement) : [],
              popper: listScrollParents(popper)
            };
            var orderedModifiers = orderModifiers(mergeByName([].concat(defaultModifiers2, state.options.modifiers)));
            state.orderedModifiers = orderedModifiers.filter(function(m) {
              return m.enabled;
            });
            if (true) {
              var modifiers = uniqueBy([].concat(orderedModifiers, state.options.modifiers), function(_ref) {
                var name = _ref.name;
                return name;
              });
              validateModifiers(modifiers);
              if (getBasePlacement(state.options.placement) === auto) {
                var flipModifier = state.orderedModifiers.find(function(_ref2) {
                  var name = _ref2.name;
                  return name === "flip";
                });
                if (!flipModifier) {
                  console.error(['Popper: "auto" placements require the "flip" modifier be', "present and enabled to work."].join(" "));
                }
              }
              var _getComputedStyle = getComputedStyle(popper), marginTop = _getComputedStyle.marginTop, marginRight = _getComputedStyle.marginRight, marginBottom = _getComputedStyle.marginBottom, marginLeft = _getComputedStyle.marginLeft;
              if ([marginTop, marginRight, marginBottom, marginLeft].some(function(margin) {
                return parseFloat(margin);
              })) {
                console.warn(['Popper: CSS "margin" styles cannot be used to apply padding', "between the popper and its reference element or boundary.", "To replicate margin, use the `offset` modifier, as well as", "the `padding` option in the `preventOverflow` and `flip`", "modifiers."].join(" "));
              }
            }
            runModifierEffects();
            return instance.update();
          },
          forceUpdate: function forceUpdate() {
            if (isDestroyed) {
              return;
            }
            var _state$elements = state.elements, reference2 = _state$elements.reference, popper2 = _state$elements.popper;
            if (!areValidElements(reference2, popper2)) {
              if (true) {
                console.error(INVALID_ELEMENT_ERROR);
              }
              return;
            }
            state.rects = {
              reference: getCompositeRect(reference2, getOffsetParent(popper2), state.options.strategy === "fixed"),
              popper: getLayoutRect(popper2)
            };
            state.reset = false;
            state.placement = state.options.placement;
            state.orderedModifiers.forEach(function(modifier) {
              return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
            });
            var __debug_loops__ = 0;
            for (var index = 0; index < state.orderedModifiers.length; index++) {
              if (true) {
                __debug_loops__ += 1;
                if (__debug_loops__ > 100) {
                  console.error(INFINITE_LOOP_ERROR);
                  break;
                }
              }
              if (state.reset === true) {
                state.reset = false;
                index = -1;
                continue;
              }
              var _state$orderedModifie = state.orderedModifiers[index], fn2 = _state$orderedModifie.fn, _state$orderedModifie2 = _state$orderedModifie.options, _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2, name = _state$orderedModifie.name;
              if (typeof fn2 === "function") {
                state = fn2({
                  state,
                  options: _options,
                  name,
                  instance
                }) || state;
              }
            }
          },
          update: debounce(function() {
            return new Promise(function(resolve) {
              instance.forceUpdate();
              resolve(state);
            });
          }),
          destroy: function destroy() {
            cleanupModifierEffects();
            isDestroyed = true;
          }
        };
        if (!areValidElements(reference, popper)) {
          if (true) {
            console.error(INVALID_ELEMENT_ERROR);
          }
          return instance;
        }
        instance.setOptions(options).then(function(state2) {
          if (!isDestroyed && options.onFirstUpdate) {
            options.onFirstUpdate(state2);
          }
        });
        function runModifierEffects() {
          state.orderedModifiers.forEach(function(_ref3) {
            var name = _ref3.name, _ref3$options = _ref3.options, options2 = _ref3$options === void 0 ? {} : _ref3$options, effect3 = _ref3.effect;
            if (typeof effect3 === "function") {
              var cleanupFn = effect3({
                state,
                name,
                instance,
                options: options2
              });
              var noopFn = function noopFn2() {
              };
              effectCleanupFns.push(cleanupFn || noopFn);
            }
          });
        }
        function cleanupModifierEffects() {
          effectCleanupFns.forEach(function(fn2) {
            return fn2();
          });
          effectCleanupFns = [];
        }
        return instance;
      };
    }
    var passive = {
      passive: true
    };
    function effect(_ref) {
      var state = _ref.state, instance = _ref.instance, options = _ref.options;
      var _options$scroll = options.scroll, scroll = _options$scroll === void 0 ? true : _options$scroll, _options$resize = options.resize, resize = _options$resize === void 0 ? true : _options$resize;
      var window2 = getWindow(state.elements.popper);
      var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);
      if (scroll) {
        scrollParents.forEach(function(scrollParent) {
          scrollParent.addEventListener("scroll", instance.update, passive);
        });
      }
      if (resize) {
        window2.addEventListener("resize", instance.update, passive);
      }
      return function() {
        if (scroll) {
          scrollParents.forEach(function(scrollParent) {
            scrollParent.removeEventListener("scroll", instance.update, passive);
          });
        }
        if (resize) {
          window2.removeEventListener("resize", instance.update, passive);
        }
      };
    }
    var eventListeners_default = {
      name: "eventListeners",
      enabled: true,
      phase: "write",
      fn: function fn() {
      },
      effect,
      data: {}
    };
    function popperOffsets(_ref) {
      var state = _ref.state, name = _ref.name;
      state.modifiersData[name] = computeOffsets({
        reference: state.rects.reference,
        element: state.rects.popper,
        strategy: "absolute",
        placement: state.placement
      });
    }
    var popperOffsets_default = {
      name: "popperOffsets",
      enabled: true,
      phase: "read",
      fn: popperOffsets,
      data: {}
    };
    var unsetSides = {
      top: "auto",
      right: "auto",
      bottom: "auto",
      left: "auto"
    };
    function roundOffsetsByDPR(_ref) {
      var x = _ref.x, y = _ref.y;
      var win = window;
      var dpr = win.devicePixelRatio || 1;
      return {
        x: round(x * dpr) / dpr || 0,
        y: round(y * dpr) / dpr || 0
      };
    }
    function mapToStyles(_ref2) {
      var _Object$assign2;
      var popper = _ref2.popper, popperRect = _ref2.popperRect, placement = _ref2.placement, variation = _ref2.variation, offsets = _ref2.offsets, position = _ref2.position, gpuAcceleration = _ref2.gpuAcceleration, adaptive = _ref2.adaptive, roundOffsets = _ref2.roundOffsets, isFixed = _ref2.isFixed;
      var _offsets$x = offsets.x, x = _offsets$x === void 0 ? 0 : _offsets$x, _offsets$y = offsets.y, y = _offsets$y === void 0 ? 0 : _offsets$y;
      var _ref3 = typeof roundOffsets === "function" ? roundOffsets({
        x,
        y
      }) : {
        x,
        y
      };
      x = _ref3.x;
      y = _ref3.y;
      var hasX = offsets.hasOwnProperty("x");
      var hasY = offsets.hasOwnProperty("y");
      var sideX = left;
      var sideY = top;
      var win = window;
      if (adaptive) {
        var offsetParent = getOffsetParent(popper);
        var heightProp = "clientHeight";
        var widthProp = "clientWidth";
        if (offsetParent === getWindow(popper)) {
          offsetParent = getDocumentElement(popper);
          if (getComputedStyle(offsetParent).position !== "static" && position === "absolute") {
            heightProp = "scrollHeight";
            widthProp = "scrollWidth";
          }
        }
        offsetParent = offsetParent;
        if (placement === top || (placement === left || placement === right) && variation === end) {
          sideY = bottom;
          var offsetY = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.height : offsetParent[heightProp];
          y -= offsetY - popperRect.height;
          y *= gpuAcceleration ? 1 : -1;
        }
        if (placement === left || (placement === top || placement === bottom) && variation === end) {
          sideX = right;
          var offsetX = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.width : offsetParent[widthProp];
          x -= offsetX - popperRect.width;
          x *= gpuAcceleration ? 1 : -1;
        }
      }
      var commonStyles = Object.assign({
        position
      }, adaptive && unsetSides);
      var _ref4 = roundOffsets === true ? roundOffsetsByDPR({
        x,
        y
      }) : {
        x,
        y
      };
      x = _ref4.x;
      y = _ref4.y;
      if (gpuAcceleration) {
        var _Object$assign;
        return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? "0" : "", _Object$assign[sideX] = hasX ? "0" : "", _Object$assign.transform = (win.devicePixelRatio || 1) <= 1 ? "translate(" + x + "px, " + y + "px)" : "translate3d(" + x + "px, " + y + "px, 0)", _Object$assign));
      }
      return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y + "px" : "", _Object$assign2[sideX] = hasX ? x + "px" : "", _Object$assign2.transform = "", _Object$assign2));
    }
    function computeStyles(_ref5) {
      var state = _ref5.state, options = _ref5.options;
      var _options$gpuAccelerat = options.gpuAcceleration, gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat, _options$adaptive = options.adaptive, adaptive = _options$adaptive === void 0 ? true : _options$adaptive, _options$roundOffsets = options.roundOffsets, roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;
      if (true) {
        var transitionProperty = getComputedStyle(state.elements.popper).transitionProperty || "";
        if (adaptive && ["transform", "top", "right", "bottom", "left"].some(function(property) {
          return transitionProperty.indexOf(property) >= 0;
        })) {
          console.warn(["Popper: Detected CSS transitions on at least one of the following", 'CSS properties: "transform", "top", "right", "bottom", "left".', "\n\n", 'Disable the "computeStyles" modifier\'s `adaptive` option to allow', "for smooth transitions, or remove these properties from the CSS", "transition declaration on the popper element if only transitioning", "opacity or background-color for example.", "\n\n", "We recommend using the popper element as a wrapper around an inner", "element that can have any CSS property transitioned for animations."].join(" "));
        }
      }
      var commonStyles = {
        placement: getBasePlacement(state.placement),
        variation: getVariation(state.placement),
        popper: state.elements.popper,
        popperRect: state.rects.popper,
        gpuAcceleration,
        isFixed: state.options.strategy === "fixed"
      };
      if (state.modifiersData.popperOffsets != null) {
        state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
          offsets: state.modifiersData.popperOffsets,
          position: state.options.strategy,
          adaptive,
          roundOffsets
        })));
      }
      if (state.modifiersData.arrow != null) {
        state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
          offsets: state.modifiersData.arrow,
          position: "absolute",
          adaptive: false,
          roundOffsets
        })));
      }
      state.attributes.popper = Object.assign({}, state.attributes.popper, {
        "data-popper-placement": state.placement
      });
    }
    var computeStyles_default = {
      name: "computeStyles",
      enabled: true,
      phase: "beforeWrite",
      fn: computeStyles,
      data: {}
    };
    function applyStyles(_ref) {
      var state = _ref.state;
      Object.keys(state.elements).forEach(function(name) {
        var style = state.styles[name] || {};
        var attributes = state.attributes[name] || {};
        var element = state.elements[name];
        if (!isHTMLElement(element) || !getNodeName(element)) {
          return;
        }
        Object.assign(element.style, style);
        Object.keys(attributes).forEach(function(name2) {
          var value = attributes[name2];
          if (value === false) {
            element.removeAttribute(name2);
          } else {
            element.setAttribute(name2, value === true ? "" : value);
          }
        });
      });
    }
    function effect2(_ref2) {
      var state = _ref2.state;
      var initialStyles = {
        popper: {
          position: state.options.strategy,
          left: "0",
          top: "0",
          margin: "0"
        },
        arrow: {
          position: "absolute"
        },
        reference: {}
      };
      Object.assign(state.elements.popper.style, initialStyles.popper);
      state.styles = initialStyles;
      if (state.elements.arrow) {
        Object.assign(state.elements.arrow.style, initialStyles.arrow);
      }
      return function() {
        Object.keys(state.elements).forEach(function(name) {
          var element = state.elements[name];
          var attributes = state.attributes[name] || {};
          var styleProperties = Object.keys(state.styles.hasOwnProperty(name) ? state.styles[name] : initialStyles[name]);
          var style = styleProperties.reduce(function(style2, property) {
            style2[property] = "";
            return style2;
          }, {});
          if (!isHTMLElement(element) || !getNodeName(element)) {
            return;
          }
          Object.assign(element.style, style);
          Object.keys(attributes).forEach(function(attribute) {
            element.removeAttribute(attribute);
          });
        });
      };
    }
    var applyStyles_default = {
      name: "applyStyles",
      enabled: true,
      phase: "write",
      fn: applyStyles,
      effect: effect2,
      requires: ["computeStyles"]
    };
    var defaultModifiers = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default];
    var createPopper = popperGenerator({
      defaultModifiers
    });
    function distanceAndSkiddingToXY(placement, rects, offset2) {
      var basePlacement = getBasePlacement(placement);
      var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
      var _ref = typeof offset2 === "function" ? offset2(Object.assign({}, rects, {
        placement
      })) : offset2, skidding = _ref[0], distance = _ref[1];
      skidding = skidding || 0;
      distance = (distance || 0) * invertDistance;
      return [left, right].indexOf(basePlacement) >= 0 ? {
        x: distance,
        y: skidding
      } : {
        x: skidding,
        y: distance
      };
    }
    function offset(_ref2) {
      var state = _ref2.state, options = _ref2.options, name = _ref2.name;
      var _options$offset = options.offset, offset2 = _options$offset === void 0 ? [0, 0] : _options$offset;
      var data = placements.reduce(function(acc, placement) {
        acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset2);
        return acc;
      }, {});
      var _data$state$placement = data[state.placement], x = _data$state$placement.x, y = _data$state$placement.y;
      if (state.modifiersData.popperOffsets != null) {
        state.modifiersData.popperOffsets.x += x;
        state.modifiersData.popperOffsets.y += y;
      }
      state.modifiersData[name] = data;
    }
    var offset_default = {
      name: "offset",
      enabled: true,
      phase: "main",
      requires: ["popperOffsets"],
      fn: offset
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popover/Popover.js
var require_Popover = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popover/Popover.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      popoverProps: () => popoverProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_popperjs = require_index_cjs2();
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_scope_id = require_use_scope_id();
    var import_use_sync_prop_ref = require_use_sync_prop_ref();
    var import_icon = require_icon();
    var import_popup = require_popup();
    var [name, bem] = (0, import_utils.createNamespace)("popover");
    var popupProps = ["overlay", "duration", "teleport", "overlayStyle", "overlayClass", "closeOnClickOverlay"];
    var popoverProps = {
      show: Boolean,
      theme: (0, import_utils.makeStringProp)("light"),
      overlay: Boolean,
      actions: (0, import_utils.makeArrayProp)(),
      actionsDirection: (0, import_utils.makeStringProp)("vertical"),
      trigger: (0, import_utils.makeStringProp)("click"),
      duration: import_utils.numericProp,
      showArrow: import_utils.truthProp,
      placement: (0, import_utils.makeStringProp)("bottom"),
      iconPrefix: String,
      overlayClass: import_utils.unknownProp,
      overlayStyle: Object,
      closeOnClickAction: import_utils.truthProp,
      closeOnClickOverlay: import_utils.truthProp,
      closeOnClickOutside: import_utils.truthProp,
      offset: {
        type: Array,
        default: () => [0, 8]
      },
      teleport: {
        type: [String, Object],
        default: "body"
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: popoverProps,
      emits: ["select", "touchstart", "update:show"],
      setup(props, {
        emit,
        slots,
        attrs
      }) {
        let popper;
        const popupRef = (0, import_vue.ref)();
        const wrapperRef = (0, import_vue.ref)();
        const popoverRef = (0, import_vue.ref)();
        const show = (0, import_use_sync_prop_ref.useSyncPropRef)(() => props.show, (value) => emit("update:show", value));
        const getPopoverOptions = () => ({
          placement: props.placement,
          modifiers: [{
            name: "computeStyles",
            options: {
              adaptive: false,
              gpuAcceleration: false
            }
          }, (0, import_utils.extend)({}, import_popperjs.offsetModifier, {
            options: {
              offset: props.offset
            }
          })]
        });
        const createPopperInstance = () => {
          if (wrapperRef.value && popoverRef.value) {
            return (0, import_popperjs.createPopper)(wrapperRef.value, popoverRef.value.popupRef.value, getPopoverOptions());
          }
          return null;
        };
        const updateLocation = () => {
          (0, import_vue.nextTick)(() => {
            if (!show.value) {
              return;
            }
            if (!popper) {
              popper = createPopperInstance();
              if (import_utils.inBrowser) {
                window.addEventListener("animationend", updateLocation);
                window.addEventListener("transitionend", updateLocation);
              }
            } else {
              popper.setOptions(getPopoverOptions());
            }
          });
        };
        const updateShow = (value) => {
          show.value = value;
        };
        const onClickWrapper = () => {
          if (props.trigger === "click") {
            show.value = !show.value;
          }
        };
        const onClickAction = (action, index) => {
          if (action.disabled) {
            return;
          }
          emit("select", action, index);
          if (props.closeOnClickAction) {
            show.value = false;
          }
        };
        const onClickAway = () => {
          if (show.value && props.closeOnClickOutside && (!props.overlay || props.closeOnClickOverlay)) {
            show.value = false;
          }
        };
        const renderActionContent = (action, index) => {
          if (slots.action) {
            return slots.action({
              action,
              index
            });
          }
          return [action.icon && (0, import_vue.createVNode)(import_icon.Icon, {
            "name": action.icon,
            "classPrefix": props.iconPrefix,
            "class": bem("action-icon")
          }, null), (0, import_vue.createVNode)("div", {
            "class": [bem("action-text"), {
              [import_utils.BORDER_BOTTOM]: props.actionsDirection === "vertical"
            }]
          }, [action.text])];
        };
        const renderAction = (action, index) => {
          const {
            icon,
            color,
            disabled,
            className
          } = action;
          return (0, import_vue.createVNode)("div", {
            "role": "menuitem",
            "class": [bem("action", {
              disabled,
              "with-icon": icon
            }), {
              [import_utils.BORDER_RIGHT]: props.actionsDirection === "horizontal"
            }, className],
            "style": {
              color
            },
            "tabindex": disabled ? void 0 : 0,
            "aria-disabled": disabled || void 0,
            "onClick": () => onClickAction(action, index)
          }, [renderActionContent(action, index)]);
        };
        (0, import_vue.onMounted)(() => {
          updateLocation();
          (0, import_vue.watchEffect)(() => {
            var _a;
            popupRef.value = (_a = popoverRef.value) == null ? void 0 : _a.popupRef.value;
          });
        });
        (0, import_vue.onBeforeUnmount)(() => {
          if (popper) {
            if (import_utils.inBrowser) {
              window.removeEventListener("animationend", updateLocation);
              window.removeEventListener("transitionend", updateLocation);
            }
            popper.destroy();
            popper = null;
          }
        });
        (0, import_vue.watch)(() => [show.value, props.offset, props.placement], updateLocation);
        (0, import_use.useClickAway)([wrapperRef, popupRef], onClickAway, {
          eventName: "touchstart"
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)(import_vue.Fragment, null, [(0, import_vue.createVNode)("span", {
            "ref": wrapperRef,
            "class": bem("wrapper"),
            "onClick": onClickWrapper
          }, [(_a = slots.reference) == null ? void 0 : _a.call(slots)]), (0, import_vue.createVNode)(import_popup.Popup, (0, import_vue.mergeProps)({
            "ref": popoverRef,
            "show": show.value,
            "class": bem([props.theme]),
            "position": "",
            "transition": "van-popover-zoom",
            "lockScroll": false,
            "onUpdate:show": updateShow
          }, attrs, (0, import_use_scope_id.useScopeId)(), (0, import_utils.pick)(props, popupProps)), {
            default: () => [props.showArrow && (0, import_vue.createVNode)("div", {
              "class": bem("arrow")
            }, null), (0, import_vue.createVNode)("div", {
              "role": "menu",
              "class": bem("content", props.actionsDirection)
            }, [slots.default ? slots.default() : props.actions.map(renderAction)])]
          })]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popover/index.js
var require_popover = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/popover/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Popover: () => Popover,
      default: () => stdin_default,
      popoverProps: () => import_Popover2.popoverProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Popover = __toESM(require_Popover());
    var import_Popover2 = require_Popover();
    var Popover = (0, import_utils.withInstall)(import_Popover.default);
    var stdin_default = Popover;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/progress/Progress.js
var require_Progress = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/progress/Progress.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      progressProps: () => progressProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("progress");
    var progressProps = {
      color: String,
      inactive: Boolean,
      pivotText: String,
      textColor: String,
      showPivot: import_utils.truthProp,
      pivotColor: String,
      trackColor: String,
      strokeWidth: import_utils.numericProp,
      percentage: {
        type: import_utils.numericProp,
        default: 0,
        validator: (value) => +value >= 0 && +value <= 100
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: progressProps,
      setup(props, {
        slots
      }) {
        const background = (0, import_vue.computed)(() => props.inactive ? void 0 : props.color);
        const format = (rate) => Math.min(Math.max(+rate, 0), 100);
        const renderPivot = () => {
          const {
            textColor,
            pivotText,
            pivotColor,
            percentage
          } = props;
          const safePercentage = format(percentage);
          const text = pivotText != null ? pivotText : `${safePercentage}%`;
          if (props.showPivot && (slots.pivot || text)) {
            const style = {
              color: textColor,
              left: `${safePercentage}%`,
              transform: `translate(-${safePercentage}%,-50%)`,
              background: pivotColor || background.value
            };
            return (0, import_vue.createVNode)("span", {
              "style": style,
              "class": bem("pivot", {
                inactive: props.inactive
              })
            }, [slots.pivot ? slots.pivot({
              percentage: safePercentage
            }) : text]);
          }
        };
        return () => {
          const {
            trackColor,
            percentage,
            strokeWidth
          } = props;
          const safePercentage = format(percentage);
          const rootStyle = {
            background: trackColor,
            height: (0, import_utils.addUnit)(strokeWidth)
          };
          const portionStyle = {
            width: `${safePercentage}%`,
            background: background.value
          };
          return (0, import_vue.createVNode)("div", {
            "class": bem(),
            "style": rootStyle
          }, [(0, import_vue.createVNode)("span", {
            "class": bem("portion", {
              inactive: props.inactive
            }),
            "style": portionStyle
          }, null), renderPivot()]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/progress/index.js
var require_progress = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/progress/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Progress: () => Progress,
      default: () => stdin_default,
      progressProps: () => import_Progress2.progressProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Progress = __toESM(require_Progress());
    var import_Progress2 = require_Progress();
    var Progress = (0, import_utils.withInstall)(import_Progress.default);
    var stdin_default = Progress;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/pull-refresh/PullRefresh.js
var require_PullRefresh = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/pull-refresh/PullRefresh.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      pullRefreshProps: () => pullRefreshProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_touch = require_use_touch();
    var import_loading = require_loading();
    var [name, bem, t] = (0, import_utils.createNamespace)("pull-refresh");
    var DEFAULT_HEAD_HEIGHT = 50;
    var TEXT_STATUS = ["pulling", "loosing", "success"];
    var pullRefreshProps = {
      disabled: Boolean,
      modelValue: Boolean,
      headHeight: (0, import_utils.makeNumericProp)(DEFAULT_HEAD_HEIGHT),
      successText: String,
      pullingText: String,
      loosingText: String,
      loadingText: String,
      pullDistance: import_utils.numericProp,
      successDuration: (0, import_utils.makeNumericProp)(500),
      animationDuration: (0, import_utils.makeNumericProp)(300)
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: pullRefreshProps,
      emits: ["change", "refresh", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        let reachTop;
        const root = (0, import_vue.ref)();
        const track = (0, import_vue.ref)();
        const scrollParent = (0, import_use.useScrollParent)(root);
        const state = (0, import_vue.reactive)({
          status: "normal",
          distance: 0,
          duration: 0
        });
        const touch = (0, import_use_touch.useTouch)();
        const getHeadStyle = () => {
          if (props.headHeight !== DEFAULT_HEAD_HEIGHT) {
            return {
              height: `${props.headHeight}px`
            };
          }
        };
        const isTouchable = () => state.status !== "loading" && state.status !== "success" && !props.disabled;
        const ease = (distance) => {
          const pullDistance = +(props.pullDistance || props.headHeight);
          if (distance > pullDistance) {
            if (distance < pullDistance * 2) {
              distance = pullDistance + (distance - pullDistance) / 2;
            } else {
              distance = pullDistance * 1.5 + (distance - pullDistance * 2) / 4;
            }
          }
          return Math.round(distance);
        };
        const setStatus = (distance, isLoading) => {
          const pullDistance = +(props.pullDistance || props.headHeight);
          state.distance = distance;
          if (isLoading) {
            state.status = "loading";
          } else if (distance === 0) {
            state.status = "normal";
          } else if (distance < pullDistance) {
            state.status = "pulling";
          } else {
            state.status = "loosing";
          }
          emit("change", {
            status: state.status,
            distance
          });
        };
        const getStatusText = () => {
          const {
            status
          } = state;
          if (status === "normal") {
            return "";
          }
          return props[`${status}Text`] || t(status);
        };
        const renderStatus = () => {
          const {
            status,
            distance
          } = state;
          if (slots[status]) {
            return slots[status]({
              distance
            });
          }
          const nodes = [];
          if (TEXT_STATUS.includes(status)) {
            nodes.push((0, import_vue.createVNode)("div", {
              "class": bem("text")
            }, [getStatusText()]));
          }
          if (status === "loading") {
            nodes.push((0, import_vue.createVNode)(import_loading.Loading, {
              "class": bem("loading")
            }, {
              default: getStatusText
            }));
          }
          return nodes;
        };
        const showSuccessTip = () => {
          state.status = "success";
          setTimeout(() => {
            setStatus(0);
          }, +props.successDuration);
        };
        const checkPosition = (event) => {
          reachTop = (0, import_utils.getScrollTop)(scrollParent.value) === 0;
          if (reachTop) {
            state.duration = 0;
            touch.start(event);
          }
        };
        const onTouchStart = (event) => {
          if (isTouchable()) {
            checkPosition(event);
          }
        };
        const onTouchMove = (event) => {
          if (isTouchable()) {
            if (!reachTop) {
              checkPosition(event);
            }
            const {
              deltaY
            } = touch;
            touch.move(event);
            if (reachTop && deltaY.value >= 0 && touch.isVertical()) {
              (0, import_utils.preventDefault)(event);
              setStatus(ease(deltaY.value));
            }
          }
        };
        const onTouchEnd = () => {
          if (reachTop && touch.deltaY.value && isTouchable()) {
            state.duration = +props.animationDuration;
            if (state.status === "loosing") {
              setStatus(+props.headHeight, true);
              emit("update:modelValue", true);
              (0, import_vue.nextTick)(() => emit("refresh"));
            } else {
              setStatus(0);
            }
          }
        };
        (0, import_vue.watch)(() => props.modelValue, (value) => {
          state.duration = +props.animationDuration;
          if (value) {
            setStatus(+props.headHeight, true);
          } else if (slots.success || props.successText) {
            showSuccessTip();
          } else {
            setStatus(0, false);
          }
        });
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: track
        });
        return () => {
          var _a;
          const trackStyle = {
            transitionDuration: `${state.duration}ms`,
            transform: state.distance ? `translate3d(0,${state.distance}px, 0)` : ""
          };
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "class": bem()
          }, [(0, import_vue.createVNode)("div", {
            "ref": track,
            "class": bem("track"),
            "style": trackStyle,
            "onTouchstartPassive": onTouchStart,
            "onTouchend": onTouchEnd,
            "onTouchcancel": onTouchEnd
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("head"),
            "style": getHeadStyle()
          }, [renderStatus()]), (_a = slots.default) == null ? void 0 : _a.call(slots)])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/pull-refresh/index.js
var require_pull_refresh = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/pull-refresh/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      PullRefresh: () => PullRefresh,
      default: () => stdin_default,
      pullRefreshProps: () => import_PullRefresh2.pullRefreshProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_PullRefresh = __toESM(require_PullRefresh());
    var import_PullRefresh2 = require_PullRefresh();
    var PullRefresh = (0, import_utils.withInstall)(import_PullRefresh.default);
    var stdin_default = PullRefresh;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rate/Rate.js
var require_Rate = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rate/Rate.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      rateProps: () => rateProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_refs = require_use_refs();
    var import_use_touch = require_use_touch();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("rate");
    function getRateStatus(value, index, allowHalf, readonly) {
      if (value >= index) {
        return {
          status: "full",
          value: 1
        };
      }
      if (value + 0.5 >= index && allowHalf && !readonly) {
        return {
          status: "half",
          value: 0.5
        };
      }
      if (value + 1 >= index && allowHalf && readonly) {
        const cardinal = 10 ** 10;
        return {
          status: "half",
          value: Math.round((value - index + 1) * cardinal) / cardinal
        };
      }
      return {
        status: "void",
        value: 0
      };
    }
    var rateProps = {
      size: import_utils.numericProp,
      icon: (0, import_utils.makeStringProp)("star"),
      color: String,
      count: (0, import_utils.makeNumericProp)(5),
      gutter: import_utils.numericProp,
      clearable: Boolean,
      readonly: Boolean,
      disabled: Boolean,
      voidIcon: (0, import_utils.makeStringProp)("star-o"),
      allowHalf: Boolean,
      voidColor: String,
      touchable: import_utils.truthProp,
      iconPrefix: String,
      modelValue: (0, import_utils.makeNumberProp)(0),
      disabledColor: String
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: rateProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit
      }) {
        const touch = (0, import_use_touch.useTouch)();
        const [itemRefs, setItemRefs] = (0, import_use_refs.useRefs)();
        const groupRef = (0, import_vue.ref)();
        const unselectable = (0, import_vue.computed)(() => props.readonly || props.disabled);
        const untouchable = (0, import_vue.computed)(() => unselectable.value || !props.touchable);
        const list = (0, import_vue.computed)(() => Array(+props.count).fill("").map((_, i) => getRateStatus(props.modelValue, i + 1, props.allowHalf, props.readonly)));
        let ranges;
        let groupRefRect;
        let minRectTop = Number.MAX_SAFE_INTEGER;
        let maxRectTop = Number.MIN_SAFE_INTEGER;
        const updateRanges = () => {
          groupRefRect = (0, import_use.useRect)(groupRef);
          const rects = itemRefs.value.map(import_use.useRect);
          ranges = [];
          rects.forEach((rect, index) => {
            minRectTop = Math.min(rect.top, minRectTop);
            maxRectTop = Math.max(rect.top, maxRectTop);
            if (props.allowHalf) {
              ranges.push({
                score: index + 0.5,
                left: rect.left,
                top: rect.top,
                height: rect.height
              }, {
                score: index + 1,
                left: rect.left + rect.width / 2,
                top: rect.top,
                height: rect.height
              });
            } else {
              ranges.push({
                score: index + 1,
                left: rect.left,
                top: rect.top,
                height: rect.height
              });
            }
          });
        };
        const getScoreByPosition = (x, y) => {
          for (let i = ranges.length - 1; i > 0; i--) {
            if (y >= groupRefRect.top && y <= groupRefRect.bottom) {
              if (x > ranges[i].left && y >= ranges[i].top && y <= ranges[i].top + ranges[i].height) {
                return ranges[i].score;
              }
            } else {
              const curTop = y < groupRefRect.top ? minRectTop : maxRectTop;
              if (x > ranges[i].left && ranges[i].top === curTop) {
                return ranges[i].score;
              }
            }
          }
          return props.allowHalf ? 0.5 : 1;
        };
        const select = (value) => {
          if (unselectable.value || value === props.modelValue) return;
          emit("update:modelValue", value);
          emit("change", value);
        };
        const onTouchStart = (event) => {
          if (untouchable.value) {
            return;
          }
          touch.start(event);
          updateRanges();
        };
        const onTouchMove = (event) => {
          if (untouchable.value) {
            return;
          }
          touch.move(event);
          if (touch.isHorizontal() && !touch.isTap.value) {
            const {
              clientX,
              clientY
            } = event.touches[0];
            (0, import_utils.preventDefault)(event);
            select(getScoreByPosition(clientX, clientY));
          }
        };
        const renderStar = (item, index) => {
          const {
            icon,
            size,
            color,
            count,
            gutter,
            voidIcon,
            disabled,
            voidColor,
            allowHalf,
            iconPrefix,
            disabledColor
          } = props;
          const score = index + 1;
          const isFull = item.status === "full";
          const isVoid = item.status === "void";
          const renderHalf = allowHalf && item.value > 0 && item.value < 1;
          let style;
          if (gutter && score !== +count) {
            style = {
              paddingRight: (0, import_utils.addUnit)(gutter)
            };
          }
          const onClickItem = (event) => {
            updateRanges();
            let value = allowHalf ? getScoreByPosition(event.clientX, event.clientY) : score;
            if (props.clearable && touch.isTap.value && value === props.modelValue) {
              value = 0;
            }
            select(value);
          };
          return (0, import_vue.createVNode)("div", {
            "key": index,
            "ref": setItemRefs(index),
            "role": "radio",
            "style": style,
            "class": bem("item"),
            "tabindex": disabled ? void 0 : 0,
            "aria-setsize": count,
            "aria-posinset": score,
            "aria-checked": !isVoid,
            "onClick": onClickItem
          }, [(0, import_vue.createVNode)(import_icon.Icon, {
            "size": size,
            "name": isFull ? icon : voidIcon,
            "class": bem("icon", {
              disabled,
              full: isFull
            }),
            "color": disabled ? disabledColor : isFull ? color : voidColor,
            "classPrefix": iconPrefix
          }, null), renderHalf && (0, import_vue.createVNode)(import_icon.Icon, {
            "size": size,
            "style": {
              width: item.value + "em"
            },
            "name": isVoid ? voidIcon : icon,
            "class": bem("icon", ["half", {
              disabled,
              full: !isVoid
            }]),
            "color": disabled ? disabledColor : isVoid ? voidColor : color,
            "classPrefix": iconPrefix
          }, null)]);
        };
        (0, import_use.useCustomFieldValue)(() => props.modelValue);
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: groupRef
        });
        return () => (0, import_vue.createVNode)("div", {
          "ref": groupRef,
          "role": "radiogroup",
          "class": bem({
            readonly: props.readonly,
            disabled: props.disabled
          }),
          "tabindex": props.disabled ? void 0 : 0,
          "aria-disabled": props.disabled,
          "aria-readonly": props.readonly,
          "onTouchstartPassive": onTouchStart
        }, [list.value.map(renderStar)]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rate/index.js
var require_rate = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rate/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Rate: () => Rate,
      default: () => stdin_default,
      rateProps: () => import_Rate2.rateProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Rate = __toESM(require_Rate());
    var import_Rate2 = require_Rate();
    var Rate = (0, import_utils.withInstall)(import_Rate.default);
    var stdin_default = Rate;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rolling-text/RollingTextItem.js
var require_RollingTextItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rolling-text/RollingTextItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      props: () => props
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var props = {
      figureArr: (0, import_utils.makeArrayProp)(),
      delay: Number,
      duration: (0, import_utils.makeNumberProp)(2),
      isStart: Boolean,
      direction: (0, import_utils.makeStringProp)("down"),
      height: (0, import_utils.makeNumberProp)(40)
    };
    var [name, bem] = (0, import_utils.createNamespace)("rolling-text-item");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props,
      setup(props2) {
        const newFigureArr = (0, import_vue.computed)(() => props2.direction === "down" ? props2.figureArr.slice().reverse() : props2.figureArr);
        const translatePx = (0, import_vue.computed)(() => {
          const totalHeight = props2.height * (props2.figureArr.length - 1);
          return `-${totalHeight}px`;
        });
        const itemStyle = (0, import_vue.computed)(() => ({
          lineHeight: (0, import_utils.addUnit)(props2.height)
        }));
        const rootStyle = (0, import_vue.computed)(() => ({
          height: (0, import_utils.addUnit)(props2.height),
          "--van-translate": translatePx.value,
          "--van-duration": props2.duration + "s",
          "--van-delay": props2.delay + "s"
        }));
        return () => (0, import_vue.createVNode)("div", {
          "class": bem([props2.direction]),
          "style": rootStyle.value
        }, [(0, import_vue.createVNode)("div", {
          "class": bem("box", {
            animate: props2.isStart
          })
        }, [Array.isArray(newFigureArr.value) && newFigureArr.value.map((figure) => (0, import_vue.createVNode)("div", {
          "class": bem("item"),
          "style": itemStyle.value
        }, [figure]))])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rolling-text/RollingText.js
var require_RollingText = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rolling-text/RollingText.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      rollingTextProps: () => rollingTextProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use = require_index_cjs();
    var import_utils = require_utils();
    var import_use_expose = require_use_expose();
    var import_RollingTextItem = __toESM(require_RollingTextItem());
    var [name, bem] = (0, import_utils.createNamespace)("rolling-text");
    var rollingTextProps = {
      startNum: (0, import_utils.makeNumberProp)(0),
      targetNum: Number,
      textList: (0, import_utils.makeArrayProp)(),
      duration: (0, import_utils.makeNumberProp)(2),
      autoStart: import_utils.truthProp,
      direction: (0, import_utils.makeStringProp)("down"),
      stopOrder: (0, import_utils.makeStringProp)("ltr"),
      height: (0, import_utils.makeNumberProp)(40)
    };
    var CIRCLE_NUM = 2;
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: rollingTextProps,
      setup(props) {
        const isCustomType = (0, import_vue.computed)(() => Array.isArray(props.textList) && props.textList.length);
        const itemLength = (0, import_vue.computed)(() => {
          if (isCustomType.value) return props.textList[0].length;
          return `${Math.max(props.startNum, props.targetNum)}`.length;
        });
        const getTextArrByIdx = (idx) => {
          const result = [];
          for (let i = 0; i < props.textList.length; i++) {
            result.push(props.textList[i][idx]);
          }
          return result;
        };
        const targetNumArr = (0, import_vue.computed)(() => {
          if (isCustomType.value) return new Array(itemLength.value).fill("");
          return (0, import_utils.padZero)(props.targetNum, itemLength.value).split("");
        });
        const startNumArr = (0, import_vue.computed)(() => (0, import_utils.padZero)(props.startNum, itemLength.value).split(""));
        const getFigureArr = (i) => {
          const start2 = +startNumArr.value[i];
          const target = +targetNumArr.value[i];
          const result = [];
          for (let i2 = start2; i2 <= 9; i2++) {
            result.push(i2);
          }
          for (let i2 = 0; i2 <= CIRCLE_NUM; i2++) {
            for (let j = 0; j <= 9; j++) {
              result.push(j);
            }
          }
          for (let i2 = 0; i2 <= target; i2++) {
            result.push(i2);
          }
          return result;
        };
        const getDelay = (i, len) => {
          if (props.stopOrder === "ltr") return 0.2 * i;
          return 0.2 * (len - 1 - i);
        };
        const rolling = (0, import_vue.ref)(props.autoStart);
        const start = () => {
          rolling.value = true;
        };
        const reset = () => {
          rolling.value = false;
          if (props.autoStart) {
            (0, import_use.raf)(() => start());
          }
        };
        (0, import_vue.watch)(() => props.autoStart, (value) => {
          if (value) {
            start();
          }
        });
        (0, import_use_expose.useExpose)({
          start,
          reset
        });
        return () => (0, import_vue.createVNode)("div", {
          "class": bem()
        }, [targetNumArr.value.map((_, i) => (0, import_vue.createVNode)(import_RollingTextItem.default, {
          "figureArr": isCustomType.value ? getTextArrByIdx(i) : getFigureArr(i),
          "duration": props.duration,
          "direction": props.direction,
          "isStart": rolling.value,
          "height": props.height,
          "delay": getDelay(i, itemLength.value)
        }, null))]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rolling-text/index.js
var require_rolling_text = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/rolling-text/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      RollingText: () => RollingText,
      default: () => stdin_default,
      rollingTextProps: () => import_RollingText2.rollingTextProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_RollingText = __toESM(require_RollingText());
    var import_RollingText2 = require_RollingText();
    var RollingText = (0, import_utils.withInstall)(import_RollingText.default);
    var stdin_default = RollingText;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/row/index.js
var require_row = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/row/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Row: () => Row,
      default: () => stdin_default,
      rowProps: () => import_Row2.rowProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Row = __toESM(require_Row());
    var import_Row2 = require_Row();
    var Row = (0, import_utils.withInstall)(import_Row.default);
    var stdin_default = Row;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/search/Search.js
var require_Search = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/search/Search.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      searchProps: () => searchProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_Field = require_Field();
    var import_use_id = require_use_id();
    var import_use_expose = require_use_expose();
    var import_field = require_field();
    var [name, bem, t] = (0, import_utils.createNamespace)("search");
    var searchProps = (0, import_utils.extend)({}, import_Field.fieldSharedProps, {
      label: String,
      shape: (0, import_utils.makeStringProp)("square"),
      leftIcon: (0, import_utils.makeStringProp)("search"),
      clearable: import_utils.truthProp,
      actionText: String,
      background: String,
      showAction: Boolean
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: searchProps,
      emits: ["blur", "focus", "clear", "search", "cancel", "clickInput", "clickLeftIcon", "clickRightIcon", "update:modelValue"],
      setup(props, {
        emit,
        slots,
        attrs
      }) {
        const id = (0, import_use_id.useId)();
        const fieldRef = (0, import_vue.ref)();
        const onCancel = () => {
          if (!slots.action) {
            emit("update:modelValue", "");
            emit("cancel");
          }
        };
        const onKeypress = (event) => {
          const ENTER_CODE = 13;
          if (event.keyCode === ENTER_CODE) {
            (0, import_utils.preventDefault)(event);
            emit("search", props.modelValue);
          }
        };
        const getInputId = () => props.id || `${id}-input`;
        const renderLabel = () => {
          if (slots.label || props.label) {
            return (0, import_vue.createVNode)("label", {
              "class": bem("label"),
              "for": getInputId(),
              "data-allow-mismatch": "attribute"
            }, [slots.label ? slots.label() : props.label]);
          }
        };
        const renderAction = () => {
          if (props.showAction) {
            const text = props.actionText || t("cancel");
            return (0, import_vue.createVNode)("div", {
              "class": bem("action"),
              "role": "button",
              "tabindex": 0,
              "onClick": onCancel
            }, [slots.action ? slots.action() : text]);
          }
        };
        const blur = () => {
          var _a;
          return (_a = fieldRef.value) == null ? void 0 : _a.blur();
        };
        const focus = () => {
          var _a;
          return (_a = fieldRef.value) == null ? void 0 : _a.focus();
        };
        const onBlur = (event) => emit("blur", event);
        const onFocus = (event) => emit("focus", event);
        const onClear = (event) => emit("clear", event);
        const onClickInput = (event) => emit("clickInput", event);
        const onClickLeftIcon = (event) => emit("clickLeftIcon", event);
        const onClickRightIcon = (event) => emit("clickRightIcon", event);
        const fieldPropNames = Object.keys(import_Field.fieldSharedProps);
        const renderField = () => {
          const fieldAttrs = (0, import_utils.extend)({}, attrs, (0, import_utils.pick)(props, fieldPropNames), {
            id: getInputId()
          });
          const onInput = (value) => emit("update:modelValue", value);
          return (0, import_vue.createVNode)(import_field.Field, (0, import_vue.mergeProps)({
            "ref": fieldRef,
            "type": "search",
            "class": bem("field", {
              "with-message": fieldAttrs.errorMessage
            }),
            "border": false,
            "labelAlign": "left",
            "onBlur": onBlur,
            "onFocus": onFocus,
            "onClear": onClear,
            "onKeypress": onKeypress,
            "onClickInput": onClickInput,
            "onClickLeftIcon": onClickLeftIcon,
            "onClickRightIcon": onClickRightIcon,
            "onUpdate:modelValue": onInput
          }, fieldAttrs), (0, import_utils.pick)(slots, ["left-icon", "right-icon"]));
        };
        (0, import_use_expose.useExpose)({
          focus,
          blur
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": bem({
              "show-action": props.showAction
            }),
            "style": {
              background: props.background
            }
          }, [(_a = slots.left) == null ? void 0 : _a.call(slots), (0, import_vue.createVNode)("div", {
            "class": bem("content", props.shape)
          }, [renderLabel(), renderField()]), renderAction()]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/search/index.js
var require_search = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/search/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Search: () => Search,
      default: () => stdin_default,
      searchProps: () => import_Search2.searchProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Search = __toESM(require_Search());
    var import_Search2 = require_Search();
    var Search = (0, import_utils.withInstall)(import_Search.default);
    var stdin_default = Search;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/share-sheet/ShareSheet.js
var require_ShareSheet = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/share-sheet/ShareSheet.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      shareSheetProps: () => shareSheetProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_shared = require_shared();
    var import_icon = require_icon();
    var import_popup = require_popup();
    var isImage = (name2) => name2 == null ? void 0 : name2.includes("/");
    var popupInheritKeys = [...import_shared.popupSharedPropKeys, "round", "closeOnPopstate", "safeAreaInsetBottom"];
    var iconMap = {
      qq: "qq",
      link: "link-o",
      weibo: "weibo",
      qrcode: "qr",
      poster: "photo-o",
      wechat: "wechat",
      "weapp-qrcode": "miniprogram-o",
      "wechat-moments": "wechat-moments"
    };
    var [name, bem, t] = (0, import_utils.createNamespace)("share-sheet");
    var shareSheetProps = (0, import_utils.extend)({}, import_shared.popupSharedProps, {
      title: String,
      round: import_utils.truthProp,
      options: (0, import_utils.makeArrayProp)(),
      cancelText: String,
      description: String,
      closeOnPopstate: import_utils.truthProp,
      safeAreaInsetBottom: import_utils.truthProp
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: shareSheetProps,
      emits: ["cancel", "select", "update:show"],
      setup(props, {
        emit,
        slots
      }) {
        const updateShow = (value) => emit("update:show", value);
        const onCancel = () => {
          updateShow(false);
          emit("cancel");
        };
        const onSelect = (option, index) => emit("select", option, index);
        const renderHeader = () => {
          const title = slots.title ? slots.title() : props.title;
          const description = slots.description ? slots.description() : props.description;
          if (title || description) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("header")
            }, [title && (0, import_vue.createVNode)("h2", {
              "class": bem("title")
            }, [title]), description && (0, import_vue.createVNode)("span", {
              "class": bem("description")
            }, [description])]);
          }
        };
        const renderIcon = (icon) => {
          if (isImage(icon)) {
            return (0, import_vue.createVNode)("img", {
              "src": icon,
              "class": bem("image-icon")
            }, null);
          }
          return (0, import_vue.createVNode)("div", {
            "class": bem("icon", [icon])
          }, [(0, import_vue.createVNode)(import_icon.Icon, {
            "name": iconMap[icon] || icon
          }, null)]);
        };
        const renderOption = (option, index) => {
          const {
            name: name2,
            icon,
            className,
            description
          } = option;
          return (0, import_vue.createVNode)("div", {
            "role": "button",
            "tabindex": 0,
            "class": [bem("option"), className, import_utils.HAPTICS_FEEDBACK],
            "onClick": () => onSelect(option, index)
          }, [renderIcon(icon), name2 && (0, import_vue.createVNode)("span", {
            "class": bem("name")
          }, [name2]), description && (0, import_vue.createVNode)("span", {
            "class": bem("option-description")
          }, [description])]);
        };
        const renderOptions = (options, border) => (0, import_vue.createVNode)("div", {
          "class": bem("options", {
            border
          })
        }, [options.map(renderOption)]);
        const renderRows = () => {
          const {
            options
          } = props;
          if (Array.isArray(options[0])) {
            return options.map((item, index) => renderOptions(item, index !== 0));
          }
          return renderOptions(options);
        };
        const renderCancelButton = () => {
          var _a;
          const cancelText = (_a = props.cancelText) != null ? _a : t("cancel");
          if (slots.cancel || cancelText) {
            return (0, import_vue.createVNode)("button", {
              "type": "button",
              "class": bem("cancel"),
              "onClick": onCancel
            }, [slots.cancel ? slots.cancel() : cancelText]);
          }
        };
        return () => (0, import_vue.createVNode)(import_popup.Popup, (0, import_vue.mergeProps)({
          "class": bem(),
          "position": "bottom",
          "onUpdate:show": updateShow
        }, (0, import_utils.pick)(props, popupInheritKeys)), {
          default: () => [renderHeader(), renderRows(), renderCancelButton()]
        });
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/share-sheet/index.js
var require_share_sheet = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/share-sheet/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      ShareSheet: () => ShareSheet,
      default: () => stdin_default,
      shareSheetProps: () => import_ShareSheet2.shareSheetProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_ShareSheet = __toESM(require_ShareSheet());
    var import_ShareSheet2 = require_ShareSheet();
    var ShareSheet = (0, import_utils.withInstall)(import_ShareSheet.default);
    var stdin_default = ShareSheet;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sidebar/Sidebar.js
var require_Sidebar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sidebar/Sidebar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SIDEBAR_KEY: () => SIDEBAR_KEY,
      default: () => stdin_default,
      sidebarProps: () => sidebarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var [name, bem] = (0, import_utils.createNamespace)("sidebar");
    var SIDEBAR_KEY = Symbol(name);
    var sidebarProps = {
      modelValue: (0, import_utils.makeNumericProp)(0)
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: sidebarProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          linkChildren
        } = (0, import_use.useChildren)(SIDEBAR_KEY);
        const getActive = () => +props.modelValue;
        const setActive = (value) => {
          if (value !== getActive()) {
            emit("update:modelValue", value);
            emit("change", value);
          }
        };
        linkChildren({
          getActive,
          setActive
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "role": "tablist",
            "class": bem()
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sidebar/index.js
var require_sidebar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sidebar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Sidebar: () => Sidebar,
      default: () => stdin_default,
      sidebarProps: () => import_Sidebar2.sidebarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Sidebar = __toESM(require_Sidebar());
    var import_Sidebar2 = require_Sidebar();
    var Sidebar = (0, import_utils.withInstall)(import_Sidebar.default);
    var stdin_default = Sidebar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sidebar-item/SidebarItem.js
var require_SidebarItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sidebar-item/SidebarItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      sidebarItemProps: () => sidebarItemProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_Sidebar = require_Sidebar();
    var import_use = require_index_cjs();
    var import_use_route = require_use_route();
    var import_badge = require_badge();
    var [name, bem] = (0, import_utils.createNamespace)("sidebar-item");
    var sidebarItemProps = (0, import_utils.extend)({}, import_use_route.routeProps, {
      dot: Boolean,
      title: String,
      badge: import_utils.numericProp,
      disabled: Boolean,
      badgeProps: Object
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: sidebarItemProps,
      emits: ["click"],
      setup(props, {
        emit,
        slots
      }) {
        const route = (0, import_use_route.useRoute)();
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_Sidebar.SIDEBAR_KEY);
        if (!parent) {
          if (true) {
            console.error("[Vant] <SidebarItem> must be a child component of <Sidebar>.");
          }
          return;
        }
        const onClick = () => {
          if (props.disabled) {
            return;
          }
          emit("click", index.value);
          parent.setActive(index.value);
          route();
        };
        return () => {
          const {
            dot,
            badge,
            title,
            disabled
          } = props;
          const selected = index.value === parent.getActive();
          return (0, import_vue.createVNode)("div", {
            "role": "tab",
            "class": bem({
              select: selected,
              disabled
            }),
            "tabindex": disabled ? void 0 : 0,
            "aria-selected": selected,
            "onClick": onClick
          }, [(0, import_vue.createVNode)(import_badge.Badge, (0, import_vue.mergeProps)({
            "dot": dot,
            "class": bem("text"),
            "content": badge
          }, props.badgeProps), {
            default: () => [slots.title ? slots.title() : title]
          })]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sidebar-item/index.js
var require_sidebar_item = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/sidebar-item/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SidebarItem: () => SidebarItem,
      default: () => stdin_default,
      sidebarItemProps: () => import_SidebarItem2.sidebarItemProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_SidebarItem = __toESM(require_SidebarItem());
    var import_SidebarItem2 = require_SidebarItem();
    var SidebarItem = (0, import_utils.withInstall)(import_SidebarItem.default);
    var stdin_default = SidebarItem;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/signature/Signature.js
var require_Signature = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/signature/Signature.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      signatureProps: () => signatureProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_button = require_button();
    var [name, bem, t] = (0, import_utils.createNamespace)("signature");
    var signatureProps = {
      tips: String,
      type: (0, import_utils.makeStringProp)("png"),
      penColor: (0, import_utils.makeStringProp)("#000"),
      lineWidth: (0, import_utils.makeNumberProp)(3),
      historySize: (0, import_utils.makeNumberProp)(20),
      undoButtonText: String,
      clearButtonText: String,
      backgroundColor: (0, import_utils.makeStringProp)(""),
      confirmButtonText: String
    };
    var hasCanvasSupport = () => {
      var _a;
      const canvas = document.createElement("canvas");
      return !!((_a = canvas.getContext) == null ? void 0 : _a.call(canvas, "2d"));
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: signatureProps,
      emits: ["submit", "clear", "start", "end", "signing"],
      setup(props, {
        emit,
        slots
      }) {
        const canvasRef = (0, import_vue.ref)();
        const wrapRef = (0, import_vue.ref)();
        const ctx = (0, import_vue.computed)(() => {
          if (!canvasRef.value) return null;
          return canvasRef.value.getContext("2d");
        });
        const isRenderCanvas = import_utils.inBrowser ? hasCanvasSupport() : true;
        let canvasWidth = 0;
        let canvasHeight = 0;
        let canvasRect;
        const history = (0, import_vue.ref)([]);
        const saveState = () => {
          if (ctx.value && canvasWidth && canvasHeight) {
            if (history.value.length >= props.historySize) {
              history.value.shift();
            }
            history.value.push(ctx.value.getImageData(0, 0, canvasWidth, canvasHeight));
          }
        };
        const touchStart = () => {
          if (!ctx.value) {
            return false;
          }
          ctx.value.beginPath();
          ctx.value.lineWidth = props.lineWidth;
          ctx.value.strokeStyle = props.penColor;
          canvasRect = (0, import_use.useRect)(canvasRef);
          emit("start");
        };
        const touchMove = (event) => {
          if (!ctx.value) {
            return false;
          }
          (0, import_utils.preventDefault)(event);
          const touch = event.touches[0];
          const mouseX = touch.clientX - ((canvasRect == null ? void 0 : canvasRect.left) || 0);
          const mouseY = touch.clientY - ((canvasRect == null ? void 0 : canvasRect.top) || 0);
          ctx.value.lineCap = "round";
          ctx.value.lineJoin = "round";
          ctx.value.lineTo(mouseX, mouseY);
          ctx.value.stroke();
          emit("signing", event);
        };
        const touchEnd = (event) => {
          (0, import_utils.preventDefault)(event);
          saveState();
          emit("end");
        };
        const isCanvasEmpty = (canvas) => {
          const empty = document.createElement("canvas");
          empty.width = canvas.width;
          empty.height = canvas.height;
          if (props.backgroundColor) {
            const emptyCtx = empty.getContext("2d");
            setCanvasBgColor(emptyCtx);
          }
          return canvas.toDataURL() === empty.toDataURL();
        };
        const setCanvasBgColor = (ctx2) => {
          if (ctx2 && props.backgroundColor) {
            ctx2.fillStyle = props.backgroundColor;
            ctx2.fillRect(0, 0, canvasWidth, canvasHeight);
          }
        };
        const submit = () => {
          var _a, _b;
          const canvas = canvasRef.value;
          if (!canvas) {
            return;
          }
          const isEmpty = isCanvasEmpty(canvas);
          const image = isEmpty ? "" : ((_b = (_a = {
            jpg: () => canvas.toDataURL("image/jpeg", 0.8),
            jpeg: () => canvas.toDataURL("image/jpeg", 0.8)
          })[props.type]) == null ? void 0 : _b.call(_a)) || canvas.toDataURL(`image/${props.type}`);
          emit("submit", {
            image,
            canvas
          });
        };
        const clear = () => {
          if (ctx.value) {
            ctx.value.clearRect(0, 0, canvasWidth, canvasHeight);
            ctx.value.closePath();
            setCanvasBgColor(ctx.value);
          }
          history.value = [];
          emit("clear");
        };
        const undo = () => {
          if (history.value.length) {
            history.value.pop();
            if (ctx.value) {
              ctx.value.clearRect(0, 0, canvasWidth, canvasHeight);
              setCanvasBgColor(ctx.value);
              if (history.value.length) {
                ctx.value.putImageData(history.value[history.value.length - 1], 0, 0);
              }
            }
          }
        };
        const initialize = () => {
          var _a, _b, _c;
          if (isRenderCanvas && canvasRef.value) {
            const canvas = canvasRef.value;
            const dpr = import_utils.inBrowser ? window.devicePixelRatio : 1;
            canvasWidth = canvas.width = (((_a = wrapRef.value) == null ? void 0 : _a.offsetWidth) || 0) * dpr;
            canvasHeight = canvas.height = (((_b = wrapRef.value) == null ? void 0 : _b.offsetHeight) || 0) * dpr;
            (_c = ctx.value) == null ? void 0 : _c.scale(dpr, dpr);
            setCanvasBgColor(ctx.value);
          }
        };
        const resize = () => {
          if (ctx.value) {
            const data = ctx.value.getImageData(0, 0, canvasWidth, canvasHeight);
            initialize();
            ctx.value.putImageData(data, 0, 0);
          }
        };
        (0, import_vue.watch)(import_utils.windowWidth, resize);
        (0, import_vue.onMounted)(initialize);
        (0, import_use_expose.useExpose)({
          resize,
          clear,
          submit,
          undo
        });
        return () => (0, import_vue.createVNode)("div", {
          "class": bem()
        }, [(0, import_vue.createVNode)("div", {
          "class": bem("content"),
          "ref": wrapRef
        }, [isRenderCanvas ? (0, import_vue.createVNode)("canvas", {
          "ref": canvasRef,
          "onTouchstartPassive": touchStart,
          "onTouchmove": touchMove,
          "onTouchend": touchEnd
        }, null) : slots.tips ? slots.tips() : (0, import_vue.createVNode)("p", null, [props.tips])]), (0, import_vue.createVNode)("div", {
          "class": bem("footer")
        }, [(0, import_vue.createVNode)(import_button.Button, {
          "size": "small",
          "onClick": clear
        }, {
          default: () => [props.clearButtonText || t("clear")]
        }), (0, import_vue.createVNode)(import_button.Button, {
          "size": "small",
          "onClick": undo
        }, {
          default: () => [props.undoButtonText || t("undo")]
        }), (0, import_vue.createVNode)(import_button.Button, {
          "type": "primary",
          "size": "small",
          "onClick": submit
        }, {
          default: () => [props.confirmButtonText || t("confirm")]
        })])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/signature/index.js
var require_signature = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/signature/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Signature: () => Signature,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Signature = __toESM(require_Signature());
    var Signature = (0, import_utils.withInstall)(import_Signature.default);
    var stdin_default = Signature;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-title/SkeletonTitle.js
var require_SkeletonTitle = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-title/SkeletonTitle.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      skeletonTitleProps: () => skeletonTitleProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("skeleton-title");
    var skeletonTitleProps = {
      round: Boolean,
      titleWidth: import_utils.numericProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: skeletonTitleProps,
      setup(props) {
        return () => (0, import_vue.createVNode)("h3", {
          "class": bem([{
            round: props.round
          }]),
          "style": {
            width: (0, import_utils.addUnit)(props.titleWidth)
          }
        }, null);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-title/index.js
var require_skeleton_title = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-title/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SkeletonTitle: () => SkeletonTitle,
      default: () => stdin_default,
      skeletonTitleProps: () => import_SkeletonTitle2.skeletonTitleProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_SkeletonTitle = __toESM(require_SkeletonTitle());
    var import_utils = require_utils();
    var import_SkeletonTitle2 = require_SkeletonTitle();
    var SkeletonTitle = (0, import_utils.withInstall)(import_SkeletonTitle.default);
    var stdin_default = SkeletonTitle;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-avatar/SkeletonAvatar.js
var require_SkeletonAvatar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-avatar/SkeletonAvatar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      skeletonAvatarProps: () => skeletonAvatarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("skeleton-avatar");
    var skeletonAvatarProps = {
      avatarSize: import_utils.numericProp,
      avatarShape: (0, import_utils.makeStringProp)("round")
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: skeletonAvatarProps,
      setup(props) {
        return () => (0, import_vue.createVNode)("div", {
          "class": bem([props.avatarShape]),
          "style": (0, import_utils.getSizeStyle)(props.avatarSize)
        }, null);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-avatar/index.js
var require_skeleton_avatar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-avatar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SkeletonAvatar: () => SkeletonAvatar,
      default: () => stdin_default,
      skeletonAvatarProps: () => import_SkeletonAvatar2.skeletonAvatarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_SkeletonAvatar = __toESM(require_SkeletonAvatar());
    var import_SkeletonAvatar2 = require_SkeletonAvatar();
    var SkeletonAvatar = (0, import_utils.withInstall)(import_SkeletonAvatar.default);
    var stdin_default = SkeletonAvatar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-paragraph/SkeletonParagraph.js
var require_SkeletonParagraph = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-paragraph/SkeletonParagraph.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      DEFAULT_ROW_WIDTH: () => DEFAULT_ROW_WIDTH,
      default: () => stdin_default,
      skeletonParagraphProps: () => skeletonParagraphProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var DEFAULT_ROW_WIDTH = "100%";
    var skeletonParagraphProps = {
      round: Boolean,
      rowWidth: {
        type: import_utils.numericProp,
        default: DEFAULT_ROW_WIDTH
      }
    };
    var [name, bem] = (0, import_utils.createNamespace)("skeleton-paragraph");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: skeletonParagraphProps,
      setup(props) {
        return () => (0, import_vue.createVNode)("div", {
          "class": bem([{
            round: props.round
          }]),
          "style": {
            width: props.rowWidth
          }
        }, null);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-paragraph/index.js
var require_skeleton_paragraph = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-paragraph/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      DEFAULT_ROW_WIDTH: () => import_SkeletonParagraph2.DEFAULT_ROW_WIDTH,
      SkeletonParagraph: () => SkeletonParagraph,
      default: () => stdin_default,
      skeletonParagraphProps: () => import_SkeletonParagraph2.skeletonParagraphProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_SkeletonParagraph = __toESM(require_SkeletonParagraph());
    var import_utils = require_utils();
    var import_SkeletonParagraph2 = require_SkeletonParagraph();
    var SkeletonParagraph = (0, import_utils.withInstall)(import_SkeletonParagraph.default);
    var stdin_default = SkeletonParagraph;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton/Skeleton.js
var require_Skeleton = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton/Skeleton.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      skeletonProps: () => skeletonProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_skeleton_title = __toESM(require_skeleton_title());
    var import_skeleton_avatar = __toESM(require_skeleton_avatar());
    var import_skeleton_paragraph = __toESM(require_skeleton_paragraph());
    var [name, bem] = (0, import_utils.createNamespace)("skeleton");
    var DEFAULT_LAST_ROW_WIDTH = "60%";
    var skeletonProps = {
      row: (0, import_utils.makeNumericProp)(0),
      round: Boolean,
      title: Boolean,
      titleWidth: import_utils.numericProp,
      avatar: Boolean,
      avatarSize: import_utils.numericProp,
      avatarShape: (0, import_utils.makeStringProp)("round"),
      loading: import_utils.truthProp,
      animate: import_utils.truthProp,
      rowWidth: {
        type: [Number, String, Array],
        default: import_skeleton_paragraph.DEFAULT_ROW_WIDTH
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      inheritAttrs: false,
      props: skeletonProps,
      setup(props, {
        slots,
        attrs
      }) {
        const renderAvatar = () => {
          if (props.avatar) {
            return (0, import_vue.createVNode)(import_skeleton_avatar.default, {
              "avatarShape": props.avatarShape,
              "avatarSize": props.avatarSize
            }, null);
          }
        };
        const renderTitle = () => {
          if (props.title) {
            return (0, import_vue.createVNode)(import_skeleton_title.default, {
              "round": props.round,
              "titleWidth": props.titleWidth
            }, null);
          }
        };
        const getRowWidth = (index) => {
          const {
            rowWidth
          } = props;
          if (rowWidth === import_skeleton_paragraph.DEFAULT_ROW_WIDTH && index === +props.row - 1) {
            return DEFAULT_LAST_ROW_WIDTH;
          }
          if (Array.isArray(rowWidth)) {
            return rowWidth[index];
          }
          return rowWidth;
        };
        const renderRows = () => Array(+props.row).fill("").map((_, i) => (0, import_vue.createVNode)(import_skeleton_paragraph.default, {
          "key": i,
          "round": props.round,
          "rowWidth": (0, import_utils.addUnit)(getRowWidth(i))
        }, null));
        const renderContents = () => {
          if (slots.template) {
            return slots.template();
          }
          return (0, import_vue.createVNode)(import_vue.Fragment, null, [renderAvatar(), (0, import_vue.createVNode)("div", {
            "class": bem("content")
          }, [renderTitle(), renderRows()])]);
        };
        return () => {
          var _a;
          if (!props.loading) {
            return (_a = slots.default) == null ? void 0 : _a.call(slots);
          }
          return (0, import_vue.createVNode)("div", (0, import_vue.mergeProps)({
            "class": bem({
              animate: props.animate,
              round: props.round
            })
          }, attrs), [renderContents()]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton/index.js
var require_skeleton = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Skeleton: () => Skeleton,
      default: () => stdin_default,
      skeletonProps: () => import_Skeleton2.skeletonProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_Skeleton = __toESM(require_Skeleton());
    var import_utils = require_utils();
    var import_Skeleton2 = require_Skeleton();
    var Skeleton = (0, import_utils.withInstall)(import_Skeleton.default);
    var stdin_default = Skeleton;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-image/SkeletonImage.js
var require_SkeletonImage = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-image/SkeletonImage.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      skeletonImageProps: () => skeletonImageProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("skeleton-image");
    var skeletonImageProps = {
      imageSize: import_utils.numericProp,
      imageShape: (0, import_utils.makeStringProp)("square")
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: skeletonImageProps,
      setup(props) {
        return () => (0, import_vue.createVNode)("div", {
          "class": bem([props.imageShape]),
          "style": (0, import_utils.getSizeStyle)(props.imageSize)
        }, [(0, import_vue.createVNode)(import_icon.Icon, {
          "name": "photo",
          "class": bem("icon")
        }, null)]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-image/index.js
var require_skeleton_image = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/skeleton-image/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SkeletonImage: () => SkeletonImage,
      default: () => stdin_default,
      skeletonImageProps: () => import_SkeletonImage2.skeletonImageProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_SkeletonImage = __toESM(require_SkeletonImage());
    var import_utils = require_utils();
    var import_SkeletonImage2 = require_SkeletonImage();
    var SkeletonImage = (0, import_utils.withInstall)(import_SkeletonImage.default);
    var stdin_default = SkeletonImage;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/slider/Slider.js
var require_Slider = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/slider/Slider.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      sliderProps: () => sliderProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_touch = require_use_touch();
    var [name, bem] = (0, import_utils.createNamespace)("slider");
    var sliderProps = {
      min: (0, import_utils.makeNumericProp)(0),
      max: (0, import_utils.makeNumericProp)(100),
      step: (0, import_utils.makeNumericProp)(1),
      range: Boolean,
      reverse: Boolean,
      disabled: Boolean,
      readonly: Boolean,
      vertical: Boolean,
      barHeight: import_utils.numericProp,
      buttonSize: import_utils.numericProp,
      activeColor: String,
      inactiveColor: String,
      modelValue: {
        type: [Number, Array],
        default: 0
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: sliderProps,
      emits: ["change", "dragEnd", "dragStart", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        let buttonIndex;
        let current;
        let startValue;
        const root = (0, import_vue.ref)();
        const slider = [(0, import_vue.ref)(), (0, import_vue.ref)()];
        const dragStatus = (0, import_vue.ref)();
        const touch = (0, import_use_touch.useTouch)();
        const scope = (0, import_vue.computed)(() => Number(props.max) - Number(props.min));
        const wrapperStyle = (0, import_vue.computed)(() => {
          const crossAxis = props.vertical ? "width" : "height";
          return {
            background: props.inactiveColor,
            [crossAxis]: (0, import_utils.addUnit)(props.barHeight)
          };
        });
        const isRange = (val) => props.range && Array.isArray(val);
        const calcMainAxis = () => {
          const {
            modelValue,
            min
          } = props;
          if (isRange(modelValue)) {
            return `${(modelValue[1] - modelValue[0]) * 100 / scope.value}%`;
          }
          return `${(modelValue - Number(min)) * 100 / scope.value}%`;
        };
        const calcOffset = () => {
          const {
            modelValue,
            min
          } = props;
          if (isRange(modelValue)) {
            return `${(modelValue[0] - Number(min)) * 100 / scope.value}%`;
          }
          return "0%";
        };
        const barStyle = (0, import_vue.computed)(() => {
          const mainAxis = props.vertical ? "height" : "width";
          const style = {
            [mainAxis]: calcMainAxis(),
            background: props.activeColor
          };
          if (dragStatus.value) {
            style.transition = "none";
          }
          const getPositionKey = () => {
            if (props.vertical) {
              return props.reverse ? "bottom" : "top";
            }
            return props.reverse ? "right" : "left";
          };
          style[getPositionKey()] = calcOffset();
          return style;
        });
        const format = (value) => {
          const min = +props.min;
          const max = +props.max;
          const step = +props.step;
          value = (0, import_utils.clamp)(value, min, max);
          const diff = Math.round((value - min) / step) * step;
          return (0, import_utils.addNumber)(min, diff);
        };
        const updateStartValue = () => {
          const current2 = props.modelValue;
          if (isRange(current2)) {
            startValue = current2.map(format);
          } else {
            startValue = format(current2);
          }
        };
        const handleRangeValue = (value) => {
          var _a, _b;
          const left = (_a = value[0]) != null ? _a : Number(props.min);
          const right = (_b = value[1]) != null ? _b : Number(props.max);
          return left > right ? [right, left] : [left, right];
        };
        const updateValue = (value, end) => {
          if (isRange(value)) {
            value = handleRangeValue(value).map(format);
          } else {
            value = format(value);
          }
          if (!(0, import_utils.isSameValue)(value, props.modelValue)) {
            emit("update:modelValue", value);
          }
          if (end && !(0, import_utils.isSameValue)(value, startValue)) {
            emit("change", value);
          }
        };
        const onClick = (event) => {
          event.stopPropagation();
          if (props.disabled || props.readonly) {
            return;
          }
          updateStartValue();
          const {
            min,
            reverse,
            vertical,
            modelValue
          } = props;
          const rect = (0, import_use.useRect)(root);
          const getDelta = () => {
            if (vertical) {
              if (reverse) {
                return rect.bottom - event.clientY;
              }
              return event.clientY - rect.top;
            }
            if (reverse) {
              return rect.right - event.clientX;
            }
            return event.clientX - rect.left;
          };
          const total = vertical ? rect.height : rect.width;
          const value = Number(min) + getDelta() / total * scope.value;
          if (isRange(modelValue)) {
            const [left, right] = modelValue;
            const middle = (left + right) / 2;
            if (value <= middle) {
              updateValue([value, right], true);
            } else {
              updateValue([left, value], true);
            }
          } else {
            updateValue(value, true);
          }
        };
        const onTouchStart = (event) => {
          if (props.disabled || props.readonly) {
            return;
          }
          touch.start(event);
          current = props.modelValue;
          updateStartValue();
          dragStatus.value = "start";
        };
        const onTouchMove = (event) => {
          if (props.disabled || props.readonly) {
            return;
          }
          if (dragStatus.value === "start") {
            emit("dragStart", event);
          }
          (0, import_utils.preventDefault)(event, true);
          touch.move(event);
          dragStatus.value = "dragging";
          const rect = (0, import_use.useRect)(root);
          const delta = props.vertical ? touch.deltaY.value : touch.deltaX.value;
          const total = props.vertical ? rect.height : rect.width;
          let diff = delta / total * scope.value;
          if (props.reverse) {
            diff = -diff;
          }
          if (isRange(startValue)) {
            const index = props.reverse ? 1 - buttonIndex : buttonIndex;
            current[index] = startValue[index] + diff;
          } else {
            current = startValue + diff;
          }
          updateValue(current);
        };
        const onTouchEnd = (event) => {
          if (props.disabled || props.readonly) {
            return;
          }
          if (dragStatus.value === "dragging") {
            updateValue(current, true);
            emit("dragEnd", event);
          }
          dragStatus.value = "";
        };
        const getButtonClassName = (index) => {
          if (typeof index === "number") {
            const position = ["left", "right"];
            return bem(`button-wrapper`, position[index]);
          }
          return bem("button-wrapper", props.reverse ? "left" : "right");
        };
        const renderButtonContent = (value, index) => {
          const dragging = dragStatus.value === "dragging";
          if (typeof index === "number") {
            const slot = slots[index === 0 ? "left-button" : "right-button"];
            let dragIndex;
            if (dragging && Array.isArray(current)) {
              dragIndex = current[0] > current[1] ? buttonIndex ^ 1 : buttonIndex;
            }
            if (slot) {
              return slot({
                value,
                dragging,
                dragIndex
              });
            }
          }
          if (slots.button) {
            return slots.button({
              value,
              dragging
            });
          }
          return (0, import_vue.createVNode)("div", {
            "class": bem("button"),
            "style": (0, import_utils.getSizeStyle)(props.buttonSize)
          }, null);
        };
        const renderButton = (index) => {
          const current2 = typeof index === "number" ? props.modelValue[index] : props.modelValue;
          return (0, import_vue.createVNode)("div", {
            "ref": slider[index != null ? index : 0],
            "role": "slider",
            "class": getButtonClassName(index),
            "tabindex": props.disabled ? void 0 : 0,
            "aria-valuemin": props.min,
            "aria-valuenow": current2,
            "aria-valuemax": props.max,
            "aria-disabled": props.disabled || void 0,
            "aria-readonly": props.readonly || void 0,
            "aria-orientation": props.vertical ? "vertical" : "horizontal",
            "onTouchstartPassive": (event) => {
              if (typeof index === "number") {
                buttonIndex = index;
              }
              onTouchStart(event);
            },
            "onTouchend": onTouchEnd,
            "onTouchcancel": onTouchEnd,
            "onClick": import_utils.stopPropagation
          }, [renderButtonContent(current2, index)]);
        };
        updateValue(props.modelValue);
        (0, import_use.useCustomFieldValue)(() => props.modelValue);
        slider.forEach((item) => {
          (0, import_use.useEventListener)("touchmove", onTouchMove, {
            target: item
          });
        });
        return () => (0, import_vue.createVNode)("div", {
          "ref": root,
          "style": wrapperStyle.value,
          "class": bem({
            vertical: props.vertical,
            disabled: props.disabled
          }),
          "onClick": onClick
        }, [(0, import_vue.createVNode)("div", {
          "class": bem("bar"),
          "style": barStyle.value
        }, [props.range ? [renderButton(0), renderButton(1)] : renderButton()])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/slider/index.js
var require_slider = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/slider/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Slider: () => Slider,
      default: () => stdin_default,
      sliderProps: () => import_Slider2.sliderProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Slider = __toESM(require_Slider());
    var import_Slider2 = require_Slider();
    var Slider = (0, import_utils.withInstall)(import_Slider.default);
    var stdin_default = Slider;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/space/Space.js
var require_Space = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/space/Space.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      spaceProps: () => spaceProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("space");
    var spaceProps = {
      align: String,
      direction: {
        type: String,
        default: "horizontal"
      },
      size: {
        type: [Number, String, Array],
        default: 8
      },
      wrap: Boolean,
      fill: Boolean
    };
    function filterEmpty(children = []) {
      const nodes = [];
      children.forEach((child) => {
        if (Array.isArray(child)) {
          nodes.push(...child);
        } else if (child.type === import_vue.Fragment) {
          nodes.push(...filterEmpty(child.children));
        } else {
          nodes.push(child);
        }
      });
      return nodes.filter((c) => {
        var _a;
        return !(c && (c.type === import_vue.Comment || c.type === import_vue.Fragment && ((_a = c.children) == null ? void 0 : _a.length) === 0 || c.type === import_vue.Text && c.children.trim() === ""));
      });
    }
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: spaceProps,
      setup(props, {
        slots
      }) {
        const mergedAlign = (0, import_vue.computed)(() => {
          var _a;
          return (_a = props.align) != null ? _a : props.direction === "horizontal" ? "center" : "";
        });
        const getMargin = (size) => {
          if (typeof size === "number") {
            return size + "px";
          }
          return size;
        };
        const getMarginStyle = (isLast) => {
          const style = {};
          const marginRight = `${getMargin(Array.isArray(props.size) ? props.size[0] : props.size)}`;
          const marginBottom = `${getMargin(Array.isArray(props.size) ? props.size[1] : props.size)}`;
          if (isLast) {
            return props.wrap ? {
              marginBottom
            } : {};
          }
          if (props.direction === "horizontal") {
            style.marginRight = marginRight;
          }
          if (props.direction === "vertical" || props.wrap) {
            style.marginBottom = marginBottom;
          }
          return style;
        };
        return () => {
          var _a;
          const children = filterEmpty((_a = slots.default) == null ? void 0 : _a.call(slots));
          return (0, import_vue.createVNode)("div", {
            "class": [bem({
              [props.direction]: props.direction,
              [`align-${mergedAlign.value}`]: mergedAlign.value,
              wrap: props.wrap,
              fill: props.fill
            })]
          }, [children.map((c, i) => (0, import_vue.createVNode)("div", {
            "key": `item-${i}`,
            "class": `${name}-item`,
            "style": getMarginStyle(i === children.length - 1)
          }, [c]))]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/space/index.js
var require_space = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/space/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Space: () => Space,
      default: () => stdin_default,
      spaceProps: () => import_Space2.spaceProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Space = __toESM(require_Space());
    var import_Space2 = require_Space();
    var Space = (0, import_utils.withInstall)(import_Space.default);
    var stdin_default = Space;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/steps/Steps.js
var require_Steps = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/steps/Steps.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      STEPS_KEY: () => STEPS_KEY,
      default: () => stdin_default,
      stepsProps: () => stepsProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var [name, bem] = (0, import_utils.createNamespace)("steps");
    var stepsProps = {
      active: (0, import_utils.makeNumericProp)(0),
      direction: (0, import_utils.makeStringProp)("horizontal"),
      activeIcon: (0, import_utils.makeStringProp)("checked"),
      iconPrefix: String,
      finishIcon: String,
      activeColor: String,
      inactiveIcon: String,
      inactiveColor: String
    };
    var STEPS_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: stepsProps,
      emits: ["clickStep"],
      setup(props, {
        emit,
        slots
      }) {
        const {
          linkChildren
        } = (0, import_use.useChildren)(STEPS_KEY);
        const onClickStep = (index) => emit("clickStep", index);
        linkChildren({
          props,
          onClickStep
        });
        return () => {
          var _a;
          return (0, import_vue.createVNode)("div", {
            "class": bem([props.direction])
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("items")
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/step/Step.js
var require_Step = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/step/Step.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_Steps = require_Steps();
    var import_use = require_index_cjs();
    var import_icon = require_icon();
    var [name, bem] = (0, import_utils.createNamespace)("step");
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      setup(props, {
        slots
      }) {
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_Steps.STEPS_KEY);
        if (!parent) {
          if (true) {
            console.error("[Vant] <Step> must be a child component of <Steps>.");
          }
          return;
        }
        const parentProps = parent.props;
        const getStatus = () => {
          const active = +parentProps.active;
          if (index.value < active) {
            return "finish";
          }
          return index.value === active ? "process" : "waiting";
        };
        const isActive = () => getStatus() === "process";
        const lineStyle = (0, import_vue.computed)(() => ({
          background: getStatus() === "finish" ? parentProps.activeColor : parentProps.inactiveColor
        }));
        const titleStyle = (0, import_vue.computed)(() => {
          if (isActive()) {
            return {
              color: parentProps.activeColor
            };
          }
          if (getStatus() === "waiting") {
            return {
              color: parentProps.inactiveColor
            };
          }
        });
        const onClickStep = () => parent.onClickStep(index.value);
        const renderCircle = () => {
          const {
            iconPrefix,
            finishIcon,
            activeIcon,
            activeColor,
            inactiveIcon
          } = parentProps;
          if (isActive()) {
            if (slots["active-icon"]) {
              return slots["active-icon"]();
            }
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "class": bem("icon", "active"),
              "name": activeIcon,
              "color": activeColor,
              "classPrefix": iconPrefix
            }, null);
          }
          if (getStatus() === "finish" && (finishIcon || slots["finish-icon"])) {
            if (slots["finish-icon"]) {
              return slots["finish-icon"]();
            }
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "class": bem("icon", "finish"),
              "name": finishIcon,
              "color": activeColor,
              "classPrefix": iconPrefix
            }, null);
          }
          if (slots["inactive-icon"]) {
            return slots["inactive-icon"]();
          }
          if (inactiveIcon) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "class": bem("icon"),
              "name": inactiveIcon,
              "classPrefix": iconPrefix
            }, null);
          }
          return (0, import_vue.createVNode)("i", {
            "class": bem("circle"),
            "style": lineStyle.value
          }, null);
        };
        return () => {
          var _a;
          const status = getStatus();
          return (0, import_vue.createVNode)("div", {
            "class": [import_utils.BORDER, bem([parentProps.direction, {
              [status]: status
            }])]
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("title", {
              active: isActive()
            }),
            "style": titleStyle.value,
            "onClick": onClickStep
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]), (0, import_vue.createVNode)("div", {
            "class": bem("circle-container"),
            "onClick": onClickStep
          }, [renderCircle()]), (0, import_vue.createVNode)("div", {
            "class": bem("line"),
            "style": lineStyle.value
          }, null)]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/step/index.js
var require_step = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/step/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Step: () => Step,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Step = __toESM(require_Step());
    var Step = (0, import_utils.withInstall)(import_Step.default);
    var stdin_default = Step;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/stepper/Stepper.js
var require_Stepper = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/stepper/Stepper.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      stepperProps: () => stepperProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var [name, bem] = (0, import_utils.createNamespace)("stepper");
    var LONG_PRESS_INTERVAL = 200;
    var isEqual = (value1, value2) => String(value1) === String(value2);
    var stepperProps = {
      min: (0, import_utils.makeNumericProp)(1),
      max: (0, import_utils.makeNumericProp)(Infinity),
      name: (0, import_utils.makeNumericProp)(""),
      step: (0, import_utils.makeNumericProp)(1),
      theme: String,
      integer: Boolean,
      disabled: Boolean,
      showPlus: import_utils.truthProp,
      showMinus: import_utils.truthProp,
      showInput: import_utils.truthProp,
      longPress: import_utils.truthProp,
      autoFixed: import_utils.truthProp,
      allowEmpty: Boolean,
      modelValue: import_utils.numericProp,
      inputWidth: import_utils.numericProp,
      buttonSize: import_utils.numericProp,
      placeholder: String,
      disablePlus: Boolean,
      disableMinus: Boolean,
      disableInput: Boolean,
      beforeChange: Function,
      defaultValue: (0, import_utils.makeNumericProp)(1),
      decimalLength: import_utils.numericProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: stepperProps,
      emits: ["plus", "blur", "minus", "focus", "change", "overlimit", "update:modelValue"],
      setup(props, {
        emit
      }) {
        const format = (value, autoFixed = true) => {
          const {
            min,
            max,
            allowEmpty,
            decimalLength
          } = props;
          if (allowEmpty && value === "") {
            return value;
          }
          if (typeof value === "number" && String(value).includes("e")) {
            value = value.toFixed(decimalLength ? +decimalLength : 17);
          }
          value = (0, import_utils.formatNumber)(String(value), !props.integer);
          value = value === "" ? 0 : +value;
          value = Number.isNaN(value) ? +min : value;
          value = autoFixed ? Math.max(Math.min(+max, value), +min) : value;
          if ((0, import_utils.isDef)(decimalLength)) {
            value = value.toFixed(+decimalLength);
          }
          return value;
        };
        const getInitialValue = () => {
          var _a;
          const defaultValue = (_a = props.modelValue) != null ? _a : props.defaultValue;
          const value = format(defaultValue);
          if (!isEqual(value, props.modelValue)) {
            emit("update:modelValue", value);
          }
          return value;
        };
        let actionType;
        const inputRef = (0, import_vue.ref)();
        const current = (0, import_vue.ref)(getInitialValue());
        const minusDisabled = (0, import_vue.computed)(() => props.disabled || props.disableMinus || +current.value <= +props.min);
        const plusDisabled = (0, import_vue.computed)(() => props.disabled || props.disablePlus || +current.value >= +props.max);
        const inputStyle = (0, import_vue.computed)(() => ({
          width: (0, import_utils.addUnit)(props.inputWidth),
          height: (0, import_utils.addUnit)(props.buttonSize)
        }));
        const buttonStyle = (0, import_vue.computed)(() => (0, import_utils.getSizeStyle)(props.buttonSize));
        const check = () => {
          const value = format(current.value);
          if (!isEqual(value, current.value)) {
            current.value = value;
          }
        };
        const setValue = (value) => {
          if (props.beforeChange) {
            (0, import_utils.callInterceptor)(props.beforeChange, {
              args: [value],
              done() {
                current.value = value;
              }
            });
          } else {
            current.value = value;
          }
        };
        const onChange = () => {
          if (actionType === "plus" && plusDisabled.value || actionType === "minus" && minusDisabled.value) {
            emit("overlimit", actionType);
            return;
          }
          const diff = actionType === "minus" ? -props.step : +props.step;
          const value = format((0, import_utils.addNumber)(+current.value, diff));
          setValue(value);
          emit(actionType);
        };
        const onInput = (event) => {
          const input = event.target;
          const {
            value
          } = input;
          const {
            decimalLength
          } = props;
          let formatted = (0, import_utils.formatNumber)(String(value), !props.integer);
          if ((0, import_utils.isDef)(decimalLength) && formatted.includes(".")) {
            const pair = formatted.split(".");
            formatted = `${pair[0]}.${pair[1].slice(0, +decimalLength)}`;
          }
          if (props.beforeChange) {
            input.value = String(current.value);
          } else if (!isEqual(value, formatted)) {
            input.value = formatted;
          }
          const isNumeric = formatted === String(+formatted);
          setValue(isNumeric ? +formatted : formatted);
        };
        const onFocus = (event) => {
          var _a;
          if (props.disableInput) {
            (_a = inputRef.value) == null ? void 0 : _a.blur();
          } else {
            emit("focus", event);
          }
        };
        const onBlur = (event) => {
          const input = event.target;
          const value = format(input.value, props.autoFixed);
          input.value = String(value);
          current.value = value;
          (0, import_vue.nextTick)(() => {
            emit("blur", event);
            (0, import_utils.resetScroll)();
          });
        };
        let isLongPress;
        let longPressTimer;
        const longPressStep = () => {
          longPressTimer = setTimeout(() => {
            onChange();
            longPressStep();
          }, LONG_PRESS_INTERVAL);
        };
        const onTouchStart = () => {
          if (props.longPress) {
            isLongPress = false;
            clearTimeout(longPressTimer);
            longPressTimer = setTimeout(() => {
              isLongPress = true;
              onChange();
              longPressStep();
            }, import_utils.LONG_PRESS_START_TIME);
          }
        };
        const onTouchEnd = (event) => {
          if (props.longPress) {
            clearTimeout(longPressTimer);
            if (isLongPress) {
              (0, import_utils.preventDefault)(event);
            }
          }
        };
        const onMousedown = (event) => {
          if (props.disableInput) {
            (0, import_utils.preventDefault)(event);
          }
        };
        const createListeners = (type) => ({
          onClick: (event) => {
            (0, import_utils.preventDefault)(event);
            actionType = type;
            onChange();
          },
          onTouchstartPassive: () => {
            actionType = type;
            onTouchStart();
          },
          onTouchend: onTouchEnd,
          onTouchcancel: onTouchEnd
        });
        (0, import_vue.watch)(() => [props.max, props.min, props.integer, props.decimalLength], check);
        (0, import_vue.watch)(() => props.modelValue, (value) => {
          if (!isEqual(value, current.value)) {
            current.value = format(value);
          }
        });
        (0, import_vue.watch)(current, (value) => {
          emit("update:modelValue", value);
          emit("change", value, {
            name: props.name
          });
        });
        (0, import_use.useCustomFieldValue)(() => props.modelValue);
        return () => (0, import_vue.createVNode)("div", {
          "role": "group",
          "class": bem([props.theme])
        }, [(0, import_vue.withDirectives)((0, import_vue.createVNode)("button", (0, import_vue.mergeProps)({
          "type": "button",
          "style": buttonStyle.value,
          "class": [bem("minus", {
            disabled: minusDisabled.value
          }), {
            [import_utils.HAPTICS_FEEDBACK]: !minusDisabled.value
          }],
          "aria-disabled": minusDisabled.value || void 0
        }, createListeners("minus")), null), [[import_vue.vShow, props.showMinus]]), (0, import_vue.withDirectives)((0, import_vue.createVNode)("input", {
          "ref": inputRef,
          "type": props.integer ? "tel" : "text",
          "role": "spinbutton",
          "class": bem("input"),
          "value": current.value,
          "style": inputStyle.value,
          "disabled": props.disabled,
          "readonly": props.disableInput,
          "inputmode": props.integer ? "numeric" : "decimal",
          "placeholder": props.placeholder,
          "autocomplete": "off",
          "aria-valuemax": props.max,
          "aria-valuemin": props.min,
          "aria-valuenow": current.value,
          "onBlur": onBlur,
          "onInput": onInput,
          "onFocus": onFocus,
          "onMousedown": onMousedown
        }, null), [[import_vue.vShow, props.showInput]]), (0, import_vue.withDirectives)((0, import_vue.createVNode)("button", (0, import_vue.mergeProps)({
          "type": "button",
          "style": buttonStyle.value,
          "class": [bem("plus", {
            disabled: plusDisabled.value
          }), {
            [import_utils.HAPTICS_FEEDBACK]: !plusDisabled.value
          }],
          "aria-disabled": plusDisabled.value || void 0
        }, createListeners("plus")), null), [[import_vue.vShow, props.showPlus]])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/stepper/index.js
var require_stepper = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/stepper/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Stepper: () => Stepper,
      default: () => stdin_default,
      stepperProps: () => import_Stepper2.stepperProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Stepper = __toESM(require_Stepper());
    var import_Stepper2 = require_Stepper();
    var Stepper = (0, import_utils.withInstall)(import_Stepper.default);
    var stdin_default = Stepper;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/steps/index.js
var require_steps = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/steps/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Steps: () => Steps,
      default: () => stdin_default,
      stepsProps: () => import_Steps2.stepsProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Steps = __toESM(require_Steps());
    var import_Steps2 = require_Steps();
    var Steps = (0, import_utils.withInstall)(import_Steps.default);
    var stdin_default = Steps;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/submit-bar/SubmitBar.js
var require_SubmitBar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/submit-bar/SubmitBar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      submitBarProps: () => submitBarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_icon = require_icon();
    var import_button = require_button();
    var import_use_placeholder = require_use_placeholder();
    var [name, bem, t] = (0, import_utils.createNamespace)("submit-bar");
    var submitBarProps = {
      tip: String,
      label: String,
      price: Number,
      tipIcon: String,
      loading: Boolean,
      currency: (0, import_utils.makeStringProp)("¥"),
      disabled: Boolean,
      textAlign: String,
      buttonText: String,
      buttonType: (0, import_utils.makeStringProp)("danger"),
      buttonColor: String,
      suffixLabel: String,
      placeholder: Boolean,
      decimalLength: (0, import_utils.makeNumericProp)(2),
      safeAreaInsetBottom: import_utils.truthProp
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: submitBarProps,
      emits: ["submit"],
      setup(props, {
        emit,
        slots
      }) {
        const root = (0, import_vue.ref)();
        const renderPlaceholder = (0, import_use_placeholder.usePlaceholder)(root, bem);
        const renderText = () => {
          const {
            price,
            label,
            currency,
            textAlign,
            suffixLabel,
            decimalLength
          } = props;
          if (typeof price === "number") {
            const pricePair = (price / 100).toFixed(+decimalLength).split(".");
            const decimal = decimalLength ? `.${pricePair[1]}` : "";
            return (0, import_vue.createVNode)("div", {
              "class": bem("text"),
              "style": {
                textAlign
              }
            }, [(0, import_vue.createVNode)("span", null, [label || t("label")]), (0, import_vue.createVNode)("span", {
              "class": bem("price")
            }, [currency, (0, import_vue.createVNode)("span", {
              "class": bem("price-integer")
            }, [pricePair[0]]), decimal]), suffixLabel && (0, import_vue.createVNode)("span", {
              "class": bem("suffix-label")
            }, [suffixLabel])]);
          }
        };
        const renderTip = () => {
          var _a;
          const {
            tip,
            tipIcon
          } = props;
          if (slots.tip || tip) {
            return (0, import_vue.createVNode)("div", {
              "class": bem("tip")
            }, [tipIcon && (0, import_vue.createVNode)(import_icon.Icon, {
              "class": bem("tip-icon"),
              "name": tipIcon
            }, null), tip && (0, import_vue.createVNode)("span", {
              "class": bem("tip-text")
            }, [tip]), (_a = slots.tip) == null ? void 0 : _a.call(slots)]);
          }
        };
        const onClickButton = () => emit("submit");
        const renderButton = () => {
          if (slots.button) {
            return slots.button();
          }
          return (0, import_vue.createVNode)(import_button.Button, {
            "round": true,
            "type": props.buttonType,
            "text": props.buttonText,
            "class": bem("button", props.buttonType),
            "color": props.buttonColor,
            "loading": props.loading,
            "disabled": props.disabled,
            "onClick": onClickButton
          }, null);
        };
        const renderSubmitBar = () => {
          var _a, _b;
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "class": [bem(), {
              "van-safe-area-bottom": props.safeAreaInsetBottom
            }]
          }, [(_a = slots.top) == null ? void 0 : _a.call(slots), renderTip(), (0, import_vue.createVNode)("div", {
            "class": bem("bar")
          }, [(_b = slots.default) == null ? void 0 : _b.call(slots), renderText(), renderButton()])]);
        };
        return () => {
          if (props.placeholder) {
            return renderPlaceholder(renderSubmitBar);
          }
          return renderSubmitBar();
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/submit-bar/index.js
var require_submit_bar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/submit-bar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SubmitBar: () => SubmitBar,
      default: () => stdin_default,
      submitBarProps: () => import_SubmitBar2.submitBarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_SubmitBar = __toESM(require_SubmitBar());
    var import_SubmitBar2 = require_SubmitBar();
    var SubmitBar = (0, import_utils.withInstall)(import_SubmitBar.default);
    var stdin_default = SubmitBar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe-cell/SwipeCell.js
var require_SwipeCell = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe-cell/SwipeCell.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      swipeCellProps: () => swipeCellProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_touch = require_use_touch();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("swipe-cell");
    var swipeCellProps = {
      name: (0, import_utils.makeNumericProp)(""),
      disabled: Boolean,
      leftWidth: import_utils.numericProp,
      rightWidth: import_utils.numericProp,
      threshold: {
        type: import_utils.numericProp,
        default: 0.15,
        validator: (value) => +value >= 0 && +value <= 1
      },
      beforeClose: Function,
      stopPropagation: Boolean
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: swipeCellProps,
      emits: ["open", "close", "click"],
      setup(props, {
        emit,
        slots
      }) {
        let opened;
        let lockClick;
        let startOffset;
        let isInBeforeClosing;
        const root = (0, import_vue.ref)();
        const leftRef = (0, import_vue.ref)();
        const rightRef = (0, import_vue.ref)();
        const state = (0, import_vue.reactive)({
          offset: 0,
          dragging: false
        });
        const touch = (0, import_use_touch.useTouch)();
        const getWidthByRef = (ref2) => ref2.value ? (0, import_use.useRect)(ref2).width : 0;
        const leftWidth = (0, import_vue.computed)(() => (0, import_utils.isDef)(props.leftWidth) ? +props.leftWidth : getWidthByRef(leftRef));
        const rightWidth = (0, import_vue.computed)(() => (0, import_utils.isDef)(props.rightWidth) ? +props.rightWidth : getWidthByRef(rightRef));
        const open = (side) => {
          state.offset = side === "left" ? leftWidth.value : -rightWidth.value;
          if (!opened) {
            opened = true;
            emit("open", {
              name: props.name,
              position: side
            });
          }
        };
        const close = (position) => {
          state.offset = 0;
          if (opened) {
            opened = false;
            emit("close", {
              name: props.name,
              position
            });
          }
        };
        const toggle = (side) => {
          const offset = Math.abs(state.offset);
          const thresholdValue = +props.threshold;
          const threshold = opened ? 1 - thresholdValue : thresholdValue;
          const width = side === "left" ? leftWidth.value : rightWidth.value;
          if (width && offset > width * threshold) {
            open(side);
          } else {
            close(side);
          }
        };
        const onTouchStart = (event) => {
          if (!props.disabled) {
            startOffset = state.offset;
            touch.start(event);
          }
        };
        const onTouchMove = (event) => {
          if (props.disabled) {
            return;
          }
          const {
            deltaX
          } = touch;
          touch.move(event);
          if (touch.isHorizontal()) {
            lockClick = true;
            state.dragging = true;
            const isEdge = !opened || deltaX.value * startOffset < 0;
            if (isEdge) {
              (0, import_utils.preventDefault)(event, props.stopPropagation);
            }
            state.offset = (0, import_utils.clamp)(deltaX.value + startOffset, -rightWidth.value, leftWidth.value);
          }
        };
        const onTouchEnd = () => {
          if (state.dragging) {
            state.dragging = false;
            toggle(state.offset > 0 ? "left" : "right");
            setTimeout(() => {
              lockClick = false;
            }, 0);
          }
        };
        const onClick = (position = "outside", event) => {
          if (isInBeforeClosing) return;
          emit("click", position);
          if (opened && !lockClick) {
            isInBeforeClosing = true;
            (0, import_utils.callInterceptor)(props.beforeClose, {
              args: [{
                event,
                name: props.name,
                position
              }],
              done: () => {
                isInBeforeClosing = false;
                close(position);
              },
              canceled: () => isInBeforeClosing = false,
              error: () => isInBeforeClosing = false
            });
          }
        };
        const getClickHandler = (position) => (event) => {
          if (lockClick || opened) {
            event.stopPropagation();
          }
          if (lockClick) {
            return;
          }
          onClick(position, event);
        };
        const renderSideContent = (side, ref2) => {
          const contentSlot = slots[side];
          if (contentSlot) {
            return (0, import_vue.createVNode)("div", {
              "ref": ref2,
              "class": bem(side),
              "onClick": getClickHandler(side)
            }, [contentSlot()]);
          }
        };
        (0, import_use_expose.useExpose)({
          open,
          close
        });
        (0, import_use.useClickAway)(root, (event) => onClick("outside", event), {
          eventName: "touchstart"
        });
        (0, import_use.useEventListener)("touchmove", onTouchMove, {
          target: root
        });
        return () => {
          var _a;
          const wrapperStyle = {
            transform: `translate3d(${state.offset}px, 0, 0)`,
            transitionDuration: state.dragging ? "0s" : ".6s"
          };
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "class": bem(),
            "onClick": getClickHandler("cell"),
            "onTouchstartPassive": onTouchStart,
            "onTouchend": onTouchEnd,
            "onTouchcancel": onTouchEnd
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("wrapper"),
            "style": wrapperStyle
          }, [renderSideContent("left", leftRef), (_a = slots.default) == null ? void 0 : _a.call(slots), renderSideContent("right", rightRef)])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe-cell/index.js
var require_swipe_cell = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/swipe-cell/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      SwipeCell: () => SwipeCell,
      default: () => stdin_default,
      swipeCellProps: () => import_SwipeCell2.swipeCellProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_SwipeCell = __toESM(require_SwipeCell());
    var import_SwipeCell2 = require_SwipeCell();
    var SwipeCell = (0, import_utils.withInstall)(import_SwipeCell.default);
    var stdin_default = SwipeCell;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabbar/Tabbar.js
var require_Tabbar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabbar/Tabbar.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      TABBAR_KEY: () => TABBAR_KEY,
      default: () => stdin_default,
      tabbarProps: () => tabbarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use = require_index_cjs();
    var import_use_placeholder = require_use_placeholder();
    var [name, bem] = (0, import_utils.createNamespace)("tabbar");
    var tabbarProps = {
      route: Boolean,
      fixed: import_utils.truthProp,
      border: import_utils.truthProp,
      zIndex: import_utils.numericProp,
      placeholder: Boolean,
      activeColor: String,
      beforeChange: Function,
      inactiveColor: String,
      modelValue: (0, import_utils.makeNumericProp)(0),
      safeAreaInsetBottom: {
        type: Boolean,
        default: null
      }
    };
    var TABBAR_KEY = Symbol(name);
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: tabbarProps,
      emits: ["change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const root = (0, import_vue.ref)();
        const {
          linkChildren
        } = (0, import_use.useChildren)(TABBAR_KEY);
        const renderPlaceholder = (0, import_use_placeholder.usePlaceholder)(root, bem);
        const enableSafeArea = () => {
          var _a;
          return (_a = props.safeAreaInsetBottom) != null ? _a : props.fixed;
        };
        const renderTabbar = () => {
          var _a;
          const {
            fixed,
            zIndex,
            border
          } = props;
          return (0, import_vue.createVNode)("div", {
            "ref": root,
            "role": "tablist",
            "style": (0, import_utils.getZIndexStyle)(zIndex),
            "class": [bem({
              fixed
            }), {
              [import_utils.BORDER_TOP_BOTTOM]: border,
              "van-safe-area-bottom": enableSafeArea()
            }]
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots)]);
        };
        const setActive = (active, afterChange) => {
          (0, import_utils.callInterceptor)(props.beforeChange, {
            args: [active],
            done() {
              emit("update:modelValue", active);
              emit("change", active);
              afterChange();
            }
          });
        };
        linkChildren({
          props,
          setActive
        });
        return () => {
          if (props.fixed && props.placeholder) {
            return renderPlaceholder(renderTabbar);
          }
          return renderTabbar();
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabbar/index.js
var require_tabbar = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabbar/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Tabbar: () => Tabbar,
      default: () => stdin_default,
      tabbarProps: () => import_Tabbar2.tabbarProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Tabbar = __toESM(require_Tabbar());
    var import_Tabbar2 = require_Tabbar();
    var Tabbar = (0, import_utils.withInstall)(import_Tabbar.default);
    var stdin_default = Tabbar;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabbar-item/TabbarItem.js
var require_TabbarItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabbar-item/TabbarItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      tabbarItemProps: () => tabbarItemProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_Tabbar = require_Tabbar();
    var import_use = require_index_cjs();
    var import_use_route = require_use_route();
    var import_icon = require_icon();
    var import_badge = require_badge();
    var [name, bem] = (0, import_utils.createNamespace)("tabbar-item");
    var tabbarItemProps = (0, import_utils.extend)({}, import_use_route.routeProps, {
      dot: Boolean,
      icon: String,
      name: import_utils.numericProp,
      badge: import_utils.numericProp,
      badgeProps: Object,
      iconPrefix: String
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: tabbarItemProps,
      emits: ["click"],
      setup(props, {
        emit,
        slots
      }) {
        const route = (0, import_use_route.useRoute)();
        const vm = (0, import_vue.getCurrentInstance)().proxy;
        const {
          parent,
          index
        } = (0, import_use.useParent)(import_Tabbar.TABBAR_KEY);
        if (!parent) {
          if (true) {
            console.error("[Vant] <TabbarItem> must be a child component of <Tabbar>.");
          }
          return;
        }
        const active = (0, import_vue.computed)(() => {
          var _a;
          const {
            route: route2,
            modelValue
          } = parent.props;
          if (route2 && "$route" in vm) {
            const {
              $route
            } = vm;
            const {
              to
            } = props;
            const config = (0, import_utils.isObject)(to) ? to : {
              path: to
            };
            return $route.matched.some((val) => {
              const pathMatched = "path" in config && config.path === val.path;
              const nameMatched = "name" in config && config.name === val.name;
              return pathMatched || nameMatched;
            });
          }
          return ((_a = props.name) != null ? _a : index.value) === modelValue;
        });
        const onClick = (event) => {
          var _a;
          if (!active.value) {
            parent.setActive((_a = props.name) != null ? _a : index.value, route);
          }
          emit("click", event);
        };
        const renderIcon = () => {
          if (slots.icon) {
            return slots.icon({
              active: active.value
            });
          }
          if (props.icon) {
            return (0, import_vue.createVNode)(import_icon.Icon, {
              "name": props.icon,
              "classPrefix": props.iconPrefix
            }, null);
          }
        };
        return () => {
          var _a;
          const {
            dot,
            badge
          } = props;
          const {
            activeColor,
            inactiveColor
          } = parent.props;
          const color = active.value ? activeColor : inactiveColor;
          return (0, import_vue.createVNode)("div", {
            "role": "tab",
            "class": bem({
              active: active.value
            }),
            "style": {
              color
            },
            "tabindex": 0,
            "aria-selected": active.value,
            "onClick": onClick
          }, [(0, import_vue.createVNode)(import_badge.Badge, (0, import_vue.mergeProps)({
            "dot": dot,
            "class": bem("icon"),
            "content": badge
          }, props.badgeProps), {
            default: renderIcon
          }), (0, import_vue.createVNode)("div", {
            "class": bem("text")
          }, [(_a = slots.default) == null ? void 0 : _a.call(slots, {
            active: active.value
          })])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabbar-item/index.js
var require_tabbar_item = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tabbar-item/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      TabbarItem: () => TabbarItem,
      default: () => stdin_default,
      tabbarItemProps: () => import_TabbarItem2.tabbarItemProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_TabbarItem = __toESM(require_TabbarItem());
    var import_TabbarItem2 = require_TabbarItem();
    var TabbarItem = (0, import_utils.withInstall)(import_TabbarItem.default);
    var stdin_default = TabbarItem;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/text-ellipsis/TextEllipsis.js
var require_TextEllipsis = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/text-ellipsis/TextEllipsis.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      textEllipsisProps: () => textEllipsisProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_use_expose = require_use_expose();
    var [name, bem] = (0, import_utils.createNamespace)("text-ellipsis");
    var textEllipsisProps = {
      rows: (0, import_utils.makeNumericProp)(1),
      dots: (0, import_utils.makeStringProp)("..."),
      content: (0, import_utils.makeStringProp)(""),
      expandText: (0, import_utils.makeStringProp)(""),
      collapseText: (0, import_utils.makeStringProp)(""),
      position: (0, import_utils.makeStringProp)("end")
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: textEllipsisProps,
      emits: ["clickAction"],
      setup(props, {
        emit,
        slots
      }) {
        const text = (0, import_vue.ref)(props.content);
        const expanded = (0, import_vue.ref)(false);
        const hasAction = (0, import_vue.ref)(false);
        const root = (0, import_vue.ref)();
        const actionRef = (0, import_vue.ref)();
        let needRecalculate = false;
        const actionText = (0, import_vue.computed)(() => expanded.value ? props.collapseText : props.expandText);
        const pxToNum = (value) => {
          if (!value) return 0;
          const match = value.match(/^\d*(\.\d*)?/);
          return match ? Number(match[0]) : 0;
        };
        const cloneContainer = () => {
          if (!root.value || !root.value.isConnected) return;
          const originStyle = window.getComputedStyle(root.value);
          const container = document.createElement("div");
          const styleNames = Array.prototype.slice.apply(originStyle);
          styleNames.forEach((name2) => {
            container.style.setProperty(name2, originStyle.getPropertyValue(name2));
          });
          container.style.position = "fixed";
          container.style.zIndex = "-9999";
          container.style.top = "-9999px";
          container.style.height = "auto";
          container.style.minHeight = "auto";
          container.style.maxHeight = "auto";
          container.innerText = props.content;
          document.body.appendChild(container);
          return container;
        };
        const calcEllipsisText = (container, maxHeight) => {
          var _a, _b;
          const {
            content,
            position,
            dots
          } = props;
          const end = content.length;
          const middle = 0 + end >> 1;
          const actionHTML = slots.action ? (_b = (_a = actionRef.value) == null ? void 0 : _a.outerHTML) != null ? _b : "" : props.expandText;
          const calcEllipse = () => {
            const tail = (left, right) => {
              if (right - left <= 1) {
                if (position === "end") {
                  return content.slice(0, left) + dots;
                }
                return dots + content.slice(right, end);
              }
              const middle2 = Math.round((left + right) / 2);
              if (position === "end") {
                container.innerText = content.slice(0, middle2) + dots;
              } else {
                container.innerText = dots + content.slice(middle2, end);
              }
              container.innerHTML += actionHTML;
              if (container.offsetHeight > maxHeight) {
                if (position === "end") {
                  return tail(left, middle2);
                }
                return tail(middle2, right);
              }
              if (position === "end") {
                return tail(middle2, right);
              }
              return tail(left, middle2);
            };
            return tail(0, end);
          };
          const middleTail = (leftPart, rightPart) => {
            if (leftPart[1] - leftPart[0] <= 1 && rightPart[1] - rightPart[0] <= 1) {
              return content.slice(0, leftPart[0]) + dots + content.slice(rightPart[1], end);
            }
            const leftMiddle = Math.floor((leftPart[0] + leftPart[1]) / 2);
            const rightMiddle = Math.ceil((rightPart[0] + rightPart[1]) / 2);
            container.innerText = props.content.slice(0, leftMiddle) + props.dots + props.content.slice(rightMiddle, end);
            container.innerHTML += actionHTML;
            if (container.offsetHeight >= maxHeight) {
              return middleTail([leftPart[0], leftMiddle], [rightMiddle, rightPart[1]]);
            }
            return middleTail([leftMiddle, leftPart[1]], [rightPart[0], rightMiddle]);
          };
          return props.position === "middle" ? middleTail([0, middle], [middle, end]) : calcEllipse();
        };
        const calcEllipsised = () => {
          const container = cloneContainer();
          if (!container) {
            needRecalculate = true;
            return;
          }
          const {
            paddingBottom,
            paddingTop,
            lineHeight
          } = container.style;
          const maxHeight = Math.ceil((Number(props.rows) + 0.5) * pxToNum(lineHeight) + pxToNum(paddingTop) + pxToNum(paddingBottom));
          if (maxHeight < container.offsetHeight) {
            hasAction.value = true;
            text.value = calcEllipsisText(container, maxHeight);
          } else {
            hasAction.value = false;
            text.value = props.content;
          }
          document.body.removeChild(container);
        };
        const toggle = (isExpanded = !expanded.value) => {
          expanded.value = isExpanded;
        };
        const onClickAction = (event) => {
          toggle();
          emit("clickAction", event);
        };
        const renderAction = () => {
          const action = slots.action ? slots.action({
            expanded: expanded.value
          }) : actionText.value;
          return (0, import_vue.createVNode)("span", {
            "ref": actionRef,
            "class": bem("action"),
            "onClick": onClickAction
          }, [action]);
        };
        (0, import_vue.onMounted)(() => {
          calcEllipsised();
          if (slots.action) {
            (0, import_vue.nextTick)(calcEllipsised);
          }
        });
        (0, import_vue.onActivated)(() => {
          if (needRecalculate) {
            needRecalculate = false;
            calcEllipsised();
          }
        });
        (0, import_vue.watch)([import_utils.windowWidth, () => [props.content, props.rows, props.position]], calcEllipsised);
        (0, import_use_expose.useExpose)({
          toggle
        });
        return () => (0, import_vue.createVNode)("div", {
          "ref": root,
          "class": bem()
        }, [expanded.value ? props.content : text.value, hasAction.value ? renderAction() : null]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/text-ellipsis/index.js
var require_text_ellipsis = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/text-ellipsis/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      TextEllipsis: () => TextEllipsis,
      default: () => stdin_default,
      textEllipsisProps: () => import_TextEllipsis2.textEllipsisProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_TextEllipsis = __toESM(require_TextEllipsis());
    var import_TextEllipsis2 = require_TextEllipsis();
    var TextEllipsis = (0, import_utils.withInstall)(import_TextEllipsis.default);
    var stdin_default = TextEllipsis;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/time-picker/TimePicker.js
var require_TimePicker = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/time-picker/TimePicker.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      timePickerProps: () => timePickerProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils7();
    var import_utils2 = require_utils();
    var import_use_expose = require_use_expose();
    var import_picker = require_picker();
    var [name] = (0, import_utils2.createNamespace)("time-picker");
    var validateTime = (val) => /^([01]\d|2[0-3]):([0-5]\d):([0-5]\d)$/.test(val);
    var fullColumns = ["hour", "minute", "second"];
    var timePickerProps = (0, import_utils2.extend)({}, import_utils.sharedProps, {
      minHour: (0, import_utils2.makeNumericProp)(0),
      maxHour: (0, import_utils2.makeNumericProp)(23),
      minMinute: (0, import_utils2.makeNumericProp)(0),
      maxMinute: (0, import_utils2.makeNumericProp)(59),
      minSecond: (0, import_utils2.makeNumericProp)(0),
      maxSecond: (0, import_utils2.makeNumericProp)(59),
      minTime: {
        type: String,
        validator: validateTime
      },
      maxTime: {
        type: String,
        validator: validateTime
      },
      columnsType: {
        type: Array,
        default: () => ["hour", "minute"]
      }
    });
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: timePickerProps,
      emits: ["confirm", "cancel", "change", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const currentValues = (0, import_vue.ref)(props.modelValue);
        const pickerRef = (0, import_vue.ref)();
        const getValidTime = (time) => {
          const timeLimitArr = time.split(":");
          return fullColumns.map((col, i) => props.columnsType.includes(col) ? timeLimitArr[i] : "00");
        };
        const confirm = () => {
          var _a;
          return (_a = pickerRef.value) == null ? void 0 : _a.confirm();
        };
        const getSelectedTime = () => currentValues.value;
        const columns = (0, import_vue.computed)(() => {
          let {
            minHour,
            maxHour,
            minMinute,
            maxMinute,
            minSecond,
            maxSecond
          } = props;
          if (props.minTime || props.maxTime) {
            const fullTime = {
              hour: 0,
              minute: 0,
              second: 0
            };
            props.columnsType.forEach((col, i) => {
              var _a;
              fullTime[col] = (_a = currentValues.value[i]) != null ? _a : 0;
            });
            const {
              hour,
              minute
            } = fullTime;
            if (props.minTime) {
              const [minH, minM, minS] = getValidTime(props.minTime);
              minHour = minH;
              minMinute = +hour <= +minHour ? minM : "00";
              minSecond = +hour <= +minHour && +minute <= +minMinute ? minS : "00";
            }
            if (props.maxTime) {
              const [maxH, maxM, maxS] = getValidTime(props.maxTime);
              maxHour = maxH;
              maxMinute = +hour >= +maxHour ? maxM : "59";
              maxSecond = +hour >= +maxHour && +minute >= +maxMinute ? maxS : "59";
            }
          }
          return props.columnsType.map((type) => {
            const {
              filter,
              formatter
            } = props;
            switch (type) {
              case "hour":
                return (0, import_utils.genOptions)(+minHour, +maxHour, type, formatter, filter, currentValues.value);
              case "minute":
                return (0, import_utils.genOptions)(+minMinute, +maxMinute, type, formatter, filter, currentValues.value);
              case "second":
                return (0, import_utils.genOptions)(+minSecond, +maxSecond, type, formatter, filter, currentValues.value);
              default:
                if (true) {
                  throw new Error(`[Vant] TimePicker: unsupported columns type: ${type}`);
                }
                return [];
            }
          });
        });
        (0, import_vue.watch)(currentValues, (newValues) => {
          if (!(0, import_utils2.isSameValue)(newValues, props.modelValue)) {
            emit("update:modelValue", newValues);
          }
        });
        (0, import_vue.watch)(() => props.modelValue, (newValues) => {
          newValues = (0, import_utils.formatValueRange)(newValues, columns.value);
          if (!(0, import_utils2.isSameValue)(newValues, currentValues.value)) {
            currentValues.value = newValues;
          }
        }, {
          immediate: true
        });
        const onChange = (...args) => emit("change", ...args);
        const onCancel = (...args) => emit("cancel", ...args);
        const onConfirm = (...args) => emit("confirm", ...args);
        (0, import_use_expose.useExpose)({
          confirm,
          getSelectedTime
        });
        return () => (0, import_vue.createVNode)(import_picker.Picker, (0, import_vue.mergeProps)({
          "ref": pickerRef,
          "modelValue": currentValues.value,
          "onUpdate:modelValue": ($event) => currentValues.value = $event,
          "columns": columns.value,
          "onChange": onChange,
          "onCancel": onCancel,
          "onConfirm": onConfirm
        }, (0, import_utils2.pick)(props, import_utils.pickerInheritKeys)), slots);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/time-picker/index.js
var require_time_picker = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/time-picker/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      TimePicker: () => TimePicker,
      default: () => stdin_default,
      timePickerProps: () => import_TimePicker2.timePickerProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_TimePicker = __toESM(require_TimePicker());
    var import_TimePicker2 = require_TimePicker();
    var TimePicker = (0, import_utils.withInstall)(import_TimePicker.default);
    var stdin_default = TimePicker;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tree-select/TreeSelect.js
var require_TreeSelect = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tree-select/TreeSelect.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      treeSelectProps: () => treeSelectProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_icon = require_icon();
    var import_sidebar = require_sidebar();
    var import_sidebar_item = require_sidebar_item();
    var [name, bem] = (0, import_utils.createNamespace)("tree-select");
    var treeSelectProps = {
      max: (0, import_utils.makeNumericProp)(Infinity),
      items: (0, import_utils.makeArrayProp)(),
      height: (0, import_utils.makeNumericProp)(300),
      selectedIcon: (0, import_utils.makeStringProp)("success"),
      mainActiveIndex: (0, import_utils.makeNumericProp)(0),
      activeId: {
        type: [Number, String, Array],
        default: 0
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: treeSelectProps,
      emits: ["clickNav", "clickItem", "update:activeId", "update:mainActiveIndex"],
      setup(props, {
        emit,
        slots
      }) {
        const isActiveItem = (id) => Array.isArray(props.activeId) ? props.activeId.includes(id) : props.activeId === id;
        const renderSubItem = (item) => {
          const onClick = () => {
            if (item.disabled) {
              return;
            }
            let activeId;
            if (Array.isArray(props.activeId)) {
              activeId = props.activeId.slice();
              const index = activeId.indexOf(item.id);
              if (index !== -1) {
                activeId.splice(index, 1);
              } else if (activeId.length < +props.max) {
                activeId.push(item.id);
              }
            } else {
              activeId = item.id;
            }
            emit("update:activeId", activeId);
            emit("clickItem", item);
          };
          return (0, import_vue.createVNode)("div", {
            "key": item.id,
            "class": ["van-ellipsis", bem("item", {
              active: isActiveItem(item.id),
              disabled: item.disabled
            })],
            "onClick": onClick
          }, [item.text, isActiveItem(item.id) && (0, import_vue.createVNode)(import_icon.Icon, {
            "name": props.selectedIcon,
            "class": bem("selected")
          }, null)]);
        };
        const onSidebarChange = (index) => {
          emit("update:mainActiveIndex", index);
        };
        const onClickSidebarItem = (index) => emit("clickNav", index);
        const renderSidebar = () => {
          const Items = props.items.map((item) => (0, import_vue.createVNode)(import_sidebar_item.SidebarItem, {
            "dot": item.dot,
            "badge": item.badge,
            "class": [bem("nav-item"), item.className],
            "disabled": item.disabled,
            "onClick": onClickSidebarItem
          }, {
            title: () => slots["nav-text"] ? slots["nav-text"](item) : item.text
          }));
          return (0, import_vue.createVNode)(import_sidebar.Sidebar, {
            "class": bem("nav"),
            "modelValue": props.mainActiveIndex,
            "onChange": onSidebarChange
          }, {
            default: () => [Items]
          });
        };
        const renderContent = () => {
          if (slots.content) {
            return slots.content();
          }
          const selected = props.items[+props.mainActiveIndex] || {};
          if (selected.children) {
            return selected.children.map(renderSubItem);
          }
        };
        return () => (0, import_vue.createVNode)("div", {
          "class": bem(),
          "style": {
            height: (0, import_utils.addUnit)(props.height)
          }
        }, [renderSidebar(), (0, import_vue.createVNode)("div", {
          "class": bem("content")
        }, [renderContent()])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tree-select/index.js
var require_tree_select = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/tree-select/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      TreeSelect: () => TreeSelect,
      default: () => stdin_default,
      treeSelectProps: () => import_TreeSelect2.treeSelectProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_TreeSelect = __toESM(require_TreeSelect());
    var import_TreeSelect2 = require_TreeSelect();
    var TreeSelect = (0, import_utils.withInstall)(import_TreeSelect.default);
    var stdin_default = TreeSelect;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/uploader/utils.js
var require_utils10 = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/uploader/utils.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      bem: () => bem,
      filterFiles: () => filterFiles,
      isImageFile: () => isImageFile,
      isImageUrl: () => isImageUrl,
      isOversize: () => isOversize,
      name: () => name,
      readFileContent: () => readFileContent,
      t: () => t
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var [name, bem, t] = (0, import_utils.createNamespace)("uploader");
    function readFileContent(file, resultType) {
      return new Promise((resolve) => {
        if (resultType === "file") {
          resolve();
          return;
        }
        const reader = new FileReader();
        reader.onload = (event) => {
          resolve(event.target.result);
        };
        if (resultType === "dataUrl") {
          reader.readAsDataURL(file);
        } else if (resultType === "text") {
          reader.readAsText(file);
        }
      });
    }
    function isOversize(items, maxSize) {
      return (0, import_utils.toArray)(items).some((item) => {
        if (item.file) {
          if ((0, import_utils.isFunction)(maxSize)) {
            return maxSize(item.file);
          }
          return item.file.size > +maxSize;
        }
        return false;
      });
    }
    function filterFiles(items, maxSize) {
      const valid = [];
      const invalid = [];
      items.forEach((item) => {
        if (isOversize(item, maxSize)) {
          invalid.push(item);
        } else {
          valid.push(item);
        }
      });
      return { valid, invalid };
    }
    var IMAGE_REGEXP = /\.(jpeg|jpg|gif|png|svg|webp|jfif|bmp|dpg|avif)/i;
    var isImageUrl = (url) => IMAGE_REGEXP.test(url);
    function isImageFile(item) {
      if (item.isImage) {
        return true;
      }
      if (item.file && item.file.type) {
        return item.file.type.indexOf("image") === 0;
      }
      if (item.url) {
        return isImageUrl(item.url);
      }
      if (typeof item.content === "string") {
        return item.content.indexOf("data:image") === 0;
      }
      return false;
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/uploader/UploaderPreviewItem.js
var require_UploaderPreviewItem = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/uploader/UploaderPreviewItem.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils10();
    var import_utils2 = require_utils();
    var import_icon = require_icon();
    var import_image = require_image();
    var import_loading = require_loading();
    var stdin_default = (0, import_vue.defineComponent)({
      props: {
        name: import_utils2.numericProp,
        item: (0, import_utils2.makeRequiredProp)(Object),
        index: Number,
        imageFit: String,
        lazyLoad: Boolean,
        deletable: Boolean,
        reupload: Boolean,
        previewSize: [Number, String, Array],
        beforeDelete: Function
      },
      emits: ["delete", "preview", "reupload"],
      setup(props, {
        emit,
        slots
      }) {
        const renderMask = () => {
          const {
            status,
            message
          } = props.item;
          if (status === "uploading" || status === "failed") {
            const MaskIcon = status === "failed" ? (0, import_vue.createVNode)(import_icon.Icon, {
              "name": "close",
              "class": (0, import_utils.bem)("mask-icon")
            }, null) : (0, import_vue.createVNode)(import_loading.Loading, {
              "class": (0, import_utils.bem)("loading")
            }, null);
            const showMessage = (0, import_utils2.isDef)(message) && message !== "";
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils.bem)("mask")
            }, [MaskIcon, showMessage && (0, import_vue.createVNode)("div", {
              "class": (0, import_utils.bem)("mask-message")
            }, [message])]);
          }
        };
        const onDelete = (event) => {
          const {
            name,
            item,
            index,
            beforeDelete
          } = props;
          event.stopPropagation();
          (0, import_utils2.callInterceptor)(beforeDelete, {
            args: [item, {
              name,
              index
            }],
            done: () => emit("delete")
          });
        };
        const onPreview = () => emit("preview");
        const onReupload = () => emit("reupload");
        const renderDeleteIcon = () => {
          if (props.deletable && props.item.status !== "uploading") {
            const slot = slots["preview-delete"];
            return (0, import_vue.createVNode)("div", {
              "role": "button",
              "class": (0, import_utils.bem)("preview-delete", {
                shadow: !slot
              }),
              "tabindex": 0,
              "aria-label": (0, import_utils.t)("delete"),
              "onClick": onDelete
            }, [slot ? slot() : (0, import_vue.createVNode)(import_icon.Icon, {
              "name": "cross",
              "class": (0, import_utils.bem)("preview-delete-icon")
            }, null)]);
          }
        };
        const renderCover = () => {
          if (slots["preview-cover"]) {
            const {
              index,
              item
            } = props;
            return (0, import_vue.createVNode)("div", {
              "class": (0, import_utils.bem)("preview-cover")
            }, [slots["preview-cover"]((0, import_utils2.extend)({
              index
            }, item))]);
          }
        };
        const renderPreview = () => {
          const {
            item,
            lazyLoad,
            imageFit,
            previewSize,
            reupload
          } = props;
          if ((0, import_utils.isImageFile)(item)) {
            return (0, import_vue.createVNode)(import_image.Image, {
              "fit": imageFit,
              "src": item.objectUrl || item.content || item.url,
              "class": (0, import_utils.bem)("preview-image"),
              "width": Array.isArray(previewSize) ? previewSize[0] : previewSize,
              "height": Array.isArray(previewSize) ? previewSize[1] : previewSize,
              "lazyLoad": lazyLoad,
              "onClick": reupload ? onReupload : onPreview
            }, {
              default: renderCover
            });
          }
          return (0, import_vue.createVNode)("div", {
            "class": (0, import_utils.bem)("file"),
            "style": (0, import_utils2.getSizeStyle)(props.previewSize)
          }, [(0, import_vue.createVNode)(import_icon.Icon, {
            "class": (0, import_utils.bem)("file-icon"),
            "name": "description"
          }, null), (0, import_vue.createVNode)("div", {
            "class": [(0, import_utils.bem)("file-name"), "van-ellipsis"]
          }, [item.file ? item.file.name : item.url]), renderCover()]);
        };
        return () => (0, import_vue.createVNode)("div", {
          "class": (0, import_utils.bem)("preview")
        }, [renderPreview(), renderMask(), renderDeleteIcon()]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/uploader/Uploader.js
var require_Uploader = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/uploader/Uploader.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      uploaderProps: () => uploaderProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var import_utils2 = require_utils10();
    var import_use = require_index_cjs();
    var import_use_expose = require_use_expose();
    var import_icon = require_icon();
    var import_image_preview = require_image_preview();
    var import_UploaderPreviewItem = __toESM(require_UploaderPreviewItem());
    var uploaderProps = {
      name: (0, import_utils.makeNumericProp)(""),
      accept: (0, import_utils.makeStringProp)("image/*"),
      capture: String,
      multiple: Boolean,
      disabled: Boolean,
      readonly: Boolean,
      lazyLoad: Boolean,
      maxCount: (0, import_utils.makeNumericProp)(Infinity),
      imageFit: (0, import_utils.makeStringProp)("cover"),
      resultType: (0, import_utils.makeStringProp)("dataUrl"),
      uploadIcon: (0, import_utils.makeStringProp)("photograph"),
      uploadText: String,
      deletable: import_utils.truthProp,
      reupload: Boolean,
      afterRead: Function,
      showUpload: import_utils.truthProp,
      modelValue: (0, import_utils.makeArrayProp)(),
      beforeRead: Function,
      beforeDelete: Function,
      previewSize: [Number, String, Array],
      previewImage: import_utils.truthProp,
      previewOptions: Object,
      previewFullImage: import_utils.truthProp,
      maxSize: {
        type: [Number, String, Function],
        default: Infinity
      }
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name: import_utils2.name,
      props: uploaderProps,
      emits: ["delete", "oversize", "clickUpload", "closePreview", "clickPreview", "clickReupload", "update:modelValue"],
      setup(props, {
        emit,
        slots
      }) {
        const inputRef = (0, import_vue.ref)();
        const urls = [];
        const reuploadIndex = (0, import_vue.ref)(-1);
        const isReuploading = (0, import_vue.ref)(false);
        const getDetail = (index = props.modelValue.length) => ({
          name: props.name,
          index
        });
        const resetInput = () => {
          if (inputRef.value) {
            inputRef.value.value = "";
          }
        };
        const onAfterRead = (items) => {
          resetInput();
          if ((0, import_utils2.isOversize)(items, props.maxSize)) {
            if (Array.isArray(items)) {
              const result = (0, import_utils2.filterFiles)(items, props.maxSize);
              items = result.valid;
              emit("oversize", result.invalid, getDetail());
              if (!items.length) {
                return;
              }
            } else {
              emit("oversize", items, getDetail());
              return;
            }
          }
          items = (0, import_vue.reactive)(items);
          if (reuploadIndex.value > -1) {
            const arr = [...props.modelValue];
            arr.splice(reuploadIndex.value, 1, items);
            emit("update:modelValue", arr);
            reuploadIndex.value = -1;
          } else {
            emit("update:modelValue", [...props.modelValue, ...(0, import_utils.toArray)(items)]);
          }
          if (props.afterRead) {
            props.afterRead(items, getDetail());
          }
        };
        const readFile = (files) => {
          const {
            maxCount,
            modelValue,
            resultType
          } = props;
          if (Array.isArray(files)) {
            const remainCount = +maxCount - modelValue.length;
            if (files.length > remainCount) {
              files = files.slice(0, remainCount);
            }
            Promise.all(files.map((file) => (0, import_utils2.readFileContent)(file, resultType))).then((contents) => {
              const fileList = files.map((file, index) => {
                const result = {
                  file,
                  status: "",
                  message: "",
                  objectUrl: URL.createObjectURL(file)
                };
                if (contents[index]) {
                  result.content = contents[index];
                }
                return result;
              });
              onAfterRead(fileList);
            });
          } else {
            (0, import_utils2.readFileContent)(files, resultType).then((content) => {
              const result = {
                file: files,
                status: "",
                message: "",
                objectUrl: URL.createObjectURL(files)
              };
              if (content) {
                result.content = content;
              }
              onAfterRead(result);
            });
          }
        };
        const onChange = (event) => {
          const {
            files
          } = event.target;
          if (props.disabled || !files || !files.length) {
            return;
          }
          const file = files.length === 1 ? files[0] : [].slice.call(files);
          if (props.beforeRead) {
            const response = props.beforeRead(file, getDetail());
            if (!response) {
              resetInput();
              return;
            }
            if ((0, import_utils.isPromise)(response)) {
              response.then((data) => {
                if (data) {
                  readFile(data);
                } else {
                  readFile(file);
                }
              }).catch(resetInput);
              return;
            }
          }
          readFile(file);
        };
        let imagePreview;
        const onClosePreview = () => emit("closePreview");
        const previewImage = (item) => {
          if (props.previewFullImage) {
            const imageFiles = props.modelValue.filter(import_utils2.isImageFile);
            const images = imageFiles.map((item2) => {
              if (item2.objectUrl && !item2.url && item2.status !== "failed") {
                item2.url = item2.objectUrl;
                urls.push(item2.url);
              }
              return item2.url;
            }).filter(Boolean);
            imagePreview = (0, import_image_preview.showImagePreview)((0, import_utils.extend)({
              images,
              startPosition: imageFiles.indexOf(item),
              onClose: onClosePreview
            }, props.previewOptions));
          }
        };
        const closeImagePreview = () => {
          if (imagePreview) {
            imagePreview.close();
          }
        };
        const deleteFile = (item, index) => {
          const fileList = props.modelValue.slice(0);
          fileList.splice(index, 1);
          emit("update:modelValue", fileList);
          emit("delete", item, getDetail(index));
        };
        const reuploadFile = (index) => {
          isReuploading.value = true;
          reuploadIndex.value = index;
          (0, import_vue.nextTick)(() => chooseFile());
        };
        const onInputClick = () => {
          if (!isReuploading.value) {
            reuploadIndex.value = -1;
          }
          isReuploading.value = false;
        };
        const renderPreviewItem = (item, index) => {
          const needPickData = ["imageFit", "deletable", "reupload", "previewSize", "beforeDelete"];
          const previewData = (0, import_utils.extend)((0, import_utils.pick)(props, needPickData), (0, import_utils.pick)(item, needPickData, true));
          return (0, import_vue.createVNode)(import_UploaderPreviewItem.default, (0, import_vue.mergeProps)({
            "item": item,
            "index": index,
            "onClick": () => emit(props.reupload ? "clickReupload" : "clickPreview", item, getDetail(index)),
            "onDelete": () => deleteFile(item, index),
            "onPreview": () => previewImage(item),
            "onReupload": () => reuploadFile(index)
          }, (0, import_utils.pick)(props, ["name", "lazyLoad"]), previewData), (0, import_utils.pick)(slots, ["preview-cover", "preview-delete"]));
        };
        const renderPreviewList = () => {
          if (props.previewImage) {
            return props.modelValue.map(renderPreviewItem);
          }
        };
        const onClickUpload = (event) => emit("clickUpload", event);
        const renderUpload = () => {
          const lessThanMax = props.modelValue.length < +props.maxCount;
          const Input = props.readonly ? null : (0, import_vue.createVNode)("input", {
            "ref": inputRef,
            "type": "file",
            "class": (0, import_utils2.bem)("input"),
            "accept": props.accept,
            "capture": props.capture,
            "multiple": props.multiple && reuploadIndex.value === -1,
            "disabled": props.disabled,
            "onChange": onChange,
            "onClick": onInputClick
          }, null);
          if (slots.default) {
            return (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", {
              "class": (0, import_utils2.bem)("input-wrapper"),
              "onClick": onClickUpload
            }, [slots.default(), Input]), [[import_vue.vShow, lessThanMax]]);
          }
          return (0, import_vue.withDirectives)((0, import_vue.createVNode)("div", {
            "class": (0, import_utils2.bem)("upload", {
              readonly: props.readonly
            }),
            "style": (0, import_utils.getSizeStyle)(props.previewSize),
            "onClick": onClickUpload
          }, [(0, import_vue.createVNode)(import_icon.Icon, {
            "name": props.uploadIcon,
            "class": (0, import_utils2.bem)("upload-icon")
          }, null), props.uploadText && (0, import_vue.createVNode)("span", {
            "class": (0, import_utils2.bem)("upload-text")
          }, [props.uploadText]), Input]), [[import_vue.vShow, props.showUpload && lessThanMax]]);
        };
        const chooseFile = () => {
          if (inputRef.value && !props.disabled) {
            inputRef.value.click();
          }
        };
        (0, import_vue.onBeforeUnmount)(() => {
          urls.forEach((url) => URL.revokeObjectURL(url));
        });
        (0, import_use_expose.useExpose)({
          chooseFile,
          reuploadFile,
          closeImagePreview
        });
        (0, import_use.useCustomFieldValue)(() => props.modelValue);
        return () => (0, import_vue.createVNode)("div", {
          "class": (0, import_utils2.bem)()
        }, [(0, import_vue.createVNode)("div", {
          "class": (0, import_utils2.bem)("wrapper", {
            disabled: props.disabled
          })
        }, [renderPreviewList(), renderUpload()])]);
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/uploader/index.js
var require_uploader = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/uploader/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Uploader: () => Uploader,
      default: () => stdin_default,
      uploaderProps: () => import_Uploader2.uploaderProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Uploader = __toESM(require_Uploader());
    var import_Uploader2 = require_Uploader();
    var Uploader = (0, import_utils.withInstall)(import_Uploader.default);
    var stdin_default = Uploader;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/watermark/Watermark.js
var require_Watermark = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/watermark/Watermark.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name2 in all)
        __defProp(target, name2, { get: all[name2], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      watermarkProps: () => watermarkProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_utils = require_utils();
    var [name, bem] = (0, import_utils.createNamespace)("watermark");
    var watermarkProps = {
      gapX: (0, import_utils.makeNumberProp)(0),
      gapY: (0, import_utils.makeNumberProp)(0),
      image: String,
      width: (0, import_utils.makeNumberProp)(100),
      height: (0, import_utils.makeNumberProp)(100),
      rotate: (0, import_utils.makeNumericProp)(-22),
      zIndex: import_utils.numericProp,
      content: String,
      opacity: import_utils.numericProp,
      fullPage: import_utils.truthProp,
      textColor: (0, import_utils.makeStringProp)("#dcdee0")
    };
    var stdin_default = (0, import_vue.defineComponent)({
      name,
      props: watermarkProps,
      setup(props, {
        slots
      }) {
        const svgElRef = (0, import_vue.ref)();
        const watermarkUrl = (0, import_vue.ref)("");
        const imageBase64 = (0, import_vue.ref)("");
        const renderWatermark = () => {
          const rotateStyle = {
            transformOrigin: "center",
            transform: `rotate(${props.rotate}deg)`
          };
          const svgInner = () => {
            if (props.image && !slots.content) {
              return (0, import_vue.createVNode)("image", {
                "href": imageBase64.value,
                "xlink:href": imageBase64.value,
                "x": "0",
                "y": "0",
                "width": props.width,
                "height": props.height,
                "style": rotateStyle
              }, null);
            }
            return (0, import_vue.createVNode)("foreignObject", {
              "x": "0",
              "y": "0",
              "width": props.width,
              "height": props.height
            }, [(0, import_vue.createVNode)("div", {
              "xmlns": "http://www.w3.org/1999/xhtml",
              "style": rotateStyle
            }, [slots.content ? slots.content() : (0, import_vue.createVNode)("span", {
              "style": {
                color: props.textColor
              }
            }, [props.content])])]);
          };
          const svgWidth = props.width + props.gapX;
          const svgHeight = props.height + props.gapY;
          return (0, import_vue.createVNode)("svg", {
            "viewBox": `0 0 ${svgWidth} ${svgHeight}`,
            "width": svgWidth,
            "height": svgHeight,
            "xmlns": "http://www.w3.org/2000/svg",
            "xmlns:xlink": "http://www.w3.org/1999/xlink",
            "style": {
              padding: `0 ${props.gapX}px ${props.gapY}px 0`,
              opacity: props.opacity
            }
          }, [svgInner()]);
        };
        const makeImageToBase64 = (url) => {
          const canvas = document.createElement("canvas");
          const image = new Image();
          image.crossOrigin = "anonymous";
          image.referrerPolicy = "no-referrer";
          image.onload = () => {
            canvas.width = image.naturalWidth;
            canvas.height = image.naturalHeight;
            const ctx = canvas.getContext("2d");
            ctx == null ? void 0 : ctx.drawImage(image, 0, 0);
            imageBase64.value = canvas.toDataURL();
          };
          image.src = url;
        };
        const makeSvgToBlobUrl = (svgStr) => {
          const svgBlob = new Blob([svgStr], {
            type: "image/svg+xml"
          });
          return URL.createObjectURL(svgBlob);
        };
        const revokeWatermarkUrl = () => {
          if (watermarkUrl.value) {
            URL.revokeObjectURL(watermarkUrl.value);
          }
        };
        const generateWatermarkUrl = () => {
          if (svgElRef.value) {
            revokeWatermarkUrl();
            watermarkUrl.value = makeSvgToBlobUrl(svgElRef.value.innerHTML);
          }
        };
        (0, import_vue.watchEffect)(() => {
          if (props.image) {
            makeImageToBase64(props.image);
          }
        });
        (0, import_vue.watch)(() => [props.content, props.textColor, props.height, props.width, props.rotate, props.gapX, props.gapY], generateWatermarkUrl);
        (0, import_vue.watch)(imageBase64, () => {
          (0, import_vue.nextTick)(generateWatermarkUrl);
        });
        (0, import_vue.onMounted)(generateWatermarkUrl);
        (0, import_vue.onUnmounted)(revokeWatermarkUrl);
        return () => {
          const style = (0, import_utils.extend)({
            backgroundImage: `url(${watermarkUrl.value})`
          }, (0, import_utils.getZIndexStyle)(props.zIndex));
          return (0, import_vue.createVNode)("div", {
            "class": bem({
              full: props.fullPage
            }),
            "style": style
          }, [(0, import_vue.createVNode)("div", {
            "class": bem("wrapper"),
            "ref": svgElRef
          }, [renderWatermark()])]);
        };
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/watermark/index.js
var require_watermark = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/watermark/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Watermark: () => Watermark,
      default: () => stdin_default,
      watermarkProps: () => import_Watermark2.watermarkProps
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_utils = require_utils();
    var import_Watermark = __toESM(require_Watermark());
    var import_Watermark2 = require_Watermark();
    var Watermark = (0, import_utils.withInstall)(import_Watermark.default);
    var stdin_default = Watermark;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/listener.js
var require_listener = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/listener.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => ReactiveListener
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_use = require_index_cjs();
    var import_util = require_util();
    var import_utils = require_utils();
    var ReactiveListener = class {
      constructor({
        el,
        src,
        error,
        loading,
        bindType,
        $parent,
        options,
        cors,
        elRenderer,
        imageCache
      }) {
        this.el = el;
        this.src = src;
        this.error = error;
        this.loading = loading;
        this.bindType = bindType;
        this.attempt = 0;
        this.cors = cors;
        this.naturalHeight = 0;
        this.naturalWidth = 0;
        this.options = options;
        this.$parent = $parent;
        this.elRenderer = elRenderer;
        this.imageCache = imageCache;
        this.performanceData = {
          loadStart: 0,
          loadEnd: 0
        };
        this.filter();
        this.initState();
        this.render("loading", false);
      }
      /*
       * init listener state
       * @return
       */
      initState() {
        if ("dataset" in this.el) {
          this.el.dataset.src = this.src;
        } else {
          this.el.setAttribute("data-src", this.src);
        }
        this.state = {
          loading: false,
          error: false,
          loaded: false,
          rendered: false
        };
      }
      /*
       * record performance
       * @return
       */
      record(event) {
        this.performanceData[event] = Date.now();
      }
      /*
       * update image listener data
       * @param  {String} image uri
       * @param  {String} loading image uri
       * @param  {String} error image uri
       * @return
       */
      update({ src, loading, error }) {
        const oldSrc = this.src;
        this.src = src;
        this.loading = loading;
        this.error = error;
        this.filter();
        if (oldSrc !== this.src) {
          this.attempt = 0;
          this.initState();
        }
      }
      /*
       *  check el is in view
       * @return {Boolean} el is in view
       */
      checkInView() {
        const rect = (0, import_use.useRect)(this.el);
        return rect.top < window.innerHeight * this.options.preLoad && rect.bottom > this.options.preLoadTop && rect.left < window.innerWidth * this.options.preLoad && rect.right > 0;
      }
      /*
       * listener filter
       */
      filter() {
        Object.keys(this.options.filter).forEach((key) => {
          this.options.filter[key](this, this.options);
        });
      }
      /*
       * render loading first
       * @params cb:Function
       * @return
       */
      renderLoading(cb) {
        this.state.loading = true;
        (0, import_util.loadImageAsync)(
          {
            src: this.loading,
            cors: this.cors
          },
          () => {
            this.render("loading", false);
            this.state.loading = false;
            cb();
          },
          () => {
            cb();
            this.state.loading = false;
            if (!this.options.silent)
              console.warn(
                `[@vant/lazyload] load failed with loading image(${this.loading})`
              );
          }
        );
      }
      /*
       * try load image and  render it
       * @return
       */
      load(onFinish = import_utils.noop) {
        if (this.attempt > this.options.attempt - 1 && this.state.error) {
          if (!this.options.silent) {
            console.log(
              `[@vant/lazyload] ${this.src} tried too more than ${this.options.attempt} times`
            );
          }
          onFinish();
          return;
        }
        if (this.state.rendered && this.state.loaded) return;
        if (this.imageCache.has(this.src)) {
          this.state.loaded = true;
          this.render("loaded", true);
          this.state.rendered = true;
          return onFinish();
        }
        this.renderLoading(() => {
          var _a, _b;
          this.attempt++;
          (_b = (_a = this.options.adapter).beforeLoad) == null ? void 0 : _b.call(_a, this, this.options);
          this.record("loadStart");
          (0, import_util.loadImageAsync)(
            {
              src: this.src,
              cors: this.cors
            },
            (data) => {
              this.naturalHeight = data.naturalHeight;
              this.naturalWidth = data.naturalWidth;
              this.state.loaded = true;
              this.state.error = false;
              this.record("loadEnd");
              this.render("loaded", false);
              this.state.rendered = true;
              this.imageCache.add(this.src);
              onFinish();
            },
            (err) => {
              !this.options.silent && console.error(err);
              this.state.error = true;
              this.state.loaded = false;
              this.render("error", false);
            }
          );
        });
      }
      /*
       * render image
       * @param  {String} state to render // ['loading', 'src', 'error']
       * @param  {String} is form cache
       * @return
       */
      render(state, cache) {
        this.elRenderer(this, state, cache);
      }
      /*
       * output performance data
       * @return {Object} performance data
       */
      performance() {
        let state = "loading";
        let time = 0;
        if (this.state.loaded) {
          state = "loaded";
          time = (this.performanceData.loadEnd - this.performanceData.loadStart) / 1e3;
        }
        if (this.state.error) state = "error";
        return {
          src: this.src,
          state,
          time
        };
      }
      /*
       * $destroy
       * @return
       */
      $destroy() {
        this.el = null;
        this.src = null;
        this.error = null;
        this.loading = null;
        this.bindType = null;
        this.attempt = 0;
      }
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/lazy.js
var require_lazy = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/lazy.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use = require_index_cjs();
    var import_util = require_util();
    var import_utils = require_utils();
    var import_listener = __toESM(require_listener());
    var DEFAULT_URL = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7";
    var DEFAULT_EVENTS = [
      "scroll",
      "wheel",
      "mousewheel",
      "resize",
      "animationend",
      "transitionend",
      "touchmove"
    ];
    var DEFAULT_OBSERVER_OPTIONS = {
      rootMargin: "0px",
      threshold: 0
    };
    function stdin_default() {
      return class Lazy {
        constructor({
          preLoad,
          error,
          throttleWait,
          preLoadTop,
          dispatchEvent,
          loading,
          attempt,
          silent = true,
          scale,
          listenEvents,
          filter,
          adapter,
          observer,
          observerOptions
        }) {
          this.mode = import_util.modeType.event;
          this.listeners = [];
          this.targetIndex = 0;
          this.targets = [];
          this.options = {
            silent,
            dispatchEvent: !!dispatchEvent,
            throttleWait: throttleWait || 200,
            preLoad: preLoad || 1.3,
            preLoadTop: preLoadTop || 0,
            error: error || DEFAULT_URL,
            loading: loading || DEFAULT_URL,
            attempt: attempt || 3,
            scale: scale || (0, import_util.getDPR)(scale),
            ListenEvents: listenEvents || DEFAULT_EVENTS,
            supportWebp: (0, import_util.supportWebp)(),
            filter: filter || {},
            adapter: adapter || {},
            observer: !!observer,
            observerOptions: observerOptions || DEFAULT_OBSERVER_OPTIONS
          };
          this.initEvent();
          this.imageCache = new import_util.ImageCache({ max: 200 });
          this.lazyLoadHandler = (0, import_util.throttle)(
            this.lazyLoadHandler.bind(this),
            this.options.throttleWait
          );
          this.setMode(this.options.observer ? import_util.modeType.observer : import_util.modeType.event);
        }
        /**
         * update config
         * @param  {Object} config params
         * @return
         */
        config(options = {}) {
          Object.assign(this.options, options);
        }
        /**
         * output listener's load performance
         * @return {Array}
         */
        performance() {
          return this.listeners.map((item) => item.performance());
        }
        /*
         * add lazy component to queue
         * @param  {Vue} vm lazy component instance
         * @return
         */
        addLazyBox(vm) {
          this.listeners.push(vm);
          if (import_use.inBrowser) {
            this.addListenerTarget(window);
            this.observer && this.observer.observe(vm.el);
            if (vm.$el && vm.$el.parentNode) {
              this.addListenerTarget(vm.$el.parentNode);
            }
          }
        }
        /*
         * add image listener to queue
         * @param  {DOM} el
         * @param  {object} binding vue directive binding
         * @param  {vnode} vnode vue directive vnode
         * @return
         */
        add(el, binding, vnode) {
          if (this.listeners.some((item) => item.el === el)) {
            this.update(el, binding);
            return (0, import_vue.nextTick)(this.lazyLoadHandler);
          }
          const value = this.valueFormatter(binding.value);
          let { src } = value;
          (0, import_vue.nextTick)(() => {
            src = (0, import_util.getBestSelectionFromSrcset)(el, this.options.scale) || src;
            this.observer && this.observer.observe(el);
            const container = Object.keys(binding.modifiers)[0];
            let $parent;
            if (container) {
              $parent = vnode.context.$refs[container];
              $parent = $parent ? $parent.$el || $parent : document.getElementById(container);
            }
            if (!$parent) {
              $parent = (0, import_use.getScrollParent)(el);
            }
            const newListener = new import_listener.default({
              bindType: binding.arg,
              $parent,
              el,
              src,
              loading: value.loading,
              error: value.error,
              cors: value.cors,
              elRenderer: this.elRenderer.bind(this),
              options: this.options,
              imageCache: this.imageCache
            });
            this.listeners.push(newListener);
            if (import_use.inBrowser) {
              this.addListenerTarget(window);
              this.addListenerTarget($parent);
            }
            this.lazyLoadHandler();
            (0, import_vue.nextTick)(() => this.lazyLoadHandler());
          });
        }
        /**
         * update image src
         * @param  {DOM} el
         * @param  {object} vue directive binding
         * @return
         */
        update(el, binding, vnode) {
          const value = this.valueFormatter(binding.value);
          let { src } = value;
          src = (0, import_util.getBestSelectionFromSrcset)(el, this.options.scale) || src;
          const exist = this.listeners.find((item) => item.el === el);
          if (!exist) {
            this.add(el, binding, vnode);
          } else {
            exist.update({
              src,
              error: value.error,
              loading: value.loading
            });
          }
          if (this.observer) {
            this.observer.unobserve(el);
            this.observer.observe(el);
          }
          this.lazyLoadHandler();
          (0, import_vue.nextTick)(() => this.lazyLoadHandler());
        }
        /**
         * remove listener form list
         * @param  {DOM} el
         * @return
         */
        remove(el) {
          if (!el) return;
          this.observer && this.observer.unobserve(el);
          const existItem = this.listeners.find((item) => item.el === el);
          if (existItem) {
            this.removeListenerTarget(existItem.$parent);
            this.removeListenerTarget(window);
            (0, import_util.remove)(this.listeners, existItem);
            existItem.$destroy();
          }
        }
        /*
         * remove lazy components form list
         * @param  {Vue} vm Vue instance
         * @return
         */
        removeComponent(vm) {
          if (!vm) return;
          (0, import_util.remove)(this.listeners, vm);
          this.observer && this.observer.unobserve(vm.el);
          if (vm.$parent && vm.$el.parentNode) {
            this.removeListenerTarget(vm.$el.parentNode);
          }
          this.removeListenerTarget(window);
        }
        setMode(mode) {
          if (!import_util.hasIntersectionObserver && mode === import_util.modeType.observer) {
            mode = import_util.modeType.event;
          }
          this.mode = mode;
          if (mode === import_util.modeType.event) {
            if (this.observer) {
              this.listeners.forEach((listener) => {
                this.observer.unobserve(listener.el);
              });
              this.observer = null;
            }
            this.targets.forEach((target) => {
              this.initListen(target.el, true);
            });
          } else {
            this.targets.forEach((target) => {
              this.initListen(target.el, false);
            });
            this.initIntersectionObserver();
          }
        }
        /*
         *** Private functions ***
         */
        /*
         * add listener target
         * @param  {DOM} el listener target
         * @return
         */
        addListenerTarget(el) {
          if (!el) return;
          let target = this.targets.find((target2) => target2.el === el);
          if (!target) {
            target = {
              el,
              id: ++this.targetIndex,
              childrenCount: 1,
              listened: true
            };
            this.mode === import_util.modeType.event && this.initListen(target.el, true);
            this.targets.push(target);
          } else {
            target.childrenCount++;
          }
          return this.targetIndex;
        }
        /*
         * remove listener target or reduce target childrenCount
         * @param  {DOM} el or window
         * @return
         */
        removeListenerTarget(el) {
          this.targets.forEach((target, index) => {
            if (target.el === el) {
              target.childrenCount--;
              if (!target.childrenCount) {
                this.initListen(target.el, false);
                this.targets.splice(index, 1);
                target = null;
              }
            }
          });
        }
        /*
         * add or remove eventlistener
         * @param  {DOM} el DOM or Window
         * @param  {boolean} start flag
         * @return
         */
        initListen(el, start) {
          this.options.ListenEvents.forEach(
            (evt) => (start ? import_util.on : import_util.off)(el, evt, this.lazyLoadHandler)
          );
        }
        initEvent() {
          this.Event = {
            listeners: {
              loading: [],
              loaded: [],
              error: []
            }
          };
          this.$on = (event, func) => {
            if (!this.Event.listeners[event]) this.Event.listeners[event] = [];
            this.Event.listeners[event].push(func);
          };
          this.$once = (event, func) => {
            const on2 = (...args) => {
              this.$off(event, on2);
              func.apply(this, args);
            };
            this.$on(event, on2);
          };
          this.$off = (event, func) => {
            if (!func) {
              if (!this.Event.listeners[event]) return;
              this.Event.listeners[event].length = 0;
              return;
            }
            (0, import_util.remove)(this.Event.listeners[event], func);
          };
          this.$emit = (event, context, inCache) => {
            if (!this.Event.listeners[event]) return;
            this.Event.listeners[event].forEach((func) => func(context, inCache));
          };
        }
        /**
         * find nodes which in viewport and trigger load
         * @return
         */
        lazyLoadHandler() {
          const freeList = [];
          this.listeners.forEach((listener) => {
            if (!listener.el || !listener.el.parentNode) {
              freeList.push(listener);
            }
            const catIn = listener.checkInView();
            if (!catIn) return;
            listener.load();
          });
          freeList.forEach((item) => {
            (0, import_util.remove)(this.listeners, item);
            item.$destroy();
          });
        }
        /**
         * init IntersectionObserver
         * set mode to observer
         * @return
         */
        initIntersectionObserver() {
          if (!import_util.hasIntersectionObserver) {
            return;
          }
          this.observer = new IntersectionObserver(
            this.observerHandler.bind(this),
            this.options.observerOptions
          );
          if (this.listeners.length) {
            this.listeners.forEach((listener) => {
              this.observer.observe(listener.el);
            });
          }
        }
        /**
         * init IntersectionObserver
         * @return
         */
        observerHandler(entries) {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              this.listeners.forEach((listener) => {
                if (listener.el === entry.target) {
                  if (listener.state.loaded)
                    return this.observer.unobserve(listener.el);
                  listener.load();
                }
              });
            }
          });
        }
        /**
         * set element attribute with image'url and state
         * @param  {object} lazyload listener object
         * @param  {string} state will be rendered
         * @param  {bool} inCache  is rendered from cache
         * @return
         */
        elRenderer(listener, state, cache) {
          if (!listener.el) return;
          const { el, bindType } = listener;
          let src;
          switch (state) {
            case "loading":
              src = listener.loading;
              break;
            case "error":
              src = listener.error;
              break;
            default:
              ({ src } = listener);
              break;
          }
          if (bindType) {
            el.style[bindType] = 'url("' + src + '")';
          } else if (el.getAttribute("src") !== src) {
            el.setAttribute("src", src);
          }
          el.setAttribute("lazy", state);
          this.$emit(state, listener, cache);
          this.options.adapter[state] && this.options.adapter[state](listener, this.options);
          if (this.options.dispatchEvent) {
            const event = new CustomEvent(state, {
              detail: listener
            });
            el.dispatchEvent(event);
          }
        }
        /**
         * generate loading loaded error image url
         * @param {string} image's src
         * @return {object} image's loading, loaded, error url
         */
        valueFormatter(value) {
          let src = value;
          let { loading, error } = this.options;
          if ((0, import_utils.isObject)(value)) {
            if (!value.src && !this.options.silent) {
              console.error("[@vant/lazyload] miss src with " + value);
            }
            ({ src } = value);
            loading = value.loading || this.options.loading;
            error = value.error || this.options.error;
          }
          return {
            src,
            loading,
            error
          };
        }
      };
    }
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/lazy-component.js
var require_lazy_component = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/lazy-component.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var import_use = require_index_cjs();
    var stdin_default = (lazy) => ({
      props: {
        tag: {
          type: String,
          default: "div"
        }
      },
      emits: ["show"],
      render() {
        return (0, import_vue.h)(
          this.tag,
          this.show && this.$slots.default ? this.$slots.default() : null
        );
      },
      data() {
        return {
          el: null,
          state: {
            loaded: false
          },
          show: false
        };
      },
      mounted() {
        this.el = this.$el;
        lazy.addLazyBox(this);
        lazy.lazyLoadHandler();
      },
      beforeUnmount() {
        lazy.removeComponent(this);
      },
      methods: {
        checkInView() {
          const rect = (0, import_use.useRect)(this.$el);
          return import_use.inBrowser && rect.top < window.innerHeight * lazy.options.preLoad && rect.bottom > 0 && rect.left < window.innerWidth * lazy.options.preLoad && rect.right > 0;
        },
        load() {
          this.show = true;
          this.state.loaded = true;
          this.$emit("show", this);
        },
        destroy() {
          return this.$destroy;
        }
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/lazy-container.js
var require_lazy_container = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/lazy-container.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => LazyContainerManager
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_util = require_util();
    var defaultOptions = {
      selector: "img"
    };
    var LazyContainer = class {
      constructor({ el, binding, vnode, lazy }) {
        this.el = null;
        this.vnode = vnode;
        this.binding = binding;
        this.options = {};
        this.lazy = lazy;
        this.queue = [];
        this.update({ el, binding });
      }
      update({ el, binding }) {
        this.el = el;
        this.options = Object.assign({}, defaultOptions, binding.value);
        const imgs = this.getImgs();
        imgs.forEach((el2) => {
          this.lazy.add(
            el2,
            Object.assign({}, this.binding, {
              value: {
                src: "dataset" in el2 ? el2.dataset.src : el2.getAttribute("data-src"),
                error: ("dataset" in el2 ? el2.dataset.error : el2.getAttribute("data-error")) || this.options.error,
                loading: ("dataset" in el2 ? el2.dataset.loading : el2.getAttribute("data-loading")) || this.options.loading
              }
            }),
            this.vnode
          );
        });
      }
      getImgs() {
        return Array.from(this.el.querySelectorAll(this.options.selector));
      }
      clear() {
        const imgs = this.getImgs();
        imgs.forEach((el) => this.lazy.remove(el));
        this.vnode = null;
        this.binding = null;
        this.lazy = null;
      }
    };
    var LazyContainerManager = class {
      constructor({ lazy }) {
        this.lazy = lazy;
        this.queue = [];
      }
      bind(el, binding, vnode) {
        const container = new LazyContainer({
          el,
          binding,
          vnode,
          lazy: this.lazy
        });
        this.queue.push(container);
      }
      update(el, binding, vnode) {
        const container = this.queue.find((item) => item.el === el);
        if (!container) return;
        container.update({ el, binding, vnode });
      }
      unbind(el) {
        const container = this.queue.find((item) => item.el === el);
        if (!container) return;
        container.clear();
        (0, import_util.remove)(this.queue, container);
      }
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/lazy-image.js
var require_lazy_image = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/lazy-image.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_use = require_index_cjs();
    var import_util = require_util();
    var import_utils = require_utils();
    var import_vue = (init_vue_runtime_esm_bundler(), __toCommonJS(vue_runtime_esm_bundler_exports));
    var stdin_default = (lazyManager) => ({
      props: {
        src: [String, Object],
        tag: {
          type: String,
          default: "img"
        }
      },
      render() {
        var _a, _b;
        return (0, import_vue.h)(
          this.tag,
          {
            src: this.renderSrc
          },
          (_b = (_a = this.$slots).default) == null ? void 0 : _b.call(_a)
        );
      },
      data() {
        return {
          el: null,
          options: {
            src: "",
            error: "",
            loading: "",
            attempt: lazyManager.options.attempt
          },
          state: {
            loaded: false,
            error: false,
            attempt: 0
          },
          renderSrc: ""
        };
      },
      watch: {
        src() {
          this.init();
          lazyManager.addLazyBox(this);
          lazyManager.lazyLoadHandler();
        }
      },
      created() {
        this.init();
      },
      mounted() {
        this.el = this.$el;
        lazyManager.addLazyBox(this);
        lazyManager.lazyLoadHandler();
      },
      beforeUnmount() {
        lazyManager.removeComponent(this);
      },
      methods: {
        init() {
          const { src, loading, error } = lazyManager.valueFormatter(this.src);
          this.state.loaded = false;
          this.options.src = src;
          this.options.error = error;
          this.options.loading = loading;
          this.renderSrc = this.options.loading;
        },
        checkInView() {
          const rect = (0, import_use.useRect)(this.$el);
          return rect.top < window.innerHeight * lazyManager.options.preLoad && rect.bottom > 0 && rect.left < window.innerWidth * lazyManager.options.preLoad && rect.right > 0;
        },
        load(onFinish = import_utils.noop) {
          if (this.state.attempt > this.options.attempt - 1 && this.state.error) {
            if (!lazyManager.options.silent) {
              console.log(
                `[@vant/lazyload] ${this.options.src} tried too more than ${this.options.attempt} times`
              );
            }
            onFinish();
            return;
          }
          const { src } = this.options;
          (0, import_util.loadImageAsync)(
            { src },
            ({ src: src2 }) => {
              this.renderSrc = src2;
              this.state.loaded = true;
            },
            () => {
              this.state.attempt++;
              this.renderSrc = this.options.error;
              this.state.error = true;
            }
          );
        }
      }
    });
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/index.js
var require_vue_lazyload = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/vue-lazyload/index.js"(exports, module) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Lazyload: () => Lazyload
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_lazy = __toESM(require_lazy());
    var import_lazy_component = __toESM(require_lazy_component());
    var import_lazy_container = __toESM(require_lazy_container());
    var import_lazy_image = __toESM(require_lazy_image());
    var Lazyload = {
      /*
       * install function
       * @param  {App} app
       * @param  {object} options lazyload options
       */
      install(app, options = {}) {
        const LazyClass = (0, import_lazy.default)();
        const lazy = new LazyClass(options);
        const lazyContainer = new import_lazy_container.default({ lazy });
        app.config.globalProperties.$Lazyload = lazy;
        if (options.lazyComponent) {
          app.component("LazyComponent", (0, import_lazy_component.default)(lazy));
        }
        if (options.lazyImage) {
          app.component("LazyImage", (0, import_lazy_image.default)(lazy));
        }
        app.directive("lazy", {
          beforeMount: lazy.add.bind(lazy),
          updated: lazy.update.bind(lazy),
          unmounted: lazy.remove.bind(lazy)
        });
        app.directive("lazy-container", {
          beforeMount: lazyContainer.bind.bind(lazyContainer),
          updated: lazyContainer.update.bind(lazyContainer),
          unmounted: lazyContainer.unbind.bind(lazyContainer)
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/index.js
var require_lazyload = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/lazyload/index.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      Lazyload: () => import_vue_lazyload.Lazyload,
      default: () => stdin_default
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_vue_lazyload = require_vue_lazyload();
    var stdin_default = import_vue_lazyload.Lazyload;
  }
});

// ../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index.js
var require_lib = __commonJS({
  "../../node_modules/.pnpm/vant@4.9.24_vue@3.5.31_typescript@6.0.2_/node_modules/vant/lib/index.js"(exports, module) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __reExport = (target, mod, secondTarget) => (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));
    var __toCommonJS2 = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var stdin_exports = {};
    __export(stdin_exports, {
      default: () => stdin_default,
      install: () => install,
      version: () => version
    });
    module.exports = __toCommonJS2(stdin_exports);
    var import_action_bar = require_action_bar();
    var import_action_bar_button = require_action_bar_button();
    var import_action_bar_icon = require_action_bar_icon();
    var import_action_sheet = require_action_sheet();
    var import_address_edit = require_address_edit();
    var import_address_list = require_address_list();
    var import_area = require_area();
    var import_back_top = require_back_top();
    var import_badge = require_badge();
    var import_barrage = require_barrage();
    var import_button = require_button();
    var import_calendar = require_calendar();
    var import_card = require_card();
    var import_cascader = require_cascader();
    var import_cell = require_cell();
    var import_cell_group = require_cell_group();
    var import_checkbox = require_checkbox();
    var import_checkbox_group = require_checkbox_group();
    var import_circle = require_circle();
    var import_col = require_col();
    var import_collapse = require_collapse();
    var import_collapse_item = require_collapse_item();
    var import_config_provider = require_config_provider();
    var import_contact_card = require_contact_card();
    var import_contact_edit = require_contact_edit();
    var import_contact_list = require_contact_list();
    var import_count_down = require_count_down();
    var import_coupon = require_coupon();
    var import_coupon_cell = require_coupon_cell();
    var import_coupon_list = require_coupon_list();
    var import_date_picker = require_date_picker();
    var import_dialog = require_dialog();
    var import_divider = require_divider();
    var import_dropdown_item = require_dropdown_item();
    var import_dropdown_menu = require_dropdown_menu();
    var import_empty = require_empty();
    var import_field = require_field();
    var import_floating_bubble = require_floating_bubble();
    var import_floating_panel = require_floating_panel();
    var import_form = require_form();
    var import_grid = require_grid();
    var import_grid_item = require_grid_item();
    var import_highlight = require_highlight();
    var import_icon = require_icon();
    var import_image = require_image();
    var import_image_preview = require_image_preview();
    var import_index_anchor = require_index_anchor();
    var import_index_bar = require_index_bar();
    var import_list = require_list();
    var import_loading = require_loading();
    var import_locale = require_locale();
    var import_nav_bar = require_nav_bar();
    var import_notice_bar = require_notice_bar();
    var import_notify = require_notify();
    var import_number_keyboard = require_number_keyboard();
    var import_overlay = require_overlay();
    var import_pagination = require_pagination();
    var import_password_input = require_password_input();
    var import_picker = require_picker();
    var import_picker_group = require_picker_group();
    var import_popover = require_popover();
    var import_popup = require_popup();
    var import_progress = require_progress();
    var import_pull_refresh = require_pull_refresh();
    var import_radio = require_radio();
    var import_radio_group = require_radio_group();
    var import_rate = require_rate();
    var import_rolling_text = require_rolling_text();
    var import_row = require_row();
    var import_search = require_search();
    var import_share_sheet = require_share_sheet();
    var import_sidebar = require_sidebar();
    var import_sidebar_item = require_sidebar_item();
    var import_signature = require_signature();
    var import_skeleton = require_skeleton();
    var import_skeleton_avatar = require_skeleton_avatar();
    var import_skeleton_image = require_skeleton_image();
    var import_skeleton_paragraph = require_skeleton_paragraph();
    var import_skeleton_title = require_skeleton_title();
    var import_slider = require_slider();
    var import_space = require_space();
    var import_step = require_step();
    var import_stepper = require_stepper();
    var import_steps = require_steps();
    var import_sticky = require_sticky();
    var import_submit_bar = require_submit_bar();
    var import_swipe = require_swipe();
    var import_swipe_cell = require_swipe_cell();
    var import_swipe_item = require_swipe_item();
    var import_switch = require_switch();
    var import_tab = require_tab();
    var import_tabbar = require_tabbar();
    var import_tabbar_item = require_tabbar_item();
    var import_tabs = require_tabs();
    var import_tag = require_tag();
    var import_text_ellipsis = require_text_ellipsis();
    var import_time_picker = require_time_picker();
    var import_toast = require_toast();
    var import_tree_select = require_tree_select();
    var import_uploader = require_uploader();
    var import_watermark = require_watermark();
    __reExport(stdin_exports, require_action_bar(), module.exports);
    __reExport(stdin_exports, require_action_bar_button(), module.exports);
    __reExport(stdin_exports, require_action_bar_icon(), module.exports);
    __reExport(stdin_exports, require_action_sheet(), module.exports);
    __reExport(stdin_exports, require_address_edit(), module.exports);
    __reExport(stdin_exports, require_address_list(), module.exports);
    __reExport(stdin_exports, require_area(), module.exports);
    __reExport(stdin_exports, require_back_top(), module.exports);
    __reExport(stdin_exports, require_badge(), module.exports);
    __reExport(stdin_exports, require_barrage(), module.exports);
    __reExport(stdin_exports, require_button(), module.exports);
    __reExport(stdin_exports, require_calendar(), module.exports);
    __reExport(stdin_exports, require_card(), module.exports);
    __reExport(stdin_exports, require_cascader(), module.exports);
    __reExport(stdin_exports, require_cell(), module.exports);
    __reExport(stdin_exports, require_cell_group(), module.exports);
    __reExport(stdin_exports, require_checkbox(), module.exports);
    __reExport(stdin_exports, require_checkbox_group(), module.exports);
    __reExport(stdin_exports, require_circle(), module.exports);
    __reExport(stdin_exports, require_col(), module.exports);
    __reExport(stdin_exports, require_collapse(), module.exports);
    __reExport(stdin_exports, require_collapse_item(), module.exports);
    __reExport(stdin_exports, require_config_provider(), module.exports);
    __reExport(stdin_exports, require_contact_card(), module.exports);
    __reExport(stdin_exports, require_contact_edit(), module.exports);
    __reExport(stdin_exports, require_contact_list(), module.exports);
    __reExport(stdin_exports, require_count_down(), module.exports);
    __reExport(stdin_exports, require_coupon(), module.exports);
    __reExport(stdin_exports, require_coupon_cell(), module.exports);
    __reExport(stdin_exports, require_coupon_list(), module.exports);
    __reExport(stdin_exports, require_date_picker(), module.exports);
    __reExport(stdin_exports, require_dialog(), module.exports);
    __reExport(stdin_exports, require_divider(), module.exports);
    __reExport(stdin_exports, require_dropdown_item(), module.exports);
    __reExport(stdin_exports, require_dropdown_menu(), module.exports);
    __reExport(stdin_exports, require_empty(), module.exports);
    __reExport(stdin_exports, require_field(), module.exports);
    __reExport(stdin_exports, require_floating_bubble(), module.exports);
    __reExport(stdin_exports, require_floating_panel(), module.exports);
    __reExport(stdin_exports, require_form(), module.exports);
    __reExport(stdin_exports, require_grid(), module.exports);
    __reExport(stdin_exports, require_grid_item(), module.exports);
    __reExport(stdin_exports, require_highlight(), module.exports);
    __reExport(stdin_exports, require_icon(), module.exports);
    __reExport(stdin_exports, require_image(), module.exports);
    __reExport(stdin_exports, require_image_preview(), module.exports);
    __reExport(stdin_exports, require_index_anchor(), module.exports);
    __reExport(stdin_exports, require_index_bar(), module.exports);
    __reExport(stdin_exports, require_lazyload(), module.exports);
    __reExport(stdin_exports, require_list(), module.exports);
    __reExport(stdin_exports, require_loading(), module.exports);
    __reExport(stdin_exports, require_locale(), module.exports);
    __reExport(stdin_exports, require_nav_bar(), module.exports);
    __reExport(stdin_exports, require_notice_bar(), module.exports);
    __reExport(stdin_exports, require_notify(), module.exports);
    __reExport(stdin_exports, require_number_keyboard(), module.exports);
    __reExport(stdin_exports, require_overlay(), module.exports);
    __reExport(stdin_exports, require_pagination(), module.exports);
    __reExport(stdin_exports, require_password_input(), module.exports);
    __reExport(stdin_exports, require_picker(), module.exports);
    __reExport(stdin_exports, require_picker_group(), module.exports);
    __reExport(stdin_exports, require_popover(), module.exports);
    __reExport(stdin_exports, require_popup(), module.exports);
    __reExport(stdin_exports, require_progress(), module.exports);
    __reExport(stdin_exports, require_pull_refresh(), module.exports);
    __reExport(stdin_exports, require_radio(), module.exports);
    __reExport(stdin_exports, require_radio_group(), module.exports);
    __reExport(stdin_exports, require_rate(), module.exports);
    __reExport(stdin_exports, require_rolling_text(), module.exports);
    __reExport(stdin_exports, require_row(), module.exports);
    __reExport(stdin_exports, require_search(), module.exports);
    __reExport(stdin_exports, require_share_sheet(), module.exports);
    __reExport(stdin_exports, require_sidebar(), module.exports);
    __reExport(stdin_exports, require_sidebar_item(), module.exports);
    __reExport(stdin_exports, require_signature(), module.exports);
    __reExport(stdin_exports, require_skeleton(), module.exports);
    __reExport(stdin_exports, require_skeleton_avatar(), module.exports);
    __reExport(stdin_exports, require_skeleton_image(), module.exports);
    __reExport(stdin_exports, require_skeleton_paragraph(), module.exports);
    __reExport(stdin_exports, require_skeleton_title(), module.exports);
    __reExport(stdin_exports, require_slider(), module.exports);
    __reExport(stdin_exports, require_space(), module.exports);
    __reExport(stdin_exports, require_step(), module.exports);
    __reExport(stdin_exports, require_stepper(), module.exports);
    __reExport(stdin_exports, require_steps(), module.exports);
    __reExport(stdin_exports, require_sticky(), module.exports);
    __reExport(stdin_exports, require_submit_bar(), module.exports);
    __reExport(stdin_exports, require_swipe(), module.exports);
    __reExport(stdin_exports, require_swipe_cell(), module.exports);
    __reExport(stdin_exports, require_swipe_item(), module.exports);
    __reExport(stdin_exports, require_switch(), module.exports);
    __reExport(stdin_exports, require_tab(), module.exports);
    __reExport(stdin_exports, require_tabbar(), module.exports);
    __reExport(stdin_exports, require_tabbar_item(), module.exports);
    __reExport(stdin_exports, require_tabs(), module.exports);
    __reExport(stdin_exports, require_tag(), module.exports);
    __reExport(stdin_exports, require_text_ellipsis(), module.exports);
    __reExport(stdin_exports, require_time_picker(), module.exports);
    __reExport(stdin_exports, require_toast(), module.exports);
    __reExport(stdin_exports, require_tree_select(), module.exports);
    __reExport(stdin_exports, require_uploader(), module.exports);
    __reExport(stdin_exports, require_watermark(), module.exports);
    var version = "4.9.24";
    function install(app) {
      const components = [
        import_action_bar.ActionBar,
        import_action_bar_button.ActionBarButton,
        import_action_bar_icon.ActionBarIcon,
        import_action_sheet.ActionSheet,
        import_address_edit.AddressEdit,
        import_address_list.AddressList,
        import_area.Area,
        import_back_top.BackTop,
        import_badge.Badge,
        import_barrage.Barrage,
        import_button.Button,
        import_calendar.Calendar,
        import_card.Card,
        import_cascader.Cascader,
        import_cell.Cell,
        import_cell_group.CellGroup,
        import_checkbox.Checkbox,
        import_checkbox_group.CheckboxGroup,
        import_circle.Circle,
        import_col.Col,
        import_collapse.Collapse,
        import_collapse_item.CollapseItem,
        import_config_provider.ConfigProvider,
        import_contact_card.ContactCard,
        import_contact_edit.ContactEdit,
        import_contact_list.ContactList,
        import_count_down.CountDown,
        import_coupon.Coupon,
        import_coupon_cell.CouponCell,
        import_coupon_list.CouponList,
        import_date_picker.DatePicker,
        import_dialog.Dialog,
        import_divider.Divider,
        import_dropdown_item.DropdownItem,
        import_dropdown_menu.DropdownMenu,
        import_empty.Empty,
        import_field.Field,
        import_floating_bubble.FloatingBubble,
        import_floating_panel.FloatingPanel,
        import_form.Form,
        import_grid.Grid,
        import_grid_item.GridItem,
        import_highlight.Highlight,
        import_icon.Icon,
        import_image.Image,
        import_image_preview.ImagePreview,
        import_index_anchor.IndexAnchor,
        import_index_bar.IndexBar,
        import_list.List,
        import_loading.Loading,
        import_locale.Locale,
        import_nav_bar.NavBar,
        import_notice_bar.NoticeBar,
        import_notify.Notify,
        import_number_keyboard.NumberKeyboard,
        import_overlay.Overlay,
        import_pagination.Pagination,
        import_password_input.PasswordInput,
        import_picker.Picker,
        import_picker_group.PickerGroup,
        import_popover.Popover,
        import_popup.Popup,
        import_progress.Progress,
        import_pull_refresh.PullRefresh,
        import_radio.Radio,
        import_radio_group.RadioGroup,
        import_rate.Rate,
        import_rolling_text.RollingText,
        import_row.Row,
        import_search.Search,
        import_share_sheet.ShareSheet,
        import_sidebar.Sidebar,
        import_sidebar_item.SidebarItem,
        import_signature.Signature,
        import_skeleton.Skeleton,
        import_skeleton_avatar.SkeletonAvatar,
        import_skeleton_image.SkeletonImage,
        import_skeleton_paragraph.SkeletonParagraph,
        import_skeleton_title.SkeletonTitle,
        import_slider.Slider,
        import_space.Space,
        import_step.Step,
        import_stepper.Stepper,
        import_steps.Steps,
        import_sticky.Sticky,
        import_submit_bar.SubmitBar,
        import_swipe.Swipe,
        import_swipe_cell.SwipeCell,
        import_swipe_item.SwipeItem,
        import_switch.Switch,
        import_tab.Tab,
        import_tabbar.Tabbar,
        import_tabbar_item.TabbarItem,
        import_tabs.Tabs,
        import_tag.Tag,
        import_text_ellipsis.TextEllipsis,
        import_time_picker.TimePicker,
        import_toast.Toast,
        import_tree_select.TreeSelect,
        import_uploader.Uploader,
        import_watermark.Watermark
      ];
      components.forEach((item) => {
        if (item.install) {
          app.use(item);
        } else if (item.name) {
          app.component(item.name, item);
        }
      });
    }
    var stdin_default = {
      install,
      version
    };
  }
});
export default require_lib();
//# sourceMappingURL=vant_lib_.js.map
